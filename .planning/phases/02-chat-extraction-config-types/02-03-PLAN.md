---
phase: 02-chat-extraction-config-types
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - src/types/chat.ts
  - src/types/ipc.ts
  - src/lib/tauri.ts
  - src/hooks/useChat.ts
  - src/hooks/useChat.test.ts
  - src/views/ChatView.tsx
  - src/App.tsx
  - e2e/helpers.ts
  - e2e/specs/agent.spec.ts
autonomous: true
requirements:
  - FE-02
  - FE-06
must_haves:
  truths:
    - "useChat owns RAG session state only: sessions, activeSessionId, messages, question, streamingDraft (D-01); agent state and streaming Channel stay in App.tsx (D-06/D-15)"
    - "ChatView is a controlled component: App passes busy, err, setErr, indexProgress, e2eMode, askHandleCount, config, and all agent props as explicit props (D-02..D-05, D-09)"
    - "RAG and Agent have separate submit controls; agent-submit testid is added (D-10/D-11); all pre-existing chat data-testid values land on the same DOM elements (FE-06)"
    - "Chat IPC is wrapped in src/lib/tauri.ts including ask_in_session_stream (D-08); ChatView/useChat do not call invoke directly for chat CRUD"
    - "askHandleCount / __JARVIS_E2E_ASK__ probe stays in App.tsx; the counter increments in App's handleRagAsk wrapper, not inside useChat (D-03, Pitfall 3)"
    - "Full E2E suite green; npm test green; tsc --noEmit clean (D-33/D-34)"
  artifacts:
    - path: src/hooks/useChat.ts
      provides: useChat hook returning RAG session state + session CRUD
      contains: "export function useChat"
    - path: src/views/ChatView.tsx
      provides: full chat + agent JSX (controlled), split RAG/agent submit, index banner, error bar
      contains: "data-testid=\"agent-submit\""
    - path: src/types/chat.ts
      provides: ChatSession, ChatMessage TS types
      contains: "export interface ChatSession"
    - path: e2e/helpers.ts
      provides: askAgentQuestion helper using agent-submit
      contains: "agent-submit"
  key_links:
    - from: src/App.tsx
      to: src/hooks/useChat.ts
      via: "const { sessions, messages, ... } = useChat()"
      pattern: "useChat\\("
    - from: src/App.tsx
      to: src/views/ChatView.tsx
      via: "<ChatView ...controlled props />"
      pattern: "<ChatView"
    - from: src/views/ChatView.tsx
      to: src/lib/tauri.ts
      via: "chat IPC wrappers (askInSession, listChatMessages, ...)"
      pattern: "from \"../lib/tauri\""
    - from: e2e/specs/agent.spec.ts
      to: e2e/helpers.ts
      via: "askAgentQuestion()"
      pattern: "askAgentQuestion"
---

<objective>
Extract the full Chat view from the `App.tsx` monolith into `src/views/ChatView.tsx` + `src/hooks/useChat.ts`, route all chat IPC through `src/lib/tauri.ts`, split RAG and Agent submit into visually distinct controls (new `agent-submit` testid), and update the E2E harness — all in one PR (D-29) gated by the full E2E suite.

Purpose: Deliver FE-02 (Chat view + `useChat` hook + tauri.ts IPC) and FE-06 (testid preservation + `agent-submit`) as a complete, user-visible vertical slice with zero behavior regression except the intentional split-submit UX (D-10).
Output: `types/chat.ts`, `lib/tauri.ts` chat wrappers, `useChat.ts` (+ unit test), full `ChatView.tsx`, slimmed `App.tsx` (controlled-parent), `helpers.ts` `askAgentQuestion`, updated `agent.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-chat-extraction-config-types/02-CONTEXT.md
@.planning/phases/02-chat-extraction-config-types/02-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.cursor/rules/e2e-required.mdc
@.cursor/rules/frontend-taste.mdc
@src/App.tsx
@src/views/ChatView.tsx
@src/lib/tauri.ts
@src/lib/citations.ts
@src/types/ipc.ts
@e2e/helpers.ts
@e2e/specs/agent.spec.ts
@e2e/specs/qa.spec.ts
@e2e/specs/full-ui.spec.ts
</context>

<threat_model>
scope: structural_refactor_only
asvs_level: L1
trust_boundaries:
  - "Tauri IPC boundary — frontend invoke names unchanged; only wrapped in tauri.ts (D-08)"
  - "DOM / E2E selector contract — data-testid values are the automation trust surface (FE-06)"
stride_register:
  - threat_id: T-01
    category: Tampering
    component: data-testid selectors in ChatView
    disposition: mitigate
    mitigation_plan: "Preserve every pre-existing chat testid on the same element; add only agent-submit. Full E2E suite (D-33) re-verifies no selector regression. No hidden compat-shim elements (D-35)."
  - threat_id: T-02
    category: Information Disclosure
    component: citation rendering / message display
    disposition: accept
    mitigation_plan: "Citation parse/render keeps using src/lib/citations.ts pure functions (D-28); no new data surfaced. Rendering moved verbatim."
  - threat_id: T-03
    category: Denial of Service
    component: useChat session refresh effect
    disposition: monitor
    mitigation_plan: "Autonomous session refresh (D-07) must avoid duplicate sessions state (Pitfall 2): App destructures useChat() as single source of truth and keeps no own sessions copy."
disposition_summary: "Frontend move + intentional split-submit UX. No new IPC, network, or auth surface; selector stability is the primary risk and is E2E-gated."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: chat types + tauri.ts chat wrappers + useChat hook (red-first unit test)</name>
  <files>src/types/chat.ts, src/types/ipc.ts, src/lib/tauri.ts, src/hooks/useChat.ts, src/hooks/useChat.test.ts</files>
  <read_first>src/App.tsx (chat state + session CRUD + handleAsk region), src/types/ipc.ts, src/lib/tauri.ts, .planning/phases/02-chat-extraction-config-types/02-RESEARCH.md (Pattern 2, tauri.ts chat wrappers, Pitfall 2/3)</read_first>
  <action>
    Per D-32: create `src/types/chat.ts` exporting `ChatSession`, `ChatMessage` (and any chat-only types currently inline in App/ipc.ts). Per Claude's discretion, `src/types/ipc.ts` MAY re-export from `types/chat.ts`; do not break existing imports.

    Per D-08/FE-02: extend `src/lib/tauri.ts` with chat IPC wrappers exactly as 02-RESEARCH "tauri.ts chat wrappers": `listChatSessions`, `createChatSession`, `deleteChatSession`, `listChatMessages`, `askInSession`, `askInSessionStream` (the stream wrapper takes a `Channel<{ token: string }>`). Use existing invoke-wrapper style; do not rename any IPC string.

    Per D-01/D-07: create `src/hooks/useChat.ts` (TDD red-first — write `src/hooks/useChat.test.ts` first). `useChat` owns ONLY: `sessions`, `activeSessionId`, `messages`, `question`, `streamingDraft`, and session CRUD (`createSession`, `switchSession`, `deleteSession`, `refreshMessages`, `setQuestion`). Session list refresh is autonomous in the hook (D-07). Report errors via an `onError` callback prop (D-02). The hook MUST NOT own `busy`, `err`, `agentMode`, `indexProgress`, `e2eMode`, `askHandleCount`, or streaming Channel state (D-03/D-04/D-05/D-06/D-15).

    `useChat.test.ts` (Vitest) asserts the hook's RAG-only boundary: initial empty state, that returned API includes the session CRUD fns, and that it exposes no agent/global keys (e.g. assert `agentMode`/`busy` are absent from the hook return).
  </action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - `src/types/chat.ts` exports `ChatSession` and `ChatMessage`
    - `src/lib/tauri.ts` exports `listChatSessions`, `createChatSession`, `deleteChatSession`, `listChatMessages`, `askInSession`, `askInSessionStream`
    - `src/hooks/useChat.ts` exports `useChat`; its return object includes `sessions`, `activeSessionId`, `messages`, `question`, `streamingDraft`, `setQuestion`, `createSession`, `switchSession`, `deleteSession`, `refreshMessages`
    - `useChat` return object does NOT include `agentMode`, `busy`, `err`, `indexProgress` (RAG-only boundary, D-01)
    - `npm test` exits 0 and runs `useChat.test.ts`
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Full ChatView.tsx (controlled) + slim App.tsx wiring + split submit</name>
  <files>src/views/ChatView.tsx, src/App.tsx</files>
  <read_first>src/App.tsx (chat JSX, agent JSX, handleAsk, handleAskRef, streaming Channel), src/views/ChatView.tsx, src/lib/citations.ts, .planning/phases/02-chat-extraction-config-types/02-RESEARCH.md (Pattern 1, Pattern 5, testid inventory, Pitfalls 2/3)</read_first>
  <action>
    Replace the `{children}` stub in `src/views/ChatView.tsx` with the full chat + agent JSX moved from `App.tsx`. Per D-04/D-09, ChatView is a CONTROLLED component receiving explicit props per 02-RESEARCH Pattern 1:
    - RAG state from useChat: `sessions`, `activeSessionId`, `messages`, `question`, `streamingDraft`, `onSetQuestion`, `onCreateSession`, `onSwitchSession`, `onDeleteSession`
    - Agent state from App (stays in App, D-01/D-15): `agentMode`, `setAgentMode`, `lastToolCalls`, `lastOrchestrationSteps`, `onAgentAsk`
    - Global from App (D-02/D-03/D-05): `busy`, `err`, `setErr`, `indexProgress`, `e2eMode`, `askHandleCount`, `config`
    - `onRagAsk` (separate RAG submit handler, D-10)
    - `onManageAgent` (navigate to Settings view, D-13)

    Layout/contents inside ChatView:
    - Chat session sidebar new/switch/delete (D-26)
    - `IndexProgressBanner` (`index-progress`) and `chat-error` (D-25) — same testids
    - Messages list with citation parse/render via `src/lib/citations.ts` pure fns (D-28); testids `chat-message-assistant`, `citation-excerpt`, `ask-busy`, `streaming-answer`
    - Header bar (same row as session controls) holds `agent-mode-toggle`, `agent-profile-select` (D-14) and the 「管理 Agent」 quick link calling `onManageAgent` (D-13)
    - Tool-call panel `agent-tool-calls` and `orchestration-steps` / `orchestration-mode-{mode}` BELOW messages (D-12)
    - RAG form: `data-testid="chat-form"`, `chat-input`, `ask-submit` (preserved); calls `onRagAsk`
    - Agent form (visually distinct, D-10): new `agent-submit` (D-11); calls `onAgentAsk`

    In `src/App.tsx`:
    - Call `const chat = useChat({ onError: setErr })` and destructure; REMOVE App's own `sessions/activeSessionId/messages/question/streamingDraft` useState (Pitfall 2 — single source of truth).
    - Keep in App: `busy`, `err`, `agentMode`, `lastToolCalls`, `lastOrchestrationSteps`, `indexProgress` listen, `e2eMode`, `askHandleCount`, `handleAskRef`, `config`, streaming `Channel` setup (D-06).
    - Provide `handleRagAsk` wrapper in App that increments `askHandleCount` / sets `__JARVIS_E2E_ASK__` (Pitfall 3) then drives the RAG ask via tauri.ts stream wrapper; `handleAgentAsk` stays in App (D-15). `handleAskRef.current` continues to point at the unified dispatcher in App.
    - Render `{view === "chat" && <ChatView ...props />}` passing the controlled props above. The `e2e-active`, `chat-session-ready`, `ask-handle-count` testids stay rendered by App.tsx (D-03).
  </action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - `src/views/ChatView.tsx` no longer matches `children` passthrough; it renders `data-testid="chat-form"`, `chat-input`, `ask-submit`, and the new `agent-submit`
    - `rg "data-testid=\"agent-submit\"" src/views/ChatView.tsx` returns a match (FE-06/D-11)
    - All pre-existing chat testids appear in ChatView: `chat-error`, `index-progress`, `chat-message-assistant`, `citation-excerpt`, `orchestration-steps`, `agent-tool-calls`, `ask-busy`, `streaming-answer`, `agent-mode-toggle`, `agent-profile-select`
    - `src/App.tsx` calls `useChat(` and renders `<ChatView`; App declares no `useState` for `sessions`/`messages` (single source of truth, Pitfall 2)
    - `e2e-active`, `chat-session-ready`, `ask-handle-count` testids still produced by `src/App.tsx` (D-03)
    - `npm test` exits 0; `npx tsc --noEmit` clean
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: E2E harness update (askAgentQuestion) + full suite green</name>
  <files>e2e/helpers.ts, e2e/specs/agent.spec.ts</files>
  <read_first>e2e/helpers.ts, e2e/specs/agent.spec.ts, e2e/specs/qa.spec.ts, e2e/specs/full-ui.spec.ts, .planning/phases/02-chat-extraction-config-types/02-RESEARCH.md ("E2E impact of split submit")</read_first>
  <action>
    Per D-11/D-35 and 02-RESEARCH "E2E impact": add `askAgentQuestion(text)` to `e2e/helpers.ts` that types into the chat/agent input and submits via `[data-testid="agent-submit"]` (mirror the existing `askQuestion`/`submitChatForm` helper which uses `ask-submit`). Keep `askQuestion` (RAG) using `ask-submit` so `qa.spec.ts` is unchanged.

    Update `e2e/specs/agent.spec.ts` to drive the agent flow via `askAgentQuestion()` (agent-submit) instead of the RAG `ask-submit` path. No hidden compatibility shim element may be added to satisfy old selectors (D-35).

    Rebuild requirement (per 01-03 note): the E2E run targets `target/release/tauri-app.exe`, so run `npm run build && cargo build --release --manifest-path src-tauri/Cargo.toml` before the suite if the local binary is stale.

    Run the FULL suite (D-33): `npm run test:e2e:local` (all specs under `e2e/specs/**/*.spec.ts`).
  </action>
  <verify>npm run test:e2e:local</verify>
  <acceptance_criteria>
    - `e2e/helpers.ts` exports `askAgentQuestion` and references `agent-submit`
    - `e2e/specs/agent.spec.ts` calls `askAgentQuestion`
    - `qa.spec.ts` still uses the RAG `ask-submit` path (no change to its submit selector)
    - `npm run test:e2e:local` passes the full suite (smoke, navigation, qa, agent, full-ui, lark, memory, settings)
    - No compat-shim duplicate of `ask-submit` is added for the agent path (D-35)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `npm test` passes (useChat unit test)
- [ ] `npx tsc --noEmit` clean
- [ ] `npm run test:e2e:local` full suite green (D-33)
- [ ] `qa.spec.ts`, `agent.spec.ts`, `full-ui.spec.ts` pass
- [ ] All pre-existing chat testids preserved; `agent-submit` added (FE-06)
- [ ] App.tsx holds no chat-session state (single source of truth via useChat)
</verification>

<success_criteria>
- ChatView + useChat own all chat/agent UI; App is a controlled parent (FE-02/D-04)
- RAG and Agent are visually distinct submit modes; agent-submit testid added (D-10/D-11)
- All chat IPC flows through tauri.ts (D-08); chat CRUD does not call invoke inline
- E2E harness updated in same PR (D-35); full suite green (D-33); npm test + tsc green (D-34)
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Location |
|---------------|------|----------|
| `ChatSession` | TS interface | `src/types/chat.ts` |
| `ChatMessage` | TS interface | `src/types/chat.ts` |
| `listChatSessions` / `createChatSession` / `deleteChatSession` | IPC wrapper | `src/lib/tauri.ts` |
| `listChatMessages` / `askInSession` / `askInSessionStream` | IPC wrapper | `src/lib/tauri.ts` |
| `useChat` | React hook | `src/hooks/useChat.ts` |
| `useChat.test.ts` | Vitest unit test | `src/hooks/useChat.test.ts` |
| `ChatView` (full) | React component | `src/views/ChatView.tsx` |
| `agent-submit` | data-testid | `src/views/ChatView.tsx` |
| `askAgentQuestion` | E2E helper | `e2e/helpers.ts` |

<output>
After completion, create `.planning/phases/02-chat-extraction-config-types/02-03-SUMMARY.md`
</output>
