# Milestone v1.9: Structural Refactor

**Status:** ✅ SHIPPED 2026-07-17  
**Phases:** 01-06  
**Total Plans:** 22  
**Closeout:** verified_closeout (all 6 phases `phase_complete` + `verification_status=passed`)  
**Note:** Formal `/gsd-audit-milestone` report was not present at close; phase-level VERIFICATION + UAT gates were used instead.

## Overview

Incremental structural refactor of Jarvis (Tauri 2 + React) to reduce maintenance cost without changing user-facing behavior: extract frontend views/hooks, split Tauri command modules, nest `AppConfig` + OS keychain secrets, harden memory URIs, JSON-first agent tool protocol, Settings extraction + architecture review.

## Phases

### Phase 1: Scaffold + Shell Foundation

**Goal:** Create target directory structure and begin Tauri command module split with zero behavior change.  
**Plans:** 3/3  

- [x] 01-01 — directory scaffold / views placeholders  
- [x] 01-02 — AppState + commands module registration  
- [x] 01-03 — first command groups relocated  

### Phase 2: Chat Extraction + Config Types

**Goal:** Extract Chat view and hook; introduce nested `AppConfig` structs with backward-compatible serde.  
**Plans:** 3/3  

- [x] 02-01 — nested AppConfig  
- [x] 02-02 — ChatView + useChat + commands/chat.rs  
- [x] 02-03 — IPC/types alignment  

### Phase 3: Library/Tasks + Keychain

**Goal:** Extract Library and Tasks views; migrate API secrets to OS keychain.  
**Plans:** 5/5  

- [x] 03-01 … 03-05 — Library/Tasks extraction, index/lark commands, keyring migration  

### Phase 4: Memory Model + View

**Goal:** Implement `memory://` URI scheme and extract Memory view.  
**Plans:** 4/4  

- [x] 04-01 … 04-04 — URI migration, MemoryView, commands/memory.rs  

### Phase 5: Agent Protocol

**Goal:** As a Jarvis user, I want to use agent mode with structured JSON tool calls and see parse warnings in chat, so that I can trust agent answers when tool parsing fails.  
**Plans:** 4/4  

- [x] 05-01 — ToolCallParser JSON + run loop  
- [x] 05-02 — Mock JSON + AskResponse warnings IPC  
- [x] 05-03 — commands/agent.rs  
- [x] 05-04 — ChatView warnings UI + E2E  

**UAT:** 05-UAT.md 7/7 passed (2026-07-17)

### Phase 6: Settings + Architecture Review

**Goal:** Extract Settings view; surface sync errors; complete refactor sign-off.  
**Plans:** 3/3  

- [x] 06-01 — SettingsView + useJarvisConfig + AppShell  
- [x] 06-02 — sync error persistence + UI  
- [x] 06-03 — shell slim + architecture VERIFICATION  

---

## Milestone Summary

**Key Decisions:**
- Incremental E2E-gated refactor (not big-bang rewrite)
- Balanced vertical MVP phases across FE / shell / config / agent / memory
- v1.9.x framing (no v2.0 breaking expectations)
- Success = arch review + E2E green + keychain secrets

**Issues Resolved:**
- Phase 05 verification stale gate cleared via human UAT + refreshed VERIFICATION.md
- Launch “窗口无响应” mid-UAT: DeferredEmbedder + deferred initial_scan (product fix)
- Vite Windows EBUSY / IPv6 bind issues mitigated in vite.config / tauri.conf

**Issues Deferred:**
- Formal milestone audit document (`/gsd-audit-milestone`) not run at close
- Wiki compile layer / Obsidian export → next milestone (v1.10 plan drafted)
- Full plugin sandbox / DAG orchestration (out of scope)

**Technical Debt Incurred:**
- Startup deferral for FastEmbed is correct but first-ask may wait on model load
- Orphan Node processes on Windows still require manual cleanup before `tauri dev`
- Uncommitted product/source changes may remain outside planning commits — ship separately

---

_For current project status, see `.planning/ROADMAP.md`_
