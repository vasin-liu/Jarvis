---
phase: 04-memory-model-view
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/memory/Cargo.toml
  - crates/memory/src/lib.rs
  - crates/memory/src/learn.rs
  - crates/memory/src/migrate.rs
  - crates/memory/tests/migration.rs
  - crates/store/src/store.rs
  - src-tauri/src/lib.rs
requirements:
  - MEM-01
  - MEM-02
must_haves:
  truths:
    - "uuid = \"1\" with features [\"v4\"] is a dependency of crates/memory ONLY (D-01)"
    - "add_memory creates uri = memory://{uuid-v4} lowercase with hyphens; returned string equals source.id (D-01, P2)"
    - "Store::rename_source_id(old, new) atomically rekeys sources.id + chunks.source_id (+ tasks.source_id if any) without re-embedding (P1)"
    - "memory::migrate_legacy_memory_uris scans SourceKind::Memory where uri matches memory:// + all-digit suffix; assigns new UUID uri; idempotent skip for existing UUID uris (D-02, D-04)"
    - "init_state calls migrate_legacy_memory_uris after Store::open when NOT is_e2e_mode() (D-04)"
    - "resolve_memory_id order: exact id → exact uri → exact title → deprecated fuzzy contains with eprintln/warn deprecation line (D-05, D-06, P6)"
    - "cargo test -p memory and cargo test -p store pass"
  artifacts:
    - path: crates/memory/src/migrate.rs
      provides: migrate_legacy_memory_uris + is_legacy_memory_uri helpers
      contains: "migrate_legacy_memory_uris"
    - path: crates/store/src/store.rs
      provides: rename_source_id transaction
      contains: "rename_source_id"
    - path: crates/memory/tests/migration.rs
      provides: integration test for timestamp → UUID migration idempotency
      contains: "migrate_legacy_memory_uris"
    - path: crates/memory/src/learn.rs
      provides: UUID-based add_memory + deprecated fuzzy resolve
      contains: "Uuid::new_v4"
  key_links:
    - from: src-tauri/src/lib.rs
      to: crates/memory/src/migrate.rs
      via: "init_state calls memory::migrate_legacy_memory_uris after Store::open"
      pattern: "migrate_legacy_memory_uris"
    - from: crates/memory/src/migrate.rs
      to: crates/store/src/store.rs
      via: "rename_source_id for each legacy memory"
      pattern: "rename_source_id"
    - from: crates/indexer/src/lib.rs
      to: crates/memory/src/learn.rs
      via: "add_memory still uses index_document with doc.uri as source id"
      pattern: "index_document"
---

<objective>
Implement the memory identity foundation: UUID v4 URIs for new memories, startup migration of legacy `memory://{timestamp}` sources to `memory://{uuid}`, store-level source rekeying, and deprecation logging on fuzzy `resolve_memory_id` — before any command relocation or frontend extraction.

Purpose: Deliver MEM-01 (stable UUID URIs + strict resolution path) and MEM-02 (migration + one-release fuzzy deprecation) at the Rust crate layer.
Output: `uuid` dep, `migrate.rs`, `Store::rename_source_id`, updated `learn.rs`, migration integration test, `init_state` hook.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-memory-model-view/04-CONTEXT.md
@.planning/phases/04-memory-model-view/04-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.cursor/rules/tdd-goal-driven.mdc
@crates/memory/src/learn.rs
@crates/indexer/src/lib.rs
@crates/store/src/store.rs
@src-tauri/src/lib.rs
</context>

<threat_model>
scope: data_migration
asvs_level: L1
trust_boundaries:
  - "SQLite store — migration must not orphan chunks or duplicate sources"
  - "Agent resolve_memory_id — fuzzy path retained one release with visible deprecation"
stride_register:
  - threat_id: T-04-01
    category: Integrity
    component: migrate_legacy_memory_uris / rename_source_id
    disposition: mitigate
    mitigation_plan: "Single SQLite transaction per rename; integration test asserts chunk count + content preserved; migration idempotent (second run migrates 0)."
  - threat_id: T-04-02
    category: Denial of Service
    component: init_state migration on large DBs
    disposition: accept
    mitigation_plan: "Migration is O(n) over memory sources only (typically small); no full-table scan beyond list_sources filter."
disposition_summary: "Primary risk is corrupting memory rows during PK rekey — mitigated by transactional rename + tests."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Store::rename_source_id + unit test (04-01-01)</name>
  <files>crates/store/src/store.rs</files>
  <read_first>crates/store/src/store.rs (upsert_source, delete_source, insert_chunks, schema FK on chunks.source_id), 04-RESEARCH.md §2 Legacy URI migration</read_first>
  <action>
    Add `pub fn rename_source_id(&self, old_id: &str, new_id: &str) -> Result<()>` that:
    1. Loads existing source via get_source(old_id)
    2. In a transaction: inserts new Source row with id=new_id, uri=new_id (same kind/title/hash/status/etc.)
    3. UPDATE chunks SET source_id = new_id WHERE source_id = old_id
    4. UPDATE tasks SET source_id = new_id WHERE source_id = old_id
    5. DELETE FROM sources WHERE id = old_id
    Return error if new_id already exists or old_id missing.

    Add #[cfg(test)] test: create source + insert_chunks, rename, assert get_source(new_id) works, chunk text retrievable via source_chunk_text, old id NotFound.
  </action>
  <verify>cargo test -p store rename_source_id</verify>
  <acceptance_criteria>
    - `rg "fn rename_source_id" crates/store/src/store.rs` matches
    - `cargo test -p store` passes including new rename test
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: UUID add_memory + migrate module + resolve deprecation (04-01-02)</name>
  <files>crates/memory/Cargo.toml, crates/memory/src/lib.rs, crates/memory/src/learn.rs, crates/memory/src/migrate.rs, crates/memory/tests/migration.rs</files>
  <read_first>crates/memory/src/learn.rs, 04-CONTEXT.md D-01/D-05/D-06, 04-RESEARCH.md Pitfalls P1/P6</read_first>
  <action>
    Add `uuid = { version = "1", features = ["v4"] }` to crates/memory/Cargo.toml.

    In learn.rs:
    - Replace `format!("memory://{now}")` with `format!("memory://{}", uuid::Uuid::new_v4())` in add_memory
    - Refactor resolve_memory_id: after exact id check, find exact uri match, then exact title match, then fuzzy contains with `eprintln!("[jarvis] memory title fuzzy match deprecated; use id or memory:// uri")` before return
    - Add unit tests: new memory uri matches UUID pattern; resolve by exact uri; fuzzy still resolves (optional: don't assert stderr)

    Create migrate.rs:
    - `pub fn is_legacy_memory_uri(uri: &str) -> bool` — prefix memory:// and suffix is all ASCII digits (and non-empty)
    - `pub fn is_uuid_memory_uri(uri: &str) -> bool` — suffix parses as Uuid
    - `pub fn migrate_legacy_memory_uris(store: &Store) -> Result<usize>` — list SourceKind::Memory, skip uuid uris, for each legacy call store.rename_source_id(old, new_uuid_uri), return count

    Export migrate from lib.rs.

    Create crates/memory/tests/migration.rs integration test:
    - Manually upsert legacy memory://12345 source with a chunk (or use add_memory pre-change pattern via store upsert + index)
    - Run migrate_legacy_memory_uris → assert count 1, uri is UUID form, content preserved
    - Run again → assert count 0
  </action>
  <verify>cargo test -p memory</verify>
  <acceptance_criteria>
    - `rg "Uuid::new_v4" crates/memory/src/learn.rs` matches
    - `rg "migrate_legacy_memory_uris" crates/memory/src/migrate.rs` matches
    - `cargo test -p memory` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire migration in init_state (04-01-03)</name>
  <files>src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/lib.rs init_state (~502-560), src-tauri/src/e2e.rs is_e2e_mode, 04-CONTEXT.md D-04</read_first>
  <action>
    After `Store::open` in init_state and before returning AppState, when `!is_e2e_mode()`:
    ```rust
    if let Ok(n) = memory::migrate_legacy_memory_uris(store.as_ref()) {
        if n > 0 { eprintln!("migrated {n} legacy memory URIs to UUID"); }
    }
    ```
    Import memory::migrate_legacy_memory_uris at top if needed. Do NOT fail startup on migration error — map to log and continue (graceful degradation per project norms) OR propagate if test shows safe; prefer log + continue for user data paths.

    Do NOT move memory commands yet (Plan 02).
  </action>
  <verify>cargo build --manifest-path src-tauri/Cargo.toml</verify>
  <acceptance_criteria>
    - `rg "migrate_legacy_memory_uris" src-tauri/src/lib.rs` matches inside init_state path
    - `cargo build --manifest-path src-tauri/Cargo.toml` succeeds
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cargo test -p store` passes
- [ ] `cargo test -p memory` passes
- [ ] `cargo build --manifest-path src-tauri/Cargo.toml` succeeds
- [ ] New memories use UUID URIs (unit test)
- [ ] Legacy timestamp URIs migrate idempotently (integration test)
- [ ] Fuzzy resolve still works with deprecation log (unit test behavior)
</verification>

<success_criteria>
All must_haves truths satisfied; MEM-01/MEM-02 backend complete; no IPC or UI changes in this plan.
</success_criteria>
