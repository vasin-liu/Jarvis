---
phase: 03-library-tasks-keychain
plan: "04"
type: execute
wave: 4
depends_on:
  - "03-03"
files_modified:
  - src/types/tasks.ts
  - src/lib/tauri.ts
  - src/hooks/useTasks.ts
  - src/hooks/useTasks.test.ts
  - src/views/TasksView.tsx
  - src/App.tsx
  - e2e/specs/full-ui.spec.ts
requirements:
  - FE-03
  - FE-06
must_haves:
  truths:
    - "useTasks owns task domain IPC: refreshTasks (list_tasks), toggleTask/updateTaskStatus (update_task_status), deleteTask (delete_task) (D-09)"
    - "useTasks is a SEPARATE hook from useLibrary — they are not merged (D-09)"
    - "TasksView is a controlled component: App passes tasks + busy + on* handlers as props; TasksView/useTasks do not call invoke directly (D-08)"
    - "ALL Tasks JSX leaves App.tsx; the TasksView {children} passthrough stub is fully replaced (D-11)"
    - "Tasks testids preserved on moved JSX: task-list, task-list-items, toggle-task-{id} (FE-06/D-27)"
    - "Task IPC routes through src/lib/tauri.ts (listTasks/updateTaskStatus/deleteTask)"
    - "npm test green; npx tsc --noEmit clean; full-ui.spec.ts task-list assertions pass"
  artifacts:
    - path: src/hooks/useTasks.ts
      provides: useTasks hook owning task IPC state
      contains: "export function useTasks"
    - path: src/views/TasksView.tsx
      provides: full controlled Tasks view (replaces children stub)
      contains: "data-testid=\"task-list\""
    - path: src/types/tasks.ts
      provides: Task TS type
      contains: "export interface Task"
    - path: src/hooks/useTasks.test.ts
      provides: Vitest hook boundary test (mock invoke; refreshTasks sets state)
      contains: "useTasks"
  key_links:
    - from: src/App.tsx
      to: src/hooks/useTasks.ts
      via: "const tasksHook = useTasks({ onError: setErr })"
      pattern: "useTasks\\("
    - from: src/App.tsx
      to: src/views/TasksView.tsx
      via: "<TasksView ...controlled props />"
      pattern: "<TasksView"
    - from: src/views/TasksView.tsx
      to: src/lib/tauri.ts
      via: "task IPC wrappers (listTasks, updateTaskStatus, deleteTask)"
      pattern: "from \"../lib/tauri\""
---

<objective>
Extract the full Tasks view from the `App.tsx` monolith into `src/views/TasksView.tsx` + `src/hooks/useTasks.ts` + `src/types/tasks.ts`, route task IPC through `src/lib/tauri.ts`, and wire `App.tsx` as a controlled parent — preserving the `task-list` / `task-list-items` / `toggle-task-{id}` testids and gated by `full-ui.spec.ts`.

Purpose: Deliver FE-03 part 2 (Tasks view + dedicated `useTasks` hook, kept separate from `useLibrary` per D-09) and FE-06 (testid preservation) with no behavior regression.
Output: `types/tasks.ts`, `lib/tauri.ts` task wrappers, `useTasks.ts` (+ Vitest test), full `TasksView.tsx`, slimmed `App.tsx`, full-ui task-list coverage confirmed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-library-tasks-keychain/03-CONTEXT.md
@.planning/phases/03-library-tasks-keychain/03-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.cursor/rules/e2e-required.mdc
@.cursor/rules/frontend-taste.mdc
@src/App.tsx
@src/views/TasksView.tsx
@src/views/ChatView.tsx
@src/hooks/useChat.ts
@src/hooks/useLibrary.ts
@src/lib/tauri.ts
@src/types/ipc.ts
@e2e/specs/full-ui.spec.ts
@e2e/specs/navigation.spec.ts
</context>

<threat_model>
scope: frontend_extraction_only
asvs_level: L1
trust_boundaries:
  - "Tauri IPC boundary — invoke names unchanged, only wrapped in tauri.ts"
  - "DOM / E2E selector contract — task-list / task-list-items / toggle-task-{id} are the automation trust surface (FE-06)"
stride_register:
  - threat_id: T-03-D1
    category: Tampering
    component: Tasks data-testid selectors during JSX move
    disposition: mitigate
    mitigation_plan: "Move task-list, task-list-items, and per-task toggle-task-{id} onto the same DOM elements in TasksView. full-ui.spec.ts (line 21-22) re-verifies task-list after the move. No compat-shim duplicates added."
  - threat_id: T-03-D2
    category: Denial of Service
    component: useTasks refresh effect
    disposition: monitor
    mitigation_plan: "App destructures useTasks() as the single source of truth for tasks and keeps no own tasks useState copy, preventing duplicate state / refresh loops (mirror useChat/useLibrary pattern)."
disposition_summary: "Pure frontend move of the Tasks view. Selector stability is the only risk, E2E-gated by full-ui.spec.ts; no new IPC/network/auth surface."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: task types + tauri.ts task wrappers + useTasks hook (red-first) (03-04-01)</name>
  <files>src/types/tasks.ts, src/lib/tauri.ts, src/hooks/useTasks.ts, src/hooks/useTasks.test.ts</files>
  <read_first>src/App.tsx (task state + handlers: loadTasks ~189, handleToggleTask ~841, handleDeleteTask ~855, handleExtractTasks ~812 produces tasks), src/types/ipc.ts (TaskItem line 28-37), src/lib/tauri.ts (wrapper style), src/hooks/useChat.ts + src/hooks/useLibrary.ts (onError + useCallback pattern), .planning/phases/03-library-tasks-keychain/03-RESEARCH.md (§1 Controlled View, §8 Frontend IPC Types — listTasks/updateTaskStatus/deleteTask), 03-CONTEXT.md D-08/D-09/D-12</read_first>
  <action>
    Per D-12: create `src/types/tasks.ts` exporting `Task` (id, source_id?, source_title?, title, description?, status: "pending" | "done", created_at, updated_at — mirror `src/types/ipc.ts` TaskItem exactly; name it `Task` per Research §8). Per Claude's discretion, `ipc.ts` MAY re-export; do not break existing imports.

    Per §8/Don't-Hand-Roll: extend `src/lib/tauri.ts` with task wrappers for the EXISTING IPC names (no renames): `listTasks()`→`list_tasks` (returns `Task[]`), `updateTaskStatus(id, status)`→`update_task_status` with args `{ id, status }`, `deleteTask(id)`→`delete_task` with args `{ id }`. Match the exact arg keys used in App.tsx today.

    Per D-09 (TDD red-first per tdd-goal-driven.mdc — write `src/hooks/useTasks.test.ts` first): create `src/hooks/useTasks.ts` exporting `useTasks({ onError })`, a SEPARATE hook from useLibrary. It owns ONLY task state + actions: `tasks`, `refreshTasks`, `toggleTask(task)` (computes next status pending↔done and calls update_task_status then refreshes), `deleteTask(id)`. Errors via `onError` callback. `useTasks.test.ts` (Vitest) mocks the tauri.ts wrappers / `invoke`, asserts `refreshTasks` populates `tasks` state, and asserts `toggleTask` flips status via `updateTaskStatus`.
  </action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - `src/types/tasks.ts` exports `Task`
    - `src/lib/tauri.ts` exports `listTasks`, `updateTaskStatus`, `deleteTask`; no IPC string renamed (`list_tasks` / `update_task_status` / `delete_task`)
    - `src/hooks/useTasks.ts` exports `useTasks`; return includes `tasks`, `refreshTasks`, `toggleTask`, `deleteTask`
    - `useTasks` is independent of `useLibrary` (no import of one from the other; D-09)
    - `npm test` exits 0 and runs `useTasks.test.ts`; `npx tsc --noEmit` clean
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Full TasksView.tsx + App wiring + full-ui task coverage (03-04-02)</name>
  <files>src/views/TasksView.tsx, src/App.tsx, e2e/specs/full-ui.spec.ts</files>
  <read_first>src/App.tsx (Tasks JSX 1274-1348, task counts, view router), src/views/TasksView.tsx (children stub), src/views/LibraryView.tsx (controlled props reference from Slice C), e2e/specs/full-ui.spec.ts (lines 21-22 task-list), e2e/specs/navigation.spec.ts (line 14-15 task-list), .planning/phases/03-library-tasks-keychain/03-RESEARCH.md (§1 Controlled View, Pitfall P4), 03-CONTEXT.md D-08/D-10/D-11</read_first>
  <action>
    Replace the `{children}` passthrough in `src/views/TasksView.tsx` with the full Tasks JSX moved from `App.tsx` (lines 1274-1348). Per D-08 TasksView is CONTROLLED — props: `tasks`, `busy`, and handlers `onToggleTask`, `onDeleteTask`. Per D-11 ALL Tasks JSX leaves App.tsx. Preserve testids on moved elements (FE-06/D-27/Pitfall P4): `task-list` (the motion.section), `task-list-items` (the ul), and per-task `toggle-task-{id}`. Keep the count header (待办/已完成) and the empty-state copy; honor frontend-taste.mdc (loading/empty states + reduced-motion).

    In `src/App.tsx`:
    - Call `const tasksHook = useTasks({ onError: setErr })` and destructure; REMOVE App's own `tasks` useState and `loadTasks` loader (single source of truth via the hook). Keep `busy` in App.
    - If LibraryView (Slice C) needs task counts, pass `tasksHook.tasks` to it as the `tasks` prop (the hook is now the source for both).
    - Render `{view === "tasks" && <TasksView tasks={...} busy={...} onToggleTask={...} onDeleteTask={...} />}`.
    - Ensure the「提取任务」flow (handleExtractTasks in Library) still triggers `tasksHook.refreshTasks()` so newly extracted tasks appear — wire the refresh through the controlled handlers (do not duplicate task state).

    Per e2e-required.mdc: confirm `full-ui.spec.ts` (lines 21-22) and `navigation.spec.ts` (14-15) `task-list` assertions still hold after the move; extend `full-ui.spec.ts` only if a task interaction (toggle) needs explicit coverage — otherwise keep assertions intact (D-13: no new tasks.spec.ts). Rebuild before E2E: `npm run build && cargo build --release --manifest-path src-tauri/Cargo.toml`.
  </action>
  <verify>npm run test:e2e:local -- --spec e2e/specs/full-ui.spec.ts</verify>
  <acceptance_criteria>
    - `src/views/TasksView.tsx` no longer matches a `children` passthrough; it renders `data-testid="task-list"` and `data-testid="task-list-items"` and per-task `toggle-task-{id}`
    - `rg "task-list\"|task-list-items|toggle-task-" src/App.tsx` returns NO match (Tasks JSX fully moved, D-11)
    - `src/App.tsx` calls `useTasks(` and renders `<TasksView`; App declares no `useState` for `tasks` (single source of truth)
    - `full-ui.spec.ts` task-list assertions pass; no new `tasks.spec.ts` file created (D-13)
    - `npm run test:e2e:local -- --spec e2e/specs/full-ui.spec.ts` passes; `npx tsc --noEmit` clean
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `npm test` passes (useTasks unit test)
- [ ] `npx tsc --noEmit` clean
- [ ] `npm run test:e2e:local -- --spec e2e/specs/full-ui.spec.ts` green
- [ ] Tasks testids preserved on moved JSX (FE-06)
- [ ] useTasks separate from useLibrary (D-09)
- [ ] App.tsx holds no task state (single source of truth via useTasks)
- [ ] No new tasks.spec.ts (D-13)
</verification>

<success_criteria>
- TasksView + useTasks own all Tasks UI; App is a controlled parent (FE-03/D-08)
- All Tasks JSX out of App.tsx (D-11); two separate hooks (D-09)
- Task IPC flows through tauri.ts; npm test + tsc green (QA-03 partial)
- full-ui task-list coverage preserved (FE-06/D-13)
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Location |
|---------------|------|----------|
| `Task` | TS interface | `src/types/tasks.ts` |
| `listTasks` / `updateTaskStatus` / `deleteTask` | IPC wrappers | `src/lib/tauri.ts` |
| `useTasks` | React hook | `src/hooks/useTasks.ts` |
| `useTasks.test.ts` | Vitest unit test | `src/hooks/useTasks.test.ts` |
| `TasksView` (full, controlled) | React component | `src/views/TasksView.tsx` |

<output>
After completion, create `.planning/phases/03-library-tasks-keychain/03-04-SUMMARY.md`
</output>
