---
phase: 13-e2e-citation-trust
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/ChatView.tsx
  - src/views/SettingsView.tsx
  - src/hooks/useLibrary.ts
  - src/hooks/useLibrary.test.ts
  - e2e/helpers.ts
autonomous: true
requirements:
  - WIKI-08
  - WIKI-09
must_haves:
  truths:
    - "Citation buttons expose data-source-uri equal to citation.source_uri so E2E can distinguish wiki:// from fixture URIs (D-09)"
    - "citation-excerpt testid remains on the excerpt div unchanged so qa.spec stays valid (D-11 safety)"
    - "When window.__JARVIS_E2E_WIKI_EXPORT_PATH__ is set, exportWiki skips plugin-dialog save and calls exportWikiZip with that path (D-05)"
    - "When the E2E path is unset, exportWiki still uses save() then exportWikiZip (production UX unchanged)"
    - "Settings「保存配置」has data-testid settings-save-config for hybrid enable clicks (D-03 Discretion / UI-SPEC)"
    - "e2e/helpers exposes setWikiExportPath, wikiE2eZipPath (os.tmpdir + jarvis-e2e-wiki.zip), and assertWikiZipNonEmpty (D-06, D-07)"
  artifacts:
    - path: src/views/ChatView.tsx
      provides: "data-source-uri on citation button"
      contains: "data-source-uri"
    - path: src/hooks/useLibrary.ts
      provides: "exportWiki E2E dest-path bypass"
      contains: "__JARVIS_E2E_WIKI_EXPORT_PATH__"
    - path: src/hooks/useLibrary.test.ts
      provides: "forced-path skips save Vitest"
      contains: "__JARVIS_E2E_WIKI_EXPORT_PATH__"
    - path: src/views/SettingsView.tsx
      provides: "settings-save-config testid"
      contains: "settings-save-config"
    - path: e2e/helpers.ts
      provides: "setWikiExportPath + wikiE2eZipPath + assertWikiZipNonEmpty"
      contains: "setWikiExportPath"
  key_links:
    - from: citation button
      to: c.source_uri
      via: "data-source-uri attribute"
      pattern: "data-source-uri"
    - from: __JARVIS_E2E_WIKI_EXPORT_PATH__
      to: exportWikiZipCmd
      via: "exportWiki skips save when path set"
      pattern: "__JARVIS_E2E_WIKI_EXPORT_PATH__"
    - from: setWikiExportPath
      to: window.__JARVIS_E2E_WIKI_EXPORT_PATH__
      via: "browser.execute in helpers"
      pattern: "setWikiExportPath"
---

<objective>
Ship Phase 13 harness hooks for WIKI-08/WIKI-09: Chat citation `data-source-uri`, useLibrary Save-dialog bypass via `__JARVIS_E2E_WIKI_EXPORT_PATH__`, Settings save testid, and E2E helper APIs for zip path/size (D-05, D-06, D-07, D-09 + Discretion).

Purpose: Plan 02 wiki.spec can enable → export → assert citation URIs without native Save dialog or missing DOM attrs.
Output: FE hooks + Vitest for forced export path + e2e/helpers zip helpers. No wiki.spec.ts in this plan.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-e2e-citation-trust/13-CONTEXT.md
@.planning/phases/13-e2e-citation-trust/13-RESEARCH.md
@.planning/phases/13-e2e-citation-trust/13-PATTERNS.md
@.planning/phases/13-e2e-citation-trust/13-VALIDATION.md
@.planning/phases/13-e2e-citation-trust/13-UI-SPEC.md
@.planning/ROADMAP.md
@.cursor/rules/tdd-goal-driven.mdc
@.cursor/rules/e2e-required.mdc
@.cursor/rules/frontend-taste.mdc
@src/views/ChatView.tsx
@src/views/SettingsView.tsx
@src/hooks/useLibrary.ts
@src/hooks/useLibrary.test.ts
@e2e/helpers.ts

<interfaces>
Planner Discretion locked for execute (CONTEXT / UI-SPEC):

1. Export bypass: read `window.__JARVIS_E2E_WIKI_EXPORT_PATH__` inside `exportWiki` after preflight; if truthy string, use it as destPath and do not call `save()`; else keep existing `save({ defaultPath, filters })` flow (D-05). Honor whenever set (no e2eMode gate — RESEARCH A3 default). Keep preflight empty path, null cancel, onSuccess, reportError unchanged.
2. Citation attr: on the same `<button>` that calls `openCitation(c.source_uri)`, add `data-source-uri={c.source_uri}` only (D-09). Do not move/rename `citation-excerpt`. Do not add `data-testid="citation-source"` (UI-SPEC skip).
3. Settings: add `data-testid="settings-save-config"` on existing「保存配置」`<button>` — no class/copy change (UI-SPEC ASSUMED).
4. Helpers in `e2e/helpers.ts`:
   - `setWikiExportPath(destPath: string)` — `browser.execute` assigns window `__JARVIS_E2E_WIKI_EXPORT_PATH__`
   - `wikiE2eZipPath()` — `path.join(os.tmpdir(), "jarvis-e2e-wiki.zip")` (D-07)
   - `assertWikiZipNonEmpty(zipPath: string)` — `fs.statSync`; throw if missing or size <= 0 (D-06)
   Import Node `fs` / `os` / `path` at top of helpers only as needed for these exports.
5. No `wiki.spec.ts`, no `full-ui` edits, no `qa.spec.ts` edits, no Mock/e2e.rs changes in this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Vitest forced E2E export path skips save (13-01-01)</name>
  <files>src/hooks/useLibrary.test.ts</files>
  <read_first>
src/hooks/useLibrary.ts (exportWiki preflight→save→exportWikiZipCmd),
src/hooks/useLibrary.test.ts (existing exportWiki cases; dialog save mock),
.planning/phases/13-e2e-citation-trust/13-CONTEXT.md (D-05, D-06, D-07),
.planning/phases/13-e2e-citation-trust/13-PATTERNS.md (useLibrary.test forced-path case),
.planning/phases/13-e2e-citation-trust/13-VALIDATION.md (Wave 0 Vitest gap)
  </read_first>
  <behavior>
    - When window.__JARVIS_E2E_WIKI_EXPORT_PATH__ is set to a forced path and preflight hasNotes true: exportWikiZip is called with that forced path; save mock is never called; onSuccess receives the forced path
    - Clear/delete the window property in afterEach or finally so other exportWiki cases stay isolated
  </behavior>
  <action>
    Per D-05 TDD: extend useLibrary.test.ts with one new case that sets window.__JARVIS_E2E_WIKI_EXPORT_PATH__ to a forced dest string before act(exportWiki), mocks wikiExportPreflight hasNotes true, asserts save not called, exportWikiZip called with forced path, onSuccess called with forced path. Ensure cleanup of the window property after the case.

    Confirm focused Vitest is RED on the new case before Task 2 (production exportWiki still always calls save). Do not edit useLibrary.ts, ChatView, SettingsView, or e2e/helpers in this task.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useLibrary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - useLibrary.test.ts contains a test that sets __JARVIS_E2E_WIKI_EXPORT_PATH__
    - That test expects save not to have been called when the forced path is set
    - npx vitest run src/hooks/useLibrary.test.ts exits non-zero until Task 2 lands (RED on new case)
  </acceptance_criteria>
  <done>Failing Vitest proves forced-path bypass contract</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — exportWiki bypass + citation URI attr + settings-save-config (13-01-02)</name>
  <files>src/hooks/useLibrary.ts, src/views/ChatView.tsx, src/views/SettingsView.tsx, src/hooks/useLibrary.test.ts</files>
  <read_first>
src/hooks/useLibrary.ts (exportWiki body lines ~154–175),
src/views/ChatView.tsx (citation button + citation-excerpt block),
src/views/SettingsView.tsx (保存配置 button near end; lark-sync-submit testid peer pattern),
.planning/phases/13-e2e-citation-trust/13-CONTEXT.md (D-05, D-09, D-11),
.planning/phases/13-e2e-citation-trust/13-UI-SPEC.md (locked testability attributes),
.planning/phases/13-e2e-citation-trust/13-PATTERNS.md (ChatView + SettingsView + exportWiki prescriptions)
  </read_first>
  <behavior>
    - Forced-path Vitest case from Task 1 passes
    - Existing exportWiki empty/cancel/save-happy/error cases still pass
  </behavior>
  <action>
    Per D-05: in useLibrary.exportWiki, after successful preflight, read window.__JARVIS_E2E_WIKI_EXPORT_PATH__; if set use as destPath without calling save; otherwise call save as today; if destPath is null or undefined return; then exportWikiZipCmd + onSuccess. Keep empty-preflight Chinese string and catch reportError.

    Per D-09 / UI-SPEC: on ChatView citation source button only, add data-source-uri={c.source_uri}. Do not change button classes, openCitation handler, or citation-excerpt testid/placement (D-11).

    Per UI-SPEC Discretion: add data-testid="settings-save-config" on SettingsView「保存配置」button; no visual/copy changes.

    Do not edit qa.spec.ts, wiki.spec.ts, MockChatModel, or e2e.rs. No optional citation-source testid.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useLibrary.test.ts && rg -n "data-source-uri" src/views/ChatView.tsx && rg -n "settings-save-config" src/views/SettingsView.tsx && rg -n "__JARVIS_E2E_WIKI_EXPORT_PATH__" src/hooks/useLibrary.ts</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "__JARVIS_E2E_WIKI_EXPORT_PATH__" src/hooks/useLibrary.ts matches
    - rg -n "data-source-uri" src/views/ChatView.tsx matches
    - citation-excerpt still appears on a div (not moved onto the button) in ChatView.tsx
    - rg -n "settings-save-config" src/views/SettingsView.tsx matches
    - npx vitest run src/hooks/useLibrary.test.ts exits 0
  </acceptance_criteria>
  <done>Harness FE hooks green; Vitest forced-path + regression cases pass</done>
</task>

<task type="auto">
  <name>Task 3: E2E helpers for wiki export path and zip size (13-01-03)</name>
  <files>e2e/helpers.ts</files>
  <read_first>
e2e/helpers.ts (setOrchestrationMode window-hook pattern; existing exports),
.planning/phases/13-e2e-citation-trust/13-CONTEXT.md (D-05, D-06, D-07),
.planning/phases/13-e2e-citation-trust/13-PATTERNS.md (helpers setWikiExportPath / wikiE2eZipPath / assertWikiZipNonEmpty),
.planning/phases/13-e2e-citation-trust/13-RESEARCH.md (Pattern 1 window hook)
  </read_first>
  <action>
    Per D-05/D-06/D-07 Discretion: add to e2e/helpers.ts — setWikiExportPath(destPath) via browser.execute assigning window.__JARVIS_E2E_WIKI_EXPORT_PATH__; wikiE2eZipPath() returning path.join(os.tmpdir(), "jarvis-e2e-wiki.zip"); assertWikiZipNonEmpty(zipPath) using fs.statSync that throws when file missing or size is not greater than 0. Add Node builtin imports as required. Do not create wiki.spec.ts here. Do not change askQuestion / setReactCheckbox / clickViaDom behavior.
  </action>
  <verify>
    <automated>rg -n "setWikiExportPath|wikiE2eZipPath|assertWikiZipNonEmpty" e2e/helpers.ts && rg -n "jarvis-e2e-wiki.zip" e2e/helpers.ts && rg -n "__JARVIS_E2E_WIKI_EXPORT_PATH__" e2e/helpers.ts</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "export async function setWikiExportPath" e2e/helpers.ts matches
    - rg -n "export function wikiE2eZipPath" e2e/helpers.ts matches OR rg -n "wikiE2eZipPath" e2e/helpers.ts matches an exported function
    - rg -n "assertWikiZipNonEmpty" e2e/helpers.ts matches
    - rg -n "jarvis-e2e-wiki.zip" e2e/helpers.ts matches
    - rg -n "__JARVIS_E2E_WIKI_EXPORT_PATH__" e2e/helpers.ts matches
  </acceptance_criteria>
  <done>Helpers ready for Plan 02 wiki.spec export + size asserts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Webview window → useLibrary.exportWiki | Untrusted script could set `__JARVIS_E2E_WIKI_EXPORT_PATH__` and skip Save dialog |
| Citation payload → DOM attribute | `source_uri` from store/RAG rendered as `data-source-uri` |
| Settings save click → set_config | E2E uses new testid; same IPC as production |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | `__JARVIS_E2E_WIKI_EXPORT_PATH__` → exportWiki | medium | mitigate | Bypass only when path string set; keep preflight + real `export_wiki_zip` (wiki.enabled + wiki_root gates unchanged); production Save path unchanged when unset — same trust model as other `__JARVIS_E2E_*` hooks |
| T-13-02 | Tampering | dest write path | low | mitigate | Plan 02 / helpers fix dest to `os.tmpdir()/jarvis-e2e-wiki.zip` only; backend still writes only caller-chosen path |
| T-13-03 | XSS / Tampering | `data-source-uri` on citation button | low | mitigate | Render via React attribute binding from app-controlled `c.source_uri`; no dangerouslySetInnerHTML; do not put URI into innerHTML |
| T-13-04 | Elevation of Privilege | export when wiki disabled | high | mitigate | Do not remove Library hide-when-disabled or crate WikiDisabled gate; this plan only adds path bypass after preflight |
| T-13-SC | Tampering | npm/cargo installs | low | accept | Zero new packages this phase — N/A Package Legitimacy Audit |
</threat_model>

<verification>
- Vitest useLibrary: forced path skips save; prior exportWiki cases green
- ChatView has data-source-uri; citation-excerpt unchanged
- Settings has settings-save-config
- e2e/helpers exports setWikiExportPath, wikiE2eZipPath, assertWikiZipNonEmpty
</verification>

<success_criteria>
Harness hooks for D-05/D-07/D-09 exist and Vitest proves Save bypass. Plan 02 can drive export + citation URI asserts.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Symbols / notes | Requirement |
|----------|------|-----------------|-------------|
| Citation URI attr | `src/views/ChatView.tsx` | `data-source-uri={c.source_uri}` on citation `<button>` | WIKI-08 (D-09) |
| Export path bypass | `src/hooks/useLibrary.ts` | `exportWiki` reads `__JARVIS_E2E_WIKI_EXPORT_PATH__` | WIKI-09 (D-05) |
| Forced-path Vitest | `src/hooks/useLibrary.test.ts` | case: save not called when path set | WIKI-09 (D-05) |
| Settings save testid | `src/views/SettingsView.tsx` | `settings-save-config` | WIKI-09 (D-03) |
| E2E helpers | `e2e/helpers.ts` | `setWikiExportPath`, `wikiE2eZipPath`, `assertWikiZipNonEmpty` | WIKI-09 (D-06, D-07) |

<output>
Create `.planning/phases/13-e2e-citation-trust/13-01-SUMMARY.md` when done
</output>
