---
phase: 09-llm-wiki-analysis
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - crates/insights/Cargo.toml
  - crates/insights/src/error.rs
  - crates/insights/src/wiki.rs
  - crates/insights/src/lib.rs
autonomous: true
requirements:
  - WIKI-05
  - WIKI-03
must_haves:
  truths:
    - "Fence-wrapped or chatter-prefixed JSON still extracts the first brace object; truncated open brace fails InvalidWikiJson (D-09)"
    - "Missing or wrong-typed WikiAnalysis fields fail InvalidWikiJson; empty summary + empty arrays present → Ok (D-10, D-11)"
    - "write_wiki_pages_to_dir writes render_wiki_pages tree (sources/entities/concepts pages + index.md) under wiki_root using draft.slug paths only (D-13)"
    - "Callers analyze first; write only on Ok — parse failure never starts a write (D-13; WIKI-05)"
    - "Three FixedReplyChat fixtures (prose, truncated brace, schema-invalid) each assert InvalidWikiJson and zero files under tempfile wiki root (D-04, D-14; ROADMAP criteria 2–3)"
  artifacts:
    - path: crates/insights/src/wiki.rs
      provides: "write_wiki_pages_to_dir + parse strictness + fail-closed tests"
      contains: "write_wiki_pages_to_dir"
    - path: crates/insights/Cargo.toml
      provides: "tempfile = 3 dev-dependency"
      contains: "tempfile"
    - path: crates/insights/src/lib.rs
      provides: "pub re-export write_wiki_pages_to_dir"
      contains: "write_wiki_pages_to_dir"
    - path: crates/insights/src/error.rs
      provides: "optional InsightsError::Io for writer"
      contains: "InvalidWikiJson"
  key_links:
    - from: analyze_source_for_wiki Err
      to: write_wiki_pages_to_dir
      via: "tests call writer only inside Ok arm — fail-closed gate"
      pattern: "InvalidWikiJson"
    - from: write_wiki_pages_to_dir
      to: WikiCompileResult.pages[].slug
      via: "join wiki_root with slug.md — never entity.name raw"
      pattern: "index.md"
    - from: FixedReplyChat
      to: D-14 fixtures
      via: "garbage ChatModel double; production Mock stays happy-path (D-04)"
      pattern: "FixedReplyChat"
---

<objective>
Complete WIKI-05 fail-closed proof and remaining WIKI-03 parse/write surface: harden `parse_wiki_analysis` for D-09…D-11, ship public `write_wiki_pages_to_dir` (D-13), and lock three garbage fixtures that return `InvalidWikiJson` with **zero** files under a tempfile wiki root (D-04, D-14; ROADMAP criteria 2–3).

Purpose: Bad model output must never start a wiki write before Phase 10 persists under app_data.
Output: tempfile dev-dep; write helper + re-export; parse edge-case tests; three fail-closed zero-file tests green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-llm-wiki-analysis/09-CONTEXT.md
@.planning/phases/09-llm-wiki-analysis/09-RESEARCH.md
@.planning/phases/09-llm-wiki-analysis/09-PATTERNS.md
@.planning/phases/09-llm-wiki-analysis/09-VALIDATION.md
@.planning/phases/09-llm-wiki-analysis/09-01-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/superpowers/plans/2026-07-16-wiki-compile-layer.md
@.cursor/rules/tdd-goal-driven.mdc
@crates/insights/src/wiki.rs
@crates/insights/src/error.rs
@crates/insights/src/lib.rs
@crates/insights/Cargo.toml
@crates/agent/src/run.rs
@crates/llm/src/lib.rs

<interfaces>
From Plan 09-01 (must exist):
- analyze_source_for_wiki, parse_wiki_analysis, InvalidWikiJson, Mock 「笔记编译」 branch

This plan adds:
- pub fn write_wiki_pages_to_dir(compiled: &WikiCompileResult, wiki_root: &Path) -> Result&lt;()&gt; (name discretionary if D-13 holds)
- Optional InsightsError::Io(#[from] std::io::Error) for writer (RESEARCH discretion)
- FixedReplyChat (or equivalent) test-only ChatModel double (D-04) — adapt FenceReplyModel from crates/agent/src/run.rs
- tempfile = "3" under insights [dev-dependencies] (Package Legitimacy Audit: OK)

Fail-closed orchestration (tests / Phase 10 — no public combined analyze_then_write helper):
match analyze… { Ok(a) => write_wiki_pages_to_dir(&render_wiki_pages(…), &wiki), Err(InvalidWikiJson) => assert zero files }

Do not index WikiPage, embed, or write under real app_data (Phase 10).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse strictness D-09…D-11 (09-02-01)</name>
  <files>crates/insights/src/wiki.rs</files>
  <read_first>
crates/insights/src/wiki.rs (parse_wiki_analysis from 09-01),
crates/insights/src/tasks.rs (fence-strip baseline only),
.planning/phases/09-llm-wiki-analysis/09-CONTEXT.md (D-09, D-10, D-11),
.planning/phases/09-llm-wiki-analysis/09-RESEARCH.md (Pattern 2 brace scanner),
.planning/phases/09-llm-wiki-analysis/09-VALIDATION.md (parse_wiki_allows_empty_struct)
  </read_first>
  <behavior>
    - parse_wiki_allows_empty_struct: JSON with empty string summary and empty entities/concepts arrays → Ok (D-11)
    - parse_wiki_accepts_fenced_or_chatter: fence-wrapped or preamble + object → Ok when object is structurally valid (D-09)
    - parse_wiki_rejects_truncated_object: truncated open-brace payload → Err(InvalidWikiJson) (D-09 / D-14 fixture b half)
    - parse_wiki_rejects_schema: object missing required field or wrong type → Err(InvalidWikiJson) (D-10 / D-14 fixture c half)
    - WikiAnalysis fields remain required — do not add serde default on summary/entities/concepts (D-10)
  </behavior>
  <action>
    RED per D-09, D-10, D-11: add unit tests `parse_wiki_allows_empty_struct`, `parse_wiki_accepts_fenced_or_chatter`, `parse_wiki_rejects_truncated_object`, and `parse_wiki_rejects_schema`. Call private `parse_wiki_analysis` (pub(crate) only if needed). Confirm targeted failures where 09-01 parser is incomplete.

    GREEN: refine `extract_first_json_object` / `parse_wiki_analysis` — strip markdown fences, then brace-depth scan from first `{` with string-aware skip of braces inside JSON strings (D-09). Strict `serde_json::from_str::&lt;WikiAnalysis&gt;` with no field defaults (D-10). Structurally valid empty analysis is Ok (D-11). Map all extract/serde errors to `InvalidWikiJson` with reason string (D-12).

    Do not add write helper or tempfile in this task. Do not change production MockChatModel.
  </action>
  <verify>
    <automated>cargo test -p insights parse_wiki_allows_empty_struct parse_wiki_accepts_fenced_or_chatter parse_wiki_rejects_truncated_object parse_wiki_rejects_schema -- --exact --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "fn extract_first_json_object|fn parse_wiki_analysis" crates/insights/src/wiki.rs` matches both
    - `cargo test -p insights parse_wiki_allows_empty_struct parse_wiki_accepts_fenced_or_chatter parse_wiki_rejects_truncated_object parse_wiki_rejects_schema -- --exact --test-threads=1` exits 0
    - `rg -n "serde\\(default\\)" crates/insights/src/wiki.rs` does not match on WikiAnalysis / WikiEntity / WikiConcept field derives (D-10)
  </acceptance_criteria>
  <done>Parse strictness locked: empty structural Ok; fence/first-object extract; truncated/schema → InvalidWikiJson</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: write_wiki_pages_to_dir + tempfile (09-02-02)</name>
  <files>crates/insights/Cargo.toml, crates/insights/src/error.rs, crates/insights/src/wiki.rs, crates/insights/src/lib.rs</files>
  <read_first>
crates/insights/Cargo.toml,
crates/insights/src/wiki.rs (render_wiki_pages, WikiCompileResult),
crates/config/src/file.rs (create_dir_all + fs::write pattern),
crates/ingest/src/error.rs (Io #[from] analog),
.planning/phases/09-llm-wiki-analysis/09-CONTEXT.md (D-13),
.planning/phases/09-llm-wiki-analysis/09-RESEARCH.md (Package Legitimacy Audit tempfile OK; Open Q3 happy write),
.planning/phases/09-llm-wiki-analysis/09-VALIDATION.md (write_wiki_pages_to_dir_writes_tree)
  </read_first>
  <behavior>
    - write_wiki_pages_to_dir_writes_tree: render a non-empty WikiAnalysis → write under tempfile wiki root → index.md exists and at least one sources/*.md path exists (D-13)
    - Writer creates parent directories; paths come only from WikiPageDraft.slug + index.md — never raw entity/concept names (D-13; T-09-05)
    - tempfile = "3" present under insights [dev-dependencies] only
  </behavior>
  <action>
    RED per D-13: add `tempfile = "3"` to `crates/insights/Cargo.toml` `[dev-dependencies]` (approved OK in RESEARCH Package Legitimacy Audit — no human legitimacy checkpoint). Add test `write_wiki_pages_to_dir_writes_tree` that builds analysis → `render_wiki_pages` → write → asserts `index.md` and source summary file under tempfile. Confirm fail until helper exists.

    GREEN: implement `pub fn write_wiki_pages_to_dir(compiled: &WikiCompileResult, wiki_root: &Path) -> Result&lt;()&gt;` — create_dir_all root; for each page write `{slug}.md` with parent dirs; write `index.md` from `compiled.index_markdown` (D-13). Prefer adding `InsightsError::Io(#[from] std::io::Error)` (discretion). Re-export from `lib.rs`. Do not embed, index WikiPage, or call analyze inside the writer. Do not introduce a public combined analyze_then_write API (RESEARCH Open Q1).
  </action>
  <verify>
    <automated>cargo test -p insights write_wiki_pages_to_dir_writes_tree -- --exact --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "tempfile = \\"3\\"" crates/insights/Cargo.toml` matches under [dev-dependencies]
    - `rg -n "pub fn write_wiki_pages_to_dir" crates/insights/src/wiki.rs` matches
    - `rg -n "write_wiki_pages_to_dir" crates/insights/src/lib.rs` matches pub use
    - `cargo test -p insights write_wiki_pages_to_dir_writes_tree -- --exact --test-threads=1` exits 0
  </acceptance_criteria>
  <done>Public directory writer + tempfile; happy-path tree includes index.md</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-14 fail-closed zero-file fixtures (09-02-03)</name>
  <files>crates/insights/src/wiki.rs</files>
  <read_first>
crates/insights/src/wiki.rs (analyze + write from prior tasks),
crates/agent/src/run.rs (FenceReplyModel ChatModel test double),
crates/llm/src/lib.rs (ChatModel trait),
.planning/phases/09-llm-wiki-analysis/09-CONTEXT.md (D-04, D-13, D-14),
.planning/phases/09-llm-wiki-analysis/09-RESEARCH.md (Pattern 3 fail-closed gate; Pattern 5 FixedReplyChat),
.planning/phases/09-llm-wiki-analysis/09-VALIDATION.md (wiki_parse_fail_*_writes_zero_files)
  </read_first>
  <behavior>
    - wiki_parse_fail_prose_writes_zero_files: FixedReplyChat prose/non-JSON → InvalidWikiJson; wiki root nonexistent or zero files (D-14 a)
    - wiki_parse_fail_truncated_writes_zero_files: FixedReplyChat truncated `{…` → InvalidWikiJson; zero files (D-14 b)
    - wiki_parse_fail_schema_writes_zero_files: FixedReplyChat object with missing/wrong-typed fields → InvalidWikiJson; zero files (D-14 c)
    - Production MockChatModel is not modified to return garbage (D-04)
  </behavior>
  <action>
    RED per D-04, D-14 and ROADMAP criteria 2–3: add test-only `FixedReplyChat` (or equivalent) implementing `ChatModel` with fixed reply string (adapt agent FenceReplyModel). Add three `#[tokio::test]` functions named `wiki_parse_fail_prose_writes_zero_files`, `wiki_parse_fail_truncated_writes_zero_files`, `wiki_parse_fail_schema_writes_zero_files`. Each: sample_indexed store; tempfile wiki path; call `analyze_source_for_wiki` with garbage double; on Err assert `matches!(…, InvalidWikiJson(_))` and that the wiki root has zero files (do not call `write_wiki_pages_to_dir` on Err — gate at call site per D-13). Confirm RED then GREEN.

    GREEN: ensure analyze→parse path returns InvalidWikiJson for all three fixtures (parser from Task 1). Keep fail-closed gate explicit in tests — write only inside Ok arm. Do not change production `MockChatModel` to emit garbage (D-04). No indexer/embedder/app_data paths.

    Final wave gate: `cargo test -p insights -p llm -- --test-threads=1` green.
  </action>
  <verify>
    <automated>cargo test -p insights wiki_parse_fail_prose_writes_zero_files wiki_parse_fail_truncated_writes_zero_files wiki_parse_fail_schema_writes_zero_files -- --exact --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "wiki_parse_fail_prose_writes_zero_files|wiki_parse_fail_truncated_writes_zero_files|wiki_parse_fail_schema_writes_zero_files" crates/insights/src/wiki.rs` matches three tests
    - `cargo test -p insights wiki_parse_fail_prose_writes_zero_files wiki_parse_fail_truncated_writes_zero_files wiki_parse_fail_schema_writes_zero_files -- --exact --test-threads=1` exits 0
    - `cargo test -p insights -p llm -- --test-threads=1` exits 0
    - ROADMAP success criteria 2–3 covered: typed parse error + zero files on parse failure
  </acceptance_criteria>
  <done>WIKI-05 proven: three garbage fixtures → InvalidWikiJson + zero files under tempfile wiki root</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted LLM text → FS | Parse must complete before any create_dir_all / write |
| WikiCompileResult.slug → filesystem paths | Only Phase 08 slugified paths may be joined under wiki_root |
| Test doubles → production Mock | Garbage replies stay in insights tests only (D-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05 | Tampering | write_wiki_pages_to_dir | high | mitigate | Join only WikiPageDraft.slug + fixed index.md; never Path::new(entity.name); unit test write_wiki_pages_to_dir_writes_tree (D-13) |
| T-09-06 | Tampering / Denial | analyze→write call site | high | mitigate | Fail-closed gate: write only after Ok(WikiAnalysis); D-14 three fixtures assert InvalidWikiJson + zero files (WIKI-05; ROADMAP #3) |
| T-09-07 | Tampering | parse_wiki_analysis | high | mitigate | Fence + first-object + strict serde; no #[serde(default)] on WikiAnalysis; truncated/schema fixtures (D-09, D-10, D-14) |
| T-09-08 | Elevation of Privilege | path traversal via slugs | medium | mitigate | Slugs already filtered in Phase 08; writer must not accept raw analysis names |
| T-09-09 | Information Disclosure | live LLM in CI | high | mitigate | Tests use MockChatModel / FixedReplyChat only; no network providers in unit tests (Pitfall 9) |
| T-09-SC | Tampering | tempfile crates.io install | low | accept | RESEARCH Package Legitimacy Audit verdict OK for tempfile; pin "3" as insights dev-dep only |
</threat_model>

<verification>
- `cargo test -p insights parse_wiki_allows_empty_struct parse_wiki_accepts_fenced_or_chatter parse_wiki_rejects_truncated_object parse_wiki_rejects_schema -- --exact --test-threads=1` green
- `cargo test -p insights write_wiki_pages_to_dir_writes_tree -- --exact --test-threads=1` green
- `cargo test -p insights wiki_parse_fail_prose_writes_zero_files wiki_parse_fail_truncated_writes_zero_files wiki_parse_fail_schema_writes_zero_files -- --exact --test-threads=1` green
- Phase gate: `cargo test -p insights -p llm -- --test-threads=1` green
- No E2E (Phase 13 owns wiki journey)
</verification>

<success_criteria>
WIKI-05 + remaining WIKI-03: invalid/non-JSON model output → InvalidWikiJson; parse failure writes zero files under tempfile wiki root; write helper ships for Phase 10; ROADMAP criteria 2–3 met (D-04, D-09…D-14).
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Requirement |
|----------|------|-------------|
| `write_wiki_pages_to_dir` | `crates/insights/src/wiki.rs` | WIKI-05 / D-13 |
| Re-export `write_wiki_pages_to_dir` | `crates/insights/src/lib.rs` | WIKI-05 |
| `tempfile = "3"` dev-dep | `crates/insights/Cargo.toml` | WIKI-05 / D-14 |
| `InsightsError::Io` (optional) | `crates/insights/src/error.rs` | WIKI-05 / D-13 |
| Parse strictness tests (`parse_wiki_allows_empty_struct`, …) | `crates/insights/src/wiki.rs` | WIKI-03 / D-09…D-11 |
| `FixedReplyChat` + three `wiki_parse_fail_*_writes_zero_files` | `crates/insights/src/wiki.rs` tests | WIKI-05 / D-04, D-14; ROADMAP #2–3 |
| `write_wiki_pages_to_dir_writes_tree` | `crates/insights/src/wiki.rs` tests | WIKI-05 / D-13 |

<output>
Create `.planning/phases/09-llm-wiki-analysis/09-02-SUMMARY.md` when done
</output>
