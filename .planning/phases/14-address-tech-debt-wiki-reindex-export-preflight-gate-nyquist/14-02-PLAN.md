---
phase: 14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/insights/src/wiki_export.rs
  - crates/insights/src/lib.rs
  - src-tauri/src/commands/wiki.rs
autonomous: true
requirements:
  - AUDIT-CLOSEOUT
must_haves:
  truths:
    - "When wiki_enabled is false, preflight returns InsightsError::WikiDisabled before scanning notes (D-06)"
    - "Disabled preflight Display/to_string equals the same wiki-disabled message as export_wiki_zip (D-07)"
    - "wiki_export_preflight_cmd reads cfg.wiki.enabled, calls crate helper, map_err to String at Tauri boundary (D-06, D-07)"
    - "FE keeps existing catch → reportError; no Chinese remapping and no useLibrary edits (D-08)"
    - "Rust unit only for disabled → WikiDisabled; no new Vitest; no new E2E (D-09, D-15)"
  artifacts:
    - path: crates/insights/src/wiki_export.rs
      provides: "wiki_export_preflight(wiki_root, wiki_enabled) -> Result<bool>"
      contains: "wiki_export_preflight"
    - path: crates/insights/src/lib.rs
      provides: "re-export wiki_export_preflight"
      contains: "wiki_export_preflight"
    - path: src-tauri/src/commands/wiki.rs
      provides: "gated wiki_export_preflight_cmd"
      contains: "wiki_export_preflight"
  key_links:
    - from: wiki_export_preflight wiki_enabled=false
      to: InsightsError::WikiDisabled
      via: "same gate pattern as export_wiki_zip (D-06, D-07)"
      pattern: "WikiDisabled"
    - from: wiki_export_preflight_cmd
      to: wiki_export_preflight(&wiki_root, cfg.wiki.enabled)
      via: "thin cmd + map_err(|e| e.to_string())"
      pattern: "cfg.wiki.enabled"
    - from: useLibrary.exportWiki catch
      to: reportError(error)
      via: "unchanged FE path (D-08)"
      pattern: "reportError"
  prohibitions:
    - "Do not invent a new error variant or alternate English/Chinese string for disabled preflight"
    - "Do not edit useLibrary.ts / LibraryView for this plan (D-08)"
    - "Do not add Vitest or E2E for preflight disabled path (D-09, D-15)"
    - "Do not implement WIKI-F01 or dual index.md writers"
---

<objective>
Close audit item 2: gate `wiki_export_preflight` on `wiki.enabled` with `InsightsError::WikiDisabled` parity to `export_wiki_zip` (D-06..D-09).

Purpose: IPC defense-in-depth when UI hide is bypassed; same error string FE already surfaces via reportError.
Output: Crate helper + unit test + thin Tauri cmd wiring. No FE changes. No new E2E (D-15 waiver).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist/14-CONTEXT.md
@.planning/phases/14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist/14-RESEARCH.md
@.planning/phases/14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist/14-PATTERNS.md
@.planning/phases/14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist/14-VALIDATION.md
@.planning/ROADMAP.md
@.cursor/rules/tdd-goal-driven.mdc
@.cursor/rules/e2e-required.mdc
@.cursor/rules/karpathy-guidelines.mdc
@crates/insights/src/wiki_export.rs
@crates/insights/src/error.rs
@crates/insights/src/lib.rs
@src-tauri/src/commands/wiki.rs

<interfaces>
Planner Discretion locked for D-09 testability (CONTEXT allows cmd-only OR crate helper — prefer helper):

1. Add `pub fn wiki_export_preflight(wiki_root: &Path, wiki_enabled: bool) -> Result<bool>` in `wiki_export.rs`:
   - if `!wiki_enabled` → `Err(InsightsError::WikiDisabled)` (reuse existing variant; Display already is the IPC contract string)
   - else → `Ok(wiki_has_exportable_notes(wiki_root))`
2. Do not change `InsightsError` variants or Display text.
3. Re-export from `crates/insights/src/lib.rs` beside existing wiki_export exports.
4. `wiki_export_preflight_cmd`: read `state.config().wiki.enabled`, call helper, map to `WikiExportPreflight { has_notes }` via `.map_err(|e| e.to_string())`. Keep DTO camelCase unchanged.
5. FE: zero edits — existing catch → reportError is enough (D-08).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN — wiki_export_preflight crate helper + disabled unit test (14-02-01)</name>
  <files>crates/insights/src/wiki_export.rs, crates/insights/src/lib.rs</files>
  <read_first>
crates/insights/src/wiki_export.rs (export_wiki_zip enabled gate ~37–43; wiki_has_exportable_notes; export_wiki_rejects_when_disabled test ~270–281),
crates/insights/src/error.rs (WikiDisabled Display),
crates/insights/src/lib.rs (wiki_export re-exports),
.planning/phases/14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist/14-CONTEXT.md (D-06, D-07, D-09),
.planning/phases/14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist/14-RESEARCH.md (Pitfall 3; recommended helper),
.planning/phases/14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist/14-PATTERNS.md (prescribed helper + matches! WikiDisabled)
  </read_first>
  <behavior>
    - wiki_export_preflight(root, false) returns Err matching InsightsError::WikiDisabled even when notes .md exist under sources/
    - err.to_string() equals the same Display string used by export_wiki_zip disabled path (WikiDisabled)
    - wiki_export_preflight(root, true) with exportable notes returns Ok(true); without notes Ok(false)
  </behavior>
  <action>
    TDD per D-06/D-07/D-09: add unit test beside export_wiki_rejects_when_disabled that calls wiki_export_preflight with enabled=false and notes present; assert matches!(WikiDisabled) and to_string parity with WikiDisabled Display. Confirm RED. Implement helper: gate then wiki_has_exportable_notes. Re-export from lib.rs. Do not edit error.rs Display. Do not touch Tauri/FE in this task.
  </action>
  <verify>
    <automated>cargo test -p insights wiki_export -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "pub fn wiki_export_preflight" crates/insights/src/wiki_export.rs matches
    - rg -n "wiki_export_preflight" crates/insights/src/lib.rs matches
    - Unit test asserts matches!(err, InsightsError::WikiDisabled) for enabled=false
    - cargo test -p insights wiki_export exits 0
  </acceptance_criteria>
  <done>Crate preflight helper rejects disabled with WikiDisabled; tests green (D-06, D-07, D-09)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire wiki_export_preflight_cmd enabled gate (14-02-02)</name>
  <files>src-tauri/src/commands/wiki.rs</files>
  <read_first>
src-tauri/src/commands/wiki.rs (wiki_export_preflight_cmd ~44–50; export_wiki_zip_cmd gated pattern ~54–65; compile_wiki_cmd enabled pass-through),
crates/insights/src/wiki_export.rs (Task 1 helper),
.planning/phases/14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist/14-CONTEXT.md (D-06, D-07, D-08),
.planning/phases/14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist/14-PATTERNS.md (thin cmd)
  </read_first>
  <behavior>
    - wiki_export_preflight_cmd passes cfg.wiki.enabled into wiki_export_preflight
    - On WikiDisabled, cmd returns Err(String) whose content is the WikiDisabled Display string
    - On success, still returns WikiExportPreflight { has_notes } camelCase JSON shape unchanged
  </behavior>
  <action>
    Update imports to use `wiki_export_preflight` (replace direct wiki_has_exportable_notes call in preflight cmd). Read `let cfg = state.config();`, call `wiki_export_preflight(&wiki_root, cfg.wiki.enabled).map_err(|e| e.to_string())?`, map bool into `WikiExportPreflight { has_notes }`. Keep cmd thin — no business logic duplication. Do not edit useLibrary.ts (D-08). Do not add Vitest/E2E (D-09, D-15 waiver).
  </action>
  <verify>
    <automated>cargo test -p insights wiki_export -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "wiki_export_preflight" src-tauri/src/commands/wiki.rs matches
    - rg -n "cfg.wiki.enabled" src-tauri/src/commands/wiki.rs matches inside wiki_export_preflight_cmd path (same pattern as export cmd)
    - wiki_export_preflight_cmd no longer calls wiki_has_exportable_notes directly
    - cargo check -p tauri-app exits 0 (or cargo test -p insights wiki_export still green if check already implied)
  </acceptance_criteria>
  <done>Preflight IPC gated with WikiDisabled parity to export (D-06, D-07); FE untouched (D-08)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FE / IPC → wiki_export_preflight_cmd | Client can invoke preflight even when export button hidden |
| wiki.enabled config → crate gate | Feature flag must hard-reject; UI hide is UX-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03 | Elevation of Privilege / Tampering | wiki_export_preflight_cmd IPC bypass | high | mitigate | Hard WikiDisabled when !cfg.wiki.enabled before notes scan (D-06); unit test disabled path (D-09) |
| T-14-04 | Information Disclosure | disabled error string | low | accept | Reuse existing English WikiDisabled Display; no new secrets (D-07, D-08) |
| T-14-SC | Tampering | npm/cargo installs | low | accept | No new packages this plan |
</threat_model>

<verification>
- `cargo test -p insights wiki_export` green
- `cargo check -p tauri-app` (or build) confirms cmd compiles
- No Vitest/FE/E2E required this plan (D-09, D-15)
</verification>

<success_criteria>
ROADMAP criterion 2: wiki_export_preflight rejects when wiki.enabled is false with WikiDisabled parity. D-06..D-09 covered.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Symbols / notes | Decision |
|----------|------|-----------------|----------|
| Preflight helper | `crates/insights/src/wiki_export.rs` | `wiki_export_preflight` → Result bool | D-06, D-07 |
| Unit test | `crates/insights/src/wiki_export.rs` tests | disabled → WikiDisabled | D-09 |
| Barrel export | `crates/insights/src/lib.rs` | `pub use … wiki_export_preflight` | D-07 |
| Gated cmd | `src-tauri/src/commands/wiki.rs` | `wiki_export_preflight_cmd` + `cfg.wiki.enabled` | D-06 |

<output>
Create `.planning/phases/14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist/14-02-SUMMARY.md` when done
</output>
