---
phase: 06-settings-architecture-review
plan: "03"
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - src-tauri/src/bootstrap.rs
  - src-tauri/src/events.rs
  - src-tauri/src/state.rs
  - src-tauri/src/lib.rs
  - src-tauri/src/commands/chat.rs
  - src-tauri/src/commands/mod.rs
  - .planning/phases/06-settings-architecture-review/06-VERIFICATION.md
autonomous: true
requirements:
  - QA-02
  - QA-04

must_haves:
  truths:
    - "bootstrap.rs contains seed_skills_dir, seed_hooks_dir, seed_plugins_dir extracted from lib.rs (D-30)"
    - "events.rs contains emit_index_progress and emit_index_complete extracted from lib.rs (D-31)"
    - "init_state() body moved from lib.rs into state.rs; startup order preserved per D-34 (D-32)"
    - "start_ask_e2e and is_e2e_mode_cmd moved to commands/chat.rs; invoke names unchanged (D-33)"
    - "lib.rs contains only run() + generate_handler! registration; line count recorded in VERIFICATION (D-03, D-32)"
    - "06-VERIFICATION.md passes architecture checklist: FE-01 Complete, SHELL-01 Complete, structural CONCERNS closed (D-36, D-37, D-38)"
    - "QA-04 confirmed: no new Tauri commands, no new crates, no new routes beyond locked refactor-scope UX (D-39)"
    - "cargo test --workspace and npm run test:e2e:local pass after all changes"
  artifacts:
    - path: src-tauri/src/bootstrap.rs
      provides: "Seed functions for skills, hooks, plugins directories"
      contains: "seed_skills_dir"
    - path: src-tauri/src/events.rs
      provides: "Index progress and complete event emission"
      contains: "emit_index_progress"
    - path: .planning/phases/06-settings-architecture-review/06-VERIFICATION.md
      provides: "Architecture review checklist with pass/fail for all structural requirements"
      contains: "FE-01"
  key_links:
    - from: src-tauri/src/lib.rs
      to: src-tauri/src/state.rs
      via: "run() calls state::init_state(app) for AppState construction"
      pattern: "init_state"
    - from: src-tauri/src/lib.rs
      to: src-tauri/src/bootstrap.rs
      via: "init_state calls bootstrap::seed_* during startup"
      pattern: "bootstrap"
    - from: src-tauri/src/lib.rs
      to: src-tauri/src/events.rs
      via: "pub(crate) use events::{emit_index_progress, emit_index_complete}"
      pattern: "emit_index_progress"
---

## Phase Goal

**As a** Jarvis power user, **I want to** have a cleanly organized Settings view with accordion sections and a slim App shell, **so that** configuration is easy to navigate and the codebase is maintainable.

<objective>
Complete SHELL-01 by extracting bootstrap, events, and init_state from lib.rs; move E2E commands to commands/chat.rs; produce architecture review VERIFICATION.md documenting sign-off.

Purpose: Finish Tauri shell slim (lib.rs = run + registration only) and deliver the formal architecture review checklist that closes QA-02 and confirms QA-04.
Output: bootstrap.rs, events.rs, extended state.rs, slimmed lib.rs, moved E2E commands, 06-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-settings-architecture-review/06-CONTEXT.md
@.planning/phases/06-settings-architecture-review/06-RESEARCH.md
@.planning/phases/06-settings-architecture-review/06-01-SUMMARY.md
@.planning/phases/06-settings-architecture-review/06-02-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONCERNS.md
@src-tauri/src/lib.rs
@src-tauri/src/state.rs
@src-tauri/src/commands/chat.rs
@src-tauri/src/commands/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract bootstrap.rs + events.rs + state.rs init + E2E commands to chat.rs (06-03-01)</name>
  <files>src-tauri/src/bootstrap.rs, src-tauri/src/events.rs, src-tauri/src/state.rs, src-tauri/src/lib.rs, src-tauri/src/commands/chat.rs, src-tauri/src/commands/mod.rs</files>
  <read_first>src-tauri/src/lib.rs (full file — seed_skills_dir, seed_hooks_dir, seed_plugins_dir, emit_index_progress, emit_index_complete, init_state, is_e2e_mode_cmd, start_ask_e2e, AskDonePayload, ask_question, run(), generate_handler! list), src-tauri/src/state.rs (AppState struct), src-tauri/src/commands/chat.rs (existing chat commands), src-tauri/src/commands/mod.rs (re-exports), 06-CONTEXT.md D-30 D-31 D-32 D-33 D-34, 06-RESEARCH.md Pattern 5 (lib.rs after slim)</read_first>
  <action>
    Create src-tauri/src/bootstrap.rs per D-30: move seed_skills_dir, seed_hooks_dir, seed_plugins_dir functions from lib.rs. Keep them pub(crate). No signature changes.

    Create src-tauri/src/events.rs per D-31: move emit_index_progress and emit_index_complete functions from lib.rs. Keep them pub(crate). No signature changes. Update any callers in index_ops.rs or sync_scheduler.rs to use events::emit_index_progress instead of crate::emit_index_progress (the pub(crate) use re-export in lib.rs handles this — add the re-export).

    Move init_state() function body from lib.rs to state.rs per D-32: append the function to state.rs (AppState struct is already there). Preserve the exact startup order per D-34: Store open → config load + migration → keychain setup → memory URI migration → provider construction → watcher start → scheduler start. Update lib.rs run() to call state::init_state(app). The function calls bootstrap::seed_* during setup — update the call paths accordingly.

    Move start_ask_e2e, is_e2e_mode_cmd, and AskDonePayload to commands/chat.rs per D-33. These are Tauri commands — keep the #[tauri::command] attribute and exact function signatures. Update commands/mod.rs re-exports so generate_handler! in lib.rs can find them. Invoke names stay identical.

    If ask_question command exists in lib.rs and is simple glue, move it to commands/chat.rs as well. If it has complex wiring, leave it with a comment noting the remaining item.

    Update lib.rs per D-32: after all extractions, lib.rs should contain:
    - mod declarations (commands, e2e, index_ops, insights_ops, state, sync_scheduler, bootstrap, events)
    - pub(crate) use re-exports for events (emit_index_progress, emit_index_complete) and state (AppState)
    - use statements for commands
    - run() function with tauri::Builder setup calling state::init_state, managing state, starting watcher/scheduler, and generate_handler! registration
    - No function bodies beyond run() glue

    Update generate_handler! macro invocation to reference the moved commands via their new paths in commands::*.

    Record actual lib.rs line count per D-03 (soft target <200).
  </action>
  <verify>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - cargo build -p tauri-app exits 0
    - cargo test --workspace exits 0
    - rg "seed_skills_dir" src-tauri/src/bootstrap.rs matches
    - rg "emit_index_progress" src-tauri/src/events.rs matches
    - rg "init_state" src-tauri/src/state.rs matches
    - rg "start_ask_e2e" src-tauri/src/commands/chat.rs matches
    - lib.rs does not contain seed_skills_dir, seed_hooks_dir, emit_index_progress, or init_state function bodies
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Architecture review VERIFICATION.md + final E2E gate (06-03-02)</name>
  <files>.planning/phases/06-settings-architecture-review/06-VERIFICATION.md</files>
  <read_first>.planning/REQUIREMENTS.md (FE-01 through QA-04 status), .planning/codebase/CONCERNS.md (structural vs non-structural items), .planning/ROADMAP.md Phase 6 success criteria, 06-CONTEXT.md D-36 D-37 D-38 D-39 D-40, .planning/phases/05-agent-protocol/05-04-SUMMARY.md (Phase 5 verification format reference)</read_first>
  <action>
    Create .planning/phases/06-settings-architecture-review/06-VERIFICATION.md per D-37 using Phase 5 table format.

    Include the following sections:

    Requirements Traceability — table with every v1 requirement ID, its description, phase, and status. Mark FE-01 and SHELL-01 as Complete (they were Pending; Plan 06-01 completed FE-01, Plan 06-03 Task 1 completed SHELL-01). Mark FE-05, QA-02, QA-04 as Complete. All other requirements should reflect their actual status from REQUIREMENTS.md.

    Architecture Checklist per D-36 — verify against these criteria:
    - Module boundaries match crate layout (single DB owner in store, thin shell, fat crates)
    - lib.rs is registration + startup only (record actual line count)
    - App.tsx is view router + coordination only (record actual line count)
    - No new Tauri IPC commands added (QA-04 per D-39)
    - No new crates added (QA-04)
    - All views extracted to src/views/ with dedicated hooks
    - All command groups in src-tauri/src/commands/
    - E2E suite green (npm run test:e2e:local)
    - cargo test --workspace green

    Structural CONCERNS per D-38 — go through each item in .planning/codebase/CONCERNS.md. For structural items addressed by the refactor (scheduled sync errors swallowed, monolith App.tsx, lib.rs too large, etc.): mark PASS. For non-structural items (IM pagination, PDF ingest, FastEmbed CI ignore, etc.): list in Known Remaining table per D-38. These do not block v1.9.6.

    QA-04 Compliance per D-39 — confirm that all changes across Phase 6 are refactor, bugfix, tests, or docs. The accordion UI, sync error surface, and Lark collapse are in-scope UX improvements, not new features.

    Phase Pass Criteria per D-40 — if all checks pass, note v1.9.6 tag target.

    Run the full verification suite before writing: cargo test --workspace, npm test, npx tsc --noEmit, and note the results. Record actual line counts for lib.rs and App.tsx.
  </action>
  <verify>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - 06-VERIFICATION.md exists with Requirements Traceability, Architecture Checklist, Structural CONCERNS, QA-04 Compliance, and Phase Pass sections
    - FE-01 and SHELL-01 marked Complete in traceability table
    - All structural CONCERNS addressed or documented as Known Remaining
    - cargo test --workspace exits 0
    - npm run test:e2e:local exits 0 (run as final gate)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lib.rs → bootstrap.rs/events.rs/state.rs | Internal code movement within same crate; no trust boundary change |
| lib.rs → commands/chat.rs | E2E commands move to commands module; invoke names unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Integrity | init_state startup order change | medium | mitigate | Preserve exact startup order per D-34 in state.rs; cargo test + E2E verify no regression |
| T-06-06 | Availability | generate_handler! registration gap after command move | medium | mitigate | Compile-time check: cargo build fails if handler references missing function; E2E start_ask_e2e invoke validates runtime |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed this plan |
</threat_model>

<verification>
- [ ] cargo build -p tauri-app passes
- [ ] cargo test --workspace passes
- [ ] npm test passes
- [ ] npm run test:e2e:local passes (all specs)
- [ ] lib.rs contains only run() + handler registration
- [ ] bootstrap.rs, events.rs exist with extracted functions
- [ ] state.rs contains init_state()
- [ ] 06-VERIFICATION.md documents complete architecture review
</verification>

<success_criteria>
SHELL-01 complete (lib.rs is thin shell). QA-02 architecture review checklist passes with all structural concerns addressed. QA-04 confirmed: refactor-only scope. 06-VERIFICATION.md documents sign-off. Full E2E suite green. Phase 6 ready for v1.9.6 tag.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Requirement |
|----------|------|-------------|
| Bootstrap seed functions | `src-tauri/src/bootstrap.rs` | SHELL-01 (D-30) |
| Event emission functions | `src-tauri/src/events.rs` | SHELL-01 (D-31) |
| init_state in state.rs | `src-tauri/src/state.rs` | SHELL-01 (D-32) |
| Slimmed lib.rs | `src-tauri/src/lib.rs` | SHELL-01 (D-32) |
| E2E commands in chat.rs | `src-tauri/src/commands/chat.rs` | SHELL-01 (D-33) |
| Architecture review | `06-VERIFICATION.md` | QA-02 (D-36, D-37, D-38) |

<output>
Create `.planning/phases/06-settings-architecture-review/06-03-SUMMARY.md` when done
</output>
