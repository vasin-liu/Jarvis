---
phase: 16-related-docs-library-panel
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/fixtures/related-neighbor.md
  - src-tauri/src/e2e.rs
autonomous: true
requirements:
  - REL-03
must_haves:
  truths:
    - "E2E mode seeds at least two indexed local markdown sources that share FTS keyword xyzzy-plugh so related_sources can return a neighbor under MockEmbedder (REL-03, D-16)"
    - "related-neighbor.md exists under e2e/fixtures/ and is indexed by seed_e2e_fixture after the primary JARVIS_E2E_FIXTURE"
    - "Existing E2E specs that assert 已索引 [1-9] remain compatible (no brittle exact-one-source count)"
  artifacts:
    - path: e2e/fixtures/related-neighbor.md
      provides: "Second overlapping markdown fixture for related-chain navigate"
    - path: src-tauri/src/e2e.rs
      provides: "Dual-index seed_e2e_fixture + optional seed summary with shared keyword"
  key_links:
    - from: seed_e2e_fixture
      to: indexer::index_path
      via: "index primary JARVIS_E2E_FIXTURE then sibling related-neighbor.md"
      pattern: "reuse existing block_on index_path path"
    - from: related-neighbor.md
      to: e2e/fixtures/sample.md
      via: "shared token xyzzy-plugh for FTS overlap"
      pattern: "Phase 15 related.rs OVERLAP_TOKEN fixture strategy"
---

<objective>
Unblock REL-03 / D-16 navigate E2E by seeding a second overlapping indexed fixture under JARVIS_E2E=1 (Wave 0 gap from 16-VALIDATION / 16-RESEARCH Pitfall 1).

Purpose: Solo sample.md seed always yields empty related list; related-chain click needs ≥2 overlapping indexed sources.
Output: related-neighbor.md + extended seed_e2e_fixture; no UI, no IPC changes (parallel with 16-01).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-related-docs-library-panel/16-CONTEXT.md
@.planning/phases/16-related-docs-library-panel/16-RESEARCH.md
@.planning/phases/16-related-docs-library-panel/16-VALIDATION.md
@.planning/phases/16-related-docs-library-panel/16-PATTERNS.md
@src-tauri/src/e2e.rs
@e2e/fixtures/sample.md
@crates/retriever/src/related.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add related-neighbor.md overlapping fixture</name>
  <read_first>
    - e2e/fixtures/sample.md
    - crates/retriever/src/related.rs (integration fixture overlap token pattern ~lines 192–223)
    - .planning/phases/16-related-docs-library-panel/16-RESEARCH.md (Pitfall 1 / Pattern 4)
  </read_first>
  <files>e2e/fixtures/related-neighbor.md</files>
  <action>
    Create e2e/fixtures/related-neighbor.md with a distinct H1 title (e.g. Jarvis E2E Related Neighbor) and body that includes the exact shared keyword xyzzy-plugh from sample.md so FTS can rank it as a neighbor under MockEmbedder. Keep the file short. Do not modify sample.md. Do not add graph/score content.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (Test-Path 'e2e/fixtures/related-neighbor.md') { Select-String -Path 'e2e/fixtures/related-neighbor.md' -Pattern 'xyzzy-plugh' -Quiet; if ($?) { 'OK' } else { exit 1 } } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - File e2e/fixtures/related-neighbor.md exists
    - File body contains xyzzy-plugh
    - Title/heading is distinct from sample.md H1
  </acceptance_criteria>
  <done>Neighbor fixture ready for dual seed.</done>
</task>

<task type="auto">
  <name>Task 2: Dual-index seed_e2e_fixture with shared-keyword summary</name>
  <read_first>
    - src-tauri/src/e2e.rs (seed_e2e_fixture full function)
    - crates/retriever/src/related.rs (set_source_summary after index in tests)
    - .planning/phases/16-related-docs-library-panel/16-PATTERNS.md (e2e.rs section)
  </read_first>
  <files>src-tauri/src/e2e.rs</files>
  <action>
    Extend seed_e2e_fixture: after indexing the primary JARVIS_E2E_FIXTURE path, resolve sibling path related-neighbor.md in the same directory as the primary fixture file and index_path it with the same store/embedder/chunker. If the neighbor file is missing, return a clear Err (do not silently skip — D-16 navigate depends on it).

    After both indexes succeed, look up the primary fixture source (by URI/path convention used by indexer for the primary file) and call store.set_source_summary with a short summary that mentions xyzzy-plugh so seed_query prefers summary FTS terms (mirror Phase 15 related test setup). Keep set_meta embedder_id behavior. Do not change non-E2E startup paths. Do not open SQLite outside store APIs.
  </action>
  <verify>
    <automated>cargo check -p tauri-app 2>&1 | findstr /I /C:"error" /C:"Finished"</automated>
  </verify>
  <acceptance_criteria>
    - seed_e2e_fixture indexes both sample fixture and related-neighbor.md
    - Neighbor path derived from primary fixture parent dir + related-neighbor.md
    - Missing neighbor file yields Err String (not Ok)
    - Primary seed gets summary containing xyzzy-plugh via store.set_source_summary when source id is resolvable
    - cargo check for tauri app crate succeeds
  </acceptance_criteria>
  <done>E2E mode dual-seeds overlapping corpus for REL-03 navigate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Env JARVIS_E2E_FIXTURE → filesystem | Path from test harness; trusted in E2E only |
| seed_e2e_fixture → store/indexer | Uses existing AppState APIs only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05 | Tampering | fixture path resolution | medium | mitigate | Require neighbor file exists; Err if missing; only index under fixtures sibling of env path |
| T-16-06 | Elevation of Privilege | e2e.rs DB access | high | mitigate | Continue using state.store + index_path only — no raw rusqlite |
| T-16-SC | Tampering | cargo/npm installs | high | mitigate | No new packages this plan |

</threat_model>

<verification>
- Test-Path e2e/fixtures/related-neighbor.md and content includes xyzzy-plugh
- cargo check tauri app succeeds after e2e.rs edit
</verification>

<success_criteria>
- Dual fixture seed enables related neighbors under MockEmbedder for D-16 E2E (REL-03)
- No UI/IPC scope creep
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | Location | Phase |
|--------|------|----------|-------|
| `related-neighbor.md` | E2E fixture file | e2e/fixtures/related-neighbor.md | 16-02 |
| `seed_e2e_fixture` dual index | fn extension | src-tauri/src/e2e.rs | 16-02 |
| seed summary with xyzzy-plugh | store.set_source_summary call | src-tauri/src/e2e.rs | 16-02 |

<output>
Create `.planning/phases/16-related-docs-library-panel/16-02-SUMMARY.md` when done
</output>
