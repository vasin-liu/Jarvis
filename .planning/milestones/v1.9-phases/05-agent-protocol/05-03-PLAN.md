---
phase: 05-agent-protocol
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - src-tauri/src/commands/agent.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - SHELL-05
must_haves:
  truths:
    - "commands/agent.rs holds all agent/profile/skill/hook/plugin IPC and agent helpers moved from lib.rs (D-23)"
    - "start_ask_e2e and is_e2e_mode_cmd remain in lib.rs — IPC names unchanged (D-24)"
    - "commands/config.rs verified complete — no functional regression (D-25, SHELL-05)"
    - "e2e.rs bootstrap path untouched unless compile requires import fix only (SHELL-05)"
    - "generate_handler! registers identical command identifiers after move (D-28 IPC stability)"
    - "cargo build --manifest-path src-tauri/Cargo.toml succeeds"
  artifacts:
    - path: src-tauri/src/commands/agent.rs
      provides: agent Tauri commands + execute_agent_question helpers
      contains: "pub async fn ask_agent_in_session"
    - path: src-tauri/src/commands/mod.rs
      provides: agent module re-export
      contains: "pub mod agent"
  key_links:
    - from: src-tauri/src/lib.rs
      to: src-tauri/src/commands/agent.rs
      via: generate_handler! and pub use agent::*
      pattern: "ask_agent_in_session"
    - from: src-tauri/src/lib.rs
      to: src-tauri/src/e2e.rs
      via: start_ask_e2e still calls run_ask_agent_in_session from agent module
      pattern: "start_ask_e2e"
---

## Phase Goal

**As a** maintainer, **I want to** agent IPC isolated in commands/agent.rs, **so that** lib.rs stays registration-only and SHELL-05 command split is complete without breaking E2E invoke names.

<objective>
Relocate agent orchestration IPC and private helpers from lib.rs to commands/agent.rs following Phase 4 memory command split pattern.

Purpose: Satisfy SHELL-05 (agent command module + verify config.rs + unchanged e2e.rs bootstrap per D-23..D-25).
Output: New commands/agent.rs, updated mod.rs, slimmed lib.rs, byte-identical IPC surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-agent-protocol/05-CONTEXT.md
@.planning/phases/04-memory-model-view/04-CONTEXT.md
@src-tauri/src/lib.rs
@src-tauri/src/commands/memory.rs
@src-tauri/src/commands/config.rs
@src-tauri/src/e2e.rs
@.planning/phases/05-agent-protocol/05-RESEARCH.md Agent IPC inventory table
</context>

<threat_model>
scope: structural_refactor_only
asvs_level: L1
trust_boundaries:
  - "Tauri generate_handler! IPC registry"
stride_register:
  - threat_id: T-05-B1
    category: Tampering
    component: generate_handler! command list
    disposition: mitigate
    mitigation_plan: "Diff handler macro before/after; cargo build; grep lib.rs for ask_agent_in_session fn body must be absent."
  - threat_id: T-05-B2
    category: Spoofing
    component: start_ask_e2e relocation
    disposition: mitigate
    mitigation_plan: "Keep start_ask_e2e in lib.rs; import run_ask_agent_in_session from commands::agent as pub(crate) if needed."
disposition_summary: "IPC drift and broken E2E bootstrap are primary risks."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create commands/agent.rs and relocate IPC (05-03-01)</name>
  <files>src-tauri/src/commands/agent.rs, src-tauri/src/commands/mod.rs, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/lib.rs agent section (list_agent_profiles through execute_agent_question), src-tauri/src/commands/memory.rs thin wrapper pattern, 05-RESEARCH.md Agent IPC inventory table, 05-CONTEXT.md D-23..D-25</read_first>
  <action>
    Create src-tauri/src/commands/agent.rs mirroring commands/memory.rs style: use crate::state::AppState, #[tauri::command] on public IPC fns, map_err to String.

    MOVE from lib.rs with unchanged signatures and serde payloads:
    - Commands: list_agent_profiles, list_skills, list_hooks, list_plugins, set_active_agent, remove_agent_profile, upsert_agent_profile, ask_agent_in_session, ask_agent_in_session_stream
    - Helpers (pub(crate) as needed for start_ask_e2e): run_ask_agent_in_session, execute_agent_question, build_agent_context, agent_response_to_ask, resolve_active_agent
    - Types used only by agent path: ConfigChatResolver, ConfigEmbedResolver (keep private to agent.rs)

    KEEP in lib.rs per D-24:
    - start_ask_e2e, is_e2e_mode_cmd
    - init_state seed helpers seed_skills_dir, seed_hooks_dir, seed_plugins_dir (discretion: seeds stay in lib.rs init)

    Update commands/mod.rs: pub mod agent; re-export command fns for generate_handler!.

    Update lib.rs: remove moved bodies; import commands::agent::* for handler macro; ensure start_ask_e2e calls commands::agent::run_ask_agent_in_session (or pub(crate) re-export).

    Do NOT rename any Tauri command strings. Do NOT modify e2e.rs logic (SHELL-05).
  </action>
  <verify>
    <automated>cargo build --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - rg "fn list_agent_profiles" src-tauri/src/lib.rs — no match
    - rg "fn list_agent_profiles" src-tauri/src/commands/agent.rs — match
    - rg "start_ask_e2e" src-tauri/src/lib.rs — match
    - cargo build --manifest-path src-tauri/Cargo.toml exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify SHELL-05 config module + handler parity (05-03-02)</name>
  <files>src-tauri/src/commands/config.rs, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/commands/config.rs exports, src-tauri/src/lib.rs generate_handler! block, .planning/REQUIREMENTS.md SHELL-05</read_first>
  <action>
    Verify commands/config.rs already exports get_config, set_config, get_api_key, set_api_key, clear_api_key, get_index_status — no functional edits unless import path fix required after agent move (D-25).

    Compare generate_handler! command list before/after move — every agent command name present exactly once.

    Grep lib.rs for duplicate agent command definitions — must be zero.

    Run cargo test --manifest-path src-tauri/Cargo.toml if agent integration tests exist; otherwise cargo build sufficient gate.

    Confirm e2e.rs unchanged (git diff src-tauri/src/e2e.rs should be empty unless import-only fix absolutely required — document in SUMMARY if so).
  </action>
  <verify>
    <automated>cargo build --manifest-path src-tauri/Cargo.toml && cargo test --manifest-path src-tauri/Cargo.toml 2>nul || cargo build --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - rg "pub async fn get_config" src-tauri/src/commands/config.rs matches
    - rg "ask_agent_in_session" src-tauri/src/lib.rs matches in generate_handler only
    - git diff --quiet src-tauri/src/e2e.rs OR diff is import-only with no behavior change
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Agent IPC fully in commands/agent.rs
- [ ] start_ask_e2e preserved in lib.rs
- [ ] config.rs complete and registered
- [ ] e2e.rs behavior unchanged
- [ ] cargo build green
</verification>

<success_criteria>
SHELL-05 satisfied: agent command module exists, config module verified, E2E bootstrap IPC stable; lib.rs reduced.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Requirement |
|----------|------|-------------|
| Agent command module | `src-tauri/src/commands/agent.rs` | SHELL-05 |
| Module registration | `src-tauri/src/commands/mod.rs` | SHELL-05 |
| Slimmed shell entry | `src-tauri/src/lib.rs` | SHELL-05 |

<output>
Create `.planning/phases/05-agent-protocol/05-03-SUMMARY.md` when done
</output>
