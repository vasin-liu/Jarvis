---
phase: 02-chat-extraction-config-types
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src-tauri/src/commands/chat.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - SHELL-02
must_haves:
  truths:
    - "commands/chat.rs contains the 6 RAG chat IPC commands: list_chat_sessions, create_chat_session, delete_chat_session, list_chat_messages, ask_in_session, ask_in_session_stream (D-16)"
    - "RAG private helpers run_ask_in_session and persist_assistant move into chat.rs; run_ask_in_session is pub(crate) so lib.rs::start_ask_e2e can still call it (D-17, Pitfall 4)"
    - "TokenEvent struct is defined in chat.rs as pub(crate) and imported by lib.rs for agent streaming (Pitfall 7)"
    - "No ask_agent_* command and start_ask_e2e are moved — they stay in lib.rs/e2e.rs (D-15, D-19)"
    - "generate_handler! still lists all 52 IPC command identifiers with identical snake_case names"
    - "cargo test --workspace exits 0"
  artifacts:
    - path: src-tauri/src/commands/chat.rs
      provides: 6 chat #[tauri::command] fns + pub(crate) run_ask_in_session + persist_assistant + pub(crate) TokenEvent
      contains: "pub(crate) async fn run_ask_in_session"
    - path: src-tauri/src/commands/mod.rs
      provides: chat module re-exports
      contains: "pub use chat::"
  key_links:
    - from: src-tauri/src/lib.rs
      to: src-tauri/src/commands/chat.rs
      via: "use commands::chat::{run_ask_in_session, TokenEvent} in start_ask_e2e + agent streaming"
      pattern: "commands::chat::run_ask_in_session"
    - from: src-tauri/src/commands/chat.rs
      to: src-tauri/src/state.rs
      via: "State<AppState> parameter (D-18)"
      pattern: "State<'_, AppState>"
    - from: src-tauri/src/commands/mod.rs
      to: src-tauri/src/commands/chat.rs
      via: "mod chat; pub use chat::{...}"
      pattern: "mod chat"
---

<objective>
Extract the 6 research-mapped RAG chat commands and their private RAG helpers from `lib.rs` into a new `src-tauri/src/commands/chat.rs` module, following the Phase 1 `commands/{config,library,sync}.rs` pattern, with byte-for-byte identical IPC signatures and names.

Purpose: Deliver SHELL-02 — isolate chat/session IPC into a dedicated command module while keeping all agent (`ask_agent_*`) commands, streaming `Channel` glue, and `start_ask_e2e` in `lib.rs` until Phase 5 (D-15/D-19).
Output: `commands/chat.rs` with 6 commands + `pub(crate) run_ask_in_session` + `persist_assistant` + `pub(crate) TokenEvent`; `commands/mod.rs` updated; `lib.rs` re-imports and drops moved bodies. RAG IPC behavior unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-chat-extraction-config-types/02-CONTEXT.md
@.planning/phases/02-chat-extraction-config-types/02-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src-tauri/src/lib.rs
@src-tauri/src/commands/mod.rs
@src-tauri/src/commands/config.rs
@src-tauri/src/state.rs
@src-tauri/src/e2e.rs
</context>

<threat_model>
scope: structural_refactor_only
asvs_level: L1
trust_boundaries:
  - "Tauri IPC boundary — invoke names list_chat_sessions/create_chat_session/delete_chat_session/list_chat_messages/ask_in_session/ask_in_session_stream must remain byte-identical"
  - "App data path — chat_sessions/chat_messages persisted via Store (single DB owner unchanged)"
stride_register:
  - threat_id: T-01
    category: Tampering
    component: ask_in_session / ask_in_session_stream
    disposition: mitigate
    mitigation_plan: "Move command bodies verbatim; do not alter session-id handling, RAG retrieval, or assistant persistence ordering. E2E qa.spec.ts/full-ui.spec.ts (Plan 02-03) re-verifies the RAG journey."
  - threat_id: T-02
    category: Denial of Service
    component: run_ask_in_session shared by start_ask_e2e
    disposition: monitor
    mitigation_plan: "Expose run_ask_in_session as pub(crate) (Pitfall 4) rather than duplicating logic; a duplicate could diverge and break the E2E ask path."
  - threat_id: T-03
    category: Information Disclosure
    component: auto_learn_from_chat trigger
    disposition: accept
    mitigation_plan: "auto_learn_from_chat logic moves verbatim; reads cfg.agent_cfg.auto_learn_from_chat (post 02-01 nesting). No new data exfiltration path."
disposition_summary: "Pure code move within the Tauri shell. No IPC rename, no new network/shell/auth surface."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create commands/chat.rs with 6 commands + helpers + TokenEvent</name>
  <files>src-tauri/src/commands/chat.rs, src-tauri/src/commands/mod.rs</files>
  <read_first>src-tauri/src/lib.rs (chat region ~L57-423), src-tauri/src/commands/config.rs, src-tauri/src/state.rs, .planning/phases/02-chat-extraction-config-types/02-RESEARCH.md ("IPC commands to move" + Pattern 3 + Pitfalls 4/7)</read_first>
  <action>
    Create `src-tauri/src/commands/chat.rs` following the `commands/config.rs` module shape (D-18: access store/embedder/chat model via `State<'_, AppState>`; no new service module).

    Move VERBATIM from `lib.rs` (only adjusting `use`/visibility, not logic):
    - `list_chat_sessions` (lib.rs ~L228) — `#[tauri::command]`
    - `create_chat_session` (~L233) — `#[tauri::command]`
    - `delete_chat_session` (~L245) — `#[tauri::command]`
    - `list_chat_messages` (~L250) — `#[tauri::command]`
    - `ask_in_session` (~L350, async) — `#[tauri::command]`
    - `ask_in_session_stream` (~L359, async, uses `Channel`) — `#[tauri::command]`
    - `run_ask_in_session` (~L265, private async) — move as `pub(crate) async fn` (Pitfall 4, so `lib.rs::start_ask_e2e` can import it)
    - `persist_assistant` (~L387, private async) — move as `pub(crate)` if referenced from lib.rs, else module-private
    - `TokenEvent` struct (lib.rs ~L57-60) — define here as `pub(crate)` and derive the same `Clone, serde::Serialize` (Pitfall 7)
    - Any `auto_learn_from_chat` trigger logic invoked by the RAG path (D-17). It now reads `cfg.agent_cfg.auto_learn_from_chat` (post 02-01).

    DO NOT move (D-15/D-19): `start_ask_e2e`, `run_ask_agent_in_session`, `ask_agent_in_session`, `ask_agent_in_session_stream`, `AskDonePayload`, or any agent helper. Keep necessary imports (`store::{ChatMessage, ChatRole, ChatSession}`, `rag::*`, `memory::learn_from_exchange`, `tauri::ipc::Channel`) local to chat.rs.

    Update `src-tauri/src/commands/mod.rs`: add `mod chat;` and `pub use chat::{list_chat_sessions, create_chat_session, delete_chat_session, list_chat_messages, ask_in_session, ask_in_session_stream};`
  </action>
  <verify>cargo test --workspace</verify>
  <acceptance_criteria>
    - `src-tauri/src/commands/chat.rs` exists and contains exactly 6 `#[tauri::command]` functions: list_chat_sessions, create_chat_session, delete_chat_session, list_chat_messages, ask_in_session, ask_in_session_stream
    - `chat.rs` contains `pub(crate) async fn run_ask_in_session`
    - `chat.rs` contains `pub(crate) struct TokenEvent` (or `pub(crate)` re-exportable definition)
    - `chat.rs` contains NO function named `ask_agent_in_session`, `run_ask_agent_in_session`, or `start_ask_e2e`
    - `commands/mod.rs` contains `pub use chat::` listing the 6 command names
    - `cargo test --workspace` exits 0 (chat.rs compiles even before lib.rs bodies are removed if names not yet duplicated — otherwise complete Task 2 to resolve duplicate-symbol errors)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rewire lib.rs to import chat commands; verify 52-command registry</name>
  <files>src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/lib.rs (imports, start_ask_e2e ~L316, generate_handler! ~L1260-1313), src-tauri/src/commands/mod.rs, src-tauri/src/commands/chat.rs</read_first>
  <action>
    In `lib.rs`:
    - Remove the now-moved definitions: the 6 chat commands, `run_ask_in_session`, `persist_assistant`, and the local `TokenEvent` struct.
    - Import moved items from the chat module: extend the existing `use commands::{...}` to include the 6 chat command fns, and add `use commands::chat::{run_ask_in_session, TokenEvent};` (or `pub(crate)`-path equivalent) where `start_ask_e2e` and agent streaming need them.
    - Update `start_ask_e2e` (D-19, stays in lib.rs) to call `commands::chat::run_ask_in_session(...)` and `run_ask_agent_in_session(...)` exactly as before (Pitfall 4 — single source of RAG ask logic).
    - Ensure `ask_agent_in_session_stream` (stays in lib.rs) still constructs `Channel<TokenEvent>` using the chat.rs `TokenEvent` (Pitfall 7).
    - `generate_handler!` must keep listing all 52 command identifiers with identical snake_case names — the chat names now resolve through the `commands::` imports, not local fns.

    Constraint: no IPC name change, no new command, no removed command. Agent commands and streaming Channel glue remain untouched in lib.rs.
  </action>
  <verify>cargo test --workspace</verify>
  <acceptance_criteria>
    - `lib.rs` contains NO local `#[tauri::command]` definition for any of the 6 chat commands (they resolve via `commands::`)
    - `lib.rs` contains NO local `struct TokenEvent` and NO local `fn run_ask_in_session` definition
    - `lib.rs` `start_ask_e2e` references `commands::chat::run_ask_in_session`
    - Total `#[tauri::command]` count across `src-tauri/src/lib.rs` + `src-tauri/src/commands/*.rs` is unchanged; `generate_handler!` lists exactly 52 command identifiers
    - `ask_agent_in_session`, `ask_agent_in_session_stream`, `run_ask_agent_in_session`, `start_ask_e2e` still defined in `lib.rs`/`e2e.rs` (not moved)
    - `cargo test --workspace` exits 0; `cargo build --manifest-path src-tauri/Cargo.toml` succeeds
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cargo test --workspace` passes
- [ ] `cargo build` in src-tauri succeeds
- [ ] `generate_handler!` lists 52 IPC commands; chat invoke names unchanged
- [ ] `ask_agent_*` and `start_ask_e2e` not moved (D-15/D-19)
- [ ] `run_ask_in_session` is `pub(crate)` and called by `start_ask_e2e`
</verification>

<success_criteria>
- 6 chat commands + RAG helpers live in `commands/chat.rs` with unchanged IPC contract (SHELL-02)
- `TokenEvent` defined once in chat.rs, shared with lib.rs agent streaming
- Agent glue and E2E ask probe untouched (Phase 5 / e2e.rs ownership preserved)
- `cargo test --workspace` green (QA-03 partial)
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Location |
|---------------|------|----------|
| `list_chat_sessions` | `#[tauri::command]` | `src-tauri/src/commands/chat.rs` |
| `create_chat_session` | `#[tauri::command]` | `src-tauri/src/commands/chat.rs` |
| `delete_chat_session` | `#[tauri::command]` | `src-tauri/src/commands/chat.rs` |
| `list_chat_messages` | `#[tauri::command]` | `src-tauri/src/commands/chat.rs` |
| `ask_in_session` | `#[tauri::command]` | `src-tauri/src/commands/chat.rs` |
| `ask_in_session_stream` | `#[tauri::command]` | `src-tauri/src/commands/chat.rs` |
| `run_ask_in_session` | `pub(crate) async fn` | `src-tauri/src/commands/chat.rs` |
| `persist_assistant` | helper fn | `src-tauri/src/commands/chat.rs` |
| `TokenEvent` | `pub(crate) struct` | `src-tauri/src/commands/chat.rs` |
| `chat` | module | `src-tauri/src/commands/mod.rs` |

<output>
After completion, create `.planning/phases/02-chat-extraction-config-types/02-02-SUMMARY.md`
</output>
