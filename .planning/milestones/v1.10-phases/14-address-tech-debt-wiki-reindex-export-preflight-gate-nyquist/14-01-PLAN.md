---
phase: 14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/index_ops.rs
  - src/views/LibraryView.tsx
  - src/views/LibraryView.test.tsx
autonomous: true
requirements:
  - AUDIT-CLOSEOUT
must_haves:
  truths:
    - "WikiPage reindex soft-skips with Ok(false) and never calls mark_failed (D-01)"
    - "Soft skip early-returns before delete_chunks_for_source so existing chunks/vectors stay intact (T-14-01 / RESEARCH critical hazard)"
    - "Soft skip is silent — no toast, no error bar, no new FE error path for skip (D-02)"
    - "LibraryView omits retry-source-{id} for kind wiki_page; other kinds still show retry (D-03)"
    - "Rust unit proves status stays non-Failed, chunk count preserved, bulk rebuild increments skipped and does not abort (D-04, D-05)"
    - "Vitest proves retry button absent for wiki_page; no new E2E (D-04, D-15 waiver)"
  artifacts:
    - path: src-tauri/src/index_ops.rs
      provides: "WikiPage soft skip early-return in reindex_source + unit test"
      contains: "SourceKind::WikiPage"
    - path: src/views/LibraryView.tsx
      provides: "hide retry for wiki_page"
      contains: "retry-source-"
    - path: src/views/LibraryView.test.tsx
      provides: "hides retry for wiki_page Vitest case"
      contains: "retry-source-wiki-1"
  key_links:
    - from: reindex_source WikiPage branch
      to: Ok(false) before delete_chunks_for_source
      via: "early return; no mark_failed"
      pattern: "SourceKind::WikiPage"
    - from: rebuild_all_sources / retry_source_by_id
      to: skipped += 1 / outcome skipped
      via: "existing Ok(false) consumers (D-05)"
      pattern: "outcome: Some(\"skipped\""
    - from: LibraryView retry button
      to: s.kind !== \"wiki_page\"
      via: "hide not disable; mirror compile hide (D-03)"
      pattern: "retry-source-"
  prohibitions:
    - "Do not call mark_failed for WikiPage (D-01)"
    - "Do not soft-skip after delete_chunks_for_source (chunk wipe hazard)"
    - "Do not add toast / reportError for soft skip (D-02)"
    - "Do not implement true wiki MD reindex or heal existing Failed rows"
    - "Do not add e2e/specs for retry hide (D-04, D-15 — cite waiver)"
    - "Do not implement WIKI-F01 or dual index.md writers"
---

<objective>
Close audit item 1: WikiPage「重新同步」fail-closed stub → silent soft skip + hide Library retry for wiki_page (D-01..D-05). Backend soft-skip is IPC safety net; UI hide is primary UX.

Purpose: Stop permanent Failed stubs and silent chunk wipes on wiki_page retry/rebuild.
Output: Early-return soft skip in `reindex_source` + Rust regression test; LibraryView hide + Vitest. No new E2E (D-04/D-15 waiver vs e2e-required.mdc).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist/14-CONTEXT.md
@.planning/phases/14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist/14-RESEARCH.md
@.planning/phases/14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist/14-PATTERNS.md
@.planning/phases/14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist/14-VALIDATION.md
@.planning/ROADMAP.md
@.cursor/rules/tdd-goal-driven.mdc
@.cursor/rules/e2e-required.mdc
@.cursor/rules/karpathy-guidelines.mdc
@src-tauri/src/index_ops.rs
@src-tauri/Cargo.toml
@src/views/LibraryView.tsx
@src/views/LibraryView.test.tsx

<interfaces>
Soft skip (locked behavior; Discretion: inline if vs tiny helper):

1. At start of `reindex_source` (or immediately before any destructive work): if `source.kind == SourceKind::WikiPage`, `return Ok(false)`.
2. MUST run before `store.delete_chunks_for_source` (today ~L555–557). Soft skip after wipe is forbidden (T-14-01).
3. Do not call `mark_failed`. Remove the WikiPage match arm that fail-closes with the unimplemented-reindex message (arm becomes dead once early-return exists — delete it).
4. Do not copy LocalFile/Memory fail-closed `mark_failed` then `Ok(false)` pattern.
5. Callers already treat `Ok(false)` as skip (`rebuild_all_sources`, `retry_source_by_id`) — no caller changes for D-05.

Library hide (D-03):

1. Wrap the retry/resync `<button data-testid={retry-source-${s.id}}>` so it renders only when `s.kind !== "wiki_page"`.
2. Kind-only gate — do NOT require `wiki.enabled` (unlike「生成笔记」).
3. Hide entirely; do not leave a disabled button.

E2E waiver: CONTEXT D-04 and D-15 explicitly waive new E2E for this hide/soft-skip. Existing `wiki.spec.ts` remains the wiki journey. Cite waiver in SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Soft skip WikiPage reindex before chunk delete (14-01-01)</name>
  <files>src-tauri/src/index_ops.rs</files>
  <read_first>
src-tauri/src/index_ops.rs (reindex_source ~536–656; delete_chunks_for_source ~555–557; WikiPage stub ~653–656; Ok(false) skip consumers ~125–135 and ~501–510; mark_failed; existing rebuild_local_file_source test ~717–749),
src-tauri/Cargo.toml (package name tauri-app, lib tauri_app_lib),
.planning/phases/14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist/14-CONTEXT.md (D-01, D-02, D-05),
.planning/phases/14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist/14-RESEARCH.md (Pitfall 1 early-return; Validation Architecture),
.planning/phases/14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist/14-PATTERNS.md (soft-skip shape; anti-pattern mark_failed arms)
  </read_first>
  <behavior>
    - Seed an Indexed WikiPage source that already has chunks &gt; 0
    - After rebuild_all_sources (or equivalent path that calls reindex_source), source status is not Failed; prior status/error unchanged; store.count_chunks() is preserved (not zeroed)
    - RebuildReport.skipped increments (≥1); indexed/failed do not treat this source as failure; batch completes (D-05)
    - reindex_source returns Ok(false) for WikiPage without calling mark_failed
  </behavior>
  <action>
    TDD per D-01/D-04/D-05 and tdd-goal-driven.mdc: write failing Rust unit test first in `index_ops` `mod tests` (tempfile Store + MockEmbedder pattern from rebuild_local_file_source). Seed WikiPage as Indexed with chunks present (index then coerce kind, or upsert WikiPage + index_document — Discretion). Call rebuild (or retry) path. Assert: not Failed, chunks preserved, skipped increments.

    Confirm RED under current stub (Failed +/or chunk wipe). Then implement soft skip: early-return `Ok(false)` for `SourceKind::WikiPage` BEFORE `delete_chunks_for_source`. Remove the WikiPage match arm that calls `mark_failed`. Do not add toast/FE hooks (D-02). Do not heal pre-existing Failed rows. Cargo package: `-p tauri-app` (lib `tauri_app_lib`).
  </action>
  <verify>
    <automated>cargo test -p tauri-app index_ops -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - New/updated test name in index_ops tests mentions wiki_page or WikiPage soft skip
    - rg -n "SourceKind::WikiPage" src-tauri/src/index_ops.rs matches soft-skip path before delete_chunks
    - Early WikiPage return appears textually above the delete_chunks_for_source call in reindex_source
    - rg -n "mark_failed\(store, source," src-tauri/src/index_ops.rs has no WikiPage arm
    - cargo test -p tauri-app index_ops exits 0
    - Test asserts chunk count preserved and status is not Failed
  </acceptance_criteria>
  <done>WikiPage soft skip green: Ok(false), no Failed, chunks intact, bulk continues (D-01, D-05)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hide Library retry for wiki_page + Vitest (14-01-02)</name>
  <files>src/views/LibraryView.tsx, src/views/LibraryView.test.tsx</files>
  <read_first>
src/views/LibraryView.tsx (compile hide ~238–248; retry button ~251–259),
src/views/LibraryView.test.tsx (indexedWikiPage fixture; hides compile for indexed wiki_page case ~94–102),
.planning/phases/14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist/14-CONTEXT.md (D-03, D-04),
.planning/phases/14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist/14-PATTERNS.md (hide retry prescribed sibling test),
.cursor/rules/e2e-required.mdc
  </read_first>
  <behavior>
    - Render LibraryView with indexedWikiPage (id wiki-1): screen.queryByTestId("retry-source-wiki-1") is null
    - Optional sibling: local_file source still exposes retry-source-src-1 (or existing local fixture id)
    - No new e2e/specs file or wiki.spec edits for this behavior
  </behavior>
  <action>
    TDD per D-03/D-04: add Vitest case "hides retry for wiki_page" beside compile-hide (reuse indexedWikiPage). Confirm RED. Then wrap retry button so it renders only when `s.kind !== "wiki_page"` (kind-only; no wiki.enabled requirement). Do not disable-in-place. Do not change useLibrary. Do not add E2E — locked waiver D-04/D-15 overrides e2e-required.mdc for this debt item; note waiver in SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/views/LibraryView.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - LibraryView.test.tsx contains case asserting queryByTestId("retry-source-wiki-1") is null
    - LibraryView.tsx retry button is gated with kind !== wiki_page (hide pattern)
    - npx vitest run src/views/LibraryView.test.tsx exits 0
    - No new file under e2e/specs/ from this plan
  </acceptance_criteria>
  <done>retry-source hidden for wiki_page; Vitest green; no new E2E (D-03, D-04)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library retry / bulk rebuild → reindex_source | UI/IPC can request reindex of any source id including wiki_page |
| reindex_source → Store chunks/vectors | Destructive delete_chunks runs unless soft-skipped first |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering / Denial of Service | reindex_source WikiPage path | critical | mitigate | Early-return Ok(false) BEFORE delete_chunks_for_source; unit test asserts chunk count preserved (D-01) |
| T-14-02 | Tampering | mark_failed on WikiPage | medium | mitigate | Never call mark_failed for WikiPage; remove fail-closed stub arm |
| T-14-SC | Tampering | npm/cargo installs | low | accept | No new packages this plan |
</threat_model>

<verification>
- `cargo test -p tauri-app index_ops` green
- `npx vitest run src/views/LibraryView.test.tsx` green
- Phase 14 gate per D-15: cargo + Vitest only — **no** `npm run test:e2e:local` (D-04/D-15 waiver)
</verification>

<success_criteria>
ROADMAP criterion 1: Library「重新同步」on WikiPage does not leave permanent fail-closed stub — soft skip + hidden button. D-01..D-05 covered.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Symbols / notes | Decision |
|----------|------|-----------------|----------|
| Soft-skip early return | `src-tauri/src/index_ops.rs` | `reindex_source` WikiPage → `Ok(false)` before `delete_chunks_for_source` | D-01, D-05 |
| Soft-skip unit test | `src-tauri/src/index_ops.rs` `mod tests` | chunks preserved; not Failed; skipped increments | D-04 |
| Hide retry UX | `src/views/LibraryView.tsx` | `s.kind !== "wiki_page"` around retry button | D-03 |
| Hide retry Vitest | `src/views/LibraryView.test.tsx` | `retry-source-wiki-1` null | D-04 |

<output>
Create `.planning/phases/14-address-tech-debt-wiki-reindex-export-preflight-gate-nyquist/14-01-SUMMARY.md` when done
</output>
