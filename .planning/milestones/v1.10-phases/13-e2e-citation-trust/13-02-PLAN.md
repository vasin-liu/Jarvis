---
phase: 13-e2e-citation-trust
plan: "02"
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - e2e/specs/wiki.spec.ts
  - e2e/specs/full-ui.spec.ts
  - .cursor/rules/e2e-required.mdc
  - e2e/README.md
autonomous: true
requirements:
  - WIKI-08
  - WIKI-09
must_haves:
  truths:
    - "wiki.spec asserts Library has no wiki-export and no wiki-compile-* when wiki disabled, then hybrid-enables via Settings toggle + settings-save-config (D-01, D-03, D-12)"
    - "After enable, first wiki-compile-* click yields source-list text containing 笔记页 (D-02, D-04)"
    - "Export uses setWikiExportPath(wikiE2eZipPath), waits wiki-export-done, assertWikiZipNonEmpty — no unzip / Obsidian stub checks (D-05, D-06, D-07)"
    - "After wiki on+compiled, askQuestion What is xyzzy-plugh? finds ≥1 data-source-uri not starting with wiki:// (D-08, D-10)"
    - "qa.spec.ts is not modified (D-11)"
    - "full-ui Library check asserts absence of wiki-export and wiki-compile-* only — no enable/compile/export (D-12)"
    - "e2e-required.mdc and e2e/README.md spec maps include Wiki → wiki.spec.ts (D-13)"
  artifacts:
    - path: e2e/specs/wiki.spec.ts
      provides: "WIKI-08 + WIKI-09 journey"
      contains: "xyzzy-plugh"
    - path: e2e/specs/full-ui.spec.ts
      provides: "light Library default-off"
      contains: "wiki-export"
    - path: .cursor/rules/e2e-required.mdc
      provides: "Wiki row in Spec map"
      contains: "wiki.spec.ts"
    - path: e2e/README.md
      provides: "Wiki row in Specs table"
      contains: "wiki.spec.ts"
  key_links:
    - from: wiki.spec enable path
      to: wiki-enabled-toggle + settings-save-config
      via: "expandSettingsSection settings-section-wiki"
      pattern: "wiki-enabled-toggle"
    - from: wiki-compile-*
      to: source-list 笔记页
      via: "compile_wiki_cmd + refreshSources"
      pattern: "笔记页"
    - from: setWikiExportPath
      to: wiki-export-done + zip size
      via: "exportWiki bypass + export_wiki_zip"
      pattern: "wiki-export-done"
    - from: askQuestion xyzzy-plugh
      to: data-source-uri non-wiki
      via: "RAG citations + ChatView attr"
      pattern: "data-source-uri"
---

<objective>
Deliver WIKI-09 full mocked wiki journey and WIKI-08 citation-trust assert in `wiki.spec.ts`, plus light `full-ui` default-off and dual spec-map docs (D-01..D-04, D-06..D-08, D-10..D-13). Depends on Plan 01 harness hooks.

Purpose: Prove enable → compile → list「笔记页」→ export zip, and that RAG still cites ≥1 non-`wiki://` source after wiki is on.
Output: Green focused wiki + full-ui + qa E2E; docs maps updated. Do not edit qa.spec.ts (D-11).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-e2e-citation-trust/13-CONTEXT.md
@.planning/phases/13-e2e-citation-trust/13-RESEARCH.md
@.planning/phases/13-e2e-citation-trust/13-PATTERNS.md
@.planning/phases/13-e2e-citation-trust/13-VALIDATION.md
@.planning/phases/13-e2e-citation-trust/13-UI-SPEC.md
@.planning/phases/13-e2e-citation-trust/13-01-SUMMARY.md
@.planning/ROADMAP.md
@.cursor/rules/e2e-required.mdc
@.cursor/rules/tdd-goal-driven.mdc
@e2e/helpers.ts
@e2e/specs/lark.spec.ts
@e2e/specs/qa.spec.ts
@e2e/specs/full-ui.spec.ts
@e2e/fixtures/sample.md
@e2e/README.md

<interfaces>
Journey contract (locked D-01..D-13):

1. File name MUST be `e2e/specs/wiki.spec.ts` (alphabetical last — Pitfall 3).
2. Opening it: openNav library → expect $$ wiki-export length 0 and $$ wiki-compile-* length 0 (D-01, D-12).
3. Positive it: Settings → expandSettingsSection("settings-section-wiki") → setReactCheckbox wiki-enabled-toggle true → clickViaDom settings-save-config → wait busy clear → Library → first [data-testid^=wiki-compile-] → wait source-list text includes「笔记页」(90s) (D-02, D-03, D-04).
4. Export: unlink wikiE2eZipPath if exists → setWikiExportPath(that path) → click wiki-export → wait wiki-export-done → assertWikiZipNonEmpty (D-05..D-07). Do not unzip; do not assert index.md / .obsidian.
5. Citation trust: askQuestion("What is xyzzy-plugh?") → browser.execute collect all [data-source-uri] → expect some URI truthy and not starting with wiki:// (D-08, D-10). Wiki URIs may co-appear.
6. Discretion: optional end-of-positive-test wiki disable + save to reduce shared-session pollution.
7. full-ui: extend library-stats it (or sibling) with same Library absence asserts only — never enable wiki there (D-12).
8. Docs: add Wiki → wiki.spec.ts row to `.cursor/rules/e2e-required.mdc` Spec map AND `e2e/README.md` Specs table (D-13).
9. Forbidden: edit e2e/specs/qa.spec.ts (D-11); seed wiki.enabled in apply_e2e_config; empty-export E2E; Rust RAG+wiki integration test (optional not locked — skip).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: wiki.spec.ts — default-off + enable → compile → export → citation trust (13-02-01)</name>
  <files>e2e/specs/wiki.spec.ts</files>
  <read_first>
e2e/helpers.ts (setWikiExportPath, wikiE2eZipPath, assertWikiZipNonEmpty, openNav, expandSettingsSection, setReactCheckbox, clickViaDom, askQuestion),
e2e/specs/lark.spec.ts (Settings expand → action → Library wait pattern),
e2e/specs/qa.spec.ts (askQuestion What is xyzzy-plugh? — read only; do not modify),
e2e/specs/memory.spec.ts (first data-testid^= prefix click),
.planning/phases/13-e2e-citation-trust/13-CONTEXT.md (D-01..D-12),
.planning/phases/13-e2e-citation-trust/13-PATTERNS.md (wiki.spec skeleton),
.planning/phases/13-e2e-citation-trust/13-RESEARCH.md (Pitfalls 2–6, citation assert),
.planning/phases/13-e2e-citation-trust/13-01-SUMMARY.md
  </read_first>
  <behavior>
    - Default-off it: Library has zero wiki-export and zero wiki-compile-* elements
    - Positive it: after Settings enable+save, first wiki-compile-* produces source-list containing 笔记页; export shows wiki-export-done and zip size greater than 0; ask What is xyzzy-plugh? yields ≥1 data-source-uri not starting with wiki://
  </behavior>
  <action>
    Create e2e/specs/wiki.spec.ts implementing D-01..D-10 and D-12 wiki ownership per interfaces: two its (default-off Library absence; enable→compile→笔记页→export→citation trust). Import helpers from ../helpers.ts including Plan 01 zip helpers. Use exact question string What is xyzzy-plugh?. Click first eligible wiki-compile-* by prefix selector — do not hard-bind fixture display name (D-04). Unlink zip before export. Wait wiki-export-done not the red err bar (D-06). Assert citation URIs via querySelectorAll data-source-uri (D-09/D-10).

    Do not edit qa.spec.ts (D-11). Do not unzip. Do not add Rust RAG test. Optionally disable wiki + save at end of positive it (Discretion / Pitfall 3). If Mock compile or citation assert fails, extend Mock only as last resort per CONTEXT Discretion — prefer leave Mock unchanged.
  </action>
  <verify>
    <automated>npm run build:e2e && cross-env JARVIS_E2E=1 JARVIS_E2E_FIXTURE=./e2e/fixtures/sample.md npx wdio run e2e/wdio.conf.ts --spec e2e/specs/wiki.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - File e2e/specs/wiki.spec.ts exists
    - rg -n "wiki-export" e2e/specs/wiki.spec.ts matches
    - rg -n "wiki-compile-" e2e/specs/wiki.spec.ts matches
    - rg -n "笔记页" e2e/specs/wiki.spec.ts matches
    - rg -n "settings-save-config" e2e/specs/wiki.spec.ts matches
    - rg -n "wiki-export-done" e2e/specs/wiki.spec.ts matches
    - rg -n "What is xyzzy-plugh" e2e/specs/wiki.spec.ts matches
    - rg -n "data-source-uri" e2e/specs/wiki.spec.ts matches
    - rg -n "wiki://" e2e/specs/wiki.spec.ts matches (non-wiki URI assert)
    - Focused wdio --spec e2e/specs/wiki.spec.ts exits 0 after build:e2e
  </acceptance_criteria>
  <done>wiki.spec proves WIKI-09 journey and WIKI-08 citation trust under mocks</done>
</task>

<task type="auto">
  <name>Task 2: full-ui light default-off + dual Spec map docs (13-02-02)</name>
  <files>e2e/specs/full-ui.spec.ts, .cursor/rules/e2e-required.mdc, e2e/README.md</files>
  <read_first>
e2e/specs/full-ui.spec.ts (shows library stats after seeding it ~49–53),
.cursor/rules/e2e-required.mdc (Spec map table),
e2e/README.md (Specs table),
.planning/phases/13-e2e-citation-trust/13-CONTEXT.md (D-12, D-13),
.planning/phases/13-e2e-citation-trust/13-PATTERNS.md (full-ui + docs map)
  </read_first>
  <action>
    Per D-12: in full-ui.spec.ts library-stats it (or immediate sibling after openNav library), assert $$('[data-testid="wiki-export"]') length 0 and $$('[data-testid^="wiki-compile-"]') length 0. Do not add Settings wiki enable, compile, or export steps to full-ui.

    Per D-13: add Spec map row Wiki → wiki.spec.ts to .cursor/rules/e2e-required.mdc AND Specs table row for wiki.spec.ts in e2e/README.md describing enable→compile→笔记页→export + citation URI trust / default-off Library controls.

    Do not modify e2e/specs/qa.spec.ts. Confirm with git that qa.spec.ts is untouched in this plan's diff.
  </action>
  <verify>
    <automated>rg -n "wiki.spec.ts" .cursor/rules/e2e-required.mdc e2e/README.md && rg -n "wiki-export" e2e/specs/full-ui.spec.ts && cross-env JARVIS_E2E=1 JARVIS_E2E_FIXTURE=./e2e/fixtures/sample.md npx wdio run e2e/wdio.conf.ts --spec e2e/specs/full-ui.spec.ts --spec e2e/specs/qa.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "wiki-export" e2e/specs/full-ui.spec.ts matches
    - rg -n "wiki-compile-" e2e/specs/full-ui.spec.ts matches
    - full-ui.spec.ts does not contain wiki-enabled-toggle enable path (no setReactCheckbox on wiki-enabled-toggle)
    - rg -n "wiki.spec.ts" .cursor/rules/e2e-required.mdc matches
    - rg -n "wiki.spec.ts" e2e/README.md matches
    - git diff --name-only does not list e2e/specs/qa.spec.ts for this plan's changes
    - wdio full-ui + qa specs exit 0
  </acceptance_criteria>
  <done>Default-off regression in full-ui; docs map dual-updated; qa.spec untouched</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — test:e2e:local green (13-02-03)</name>
  <files>e2e/specs/wiki.spec.ts, e2e/specs/full-ui.spec.ts</files>
  <read_first>
.planning/phases/13-e2e-citation-trust/13-VALIDATION.md (phase gate),
.planning/ROADMAP.md (Phase 13 success criteria),
package.json (test:e2e:local script)
  </read_first>
  <action>
    Run full local E2E suite per VALIDATION phase gate and ROADMAP success criteria 1–4. Fix only Phase 13 regressions in wiki.spec / full-ui / Plan 01 harness files if suite fails (session order, busy waits, zip unlink). Do not weaken D-10 to allow all-wiki citations. Do not edit qa.spec.ts. Do not add unzip assertions.
  </action>
  <verify>
    <automated>npm run test:e2e:local</automated>
  </verify>
  <acceptance_criteria>
    - npm run test:e2e:local exits 0
    - e2e/specs/wiki.spec.ts still asserts non-wiki data-source-uri after What is xyzzy-plugh?
    - e2e/specs/qa.spec.ts still exists unchanged relative to plan start (D-11)
  </acceptance_criteria>
  <done>Phase 13 E2E gate green; WIKI-08 and WIKI-09 verified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WDIO Node → webview | Spec drives UI; sets export path hook; reads citation attrs |
| WDIO Node → OS temp FS | Unlink/stat `jarvis-e2e-wiki.zip` under os.tmpdir |
| Shared Tauri session across specs | Config mutations in wiki.spec can affect later/earlier specs if order changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Tampering | wiki.spec export dest | medium | mitigate | Only write via wikiE2eZipPath (os.tmpdir + jarvis-e2e-wiki.zip); unlink before export; assert size greater than 0 after wiki-export-done (D-06, D-07) |
| T-13-06 | Spoofing | false citation trust | high | mitigate | Require ≥1 data-source-uri not starting with wiki:// after xyzzy-plugh ask (D-10); do not accept excerpt-only or all-wiki; leave qa.spec regression intact (D-11) |
| T-13-07 | Tampering | suite-order wiki.enabled leak | medium | mitigate | Keep filename wiki.spec.ts last alphabetically; full-ui only asserts absence; optional end-of-spec disable (Pitfall 3) |
| T-13-08 | Information Disclosure | zip contents in E2E logs | low | accept | Assert size only — no unzip of vault contents in E2E (deferred; Phase 12 units own stub safety) |
| T-13-SC | Tampering | npm installs | low | accept | No new packages — WDIO/Vitest already present |
</threat_model>

<verification>
- Focused wiki.spec green (default-off + journey + citation trust)
- full-ui light default-off green; qa.spec green unchanged
- Spec maps list Wiki → wiki.spec.ts in both rule and README
- npm run test:e2e:local exits 0
</verification>

<success_criteria>
ROADMAP Phase 13 criteria 1–4 met: wiki.spec journey passes; full-ui default-off hides compile/export; qa citation regression intact; post-compile Q&A has ≥1 non-wiki citation URI. WIKI-08 and WIKI-09 complete.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Symbols / notes | Requirement |
|----------|------|-----------------|-------------|
| Wiki E2E journey | `e2e/specs/wiki.spec.ts` | default-off it + enable→compile→笔记页→export→citation trust | WIKI-08, WIKI-09 (D-01..D-10, D-12) |
| full-ui default-off | `e2e/specs/full-ui.spec.ts` | Library absence of wiki-export / wiki-compile-* | WIKI-09 (D-12) |
| Spec map rule | `.cursor/rules/e2e-required.mdc` | Wiki → wiki.spec.ts row | WIKI-09 (D-13) |
| Spec map README | `e2e/README.md` | wiki.spec.ts coverage row | WIKI-09 (D-13) |

<output>
Create `.planning/phases/13-e2e-citation-trust/13-02-SUMMARY.md` when done
</output>
