---
phase: 18-mcp-tools-search-list-sources
plan: 04
type: execute
wave: 4
depends_on:
  - 18-03
files_modified:
  - docs/mcp.md
autonomous: true
requirements:
  - MCP-01
  - MCP-02
must_haves:
  truths:
    - "docs/mcp.md documents real search hybrid path, limit ≤ final_k (8), excerpt ≤200, JSON results fields (D-12, MCP-01)"
    - "docs/mcp.md documents list_sources Indexed-only, id/title/kind, cap 200 + truncated/total_indexed, no filesystem paths (D-12, MCP-02)"
    - "docs/mcp.md no longer claims Phase-17 stub-only / not-implemented tool bodies (D-12)"
  artifacts:
    - path: docs/mcp.md
      provides: "Host-facing real tool behavior + caps + embedder/config notes"
  key_links:
    - from: docs/mcp.md
      to: crates/mcp/src/server.rs
      via: "Documented JSON shapes match handlers"
      pattern: "results|list_sources|truncated"
---

<objective>
Update `docs/mcp.md` so hosts know MCP search/list_sources are live read-only KB tools with documented caps and JSON shapes.

Purpose: D-12 docs bar; remove Phase 17 stub wording after 18-03 wires real handlers.
Output: Accurate mcp.md; phase cargo gate still green (no code changes required beyond docs).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-mcp-tools-search-list-sources/18-CONTEXT.md
@.planning/phases/18-mcp-tools-search-list-sources/18-RESEARCH.md
@.planning/phases/18-mcp-tools-search-list-sources/18-03-SUMMARY.md
@docs/mcp.md
@crates/mcp/src/server.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite docs/mcp.md Tools section for real KB semantics</name>
  <read_first>
    - .planning/phases/18-mcp-tools-search-list-sources/18-CONTEXT.md (D-12)
    - .planning/phases/18-mcp-tools-search-list-sources/18-RESEARCH.md (JSON shapes; caps)
    - docs/mcp.md
    - crates/mcp/src/server.rs (final handler field names)
  </read_first>
  <files>docs/mcp.md</files>
  <action>
    Per D-12: keep Build / Prerequisite / Path precedence / Cursor + Claude Desktop config tables from Phase 17.

    Replace Phase-17 status / stub-only wording with Phase 18 real-tool status. Document:

    - search: same hybrid retrieve as in-app agent (vector+FTS+RRF via RetrieverConfig defaults); optional limit clamped to ≤ final_k 8; excerpt ≤200 Unicode chars; empty query → error; JSON results with chunk_id, source_id, title, loc, excerpt; no paths.
    - list_sources: Indexed only; fields id, title, kind; hard-cap 200; truncated + total_indexed; no absolute filesystem or file URI paths.
    - Embedder: built from sibling config.json via build_embedder (GUI parity); startup fails closed if provider cannot be built; empty KB hits remain valid empty results array.

    Do not document write/mutate tools, get_chunk/read (MCP-F01), Settings toggle (MCP-F02), or HTTP/SSE (MCP-F03). Do not claim WebDriver E2E coverage this phase (Phase 19).

    If README already points at docs/mcp.md, leave pointer unless text still says stub-only — then one-line sync only.
  </action>
  <verify>
    <automated>rg -n "not_implemented|stub-only|Phase 17 status" docs/mcp.md; rg -n "final_k|200|truncated|chunk_id|Indexed" docs/mcp.md; cargo test -p retriever -p mcp -p agent -- --test-threads=4</automated>
  </verify>
  <done>docs/mcp.md matches live tools; phase three-crate suite green (D-12).</done>
</task>

<task type="auto">
  <name>Task 2: Docs consistency spot-check against handlers</name>
  <read_first>
    - docs/mcp.md
    - crates/mcp/src/server.rs
  </read_first>
  <files>docs/mcp.md</files>
  <action>
    Confirm documented JSON field names and caps match server.rs / kb_readonly constants (200 / final_k 8). Fix any drift in docs only. No code changes unless a doc claim is wrong because handler fields differ — then prefer aligning docs to shipped code.
  </action>
  <verify>
    <automated>cargo test -p mcp -- --test-threads=4</automated>
  </verify>
  <done>Docs and handlers agree on MCP-01/MCP-02 contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → operator config | Operators copy mcp.json from docs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-13 | Information Disclosure | docs examples | low | mitigate | Document that payloads omit paths; do not show example URIs with home dirs |
| T-18-14 | Tampering | docs tool list | medium | mitigate | Document only search + list_sources; do not imply write tools exist |
| T-18-SC | Tampering | cargo installs | low | accept | Docs-only plan |
</threat_model>

<verification>
Phase gate: cargo test -p retriever -p mcp -p agent; docs describe real tools.
</verification>

<success_criteria>
- Stub wording removed from docs/mcp.md
- Caps and JSON shapes documented
- No WebDriver claimed for this phase
</success_criteria>

<output>
Create `.planning/phases/18-mcp-tools-search-list-sources/18-04-SUMMARY.md` when done
</output>
