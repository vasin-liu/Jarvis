---
phase: 05-agent-protocol
plan: "04"
type: execute
wave: 4
depends_on:
  - "05-03"
files_modified:
  - e2e/specs/agent.spec.ts
  - e2e/specs/full-ui.spec.ts
  - crates/agent/tests/tool_parse_e2e.rs
requirements:
  - AGT-03
  - FE-06
must_haves:
  truths:
    - "npm run build:e2e succeeds before E2E"
    - "agent.spec.ts passes with JSON-based Mock tool path (D-18, AGT-03)"
    - "full-ui.spec.ts agent-related steps pass"
    - "cargo test -p agent && npm test green"
    - "Optional: extend agent.spec.ts to assert agent-tool-calls visible after agent question"
  artifacts:
    - path: e2e/specs/agent.spec.ts
      provides: agent E2E regression with structured format
      contains: "agent mode"
  key_links:
    - from: e2e/specs/agent.spec.ts
      to: crates/llm/src/mock.rs
      via: "JSON tool response in agent mode"
      pattern: "askAgentQuestion"
---

<objective>
Run Phase 5 E2E gate: rebuild packaged app, verify agent specs with structured JSON tool protocol, optionally strengthen tool-call UI assertions.

Purpose: Close AGT-03 and phase success criteria.
Output: Green agent + full-ui E2E; any minimal spec fixes only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-agent-protocol/05-CONTEXT.md
@e2e/specs/agent.spec.ts
@e2e/specs/full-ui.spec.ts
@e2e/helpers.ts
@.cursor/rules/e2e-required.mdc
</context>

<threat_model>
scope: e2e_verification
asvs_level: L1
trust_boundaries:
  - "E2E Mock agent path"
stride_register:
  - threat_id: T-05-D1
    category: Denial of Service
    component: agent ask stuck on 思考中
    disposition: mitigate
    mitigation_plan: "Keep existing Chinese question regression test; verify tool parse does not panic."
disposition_summary: "Verification-only; fix regressions not features."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Unit gate (05-04-01)</name>
  <files></files>
  <action>
    Run:
    - `cargo test -p agent`
    - `cargo test -p llm`
    - `npm test`
    - `npx tsc --noEmit`
    Fix failures before E2E.
  </action>
  <verify>cargo test -p agent && npm test</verify>
  <acceptance_criteria>All commands exit 0</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: E2E agent gate (05-04-02)</name>
  <files>e2e/specs/agent.spec.ts, e2e/specs/full-ui.spec.ts</files>
  <action>
    Run `npm run build:e2e` then:
    - `npm run test:e2e:local -- --spec e2e/specs/agent.spec.ts`
    - `npm run test:e2e:local -- --spec e2e/specs/full-ui.spec.ts`

    If agent tool UI assertion is missing, extend agent.spec.ts:
    - After askAgentQuestion, optionally wait for `[data-testid="agent-tool-calls"]` when Mock executes search_knowledge (non-blocking if flaky — prefer stable answer assertion first).

    Do not weaken existing Chinese regression test.
  </action>
  <verify>npm run test:e2e:local -- --spec e2e/specs/agent.spec.ts</verify>
  <acceptance_criteria>
    - agent.spec.ts green
    - full-ui.spec.ts green
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Full unit gate green
- [ ] agent.spec.ts green
- [ ] full-ui.spec.ts green
- [ ] Phase 5 ROADMAP criteria met
</verification>

<success_criteria>
Phase 5 ready for `/gsd-verify-work 5` sign-off.
</success_criteria>
