---
phase: 17-mcp-transport-read-only-scaffold
plan: 03
type: tdd
wave: 2
depends_on:
  - 17-01
files_modified:
  - crates/mcp/src/lib.rs
  - crates/mcp/tests/allowlist.rs
autonomous: true
requirements:
  - MCP-04
must_haves:
  truths:
    - "Registered MCP tools are exactly {search, list_sources} — no other names (D-03, MCP-04)"
    - "Both tools have stub handlers returning structured not-implemented JSON for Phase 18 (D-04)"
    - "Stub handlers do not mutate Store / do not call agent execute_tool (D-09)"
  artifacts:
    - path: crates/mcp/src/lib.rs
      provides: "JarvisMcp + #[tool_router(server_handler)] with search + list_sources stubs"
    - path: crates/mcp/tests/allowlist.rs
      provides: "Allowlist + stub_smoke integration tests"
  key_links:
    - from: crates/mcp/src/lib.rs
      to: rmcp 2.2.0
      via: "#[tool] / #[tool_router(server_handler)] per RESEARCH §2"
      pattern: "tool_router(server_handler)"
    - from: crates/mcp/tests/allowlist.rs
      to: crates/mcp/src/lib.rs
      via: "Enumerate tool names from router / list_tools"
      pattern: "search|list_sources"
---

<objective>
Register structurally read-only MCP tool surface: exactly `search` and `list_sources` with Phase-17 stub bodies.

Purpose: MCP-04 / D-03 / D-04 — hosts must see fixed tool names; semantics wait for Phase 18.
Output: `JarvisMcp` tool router; green allowlist + stub smoke tests via `cargo test -p mcp`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-mcp-transport-read-only-scaffold/17-CONTEXT.md
@.planning/phases/17-mcp-transport-read-only-scaffold/17-RESEARCH.md
@.planning/phases/17-mcp-transport-read-only-scaffold/17-PATTERNS.md
@.planning/phases/17-mcp-transport-read-only-scaffold/17-01-SUMMARY.md
@crates/mcp/src/lib.rs
@crates/agent/src/tools.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED allowlist + stub_smoke tests for exact tool set</name>
  <read_first>
    - .planning/phases/17-mcp-transport-read-only-scaffold/17-CONTEXT.md (D-03, D-04, D-09)
    - .planning/phases/17-mcp-transport-read-only-scaffold/17-RESEARCH.md (§2 rmcp wiring)
    - .planning/phases/17-mcp-transport-read-only-scaffold/17-VALIDATION.md (allowlist / stub_smoke rows)
    - crates/mcp/src/lib.rs (from 17-01)
    - crates/agent/src/tools.rs (name reference only — do not wire execute_tool)
  </read_first>
  <files>crates/mcp/tests/allowlist.rs, crates/mcp/src/lib.rs</files>
  <behavior>
    - tools_list_is_exact_allowlist: collected tool names as a set equal exactly {"search", "list_sources"} (len 2; no ingest/delete/memory/write aliases)
    - stub_search_returns_not_implemented_json: invoking search stub returns parseable JSON with status indicating not implemented / phase 18
    - stub_list_sources_returns_not_implemented_json: same for list_sources
  </behavior>
  <action>
    Write crates/mcp/tests/allowlist.rs first describing the three behaviors (RED). Prefer inspecting JarvisMcp tool router list API exposed by rmcp 2.2.0 macros (list tool names from the generated router) — if in-process list API differs, use the documented rmcp 2.2.0 ServerHandler tools/list path from RESEARCH without spawning a live host. Do not call agent::execute_tool. Do not register any third tool name.

    Then implement in lib.rs per D-03/D-04: struct JarvisMcp (Clone; optional Arc&lt;Store&gt; unused in stubs), #[tool_router(server_handler)] impl with exactly two #[tool] methods named search and list_sources. search takes Parameters&lt;SearchParams&gt; where SearchParams has query: String and optional limit; list_sources takes no mutating args. Stub body returns serde_json string like status not_implemented + phase 18 message (discretion). Optional readOnlyHint if macro supports — advisory only; allowlist test is enforcement.

    Follow RESEARCH §2 imports (Parameters, tool, tool_router, schemars). Keep paths/error modules. Do not import retriever/embedder/agent. Do not implement hybrid search (Phase 18).
  </action>
  <verify>
    <automated>cargo test -p mcp allowlist -- --test-threads=4</automated>
  </verify>
  <acceptance_criteria>
    - Exactly two tool names registered: search and list_sources
    - cargo test -p mcp allowlist exits 0
    - Stub JSON mentions Phase 18 / not_implemented semantics
    - No dependency on crates/agent in crates/mcp/Cargo.toml
  </acceptance_criteria>
  <done>MCP-04 structural allowlist enforced by tests; stubs callable (D-03, D-04, D-09).</done>
</task>

<task type="auto">
  <name>Task 2: Export JarvisMcp for main and keep path tests green</name>
  <read_first>
    - crates/mcp/src/lib.rs
    - crates/mcp/src/main.rs
  </read_first>
  <files>crates/mcp/src/lib.rs</files>
  <action>
    pub use JarvisMcp (and SearchParams if needed by main). Ensure lib compiles with rmcp macros. Re-run full `cargo test -p mcp` so paths + allowlist both green. Do not wire stdio serve in main yet (Plan 17-04).
  </action>
  <verify>
    <automated>cargo test -p mcp -- --test-threads=4</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p mcp exits 0
    - JarvisMcp is reachable as mcp::JarvisMcp from the binary crate
  </acceptance_criteria>
  <done>Tool surface ready for stdio main wiring in 17-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP host tools/list → JarvisMcp | Host discovers callable tools; surface must stay read-only |
| Tool call args → stub handlers | Untrusted JSON args; stubs must not mutate KB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-06 | Elevation of Privilege | tool registry | high | mitigate | Structural allowlist exactly search + list_sources (D-03); automated allowlist test; no agent tool loop |
| T-17-07 | Tampering | stub handlers | high | mitigate | Stubs return not_implemented JSON only; no Store writes / ingest / memory mutate (D-04) |
| T-17-08 | Information Disclosure | stub payloads | low | accept | Stub messages are static; no config secrets in tool output |
| T-17-SC | Tampering | rmcp usage | medium | mitigate | Use already-pinned workspace rmcp 2.2.0 only; no extra crates |
</threat_model>

<verification>
- cargo test -p mcp allowlist
- cargo test -p mcp
</verification>

<success_criteria>
- MCP-04: tools/list name set is exactly {search, list_sources}
- D-03/D-04/D-09 satisfied at cargo layer
</success_criteria>

## Artifacts

| Symbol | Kind | Location | Phase |
|--------|------|----------|-------|
| `JarvisMcp` | struct + tool_router | crates/mcp/src/lib.rs | 17-03 |
| `search` / `list_sources` | #[tool] stubs | crates/mcp/src/lib.rs | 17-03 |
| `SearchParams` | schemars+serde args | crates/mcp/src/lib.rs | 17-03 |
| allowlist + stub_smoke tests | integration | crates/mcp/tests/allowlist.rs | 17-03 |

<output>
Create `.planning/phases/17-mcp-transport-read-only-scaffold/17-03-SUMMARY.md` when done
</output>
