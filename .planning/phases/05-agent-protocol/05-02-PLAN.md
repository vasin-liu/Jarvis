---
phase: 05-agent-protocol
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - crates/llm/src/mock.rs
  - crates/agent/src/tools.rs
  - crates/rag/src/types.rs
  - src-tauri/src/lib.rs
  - src/types/ipc.ts
  - src/App.tsx
  - src/views/ChatView.tsx
requirements:
  - AGT-01
  - AGT-02
  - AGT-03
must_haves:
  truths:
    - "MockChatModel agent tool round emits JSON line without XML wrapper (D-10)"
    - "build_tools_prompt documents JSON first, XML marked deprecated (D-11)"
    - "AskResponse.tool_parse_warnings with #[serde(default)] wired through agent_response_to_ask (D-07)"
    - "ChatView shows agent-tool-parse-warnings panel when warnings present (D-08)"
    - "agent-tool-calls testid unchanged (D-09)"
    - "npm test && npx tsc --noEmit clean"
  artifacts:
    - path: crates/rag/src/types.rs
      provides: AskResponse.tool_parse_warnings
      contains: "tool_parse_warnings"
    - path: src/views/ChatView.tsx
      provides: parse warnings UI
      contains: "agent-tool-parse-warnings"
  key_links:
    - from: src/App.tsx
      to: src/views/ChatView.tsx
      via: "lastToolParseWarnings prop"
      pattern: "tool_parse_warnings"
---

<objective>
Switch Mock/E2E to JSON tool protocol, extend AskResponse metadata for parse warnings, and surface warnings in ChatView — completing AGT-02 user-visible half.

Purpose: AGT-01 Mock path + AGT-02 UI surfacing + AGT-03 prerequisite for E2E.
Output: JSON Mock, updated prompt, AskResponse field, ChatView warnings panel, App wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-agent-protocol/05-CONTEXT.md
@.planning/phases/05-agent-protocol/05-RESEARCH.md
@crates/llm/src/mock.rs
@src/views/ChatView.tsx
@src/App.tsx
@.cursor/rules/frontend-taste.mdc
</context>

<threat_model>
scope: frontend_metadata
asvs_level: L1
trust_boundaries:
  - "IPC AskResponse → React state → ChatView testids"
stride_register:
  - threat_id: T-05-02
    category: Tampering
    component: agent testid contract
    disposition: mitigate
    mitigation_plan: "Add new testid only for warnings; do not rename agent-tool-calls or agent-mode-toggle."
disposition_summary: "UI change is additive; E2E agent specs should remain stable."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Mock JSON + prompt update (05-02-01)</name>
  <files>crates/llm/src/mock.rs, crates/agent/src/tools.rs</files>
  <action>
    Update MockChatModel agent branch (system contains "可用工具") to return:
    `{"name":"search_knowledge","arguments":{"query":"agent"}}`
    (no XML wrapper)

    Update `build_tools_prompt` to show JSON examples first; keep one XML example labeled deprecated.

    Add/adjust agent crate test if prompt assertion needed.

    Run `cargo test -p agent` and `cargo test -p llm` to confirm Mock agent integration test still passes via JSON parser.
  </action>
  <verify>cargo test -p agent agent_uses_search_tool</verify>
  <acceptance_criteria>
    - Mock agent response has no `<tool_call>` substring
    - `cargo test -p agent` green
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: AskResponse metadata + ChatView UI (05-02-02)</name>
  <files>crates/rag/src/types.rs, src-tauri/src/lib.rs, src/types/ipc.ts, src/App.tsx, src/views/ChatView.tsx</files>
  <action>
    Add `tool_parse_warnings: Vec<String>` to `AskResponse` with `#[serde(default)]`.
    Mirror in `src/types/ipc.ts` as optional/default empty array.

    Update `agent_response_to_ask` in lib.rs (until 05-03 move) to map warnings.

    App.tsx: state `lastToolParseWarnings`; set from `resp.tool_parse_warnings ?? []` on agent ask; clear on new ask.

    ChatView: new optional prop `lastToolParseWarnings: string[]`; render amber panel with `data-testid="agent-tool-parse-warnings"` when length > 0 (similar styling to agent-tool-calls).

    Add Vitest or ensure tsc clean.
  </action>
  <verify>npm test && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `rg "agent-tool-parse-warnings" src/views/ChatView.tsx` matches
    - `rg "tool_parse_warnings" crates/rag/src/types.rs` matches
    - `npm test` and `npx tsc --noEmit` pass
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Mock uses JSON tool format
- [ ] AskResponse carries warnings
- [ ] ChatView renders warnings panel
- [ ] `cargo test -p agent` green
</verification>

<success_criteria>
Structured JSON path end-to-end for Mock; parse warnings visible in UI when triggered.
</success_criteria>
