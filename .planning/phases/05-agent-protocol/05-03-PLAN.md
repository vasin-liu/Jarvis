---
phase: 05-agent-protocol
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - src-tauri/src/commands/agent.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/lib.rs
requirements:
  - SHELL-04
  - SHELL-05
must_haves:
  truths:
    - "commands/agent.rs holds all agent IPC moved from lib.rs (D-13)"
    - "start_ask_e2e IPC unchanged — stays in lib.rs or commands/chat.rs, NOT renamed (D-14)"
    - "commands/config.rs untouched functionally — verify exports complete (D-16)"
    - "e2e.rs bootstrap path unchanged (SHELL-05)"
    - "generate_handler! agent command names byte-identical (D-15, P4)"
    - "cargo build --manifest-path src-tauri/Cargo.toml succeeds"
  artifacts:
    - path: src-tauri/src/commands/agent.rs
      provides: agent Tauri commands + helpers
      contains: "pub async fn ask_agent_in_session"
    - path: src-tauri/src/commands/mod.rs
      provides: agent module exports
      contains: "mod agent"
  key_links:
    - from: src-tauri/src/lib.rs
      to: src-tauri/src/commands/agent.rs
      via: "generate_handler! imports"
      pattern: "ask_agent_in_session"
---

<objective>
Relocate agent orchestration IPC and helpers from `lib.rs` to `commands/agent.rs` — pure structural move satisfying SHELL-04 agent portion and SHELL-05 verification.

Purpose: Complete command module split for agent surface; shrink lib.rs monolith.
Output: `commands/agent.rs`, updated mod.rs, slimmed lib.rs, unchanged IPC names.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-agent-protocol/05-CONTEXT.md
@src-tauri/src/lib.rs
@src-tauri/src/commands/memory.rs
@src-tauri/src/commands/chat.rs
@src-tauri/src/e2e.rs
</context>

<threat_model>
scope: structural_refactor_only
asvs_level: L1
trust_boundaries:
  - "Tauri IPC registry"
stride_register:
  - threat_id: T-05-B1
    category: Tampering
    component: generate_handler!
    disposition: mitigate
    mitigation_plan: "Diff handler list before/after; cargo build + E2E agent specs."
disposition_summary: "IPC drift is primary risk."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create commands/agent.rs and relocate (05-03-01)</name>
  <files>src-tauri/src/commands/agent.rs, src-tauri/src/commands/mod.rs, src-tauri/src/lib.rs</files>
  <action>
    Create `commands/agent.rs` and MOVE from lib.rs (make pub, keep #[tauri::command] signatures):
    - `list_agent_profiles`, `list_skills`, `list_hooks`, `list_plugins`
    - `upsert_agent_profile`, `remove_agent_profile`, `set_active_agent`
    - `ask_agent_in_session`, `ask_agent_in_session_stream`

    Also move agent-only helpers into agent.rs (pub(crate) as needed):
    - `run_ask_agent_in_session`, `execute_agent_question`, `build_agent_context`, `agent_response_to_ask`
    - `ConfigChatResolver`, `ConfigEmbedResolver` if only used by agent path

    Keep in lib.rs:
    - `start_ask_e2e`, `is_e2e_mode_cmd` (E2E bootstrap — D-14)
    - `seed_skills_dir`, `seed_hooks_dir`, `seed_plugins_dir` (startup — can stay lib.rs)

    Update commands/mod.rs exports.

    Update lib.rs imports + generate_handler! — same command identifiers.

    Verify `commands/config.rs` already exports get/set config + API key — no edits unless import fix required (SHELL-05).

    Do NOT modify `e2e.rs` behavior (SHELL-05).
  </action>
  <verify>cargo build --manifest-path src-tauri/Cargo.toml</verify>
  <acceptance_criteria>
    - `rg "fn list_agent_profiles" src-tauri/src/lib.rs` — no match
    - `rg "fn list_agent_profiles" src-tauri/src/commands/agent.rs` — match
    - `rg "start_ask_e2e" src-tauri/src/lib.rs` — still present
    - `cargo build --manifest-path src-tauri/Cargo.toml` succeeds
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Agent IPC relocated
- [ ] E2E bootstrap IPC preserved
- [ ] config.rs complete
- [ ] cargo build green
</verification>

<success_criteria>
SHELL-04 agent module + SHELL-05 verified; lib.rs further reduced.
</success_criteria>
