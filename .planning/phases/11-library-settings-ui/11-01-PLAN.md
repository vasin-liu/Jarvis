---
phase: 11-library-settings-ui
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/SettingsView.tsx
  - src/views/SettingsView.test.tsx
autonomous: true
requirements:
  - WIKI-06
must_haves:
  truths:
    - "Settings shows independent AccordionSection titled Wiki 笔记 with data-testid settings-section-wiki (D-01, D-15)"
    - "Only wiki.enabled is exposed; checkbox label is 启用 Wiki 笔记层 with data-testid wiki-enabled-toggle (D-02, D-13, D-16)"
    - "Toggle updates local config via setConfig only; persist remains bottom 保存配置 — no immediate save on checkbox (D-03)"
    - "Spreading wiki preserves auto_on_insights when toggling enabled (D-02 preserve field; RESEARCH pitfall)"
    - "Full wiki E2E journey deferred to Phase 13; phase gate is Vitest"
  artifacts:
    - path: src/views/SettingsView.tsx
      provides: "Wiki 笔记 AccordionSection + wiki-enabled-toggle"
      contains: "settings-section-wiki"
    - path: src/views/SettingsView.test.tsx
      provides: "Vitest for section, toggle, local-only mutate"
      contains: "wiki-enabled-toggle"
  key_links:
    - from: wiki-enabled-toggle
      to: setConfig
      via: "nested wiki.enabled local state; handleSaveConfig only on 保存配置"
      pattern: "wiki-enabled-toggle"
    - from: SettingsView
      to: AppConfig.wiki
      via: "config.wiki?.enabled ?? false; preserve auto_on_insights on spread"
      pattern: "auto_on_insights"
---

<objective>
Ship WIKI-06 Settings half: independent「Wiki 笔记」AccordionSection with wiki.enabled toggle and stable testids (D-01, D-02, D-03, D-13, D-15, D-16). No Library/compile wiring in this plan.

Purpose: Users can feature-flag the wiki layer via existing「保存配置」flow without implying auto-compile or export.
Output: SettingsView wiki section + SettingsView.test.tsx green under Vitest.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-library-settings-ui/11-CONTEXT.md
@.planning/phases/11-library-settings-ui/11-RESEARCH.md
@.planning/phases/11-library-settings-ui/11-PATTERNS.md
@.planning/phases/11-library-settings-ui/11-UI-SPEC.md
@.planning/phases/11-library-settings-ui/11-VALIDATION.md
@.planning/ROADMAP.md
@.cursor/rules/tdd-goal-driven.mdc
@.cursor/rules/e2e-required.mdc
@.cursor/rules/frontend-taste.mdc
@src/views/SettingsView.tsx
@src/views/ChatView.test.tsx
@src/hooks/useJarvisConfig.ts

<interfaces>
SettingsView uses useJarvisConfig internally (not props-driven config). Tests MUST vi.mock("../hooks/useJarvisConfig") returning controllable config, setConfig, handleSaveConfig, and stubs for other destructured fields SettingsView reads (minimal no-op stubs OK).

AccordionSection: local helper in SettingsView; testId + title; defaultExpanded false — expand via click on section header before querying checkbox.

Locked copy (UI-SPEC / D-13..D-16):
- Accordion title: Wiki 笔记 — testId settings-section-wiki
- Checkbox label: 启用 Wiki 笔记层 — input data-testid wiki-enabled-toggle
- Helper (Discretion, PATTERNS prescribed): 开启后可在资料库为已索引来源生成笔记页。关闭后仅隐藏生成入口，已有笔记页仍保留。

Placement (D-01 + PATTERNS): after settings-section-sync closes, before settings-section-lark.

Toggle mutate (D-03):
setConfig({ ...config, wiki: { enabled: e.target.checked, auto_on_insights: config.wiki?.auto_on_insights ?? false } })
Do not call handleSaveConfig from checkbox.

Do not add export UI, auto_on_insights checkbox, Library changes, or wiki.spec.ts (Phase 12/13 / deferred).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — SettingsView wiki section Vitest (11-01-01)</name>
  <files>src/views/SettingsView.test.tsx</files>
  <read_first>
src/views/SettingsView.tsx (AccordionSection, settings-section-sync / settings-section-lark placement, 自动洞察 checkbox row, 保存配置 button),
src/views/ChatView.test.tsx (jsdom + motion mock pattern),
src/hooks/useJarvisConfig.ts (return shape SettingsView destructures),
.planning/phases/11-library-settings-ui/11-CONTEXT.md (D-01, D-02, D-03, D-13, D-15, D-16),
.planning/phases/11-library-settings-ui/11-UI-SPEC.md (Copywriting Contract),
.planning/phases/11-library-settings-ui/11-PATTERNS.md (SettingsView prescribed wiki section),
.planning/phases/11-library-settings-ui/11-VALIDATION.md (Wave 0 SettingsView.test.tsx)
  </read_first>
  <behavior>
    - After expand settings-section-wiki: getByTestId("settings-section-wiki") and getByTestId("wiki-enabled-toggle") exist; visible text includes Wiki 笔记 and 启用 Wiki 笔记层 (D-01, D-13, D-15, D-16)
    - Checkbox checked reflects config.wiki.enabled (false → unchecked; true → checked)
    - Clicking wiki-enabled-toggle calls setConfig with wiki.enabled flipped and auto_on_insights preserved from fixture; handleSaveConfig is NOT called (D-02, D-03)
    - Within settings-section-wiki there is exactly one checkbox (no auto_on_insights control) (D-02)
  </behavior>
  <action>
    Wave 0 / RED per VALIDATION and TDD: create src/views/SettingsView.test.tsx with @vitest-environment jsdom. Mock motion/react like ChatView.test.tsx. Mock useJarvisConfig with a mutable fixture AppConfig including wiki: { enabled: false, auto_on_insights: false }, vi.fn setConfig, vi.fn handleSaveConfig, and stub remaining fields SettingsView destructures so render does not throw.

    Write failing tests for the behaviors above (expand accordion first). Confirm npx vitest run src/views/SettingsView.test.tsx fails RED before Task 2. Do not edit SettingsView.tsx in this task. Do not add E2E specs (Phase 13 owns wiki journey; D-E2E deferral).
  </action>
  <verify>
    <automated>npx vitest run src/views/SettingsView.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File src/views/SettingsView.test.tsx exists
    - Test file references settings-section-wiki and wiki-enabled-toggle
    - npx vitest run src/views/SettingsView.test.tsx exits non-zero (RED) until Task 2 implements UI
  </acceptance_criteria>
  <done>Failing SettingsView Vitest exists for wiki section and local-only toggle</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Wiki 笔记 AccordionSection + enabled toggle (11-01-02)</name>
  <files>src/views/SettingsView.tsx, src/views/SettingsView.test.tsx</files>
  <read_first>
src/views/SettingsView.tsx (lines around settings-section-sync close and settings-section-lark open; 自动洞察 checkbox pattern ~595–607),
.planning/phases/11-library-settings-ui/11-PATTERNS.md (Prescribed wiki section),
.planning/phases/11-library-settings-ui/11-UI-SPEC.md,
.planning/phases/11-library-settings-ui/11-CONTEXT.md (D-01, D-02, D-03, D-13, D-15, D-16)
  </read_first>
  <behavior>
    - Same as Task 1 — all SettingsView.test.tsx cases pass
  </behavior>
  <action>
    Per D-01, D-02, D-03, D-13, D-15, D-16 and PATTERNS prescribed block: insert AccordionSection testId="settings-section-wiki" title="Wiki 笔记" after settings-section-sync closes and before settings-section-lark. Inside: helper paragraph (UI-SPEC Discretion Chinese copy from PATTERNS) + single checkbox row with data-testid="wiki-enabled-toggle", label 启用 Wiki 笔记层, checked={config.wiki?.enabled ?? false}, onChange spreads wiki.enabled while preserving auto_on_insights via config.wiki?.auto_on_insights ?? false.

    Do not nest under 自动洞察. Do not add a second checkbox for auto_on_insights (D-02). Do not call handleSaveConfig from the toggle (D-03). Do not add cyan/accent styling beyond existing checkbox row chrome (UI-SPEC). Do not touch LibraryView, App.tsx, tauri.ts, or add export controls. Adjust test mocks only if needed for GREEN; keep assertions locked to CONTEXT/UI-SPEC.
  </action>
  <verify>
    <automated>npx vitest run src/views/SettingsView.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "settings-section-wiki" src/views/SettingsView.tsx matches
    - rg -n "wiki-enabled-toggle" src/views/SettingsView.tsx matches
    - rg -n "启用 Wiki 笔记层" src/views/SettingsView.tsx matches
    - rg -n "Wiki 笔记" src/views/SettingsView.tsx matches Accordion title
    - settings-section-wiki appears after settings-section-sync and before settings-section-lark in file order
    - Wiki section has exactly one input type checkbox (wiki-enabled-toggle only)
    - npx vitest run src/views/SettingsView.test.tsx exits 0
  </acceptance_criteria>
  <done>Settings wiki toggle ships with Vitest green; save still via 保存配置</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings UI → setConfig local state | Untrusted checkbox events mutate in-memory AppConfig only until 保存配置 |
| Settings UI → set_config IPC | Existing handleSaveConfig persists full AppConfig including nested wiki |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | wiki-enabled-toggle | medium | mitigate | Local-only mutate (D-03); persist only via existing 保存配置; Vitest asserts handleSaveConfig not called on toggle |
| T-11-02 | Tampering | wiki.auto_on_insights wipe | medium | mitigate | Spread preserves auto_on_insights when toggling enabled; Vitest fixture asserts preserve |
| T-11-03 | Spoofing | Premature auto-compile UI | low | mitigate | No auto_on_insights checkbox (D-02); exactly one checkbox in wiki section |
| T-11-04 | Information Disclosure | Settings helper copy | low | accept | Non-sensitive product copy; no secrets |
| T-11-SC | Tampering | npm installs | low | accept | No new packages — N/A |
</threat_model>

<verification>
- Vitest SettingsView: section + toggle + local-only setConfig + no auto_on_insights control
- No Library compile button work in this plan
- No wiki.spec.ts / E2E (Phase 13)
</verification>

<success_criteria>
ROADMAP criterion 1: Settings exposes wiki enabled toggle with stable data-testid. WIKI-06 Settings half complete.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Symbols / notes | Requirement |
|----------|------|-----------------|-------------|
| Wiki Settings AccordionSection | `src/views/SettingsView.tsx` | `settings-section-wiki`, `wiki-enabled-toggle`, label「启用 Wiki 笔记层」 | WIKI-06 (D-01..D-03, D-13, D-15, D-16) |
| Settings Vitest | `src/views/SettingsView.test.tsx` | section/toggle/local-mutate cases | WIKI-06 / VALIDATION |

<output>
Create `.planning/phases/11-library-settings-ui/11-01-SUMMARY.md` when done
</output>
