---
phase: 04-memory-model-view
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - src/types/memory.ts
  - src/lib/tauri.ts
  - src/hooks/useMemory.ts
  - src/hooks/useMemory.test.ts
  - src/views/MemoryView.tsx
  - src/App.tsx
requirements:
  - FE-04
  - FE-06
must_haves:
  truths:
    - "useMemory owns memory domain IPC via src/lib/tauri.ts — no direct invoke in MemoryView (D-08, D-11)"
    - "useMemory is SEPARATE from useLibrary/useTasks (D-10)"
    - "MemoryView is controlled component; ALL Memory JSX leaves App.tsx; stub {children} replaced (D-09)"
    - "Inline edit panel preserved: memory-edit-panel below list item (D-09)"
    - "Save edit passes title: null to backend for title regeneration (D-12, P3)"
    - "All memory-* testids preserved on moved JSX (D-13, FE-06)"
    - "refreshLibrary in App calls useMemory.refreshMemories (P5)"
    - "npm test green; npx tsc --noEmit clean"
  artifacts:
    - path: src/hooks/useMemory.ts
      provides: useMemory hook owning memory IPC + edit state
      contains: "export function useMemory"
    - path: src/views/MemoryView.tsx
      provides: full controlled Memory view
      contains: "data-testid=\"memory-list\""
    - path: src/types/memory.ts
      provides: MemorySource type
      contains: "export"
    - path: src/hooks/useMemory.test.ts
      provides: Vitest hook boundary test
      contains: "useMemory"
    - path: src/lib/tauri.ts
      provides: listMemories/getMemoryContent/addMemory/updateMemory/forgetMemory wrappers
      contains: "listMemories"
  key_links:
    - from: src/App.tsx
      to: src/hooks/useMemory.ts
      via: "const memoryHook = useMemory({ onError: setErr })"
      pattern: "useMemory\\("
    - from: src/App.tsx
      to: src/views/MemoryView.tsx
      via: "<MemoryView ...controlled props />"
      pattern: "<MemoryView"
    - from: src/hooks/useMemory.ts
      to: src/lib/tauri.ts
      via: "memory IPC wrappers"
      pattern: "from \"../lib/tauri\""
---

<objective>
Extract the full Memory view from `App.tsx` into `MemoryView.tsx` + `useMemory.ts` + `src/types/memory.ts`, route memory IPC through `src/lib/tauri.ts`, and wire App as controlled parent — preserving inline edit UX and all `memory-*` testids.

Purpose: Deliver FE-04 (Memory view + dedicated hook) and FE-06 (testid preservation).
Output: types, tauri wrappers, hook + Vitest, full MemoryView, slimmed App.tsx.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-memory-model-view/04-CONTEXT.md
@.planning/phases/04-memory-model-view/04-RESEARCH.md
@.cursor/rules/e2e-required.mdc
@.cursor/rules/frontend-taste.mdc
@src/App.tsx
@src/views/MemoryView.tsx
@src/views/TasksView.tsx
@src/hooks/useTasks.ts
@src/hooks/useLibrary.ts
@src/lib/tauri.ts
@e2e/specs/memory.spec.ts
@e2e/specs/full-ui.spec.ts
</context>

<threat_model>
scope: frontend_extraction_only
asvs_level: L1
trust_boundaries:
  - "Tauri IPC boundary — invoke names unchanged, only wrapped in tauri.ts"
  - "DOM / E2E selector contract — memory-* testids are automation trust surface"
stride_register:
  - threat_id: T-04-C1
    category: Tampering
    component: memory data-testid selectors during JSX move
    disposition: mitigate
    mitigation_plan: "Move testids onto same semantic elements in MemoryView. Plan 04-04 E2E verifies memory.spec + full-ui."
  - threat_id: T-04-C2
    category: Integrity
    component: memory edit save title regeneration
    disposition: mitigate
    mitigation_plan: "useMemory.saveEdit must pass title: null (D-12) — same as current App.tsx handleSaveMemoryEdit."
disposition_summary: "Frontend relocation risk is selector drift and edit regression — mitigated by testids + title:null contract."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: types/memory.ts + tauri.ts memory wrappers (04-03-01)</name>
  <files>src/types/memory.ts, src/lib/tauri.ts</files>
  <read_first>src/types/library.ts (Source shape), src/lib/tauri.ts (listTasks pattern), src/App.tsx memory invoke calls</read_first>
  <action>
    Create `src/types/memory.ts`:
    - Export `MemorySource` type matching fields used in Memory UI (id, title, uri, kind, status, etc.) — can alias/re-export from library Source if identical.

    Extend `src/lib/tauri.ts` with:
    - `listMemories()` → invoke Source[]("list_memories_cmd")
    - `getMemoryContent(sourceId: string)` → invoke string("get_memory_content_cmd", { sourceId })
    - `addMemory(content: string, title?: string | null)` → invoke string("add_memory_cmd", { content, title })
    - `updateMemory(id: string, content: string, title?: string | null)` → invoke string("update_memory_cmd", { id, content, title })
    - `forgetMemory(id: string)` → invoke void("forget_memory_cmd", { id })

    Match serde camelCase arg names exactly as current App.tsx invoke calls.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `rg "listMemories|forgetMemory" src/lib/tauri.ts` matches
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: useMemory hook + Vitest (04-03-02)</name>
  <files>src/hooks/useMemory.ts, src/hooks/useMemory.test.ts</files>
  <read_first>src/hooks/useTasks.ts, src/hooks/useTasks.test.ts, src/App.tsx memory handlers (~583-880, 142-145)</read_first>
  <action>
    Create `useMemory.ts` mirroring useTasks pattern:
    - State: memories[], newMemoryText, editingMemoryId, editingMemoryContent, editingMemoryTitle (or keep edit state in view — prefer hook for parity with current App logic)
    - `refreshMemories()` calls listMemories, sets state
    - `addMemory()` — trim text, call addMemory IPC, clear input, refresh
    - `startEdit(id)` — getMemoryContent + find title from list
    - `saveEdit()` — **pass title: null** to updateMemory (D-12)
    - `cancelEdit()`, `forgetMemory(id)`
    - Accept `{ onError?: (msg: string) => void }` and `{ setBusy?: (b: boolean) => void }` OR let App wrap with busy — match Tasks pattern: hook catches errors via onError; App sets busy around handlers if needed

    Prefer: hook methods don't set global busy — App passes busy prop to MemoryView and wraps handlers with setBusy like current code.

    Create useMemory.test.ts:
    - Mock tauri.ts listMemories/addMemory
    - Assert refreshMemories populates memories state
  </action>
  <verify>npm test -- src/hooks/useMemory.test.ts</verify>
  <acceptance_criteria>
    - `rg "export function useMemory" src/hooks/useMemory.ts` matches
    - `rg "title: null" src/hooks/useMemory.ts` matches in save path
    - `npm test -- src/hooks/useMemory.test.ts` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: MemoryView extraction + App.tsx wiring (04-03-03)</name>
  <files>src/views/MemoryView.tsx, src/App.tsx</files>
  <read_first>src/App.tsx memory JSX ~1052-1177, src/views/TasksView.tsx (controlled props pattern), .cursor/rules/frontend-taste.mdc</read_first>
  <action>
    Replace MemoryView stub with full controlled component receiving props:
    - memories, busy, err (optional display)
    - newMemoryText, onNewMemoryTextChange
    - editingMemoryId, editingMemoryContent, editingMemoryTitle, memoryEditContentRef (or internal ref in view)
    - onAddMemory, onEditMemory, onSaveEdit, onCancelEdit, onForgetMemory

    Move ALL memory JSX from App.tsx into MemoryView preserving:
    - motion wrappers if present
    - Tabler icons
    - Every data-testid: new-memory-input, add-memory-submit, memory-list, memory-edit-{id}, memory-forget-{id}, memory-edit-panel, memory-edit-title, memory-edit-content, memory-save-edit

    In App.tsx:
    - Instantiate useMemory({ onError: setErr })
    - Remove inline memory useState/handlers replaced by hook
    - Wire refreshLibrary to call memoryHook.refreshMemories (replace direct refreshMemories callback)
    - Replace MemoryView {children} block with `<MemoryView ...props />`
    - Keep view === "memory" conditional in App; MemoryView is presentational only

    Run `npm run build` or `npm run build:e2e` prep if needed for E2E plan.
  </action>
  <verify>npm test && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `rg "data-testid=\"memory-list\"" src/views/MemoryView.tsx` matches
    - `rg "new-memory-input" src/App.tsx` returns no match (moved out)
    - `rg "<MemoryView" src/App.tsx` matches with props (not children-only stub)
    - `npm test` and `npx tsc --noEmit` pass
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `npm test` passes
- [ ] `npx tsc --noEmit` clean
- [ ] All memory JSX in MemoryView.tsx
- [ ] No direct invoke("list_memories_cmd") in App.tsx or MemoryView.tsx
- [ ] title: null on save edit preserved
</verification>

<success_criteria>
FE-04 complete: Memory view extracted with useMemory; testids preserved; ready for E2E gate in Plan 04.
</success_criteria>
