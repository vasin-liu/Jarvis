---
phase: 18-mcp-tools-search-list-sources
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - crates/retriever/src/kb_readonly.rs
  - crates/retriever/src/lib.rs
  - crates/retriever/Cargo.toml
autonomous: true
requirements:
  - MCP-01
  - MCP-02
must_haves:
  truths:
    - "search_kb calls retrieve with RetrieverConfig::default then clamps optional limit to ≤ final_k (8) (D-03, D-04, MCP-01)"
    - "search_kb excerpts are ≤200 Unicode chars with ellipsis; empty query returns RetrieveError::EmptyQuery (D-05)"
    - "list_indexed_sources returns only IndexStatus::Indexed entries with id/title/kind, hard-cap 200 + truncated/total_indexed, no uri (D-06, D-07, D-08, MCP-02)"
  artifacts:
    - path: crates/retriever/src/kb_readonly.rs
      provides: "search_kb, list_indexed_sources, KbHit, KbSourceList, EXCERPT_MAX_CHARS=200, LIST_SOURCES_MAX=200"
    - path: crates/retriever/src/lib.rs
      provides: "mod kb_readonly + pub use of helpers/types/consts"
  key_links:
    - from: crates/retriever/src/kb_readonly.rs
      to: crates/retriever/src/retrieve.rs
      via: "retrieve + RetrieverConfig::default; clamp final_k (D-03, D-04)"
      pattern: "RetrieverConfig::default"
    - from: crates/retriever/src/kb_readonly.rs
      to: store::Store
      via: "list_sources + get_source; never open SQLite outside store"
      pattern: "list_sources"
---

<objective>
Add shared `retriever::kb_readonly` structured helpers (`search_kb`, `list_indexed_sources`) with TDD coverage so agent and MCP cannot fork hybrid search or Indexed listing.

Purpose: MCP-01/MCP-02 anti-drift core (D-01); all retrieval inputs/filters/limits live in one module before presenters exist.
Output: `kb_readonly.rs` + re-exports; green `cargo test -p retriever` covering search clamp, excerpt, empty query, Indexed filter, cap 200, no URI fields.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-mcp-tools-search-list-sources/18-CONTEXT.md
@.planning/phases/18-mcp-tools-search-list-sources/18-RESEARCH.md
@.planning/phases/18-mcp-tools-search-list-sources/18-PATTERNS.md
@.planning/phases/18-mcp-tools-search-list-sources/18-VALIDATION.md
@crates/retriever/src/related.rs
@crates/retriever/src/retrieve.rs
@crates/retriever/src/lib.rs
@crates/agent/src/tools.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED kb_readonly tests for search_kb + list_indexed_sources</name>
  <read_first>
    - .planning/phases/18-mcp-tools-search-list-sources/18-CONTEXT.md (D-01, D-03..D-08)
    - .planning/phases/18-mcp-tools-search-list-sources/18-RESEARCH.md (Pattern 1; Validation Architecture; discretion locks)
    - .planning/phases/18-mcp-tools-search-list-sources/18-PATTERNS.md (related.rs analog; fixture pattern)
    - crates/retriever/src/related.rs
    - crates/retriever/src/retrieve.rs (RetrieverConfig::default, EmptyQuery, hybrid fixture tests)
  </read_first>
  <files>crates/retriever/src/kb_readonly.rs, crates/retriever/src/lib.rs, crates/retriever/Cargo.toml</files>
  <behavior>
    - search_kb_uses_default_final_k: without limit, result len ≤ RetrieverConfig::default().final_k (8)
    - search_kb_clamps_limit_to_final_k: limit Some(100) still yields ≤ 8 hits
    - search_kb_empty_query_errors: whitespace/empty query → Err(EmptyQuery); no panic
    - search_kb_excerpt_max_200_chars: each hit.excerpt char count ≤ 200 (ellipsis if truncated)
    - list_indexed_sources_filters_status: Pending/Error sources absent; Indexed present
    - list_indexed_sources_cap_200: when &gt;200 Indexed seeded (or mocked list), sources.len()≤200, truncated=true, total_indexed reflects full count
    - list_indexed_sources_fields_omit_uri: entries expose only id, title, kind — no uri field on KbSourceEntry
  </behavior>
  <action>
    Per D-01 and RESEARCH discretion lock: create module at crates/retriever/src/kb_readonly.rs (not a new workspace crate; not inside mcp/agent).

    RED first: add #[cfg(test)] module in kb_readonly.rs (preferred, mirrors related/retrieve) OR integration tests under crates/retriever — use tempfile + MockEmbedder + indexer::index_path fixture pattern from retrieve.rs tests. Ensure cargo test -p retriever shows the behaviors above failing before implementation (assertion failures, not missing-module-only if avoidable).

    Confirm crates/retriever already has tempfile/indexer/embedder/tokio test deps; add only if missing for fixtures.

    Do not touch agent tools or mcp yet (Wave 2–3). Do not invent alternate fusion or second vector store (D-03).
  </action>
  <verify>
    <automated>cargo test -p retriever kb_readonly -- --test-threads=4</automated>
  </verify>
  <done>Failing kb_readonly tests exist describing MCP-01/MCP-02 helper contracts (D-01, D-03..D-08).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN implement search_kb + list_indexed_sources + re-export</name>
  <read_first>
    - crates/retriever/src/kb_readonly.rs (RED tests from Task 1)
    - crates/retriever/src/related.rs (DTO + truncate + retrieve wrapper)
    - crates/retriever/src/error.rs
    - store IndexStatus / SourceKind::as_str
  </read_first>
  <files>crates/retriever/src/kb_readonly.rs, crates/retriever/src/lib.rs</files>
  <behavior>
    - Same seven behaviors as Task 1 must pass
  </behavior>
  <action>
    Implement per D-03/D-04/D-05/D-06/D-07/D-08 and RESEARCH sketch:

    Constants: EXCERPT_MAX_CHARS = 200, LIST_SOURCES_MAX = 200. Public truncate_excerpt using chars().take + ellipsis (never byte-slice).

    Types: KbHit { chunk_id, source_id, title, loc, excerpt }; KbSourceEntry { id, title, kind: String }; KbSourceList { sources, truncated, total_indexed }. Prefer snake_case serde if Serialize added for MCP reuse; no uri/score/full-text on these DTOs.

    search_kb(store, embedder, query, limit: Option&lt;usize&gt;): start from RetrieverConfig::default(); if Some(n), set final_k = n.clamp(1, default_final_k); await retrieve; map ChunkHit → KbHit via store.get_source for title; truncate excerpt; propagate EmptyQuery/Store/Embed errors.

    list_indexed_sources(store): store.list_sources → filter IndexStatus::Indexed → preserve store ORDER BY id; total_indexed = full filtered len; take LIST_SOURCES_MAX; truncated = total_indexed &gt; LIST_SOURCES_MAX; map id/title/kind.as_str only — never copy Source.uri.

    Wire lib.rs: mod kb_readonly; pub use search_kb, list_indexed_sources, KbHit, KbSourceEntry, KbSourceList, EXCERPT_MAX_CHARS, LIST_SOURCES_MAX (and truncate_excerpt if useful).

    Leave related.rs / retrieve.rs behavior unchanged.
  </action>
  <verify>
    <automated>cargo test -p retriever -- --test-threads=4</automated>
  </verify>
  <done>Shared kb_readonly helpers green; hybrid path + Indexed inventory caps locked (MCP-01, MCP-02, D-01..D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Helper caller → KB read API | Untrusted query string / optional limit cross into retrieve |
| Helper → Store | Structured inventory must not leak filesystem paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Information Disclosure | list_indexed_sources | high | mitigate | Omit uri/paths from KbSourceEntry (D-08); tests assert no uri field |
| T-18-02 | Denial of Service | list_indexed_sources / search_kb | medium | mitigate | LIST_SOURCES_MAX=200; clamp limit ≤ final_k=8 (D-04, D-07) |
| T-18-03 | Tampering | search_kb | low | mitigate | Reuse retrieve + Store parameterized queries only; no rusqlite in retriever |
| T-18-04 | Spoofing | empty query | low | mitigate | Propagate EmptyQuery; never panic or silent empty-as-success for invalid query |
| T-18-SC | Tampering | cargo installs | high | accept | No new registry packages this plan |
</threat_model>

<verification>
cargo test -p retriever (full crate) green; kb_readonly behaviors covered.
</verification>

<success_criteria>
- kb_readonly module exists and is re-exported from retriever
- search uses default hybrid config with limit clamp
- list is Indexed-only, capped at 200 with truncation metadata, no paths
</success_criteria>

<output>
Create `.planning/phases/18-mcp-tools-search-list-sources/18-01-SUMMARY.md` when done
</output>
