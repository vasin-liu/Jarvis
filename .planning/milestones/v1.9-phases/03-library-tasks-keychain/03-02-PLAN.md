---
phase: 03-library-tasks-keychain
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src-tauri/src/commands/index.rs
  - src-tauri/src/commands/lark.rs
  - src-tauri/src/commands/library.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/state.rs
  - src-tauri/src/lib.rs
requirements:
  - SHELL-03
must_haves:
  truths:
    - "commands/index.rs holds the local-index IPC: rebuild_index, reinit_and_rebuild_index, retry_source, list_cursor_transcripts, sync_cursor_transcripts_cmd, add_watch_folder, remove_watch_folder, index_file (D-20)"
    - "commands/lark.rs holds all Lark IPC: check_lark_connection, detect_lark_cli, sync_lark_doc, sync_lark_url, sync_lark_sheet, sync_lark_mail, sync_lark_im (D-21)"
    - "commands/library.rs is extended with the task commands list_tasks, update_task_status, delete_task AND the insights commands summarize_source_cmd, extract_tasks_cmd, run_insights_all_cmd — NO commands/tasks.rs module is created (D-22, Pitfall P9)"
    - "lark_opts becomes a method AppState::lark_opts(&self) -> LarkCliOptions in state.rs; the free fn in lib.rs is removed and both index.rs and lark.rs call state.lark_opts() (Pitfall P8)"
    - "Commands are thin wrappers; all orchestration stays in index_ops.rs which is structurally untouched (D-23, D-25)"
    - "Every IPC command name is byte-for-byte identical to before; tauri::generate_handler! lists the same command set, just resolved from commands::* (no renames)"
    - "cargo build (src-tauri) and cargo test --workspace pass"
  artifacts:
    - path: src-tauri/src/commands/index.rs
      provides: relocated local-index Tauri commands (thin wrappers over index_ops)
      contains: "pub async fn rebuild_index"
    - path: src-tauri/src/commands/lark.rs
      provides: relocated Lark Tauri commands (sync/auth/health)
      contains: "pub async fn sync_lark_doc"
    - path: src-tauri/src/commands/library.rs
      provides: sources + tasks + insights commands (no separate tasks.rs)
      contains: "pub fn list_tasks"
    - path: src-tauri/src/state.rs
      provides: AppState::lark_opts helper shared by index + lark commands
      contains: "fn lark_opts"
  key_links:
    - from: src-tauri/src/lib.rs
      to: src-tauri/src/commands/mod.rs
      via: "tauri::generate_handler! references commands::index::* / commands::lark::* / commands::library::*"
      pattern: "generate_handler!"
    - from: src-tauri/src/commands/index.rs
      to: src-tauri/src/state.rs
      via: "state.lark_opts() for LarkCliOptions"
      pattern: "lark_opts"
    - from: src-tauri/src/commands/lark.rs
      to: src-tauri/src/index_ops.rs
      via: "delegates fetch/sync helpers; no logic moved"
      pattern: "index_ops"
---

<objective>
Relocate the index and Lark Tauri commands out of the `lib.rs` monolith into `src-tauri/src/commands/index.rs` and `src-tauri/src/commands/lark.rs`, extend `commands/library.rs` with the task + insights commands (no separate `tasks.rs`), and move the `lark_opts` helper to `AppState::lark_opts` so both new modules share it — a pure relocation with zero IPC or behavior change.

Purpose: Deliver SHELL-03 — index/Lark commands grouped in dedicated modules with progress events and IPC signatures preserved, shrinking `lib.rs` toward the Phase 6 thin-shell target.
Output: `commands/index.rs`, `commands/lark.rs`, extended `commands/library.rs`, `AppState::lark_opts`, updated `commands/mod.rs` exports, and a `generate_handler!` that lists the identical command set resolved from the new modules. `index_ops.rs` untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-library-tasks-keychain/03-CONTEXT.md
@.planning/phases/03-library-tasks-keychain/03-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src-tauri/src/lib.rs
@src-tauri/src/state.rs
@src-tauri/src/commands/mod.rs
@src-tauri/src/commands/library.rs
@src-tauri/src/commands/config.rs
@src-tauri/src/commands/sync.rs
@src-tauri/src/index_ops.rs
@src-tauri/src/e2e.rs
</context>

<threat_model>
scope: structural_refactor_only
asvs_level: L1
trust_boundaries:
  - "Tauri IPC boundary — command names are the frontend contract; no rename permitted"
  - "Lark subprocess boundary — lark-cli invoked via ProcessRunner inside index_ops (unchanged)"
stride_register:
  - threat_id: T-03-B1
    category: Tampering
    component: tauri::generate_handler! command registry
    disposition: mitigate
    mitigation_plan: "Relocation only — the same command identifiers are registered, just imported from commands::*. A git diff of generate_handler! must show no name added/removed/renamed; cargo build fails fast if a handler is dropped."
  - threat_id: T-03-B2
    category: Information Disclosure
    component: Lark command error strings
    disposition: accept
    mitigation_plan: "Existing Result<_, String> mapping is moved verbatim; no new error content surfaced. E2E lark fixture path (e2e::lark_fixture_from_input) is preserved unchanged."
disposition_summary: "Pure command relocation + shared helper. No new attack surface; risk is accidental IPC drift, mitigated by compile + unchanged-registry diff."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Move lark_opts to AppState + create commands/index.rs (03-02-01a)</name>
  <files>src-tauri/src/state.rs, src-tauri/src/commands/index.rs, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/lib.rs (lark_opts at line 56-61; index commands rebuild_index 71-, reinit_and_rebuild_index 93-, retry_source 121-, list_cursor_transcripts 144-, sync_cursor_transcripts_cmd 156-, add_watch_folder 176-, remove_watch_folder 213-, index_file 836-), src-tauri/src/state.rs, src-tauri/src/index_ops.rs, .planning/phases/03-library-tasks-keychain/03-RESEARCH.md (§2 Command Module Pattern, §7 Registration, Pitfall P8), 03-CONTEXT.md D-20/D-23/D-25</read_first>
  <action>
    Per Pitfall P8 / Research §"lark_opts helper": move the free `fn lark_opts(cfg: &AppConfig) -> LarkCliOptions<'_>` from `lib.rs` (lines 56-61) into `state.rs` as a method `impl AppState { pub(crate) fn lark_opts(&self) -> LarkCliOptions<'_> }` building from `self.config().lark.lark_cli_bin` + `LarkIdentity::parse(&self.config().lark.lark_identity)`. Keep the borrow lifetimes correct (clone the config or hold the cfg as needed). Remove the free fn from `lib.rs`.

    Per D-20: create `src-tauri/src/commands/index.rs` and MOVE these command fns verbatim from `lib.rs` (change `fn` privacy to `pub`, keep `#[tauri::command]`, keep async-ness and exact signatures): `rebuild_index`, `reinit_and_rebuild_index`, `retry_source`, `list_cursor_transcripts`, `sync_cursor_transcripts_cmd`, `add_watch_folder`, `remove_watch_folder`, `index_file`. Fix `use` paths: import `AppState` from `crate::state`, `index_ops` helpers from `crate::index_ops`, emit helpers (`emit_index_progress`/`emit_index_complete`) from `crate`, and call `state.lark_opts()` where the old code called the free `lark_opts(&cfg)`.

    Per D-23/D-25: do NOT move any orchestration logic into `index.rs`; commands stay thin wrappers delegating to `index_ops.rs`. `index_ops.rs` is not edited.

    In `lib.rs`: delete the moved fn bodies (they now live in `commands/index.rs`). Do not yet touch `generate_handler!` beyond what is needed to keep it compiling against `commands::index::*` — registration consolidation finishes in Task 2.
  </action>
  <verify>cargo build --manifest-path src-tauri/Cargo.toml</verify>
  <acceptance_criteria>
    - `src-tauri/src/state.rs` defines `fn lark_opts`; `rg -n "fn lark_opts" src-tauri/src/lib.rs` returns no match
    - `src-tauri/src/commands/index.rs` defines `rebuild_index`, `reinit_and_rebuild_index`, `retry_source`, `list_cursor_transcripts`, `sync_cursor_transcripts_cmd`, `add_watch_folder`, `remove_watch_folder`, `index_file`, each with `#[tauri::command]`
    - `rg -n "fn rebuild_index" src-tauri/src/lib.rs` returns no match (moved out)
    - `rg "index_ops" src-tauri/src/commands/index.rs` matches (delegation preserved); no `RebuildReport` orchestration body was inlined
    - `cargo build --manifest-path src-tauri/Cargo.toml` succeeds
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: create commands/lark.rs, extend commands/library.rs, update mod.rs + generate_handler! (03-02-01b)</name>
  <files>src-tauri/src/commands/lark.rs, src-tauri/src/commands/library.rs, src-tauri/src/commands/mod.rs, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/lib.rs (lark cmds check_lark_connection 596-, detect_lark_cli 607-, sync_lark_doc 760-, sync_lark_url 777-, sync_lark_sheet 800-, sync_lark_mail 812-, sync_lark_im 824-; insights summarize_source_cmd 639-, extract_tasks_cmd 653-, run_insights_all_cmd 667-; tasks list_tasks 683-, update_task_status 688-, delete_task 701-; generate_handler! 947-1000), src-tauri/src/commands/mod.rs, src-tauri/src/commands/library.rs, src-tauri/src/commands/sync.rs, .planning/phases/03-library-tasks-keychain/03-RESEARCH.md (§7 Registration pattern, Pitfall P9), 03-CONTEXT.md D-21/D-22</read_first>
  <action>
    Per D-21: create `src-tauri/src/commands/lark.rs` and MOVE verbatim from `lib.rs`: `check_lark_connection`, `detect_lark_cli`, `sync_lark_doc`, `sync_lark_url`, `sync_lark_sheet`, `sync_lark_mail`, `sync_lark_im` (make `pub`, keep `#[tauri::command]`, signatures unchanged). Preserve the `is_e2e_mode()` / `e2e::lark_fixture_from_input` branches exactly. Call `state.lark_opts()` for `LarkCliOptions`. Import `LarkAuthStatus` and lark types from their existing paths.

    Per D-22/Pitfall P9: extend `src-tauri/src/commands/library.rs` (do NOT create `commands/tasks.rs`) with the task commands `list_tasks`, `update_task_status`, `delete_task` and the insights commands `summarize_source_cmd`, `extract_tasks_cmd`, `run_insights_all_cmd`, moved verbatim from `lib.rs` (`pub`, `#[tauri::command]`, unchanged signatures). Keep the existing `source_count`/`list_sources`/`remove_source` in place.

    Update `src-tauri/src/commands/mod.rs`: add `pub(crate) mod index;` and `pub(crate) mod lark;`; add `pub use index::{rebuild_index, reinit_and_rebuild_index, retry_source, list_cursor_transcripts, sync_cursor_transcripts_cmd, add_watch_folder, remove_watch_folder, index_file};`, `pub use lark::{check_lark_connection, detect_lark_cli, sync_lark_doc, sync_lark_url, sync_lark_sheet, sync_lark_mail, sync_lark_im};` and extend the `library::{...}` re-export with `list_tasks, update_task_status, delete_task, summarize_source_cmd, extract_tasks_cmd, run_insights_all_cmd`.

    In `lib.rs`: delete the moved fn bodies; ensure the `use` brings the relocated commands into scope (e.g. `use commands::{...}`) so `tauri::generate_handler![...]` (lines 947-1000) keeps listing the SAME identifiers with NO additions, removals, or renames. The handler list contents must be unchanged set-wise.
  </action>
  <verify>cargo test --workspace</verify>
  <acceptance_criteria>
    - `src-tauri/src/commands/lark.rs` defines all 7 Lark commands; `rg -n "fn sync_lark_doc" src-tauri/src/lib.rs` returns no match
    - `rg -n "fn list_tasks|fn update_task_status|fn delete_task|fn summarize_source_cmd|fn extract_tasks_cmd|fn run_insights_all_cmd" src-tauri/src/commands/library.rs` matches all six
    - No file named `src-tauri/src/commands/tasks.rs` exists (P9)
    - `src-tauri/src/commands/mod.rs` exports `index::*`, `lark::*`, and the extended `library::*` set
    - The `tauri::generate_handler!` list in `lib.rs` registers the identical command names as before (set unchanged; verify against the pre-change list of 50 commands)
    - `cargo build --manifest-path src-tauri/Cargo.toml` succeeds and `cargo test --workspace` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cargo build --manifest-path src-tauri/Cargo.toml` succeeds
- [ ] `cargo test --workspace` passes
- [ ] `generate_handler!` command set unchanged (no IPC rename/drop) — diff review
- [ ] `lark_opts` is an `AppState` method; no free fn remains in `lib.rs` (P8)
- [ ] No `commands/tasks.rs` created (P9)
- [ ] `index_ops.rs` unchanged (D-25) — `git diff --stat` shows no edit
</verification>

<success_criteria>
- Index commands in `commands/index.rs`; Lark commands in `commands/lark.rs` (D-20/D-21)
- Task + insights commands in `commands/library.rs`, not a new module (D-22)
- Thin wrappers only; `index_ops.rs` orchestration untouched (D-23/D-25)
- IPC names byte-identical; `cargo test --workspace` green (SHELL-03, QA-03 partial)
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Location |
|---------------|------|----------|
| `rebuild_index` … `index_file` (relocated) | Tauri commands | `src-tauri/src/commands/index.rs` |
| `check_lark_connection` … `sync_lark_im` (relocated) | Tauri commands | `src-tauri/src/commands/lark.rs` |
| `list_tasks` / `update_task_status` / `delete_task` | Tauri commands | `src-tauri/src/commands/library.rs` |
| `summarize_source_cmd` / `extract_tasks_cmd` / `run_insights_all_cmd` | Tauri commands | `src-tauri/src/commands/library.rs` |
| `AppState::lark_opts` | method | `src-tauri/src/state.rs` |
| Updated `commands/mod.rs` exports | module re-exports | `src-tauri/src/commands/mod.rs` |

<output>
After completion, create `.planning/phases/03-library-tasks-keychain/03-02-SUMMARY.md`
</output>
