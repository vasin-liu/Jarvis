---
phase: 19-e2e-citation-regression-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/mcp.md
  - README.md
autonomous: true
requirements:
  - TRUST-02
must_haves:
  truths:
    - "cargo test -p mcp is green (allowlist + tools_kb + paths) (D-05)"
    - "docs/mcp.md describes live hybrid search and Indexed list_sources — no obsolete stub-only host guidance (D-06)"
    - "README still points hosts to docs/mcp.md for MCP stdio (D-06)"
  artifacts:
    - path: docs/mcp.md
      provides: "Accurate live-tool MCP host docs (docs-only edits if drift)"
    - path: README.md
      provides: "MCP (stdio) pointer to docs/mcp.md"
  key_links:
    - from: cargo test -p mcp
      to: crates/mcp/tests/allowlist.rs
      via: "Exact tool allowlist {search, list_sources}"
      pattern: "tool_allowlist"
    - from: docs/mcp.md
      to: crates/mcp/src/server.rs
      via: "Documented tools match Phase 18 handlers"
      pattern: "search|list_sources"
---

<objective>
Prove MCP offline happy paths via cargo and confirm host docs/README match Phase 18 live tools.

Purpose: TRUST-02 / D-05 / D-06 — MCP half of the ship gate without WebDriver stdio E2E.
Output: Green cargo mcp suite; docs/mcp.md + README pointer accurate (docs-only edits if needed).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-e2e-citation-regression-gate/19-CONTEXT.md
@.planning/phases/19-e2e-citation-regression-gate/19-RESEARCH.md
@.planning/phases/19-e2e-citation-regression-gate/19-VALIDATION.md
@docs/mcp.md
@README.md
@crates/mcp/tests/allowlist.rs
@crates/mcp/tests/tools_kb.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run cargo test -p mcp offline gate</name>
  <read_first>
    - .planning/phases/19-e2e-citation-regression-gate/19-CONTEXT.md (D-05)
    - .planning/phases/19-e2e-citation-regression-gate/19-RESEARCH.md (Pattern 4; test inventory)
  </read_first>
  <files></files>
  <action>
    Per D-05: from repo root run cargo test -p mcp and require exit 0. Confirm allowlist, tools_kb, and paths tests run (RESEARCH inventory). Do not add WebDriver or stdio-host E2E for MCP.

    If failures are product regressions in mcp/retriever/agent shared helpers, fix surgically with a regression test — do not change RetrieverConfig::default or rag::ask fusion for quality (D-01). Optional cargo test -p retriever / -p agent only if those crates were touched to fix a failure.

    Record exact command and pass/fail in the plan SUMMARY (D-08 evidence will be finalized in Plan 19-03 VERIFICATION; this SUMMARY must still note cargo result).
  </action>
  <verify>
    <automated>cargo test -p mcp -- --test-threads=4</automated>
  </verify>
  <done>cargo test -p mcp green under offline mocks (D-05).</done>
</task>

<task type="auto">
  <name>Task 2: Confirm docs/mcp.md and README MCP pointer</name>
  <read_first>
    - .planning/phases/19-e2e-citation-regression-gate/19-CONTEXT.md (D-06)
    - .planning/phases/19-e2e-citation-regression-gate/19-RESEARCH.md (Docs drift checklist)
    - docs/mcp.md (Status + Tools sections)
    - README.md (MCP pointer line)
    - crates/mcp/src/server.rs (live tool names)
  </read_first>
  <files>docs/mcp.md, README.md</files>
  <action>
    Per D-06: drift-check only. Expected state from Phase 18: docs/mcp.md Status describes live hybrid search and Indexed list_sources via kb_readonly; tools allowlist exactly search and list_sources; caps final_k=8, excerpt 200 chars, list cap 200; README has an MCP stdio pointer to docs/mcp.md.

    If wording drifted back to obsolete Phase-17-only / not-yet-wired host guidance, make docs-only edits to restore live-tool accuracy. Do not add Settings UI (MCP-F02), new tools, or claim WebDriver MCP coverage.

    If already accurate, leave files unchanged and state confirmation in SUMMARY.
  </action>
  <verify>
    <automated>rg -n "search|list_sources|final_k|200|kb_readonly" docs/mcp.md; rg -n "docs/mcp.md" README.md; cargo test -p mcp --test allowlist -- --test-threads=4</automated>
  </verify>
  <done>docs/mcp.md + README match live Phase 18 tools (D-06); allowlist still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → jarvis-mcp stdio | External agents call read-only tools; allowlist must stay exact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-03 | Elevation of Privilege | MCP tool surface | high | mitigate | Re-run allowlist cargo test — exactly search + list_sources (D-05) |
| T-19-04 | Information Disclosure | docs/mcp.md drift | medium | mitigate | Confirm live-tool docs; no mutate tools documented (D-06) |
| T-19-05 | Spoofing | Docs claim vs handlers | medium | mitigate | Spot-check docs against server.rs tool names |
| T-19-SC | Tampering | npm/cargo installs | high | accept | No new packages this plan |
</threat_model>

<verification>
cargo test -p mcp green; docs/mcp.md and README MCP pointer accurate.
</verification>

<success_criteria>
MCP offline happy path verified (D-05); host docs/README accurate (D-06); no WebDriver MCP.
</success_criteria>

<output>
Create `.planning/phases/19-e2e-citation-regression-gate/19-02-SUMMARY.md` when done
</output>
