---
phase: 04-memory-model-view
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src-tauri/src/commands/memory.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/lib.rs
requirements:
  - SHELL-04
must_haves:
  truths:
    - "commands/memory.rs holds all 5 memory CRUD IPC commands moved verbatim from lib.rs (D-14)"
    - "learn_from_exchange remains ONLY in commands/chat.rs — not moved to memory.rs (D-15)"
    - "Commands are thin wrappers over crates/memory functions — no business logic inlined (D-16)"
    - "tauri::generate_handler! registers identical command names: list_memories_cmd, get_memory_content_cmd, forget_memory_cmd, update_memory_cmd, add_memory_cmd (D-17, P4)"
    - "cargo build --manifest-path src-tauri/Cargo.toml and cargo test -p tauri-app pass"
  artifacts:
    - path: src-tauri/src/commands/memory.rs
      provides: relocated memory Tauri commands
      contains: "pub fn list_memories_cmd"
    - path: src-tauri/src/commands/mod.rs
      provides: memory command re-exports
      contains: "mod memory"
  key_links:
    - from: src-tauri/src/lib.rs
      to: src-tauri/src/commands/memory.rs
      via: "generate_handler! + use commands::memory::*"
      pattern: "list_memories_cmd"
    - from: src-tauri/src/commands/memory.rs
      to: crates/memory/src/learn.rs
      via: "delegates to list_memories, add_memory, etc."
      pattern: "memory::"
---

<objective>
Relocate the five memory CRUD Tauri commands from `lib.rs` into `src-tauri/src/commands/memory.rs` — pure structural move with zero IPC rename or behavior change.

Purpose: Deliver SHELL-04 memory portion (agent commands deferred to Phase 5).
Output: `commands/memory.rs`, updated `mod.rs`, slimmed `lib.rs`, unchanged `generate_handler!` command set.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-memory-model-view/04-CONTEXT.md
@.planning/phases/04-memory-model-view/04-RESEARCH.md
@src-tauri/src/lib.rs
@src-tauri/src/commands/mod.rs
@src-tauri/src/commands/library.rs
@src-tauri/src/commands/chat.rs
</context>

<threat_model>
scope: structural_refactor_only
asvs_level: L1
trust_boundaries:
  - "Tauri IPC boundary — frontend invoke names are the contract"
stride_register:
  - threat_id: T-04-B1
    category: Tampering
    component: generate_handler! registry
    disposition: mitigate
    mitigation_plan: "Move fns verbatim; diff handler list before/after — zero name changes. cargo build fails if handler references missing symbol."
disposition_summary: "IPC drift is the only risk; mitigated by compile + name-set diff."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create commands/memory.rs and relocate commands (04-02-01)</name>
  <files>src-tauri/src/commands/memory.rs, src-tauri/src/commands/mod.rs, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/lib.rs (list_memories_cmd ~432, get_memory_content_cmd, forget_memory_cmd, update_memory_cmd, add_memory_cmd; generate_handler! ~622), src-tauri/src/commands/library.rs (thin wrapper pattern), 04-CONTEXT.md D-14/D-17</read_first>
  <action>
    Create `src-tauri/src/commands/memory.rs` and MOVE verbatim from lib.rs (change to `pub`, keep `#[tauri::command]`, exact signatures):
    - `list_memories_cmd`
    - `get_memory_content_cmd`
    - `forget_memory_cmd`
    - `update_memory_cmd` (async)
    - `add_memory_cmd` (async)

    Imports: `AppState` from `crate::state`, memory fns from `memory` crate, `Source` from `store` as today.

    Update `commands/mod.rs`:
    - `mod memory;`
    - `pub use memory::{list_memories_cmd, get_memory_content_cmd, forget_memory_cmd, update_memory_cmd, add_memory_cmd};`

    In lib.rs:
    - Delete moved fn bodies
    - Ensure `use commands::{..., list_memories_cmd, ...}` or wildcard brings handlers into scope for `generate_handler!`
    - Verify `learn_from_exchange` usage in commands/chat.rs is UNTOUCHED

    Remove redundant `use memory::{add_memory, ...}` from lib.rs if only used by moved commands (keep if still used elsewhere).
  </action>
  <verify>cargo test -p tauri-app</verify>
  <acceptance_criteria>
    - `rg -n "fn list_memories_cmd" src-tauri/src/lib.rs` returns no match
    - `rg -n "fn list_memories_cmd" src-tauri/src/commands/memory.rs` matches
    - `rg "learn_from_exchange" src-tauri/src/commands/chat.rs` still matches; NOT in memory.rs
    - `generate_handler!` still includes all five `*_cmd` memory commands with same names
    - `cargo build --manifest-path src-tauri/Cargo.toml` succeeds
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cargo build --manifest-path src-tauri/Cargo.toml` succeeds
- [ ] `cargo test -p tauri-app` passes (or `cargo test --workspace` scoped)
- [ ] No IPC command renames in generate_handler!
- [ ] learn_from_exchange still in chat.rs only
</verification>

<success_criteria>
SHELL-04 memory command module complete; lib.rs monolith further reduced; behavior unchanged.
</success_criteria>
