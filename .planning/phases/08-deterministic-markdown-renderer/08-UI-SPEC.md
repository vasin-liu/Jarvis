---
phase: 8
slug: deterministic-markdown-renderer
status: draft
shadcn_initialized: false
preset: none
created: 2026-07-18
---

# Phase 08 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Phase scope (no frontend)

**This phase has no frontend deliverables.**

| Surface | In Phase 08? |
|---------|----------------|
| React screens / components | **No** |
| Tauri IPC / commands for UI | **No** |
| Settings / Library UX | **No** — deferred to **Phase 11** |
| Disk write / Obsidian zip | **No** — Phases **10** / **12** |
| LLM analysis | **No** — Phase **09** |

**Deliverable:** pure Rust `render_wiki_pages` in `crates/insights` — no LLM, no disk I/O, no Tauri/UI (locked: `08-CONTEXT.md` phase boundary).

**UI gate purpose:** This file exists so plan-phase `hasUiSpec: true` can pass. Design tokens below are intentionally **N/A** — do not invent fake screens or visual tokens for wiki pages.

**Content contract (not UI):** Markdown output shape (frontmatter, `[[path|display]]` wikilinks, `index_markdown`) is specified in:

- `08-CONTEXT.md` decisions **D-01…D-13**
- `08-RESEARCH.md` (stack, architecture, test vectors)

Planners/executors must use those docs for string/layout contracts — not this file’s Design System sections.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | **N/A — library-only phase** |
| Preset | **N/A — library-only phase** |
| Component library | **N/A — library-only phase** |
| Icon library | **N/A — library-only phase** |
| Font | **N/A — library-only phase** |

Source: `08-CONTEXT.md` — no Tauri/UI in phase boundary. Project UI taste (`.cursor/rules/frontend-taste.mdc`) applies to Phase 11+, not here.

---

## Spacing Scale

**N/A — library-only phase.** No React layout, padding, or touch targets.

| Token | Value | Usage |
|-------|-------|-------|
| — | N/A | No UI spacing tokens for this phase |

Exceptions: none (no UI)

---

## Typography

**N/A — library-only phase.** No UI font roles. Markdown heading structure for `index_markdown` (`# Wiki`, `## Sources` / `## Entities` / `## Concepts`) is a **content** contract — see CONTEXT **D-11…D-13**, not display type sizes.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| — | N/A | N/A | N/A |

---

## Color

**N/A — library-only phase.** No surfaces, accents, or semantic UI colors.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | N/A | — |
| Secondary (30%) | N/A | — |
| Accent (10%) | N/A | — |
| Destructive | N/A | — |

Accent reserved for: **none** (no UI)

---

## Copywriting Contract

**N/A — library-only phase.** No CTAs, empty/error UI copy, or destructive confirmations.

| Element | Copy |
|---------|------|
| Primary CTA | N/A — no user-facing actions in Phase 08 |
| Empty state heading | N/A — empty entities/concepts still produce a source-summary **string** (ROADMAP / D-08); not a UI empty state |
| Empty state body | N/A |
| Error state | N/A — pure render is expected infallible (see RESEARCH); no UI error surface |
| Destructive confirmation | N/A — none |

User-facing compile copy belongs in **Phase 11** (Library / Settings).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | — | **not applicable** — no shadcn / no frontend components in this phase |

Third-party UI registries: **none**

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
