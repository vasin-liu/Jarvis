---
phase: 18-mcp-tools-search-list-sources
plan: 03
type: execute
wave: 3
depends_on:
  - 18-01
  - 18-02
files_modified:
  - crates/mcp/Cargo.toml
  - crates/mcp/src/server.rs
  - crates/mcp/src/main.rs
  - crates/mcp/src/lib.rs
  - crates/mcp/tests/allowlist.rs
  - crates/mcp/tests/tools_kb.rs
autonomous: true
requirements:
  - MCP-01
  - MCP-02
must_haves:
  truths:
    - "MCP search returns JSON results array via hybrid search_kb; optional limit clamped; empty query is tool error not panic (D-03, D-04, D-05, D-11, MCP-01)"
    - "MCP list_sources returns Indexed sources JSON with id/title/kind + truncated/total_indexed; no absolute paths/uri keys (D-06, D-07, D-08, D-11, MCP-02)"
    - "JarvisMcp holds Arc Store + Embedder from config::build_embedder; startup fails closed if embedder cannot build; tools/list still exactly {search, list_sources} (D-09, D-10)"
  artifacts:
    - path: crates/mcp/src/server.rs
      provides: "Real search/list_sources handlers; JarvisMcp::new(store, embedder)"
    - path: crates/mcp/src/main.rs
      provides: "Same AppConfig drives dim + build_embedder; fail-closed exit"
    - path: crates/mcp/tests/tools_kb.rs
      provides: "tempfile + MockEmbedder happy-path / error / privacy tests"
  key_links:
    - from: crates/mcp/src/server.rs
      to: crates/retriever/src/kb_readonly.rs
      via: "search_kb / list_indexed_sources (D-01)"
      pattern: "search_kb|list_indexed_sources"
    - from: crates/mcp/src/main.rs
      to: crates/config/src/providers.rs
      via: "build_embedder fail-closed (D-09)"
      pattern: "build_embedder"
    - from: crates/mcp/tests/allowlist.rs
      to: crates/mcp/src/server.rs
      via: "Keep exact name allowlist; drop Phase-17 stub body asserts"
      pattern: "search|list_sources"
---

<objective>
Replace Phase 17 stub MCP handlers with real `kb_readonly` JSON tools and wire `build_embedder` in `jarvis-mcp` main (fail closed).

Purpose: External hosts get MCP-01/MCP-02 semantics; D-09/D-10 preserve embedder parity and read-only allowlist.
Output: Live handlers; updated mcp deps; green `cargo test -p mcp` (allowlist + tools_kb); no WebDriver.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-mcp-tools-search-list-sources/18-CONTEXT.md
@.planning/phases/18-mcp-tools-search-list-sources/18-RESEARCH.md
@.planning/phases/18-mcp-tools-search-list-sources/18-PATTERNS.md
@.planning/phases/18-mcp-tools-search-list-sources/18-01-SUMMARY.md
@.planning/phases/17-mcp-transport-read-only-scaffold/17-CONTEXT.md
@crates/mcp/src/server.rs
@crates/mcp/src/main.rs
@crates/mcp/tests/allowlist.rs
@crates/config/src/providers.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED replace stub tests with real KB tool semantics</name>
  <read_first>
    - .planning/phases/18-mcp-tools-search-list-sources/18-CONTEXT.md (D-09..D-12)
    - .planning/phases/18-mcp-tools-search-list-sources/18-RESEARCH.md (MCP JSON shapes; Pitfall D constructor break)
    - .planning/phases/18-mcp-tools-search-list-sources/18-PATTERNS.md (server.rs + allowlist + tools_kb)
    - crates/mcp/tests/allowlist.rs
    - crates/mcp/src/server.rs
    - crates/retriever/src/retrieve.rs (fixture seeding pattern)
  </read_first>
  <files>crates/mcp/tests/allowlist.rs, crates/mcp/tests/tools_kb.rs, crates/mcp/Cargo.toml</files>
  <behavior>
    - tool_allowlist_is_exactly_search_and_list_sources: unchanged exact set {search, list_sources} (D-10 / MCP-04 continuity)
    - search_returns_results_json: indexed fixture + MockEmbedder → success CallToolResult text JSON with results array containing chunk_id/source_id/title/loc/excerpt; no uri key
    - search_empty_query_is_error: blank query → CallToolResult error (or is_error) with empty_query style JSON — not success empty results
    - search_limit_clamped: large limit still ≤ 8 results
    - list_sources_indexed_only_no_uri: JSON sources entries have id/title/kind only; truncated/total_indexed present; serialized body must not include absolute path / file URI substrings from fixture paths
  </behavior>
  <action>
    Per D-12: keep allowlist name test in allowlist.rs. Remove Phase-17 stub-body tests that assert not-implemented status (replace — do not leave them green).

    Add crates/mcp/tests/tools_kb.rs (preferred) with tempfile Store + MockEmbedder + indexer seed; construct JarvisMcp::new(store, embedder) — tests will fail until Task 2 updates constructor.

    Add mcp Cargo.toml path deps retriever + embedder; add/align dev-deps tempfile, indexer, chunker, tokio as needed mirroring retriever tests. Do not add agent as a dependency. Do not bump rmcp off 2.2.0. Do not add rusqlite.

    Keep first_text helper pattern for ContentBlock::text JSON parsing.
  </action>
  <verify>
    <automated>cargo test -p mcp -- --test-threads=4</automated>
  </verify>
  <done>RED/updated mcp tests describe real search/list_sources semantics; allowlist names preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN JarvisMcp handlers + main build_embedder fail-closed</name>
  <read_first>
    - crates/mcp/src/server.rs
    - crates/mcp/src/main.rs
    - crates/mcp/src/lib.rs
    - crates/config/src/providers.rs
    - crates/retriever/src/kb_readonly.rs
  </read_first>
  <files>crates/mcp/src/server.rs, crates/mcp/src/main.rs, crates/mcp/src/lib.rs, crates/mcp/Cargo.toml</files>
  <behavior>
    - Same behaviors as Task 1 pass
    - main uses one AppConfig for embedding_dim and build_embedder; embedder build failure exits non-zero with eprintln (D-09)
  </behavior>
  <action>
    Per D-01/D-09/D-10/D-11:

    JarvisMcp fields: Arc&lt;Store&gt;, Arc&lt;dyn Embedder&gt;. new(store, embedder) required — drop Option stub constructor.

    search: Parameters&lt;SearchParams&gt; → search_kb(&store, embedder.as_ref(), &query, limit.map(|n| n as usize)).await; map EmptyQuery / retrieve/embed/store errors to CallToolResult::error with JSON text ContentBlock (error + message); success → serde_json object { "results": [ { chunk_id, source_id, title, loc, excerpt } ] } as ContentBlock::text (Phase 17 continuity — not Chinese agent strings). Never serialize uri/score/full text.

    list_sources: list_indexed_sources → JSON { sources: [{id,title,kind}], truncated, total_indexed } via ContentBlock::text.

    Update tool description strings to document real hybrid/Indexed behavior (remove Phase-17 stub wording). Keep annotations read_only_hint true; exactly two #[tool] methods.

    main.rs: load_config → unwrap_or AppConfig::default(); dim = cfg.embedding_dim(); embedder = build_embedder(&cfg) or eprintln + process::exit(1); Store::open(&db_path, dim); JarvisMcp::new(store, embedder).serve(stdio). Same cfg for dim and embedder (Pitfall E). Keep ensure_db_exists fail-closed; still no Windows GUI subsystem attribute on this binary.

    Do not call agent::execute_tool. Do not register extra tools. Do not open SQLite outside store.
  </action>
  <verify>
    <automated>cargo test -p mcp -- --test-threads=4</automated>
  </verify>
  <done>MCP clients can search + list Indexed sources via shared helpers; allowlist intact (MCP-01, MCP-02, D-09..D-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External MCP host → jarvis-mcp stdio | Untrusted tool args (query, limit) |
| MCP JSON → host context | Must not exfiltrate home-dir paths |
| main startup → embedder/config | Broken provider must not look like empty KB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-08 | Information Disclosure | list_sources / search JSON | high | mitigate | No uri/path fields; tools_kb asserts absence (D-08) |
| T-18-09 | Denial of Service | search limit / list size | medium | mitigate | Clamp via search_kb; LIST_SOURCES_MAX in helper (D-04, D-07) |
| T-18-10 | Tampering | tool surface | critical | mitigate | Allowlist remains exactly two read tools; no execute_tool wrap (D-10) |
| T-18-11 | Spoofing | broken embedder | high | mitigate | build_embedder fail-closed at startup; per-call errors → tool error not empty success (D-09) |
| T-18-12 | Elevation of Privilege | mcp→agent dep | high | mitigate | mcp depends on retriever/embedder/store/config only — never agent tool loop |
| T-18-SC | Tampering | cargo installs | high | accept | Path deps only; rmcp pin unchanged 2.2.0 |
</threat_model>

<verification>
cargo test -p mcp green; cargo test -p retriever -p mcp -p agent as wave merge gate.
</verification>

<success_criteria>
- Stub handlers gone; real JSON search/list_sources
- Embedder wired fail-closed
- Allowlist still {search, list_sources}
- Offline MockEmbedder tests pass
</success_criteria>

<output>
Create `.planning/phases/18-mcp-tools-search-list-sources/18-03-SUMMARY.md` when done
</output>
