---
phase: 01-scaffold-shell-foundation
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - .planning/phases/01-scaffold-shell-foundation/01-VERIFICATION.md
  - .planning/phases/01-scaffold-shell-foundation/01-VALIDATION.md
autonomous: true
requirements:
  - QA-01
  - QA-03
must_haves:
  truths:
    - "cargo test --workspace passes with state.rs and commands/* in tree"
    - "npm test passes after frontend scaffold"
    - "E2E smoke.spec.ts and navigation.spec.ts pass via npm run test:e2e:local"
    - "FE-01 and SHELL-01 documented as partial completion in VERIFICATION.md"
    - "No IPC renames; 52 commands in generate_handler!"
    - "D-21: Single Phase 1 PR preferred (shell + FE scaffold together); if split, shell lands first and each PR passes full gate"
  artifacts:
    - path: .planning/phases/01-scaffold-shell-foundation/01-VERIFICATION.md
      provides: Phase 1 verification record with requirement status
      contains: "FE-01"
  key_links:
    - from: e2e/specs/smoke.spec.ts
      to: src/App.tsx
      via: "data-testid selectors unchanged"
      pattern: "nav-chat"
    - from: e2e/specs/navigation.spec.ts
      to: src/App.tsx
      via: "openNav + view testids"
      pattern: "settings-panel"
---

<objective>
Run full Phase 1 quality gates and document partial requirement completion (FE-01, SHELL-01) plus regression verification (QA-01, QA-03).

Purpose: Prove zero behavior change before phase merge per D-17/D-18/D-19. Merge strategy per D-21: prefer one PR combining plans 01-01 and 01-02; if split, land shell (01-01) first with full gate green before FE scaffold PR.
Output: Green test suites; `01-VERIFICATION.md` with requirement traceability; updated validation task statuses.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@$HOME/.claude/gsd-core/workflows/verify-phase.md
</execution_context>

<context>
@.planning/phases/01-scaffold-shell-foundation/01-CONTEXT.md
@.planning/phases/01-scaffold-shell-foundation/01-VALIDATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@e2e/specs/smoke.spec.ts
@e2e/specs/navigation.spec.ts
@package.json
</context>

<threat_model>
scope: verification_only
trust_boundaries:
  - "E2E mock path (JARVIS_E2E=1) must remain intact after shell refactor"
stride_register:
  - threat_id: T-04
    category: Elevation of Privilege
    component: init_state / E2E bootstrap
    disposition: monitor
    mitigation_plan: "Verify e2e.rs tests still pass; confirm restart_scheduler skips spawn when is_e2e_mode() (PITFALLS §5)"
disposition_summary: "Verification-only plan; no code changes except docs unless gates fail."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Run Rust and Vitest regression gates</name>
  <files></files>
  <read_first>.planning/phases/01-scaffold-shell-foundation/01-VALIDATION.md, .planning/REQUIREMENTS.md</read_first>
  <action>
    Per D-18/QA-03: From project root run sequentially:
    1. `cargo test --workspace` — all workspace crates including src-tauri e2e unit tests
    2. `npm test` — Vitest for src/lib/*.test.ts
    If either fails: fix regressions in 01-01 or 01-02 artifacts (do not add features). Re-run until both exit 0.
    Spot-check IPC registry per RESEARCH: grep `generate_handler!` in `src-tauri/src/lib.rs` — confirm 52 command names present including `get_config`, `list_sources`, `get_sync_status`.
    Confirm `src-tauri/src/index_ops.rs` has no diff from pre-phase baseline (D-16).
  </action>
  <verify>cargo test --workspace && npm test</verify>
  <acceptance_criteria>
    - `cargo test --workspace` exits 0
    - `npm test` exits 0
    - `generate_handler!` in lib.rs lists 52 command identifiers
    - `index_ops.rs` unchanged from phase start
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run E2E smoke + navigation gate</name>
  <files></files>
  <read_first>e2e/specs/smoke.spec.ts, e2e/specs/navigation.spec.ts, e2e/README.md, package.json</read_first>
  <action>
    Per D-17/QA-01: From project root run `npm run test:e2e:local` (sets JARVIS_E2E=1 and JARVIS_E2E_FIXTURE per package.json).
    Minimum merge gate: `e2e/specs/smoke.spec.ts` and `e2e/specs/navigation.spec.ts` must pass (D-17). Full wdio suite may run — all must pass for QA-01.
    If E2E fails on nav testids: verify App.tsx still mounts `data-testid="nav-chat"`, `nav-settings`, `settings-panel`, `index-status`, `library-stats`, `task-list`, `new-memory-input`, `memory-list` (D-11). Fix stub wrapping only — do not move testids into view files.
    If E2E fails on IPC: verify command names unchanged in moved modules (PITFALLS §5).
  </action>
  <verify>npm run test:e2e:local</verify>
  <acceptance_criteria>
    - `npm run test:e2e:local` exits 0
    - smoke.spec.ts assertions pass: `nav-chat` visible; settings nav shows `settings-panel` and `index-status`
    - navigation.spec.ts passes all 5 view transitions via `nav-*` testids
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Document partial FE-01 / SHELL-01 completion in VERIFICATION.md</name>
  <files>.planning/phases/01-scaffold-shell-foundation/01-VERIFICATION.md, .planning/phases/01-scaffold-shell-foundation/01-VALIDATION.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/01-scaffold-shell-foundation/01-CONTEXT.md</read_first>
  <action>
    Create `.planning/phases/01-scaffold-shell-foundation/01-VERIFICATION.md` with YAML frontmatter:
    `phase: 1`, `slug: scaffold-shell-foundation`, `status: passed` (if all gates green) or `failed`, `verified: {date}`.
    Document requirement status:
    - **FE-01**: `partial` — stubs + types extracted; App.tsx retains all logic (completes Phase 6 per roadmap)
    - **SHELL-01**: `partial` — AppState in state.rs + 2-3 command modules; lib.rs still has ~44 commands + glue (completes Phase 6)
    - **QA-01**: `met` — test:e2e:local green
    - **QA-03**: `met` — cargo test --workspace + npm test green
    List artifacts created (cross-ref 01-01 and 01-02 Artifacts sections). Note zero behavior change (D-19).
    Update `01-VALIDATION.md` Per-Task Verification Map statuses to ✅ for tasks 01-01, 01-02, 01-03.
    Include IPC count check result (52 commands) and E2E env vars preserved (JARVIS_E2E, JARVIS_E2E_FIXTURE).
  </action>
  <verify>test -f .planning/phases/01-scaffold-shell-foundation/01-VERIFICATION.md</verify>
  <acceptance_criteria>
    - `01-VERIFICATION.md` exists with status `passed` (if gates green)
    - Document lists FE-01 and SHELL-01 as `partial`
    - Document lists QA-01 and QA-03 as `met`
    - `01-VALIDATION.md` task rows show ✅ for 01-01, 01-02, 01-03
    - All four requirement IDs (FE-01, SHELL-01, QA-01, QA-03) referenced in verification doc
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cargo test --workspace` green
- [ ] `npm test` green
- [ ] `npm run test:e2e:local` green
- [ ] `01-VERIFICATION.md` created with partial/passed status
</verification>

<success_criteria>
- All Phase 1 quality gates pass (QA-01, QA-03)
- Partial scaffold documented; phase ready for Phase 2
- Zero user-visible behavior change confirmed by E2E
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Location |
|---------------|------|----------|
| `01-VERIFICATION.md` | verification record | `.planning/phases/01-scaffold-shell-foundation/` |
| Updated validation statuses | doc edit | `.planning/phases/01-scaffold-shell-foundation/01-VALIDATION.md` |

<output>
After completion, create `.planning/phases/01-scaffold-shell-foundation/01-03-SUMMARY.md`
</output>
