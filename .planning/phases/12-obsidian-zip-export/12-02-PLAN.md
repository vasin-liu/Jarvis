---
phase: 12-obsidian-zip-export
plan: "02"
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - src-tauri/src/commands/wiki.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/lib.rs
  - src/types/ipc.ts
  - src/lib/tauri.ts
  - src/hooks/useLibrary.ts
  - src/hooks/useLibrary.test.ts
  - src/views/LibraryView.tsx
  - src/views/LibraryView.test.tsx
  - src/App.tsx
  - src/AppShell.tsx
# Scope note: 11 files accepted (warn threshold 10). Do not split AppShell soft
# notice — coherence with App busy + Library export flow. Enforce surgical diffs
# at execute (touch only export/notice wiring; no drive-by refactors).
autonomous: true
requirements:
  - WIKI-07
must_haves:
  truths:
    - "Library toolbar shows「导出 Wiki」with data-testid wiki-export only when config.wiki.enabled === true, placed after「选择文件索引」(D-01, D-02, D-03)"
    - "Click runs preflight first; empty → exact message「还没有可导出的笔记，请先生成笔记」via reportError/err bar and Save dialog never opens (D-09, D-11)"
    - "Non-empty → system save dialog default jarvis-wiki-YYYY-MM-DD.zip (local date) with zip filter; cancel → no export invoke (D-04, D-05, D-06)"
    - "Successful export shows soft non-red notice including path or filename with data-testid wiki-export-done (D-07 Discretion)"
    - "Failures use existing reportError / shared red err bar; App busy disables export and other Library toolbar actions during run (D-07, D-08)"
    - "Backend cmds resolve wiki_root as config_path.parent()/wiki, gate wiki.enabled, call Plan 01 crate APIs; thin commands only"
    - "Full wiki.spec.ts E2E deferred to Phase 13; Phase 12 gate is Vitest + Rust units"
  artifacts:
    - path: src-tauri/src/commands/wiki.rs
      provides: "wiki_export_preflight_cmd + export_wiki_zip_cmd"
      contains: "wiki_export_preflight_cmd"
    - path: src/lib/tauri.ts
      provides: "wikiExportPreflight + exportWikiZip wrappers"
      contains: "wiki_export_preflight_cmd"
    - path: src/views/LibraryView.tsx
      provides: "toolbar 导出 Wiki button"
      contains: "wiki-export"
    - path: src/hooks/useLibrary.ts
      provides: "exportWiki flow preflight→save→export"
      contains: "exportWiki"
    - path: src/AppShell.tsx
      provides: "soft notice slot wiki-export-done"
      contains: "wiki-export-done"
  key_links:
    - from: wiki-export button
      to: onExportWiki → handleExportWiki → useLibrary.exportWiki
      via: "busy wrapper; preflight before save"
      pattern: "wiki-export"
    - from: wiki_export_preflight_cmd
      to: wiki_has_exportable_notes
      via: "wiki_root + { hasNotes } camelCase"
      pattern: "hasNotes"
    - from: export_wiki_zip_cmd
      to: export_wiki_zip
      via: "dest_path + wiki.enabled + wiki_root"
      pattern: "export_wiki_zip_cmd"
---

<objective>
Ship WIKI-07 UI/IPC half: thin Tauri preflight/export commands, Library toolbar「导出 Wiki」, Save dialog flow, soft success notice, Vitest coverage (D-01..D-11, D-07 Discretion). Depends on Plan 01 crate API.

Purpose: Users can export an Obsidian-compatible zip from Library when wiki is enabled, with empty blocked before dialog.
Output: Registered cmds + FE wiring + Vitest green; no wiki.spec.ts (Phase 13).

Scope: 11 `files_modified` accepted without split — AppShell soft notice stays in this plan for UX coherence with App busy + Library export. Execute with surgical diffs only (no adjacent refactors).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-obsidian-zip-export/12-CONTEXT.md
@.planning/phases/12-obsidian-zip-export/12-RESEARCH.md
@.planning/phases/12-obsidian-zip-export/12-PATTERNS.md
@.planning/phases/12-obsidian-zip-export/12-VALIDATION.md
@.planning/phases/12-obsidian-zip-export/12-01-SUMMARY.md
@.planning/ROADMAP.md
@.cursor/rules/tdd-goal-driven.mdc
@.cursor/rules/e2e-required.mdc
@.cursor/rules/frontend-taste.mdc
@src-tauri/src/commands/wiki.rs
@src-tauri/src/commands/mod.rs
@src-tauri/src/lib.rs
@src/lib/tauri.ts
@src/types/ipc.ts
@src/hooks/useLibrary.ts
@src/hooks/useLibrary.test.ts
@src/views/LibraryView.tsx
@src/views/LibraryView.test.tsx
@src/App.tsx
@src/AppShell.tsx

<interfaces>
Discretion locked by planner (CONTEXT Claude's Discretion):

1. Preflight command name: `wiki_export_preflight_cmd` → returns struct `WikiExportPreflight { has_notes: bool }` with `#[serde(rename_all = "camelCase")]` → FE `{ hasNotes: boolean }` (not bare bool).
2. Export command: `export_wiki_zip_cmd(dest_path: String)` → `Result<(), String>`; resolve `wiki_root = config_path.parent()/wiki` like `compile_wiki_cmd`; pass `cfg.wiki.enabled` into `insights::export_wiki_zip`; `.map_err(|e| e.to_string())`.
3. Success UI: App-level soft notice (non-red: cyan/emerald border/text), `data-testid="wiki-export-done"`, text includes dest path or filename; dismissible like err. Do NOT put success on red `err` (D-07 + RESEARCH Pitfall 6). Empty copy still uses `reportError` / `setErr` with exact string「还没有可导出的笔记，请先生成笔记」(D-09).
4. Default zip filename: local calendar `YYYY-MM-DD` (not UTC-only ISO slice if that drifts near midnight) → `jarvis-wiki-${date}.zip` (D-06; RESEARCH A2).

FE wrappers (`src/lib/tauri.ts`):
- `wikiExportPreflight()` → invoke `wiki_export_preflight_cmd`
- `exportWikiZip(destPath: string)` → invoke `export_wiki_zip_cmd` with `{ destPath }`

useLibrary.exportWiki({ onSuccess?: (path: string) => void } or return path):
1. await wikiExportPreflight(); if !hasNotes → reportError(exact D-09 Chinese string); return (no save)
2. save({ defaultPath: `jarvis-wiki-${localYmd}.zip`, filters: [{ name: "Zip", extensions: ["zip"] }] }) from `@tauri-apps/plugin-dialog`
3. if null → return (D-05)
4. await exportWikiZip(destPath); on success notify caller with path/filename; catch reportError
Hook must NOT own busy (D-08 App-owned).

LibraryView: after「选择文件索引」button, when `config?.wiki?.enabled === true`, render btn-ghost「导出 Wiki」`data-testid="wiki-export"` `disabled={busy}` `onClick={onExportWiki}` (D-01..D-03, D-08). Hide when disabled/missing — do not disable-only.

App: `handleExportWiki` mirrors compile busy wrapper; clear err; set soft notice on success; pass notice + setNotice into AppShell.

Register cmds in commands/mod.rs + lib.rs generate_handler / use list.

E2E exception: do not add `e2e/specs/wiki.spec.ts` — Phase 13 (WIKI-09); document in SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Library export visibility + useLibrary exportWiki Vitest (12-02-01)</name>
  <files>src/views/LibraryView.test.tsx, src/hooks/useLibrary.test.ts</files>
  <read_first>
src/views/LibraryView.tsx (toolbar buttons; wiki compile gate pattern),
src/views/LibraryView.test.tsx (baseProps; wiki.enabled visibility cases),
src/hooks/useLibrary.ts (pickAndIndex dialog cancel; compileWiki),
src/hooks/useLibrary.test.ts (tauri + dialog mocks),
.planning/phases/12-obsidian-zip-export/12-CONTEXT.md (D-01..D-11),
.planning/phases/12-obsidian-zip-export/12-PATTERNS.md (LibraryView + useLibrary.test prescriptions),
.planning/phases/12-obsidian-zip-export/12-VALIDATION.md (Wave 0 FE gaps)
  </read_first>
  <behavior>
    - LibraryView: wiki.enabled true → getByTestId("wiki-export") text includes「导出 Wiki」; button appears after the file-index control in toolbar DOM order (D-01, D-02)
    - LibraryView: wiki.enabled false OR missing wiki → queryByTestId("wiki-export") null (D-03)
    - LibraryView: busy true → wiki-export disabled (D-08)
    - LibraryView: click wiki-export calls onExportWiki (D-01)
    - useLibrary: preflight hasNotes false → onError called with exact「还没有可导出的笔记，请先生成笔记」; save mock never called (D-09, D-11)
    - useLibrary: preflight ok + save returns null → exportWikiZip never called (D-05)
    - useLibrary: preflight ok + save returns path → exportWikiZip called with that path; success callback/notice path receives path or filename (D-04, D-07)
    - useLibrary: exportWikiZip rejects → onError called; keys still exclude busy
  </behavior>
  <action>
    Wave 0 / RED: extend LibraryView.test.tsx baseProps with onExportWiki: vi.fn(); add visibility/placement/busy/click cases for wiki-export. Extend useLibrary.test.ts mocks: wikiExportPreflight, exportWikiZip; mock save from `@tauri-apps/plugin-dialog` beside open. Add preflight empty / cancel / happy / reject cases with exact D-09 Chinese string.

    Confirm focused Vitest is RED on new cases before Task 2/3. Do not edit production LibraryView/useLibrary/App/AppShell/tauri/commands in this task. Do not add wiki.spec.ts.
  </action>
  <verify>
    <automated>npx vitest run src/views/LibraryView.test.tsx src/hooks/useLibrary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - LibraryView.test.tsx references wiki-export and 导出 Wiki
    - useLibrary.test.ts references wikiExportPreflight or exportWiki and the exact empty Chinese string
    - New export cases fail RED (suite non-zero) until implementation tasks land
  </acceptance_criteria>
  <done>Failing Vitest covers toolbar gates and preflight→save→export flow</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Tauri cmds + tauri.ts + useLibrary.exportWiki (12-02-02)</name>
  <files>src-tauri/src/commands/wiki.rs, src-tauri/src/commands/mod.rs, src-tauri/src/lib.rs, src/types/ipc.ts, src/lib/tauri.ts, src/hooks/useLibrary.ts, src/hooks/useLibrary.test.ts</files>
  <read_first>
src-tauri/src/commands/wiki.rs (compile_wiki_cmd thin pattern),
src-tauri/src/commands/mod.rs,
src-tauri/src/lib.rs (generate_handler + use commands list),
crates/insights/src/wiki_export.rs (Plan 01 exports),
src/lib/tauri.ts (compileWiki wrapper),
src/types/ipc.ts (WikiCompileSummary style),
src/hooks/useLibrary.ts (pickAndIndex + compileWiki),
.planning/phases/12-obsidian-zip-export/12-01-SUMMARY.md,
.planning/phases/12-obsidian-zip-export/12-PATTERNS.md (thin command + FE flow),
.planning/phases/12-obsidian-zip-export/12-CONTEXT.md (D-04..D-12)
  </read_first>
  <behavior>
    - useLibrary.test.ts exportWiki cases pass; hook still does not expose busy
  </behavior>
  <action>
    Add thin `wiki_export_preflight_cmd` and `export_wiki_zip_cmd` in commands/wiki.rs per interfaces (wiki_root from config_path.parent()/wiki; enabled gate; call `wiki_has_exportable_notes` / `export_wiki_zip`; map errors to String). Re-export + register in mod.rs and lib.rs generate_handler.

    Add `WikiExportPreflight { hasNotes: boolean }` to ipc.ts. Add wikiExportPreflight + exportWikiZip in tauri.ts.

    Implement useLibrary.exportWiki: preflight → empty reportError with exact D-09 string and stop; else save with local-date defaultPath and zip filter; null cancel stop; else exportWikiZip(destPath); surface success path to optional onSuccess or return value for App notice; catch reportError. Import save from plugin-dialog. Do not setBusy in hook (D-08).

    Do not edit LibraryView/AppShell yet (Task 3). No zip logic inside Tauri commands. No E2E.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useLibrary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "wiki_export_preflight_cmd" src-tauri/src/commands/wiki.rs matches
    - rg -n "export_wiki_zip_cmd" src-tauri/src/commands/wiki.rs matches
    - rg -n "wiki_export_preflight_cmd" src-tauri/src/lib.rs matches
    - rg -n "export_wiki_zip_cmd" src-tauri/src/lib.rs matches
    - rg -n "WikiExportPreflight" src/types/ipc.ts matches
    - rg -n "wiki_export_preflight_cmd" src/lib/tauri.ts matches
    - rg -n "export_wiki_zip_cmd" src/lib/tauri.ts matches
    - rg -n "exportWiki" src/hooks/useLibrary.ts matches
    - rg -n "还没有可导出的笔记，请先生成笔记" src/hooks/useLibrary.ts matches
    - npx vitest run src/hooks/useLibrary.test.ts exits 0
  </acceptance_criteria>
  <done>IPC + hook exportWiki green with preflight and cancel semantics</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — Library toolbar + App busy + soft success notice (12-02-03)</name>
  <files>src/views/LibraryView.tsx, src/App.tsx, src/AppShell.tsx, src/views/LibraryView.test.tsx</files>
  <read_first>
src/views/LibraryView.tsx (toolbar after 选择文件索引; LibraryViewProps),
src/App.tsx (handleCompileWiki busy; LibraryView props; useLibrary destructure; AppShell err wiring),
src/AppShell.tsx (err alert slot ~79–95),
.planning/phases/12-obsidian-zip-export/12-CONTEXT.md (D-01..D-08),
.planning/phases/12-obsidian-zip-export/12-PATTERNS.md (LibraryView button; AppShell soft notice),
.planning/phases/12-obsidian-zip-export/12-RESEARCH.md (Pitfall 6 success not red)
  </read_first>
  <behavior>
    - All LibraryView.test.tsx wiki-export visibility/placement/busy/click cases pass
  </behavior>
  <action>
    Per D-01..D-03, D-08: add onExportWiki prop; render「导出 Wiki」after「选择文件索引」when config?.wiki?.enabled === true with data-testid wiki-export, btn-ghost, disabled={busy}.

    Per D-07 Discretion + D-08: in App.tsx add handleExportWiki with setErr(null), setBusy(true), try await lib exportWiki, on success setNotice text including path/filename, finally setBusy(false). Wire onExportWiki to LibraryView.

    Extend AppShell with optional notice: string | null + setNotice; render soft non-red banner (cyan or emerald glass styling matching existing panels — not border-red) with data-testid wiki-export-done and dismiss control. Keep failures on existing err bar only.

    Match frontend-taste: reuse btn-ghost / glass-panel; no redesign. Do not add Settings export button. Do not add wiki.spec.ts (Phase 13 E2E exception).
  </action>
  <verify>
    <automated>npx vitest run src/views/LibraryView.test.tsx src/hooks/useLibrary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "wiki-export" src/views/LibraryView.tsx matches
    - rg -n "导出 Wiki" src/views/LibraryView.tsx matches
    - rg -n "onExportWiki" src/views/LibraryView.tsx matches
    - wiki-export button appears after 选择文件索引 in LibraryView.tsx source order
    - rg -n "handleExportWiki" src/App.tsx matches
    - rg -n "wiki-export-done" src/AppShell.tsx matches
    - AppShell success notice className does not include border-red-400
    - npx vitest run src/views/LibraryView.test.tsx src/hooks/useLibrary.test.ts exits 0
  </acceptance_criteria>
  <done>Library export UX gated, busy-wrapped, soft success notice live; Vitest green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library UI → wiki_export_preflight_cmd / export_wiki_zip_cmd | FE invokes IPC; destPath from OS Save dialog crosses into Rust |
| wiki.enabled config → UI + cmds | Dual gate: hide button + backend WikiDisabled |
| Success/empty messaging → AppShell | Empty uses err bar; success uses separate soft notice (no spoof-as-error) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-07 | Elevation of Privilege | export when wiki disabled | high | mitigate | Hide wiki-export when disabled (D-03) + export_wiki_zip_cmd passes wiki.enabled into crate WikiDisabled hard gate |
| T-12-08 | Tampering | destPath from FE | medium | mitigate | Path only from OS save dialog; crate writes only to that path; zip entry safety owned by Plan 01 (T-12-01) |
| T-12-09 | Tampering | skip preflight / empty export | high | mitigate | FE preflight before dialog (D-11) + crate WikiEmpty hard reject (D-12) |
| T-12-10 | Information Disclosure | packing secrets / on-disk .obsidian | medium | mitigate | Crate skips on-disk .obsidian and stays under wiki_root (Plan 01); UI does not invent paths |
| T-12-11 | Denial of Service | double-click export | low | mitigate | App busy disables toolbar including wiki-export (D-08) |
| T-12-12 | Spoofing | success shown as error | low | mitigate | Soft notice wiki-export-done non-red (D-07 Discretion); failures stay on err bar |
| T-12-SC | Tampering | npm installs | low | accept | No new npm packages — dialog plugin already present |
</threat_model>

<verification>
- Vitest LibraryView: wiki-export show/hide/placement/busy/click
- Vitest useLibrary: empty preflight (exact Chinese), cancel, happy path, error
- Commands registered; tauri wrappers present
- Phase gate: `cargo test -p insights` + focused Vitest; **do not** require `npm run test:e2e:local` (CONTEXT deferral to Phase 13 / WIKI-09)
</verification>

<success_criteria>
ROADMAP criteria 1–3 complete end-to-end in-app (unit+Vitest): export zip IPC+UI, path-safe crate, wiki.enabled gate. WIKI-07 done for Phase 12; E2E journey owned by Phase 13.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Symbols / notes | Requirement |
|----------|------|-----------------|-------------|
| Preflight + export cmds | `src-tauri/src/commands/wiki.rs` | `wiki_export_preflight_cmd`, `export_wiki_zip_cmd` | WIKI-07 (D-11, D-12) |
| Command registration | `src-tauri/src/commands/mod.rs`, `src-tauri/src/lib.rs` | handler list | WIKI-07 |
| IPC type | `src/types/ipc.ts` | `WikiExportPreflight.hasNotes` | WIKI-07 |
| FE wrappers | `src/lib/tauri.ts` | `wikiExportPreflight`, `exportWikiZip` | WIKI-07 |
| Hook flow | `src/hooks/useLibrary.ts` | `exportWiki` preflight→save→export | WIKI-07 (D-04..D-06, D-09, D-11) |
| Toolbar button | `src/views/LibraryView.tsx` | `wiki-export` 「导出 Wiki」 | WIKI-07 (D-01..D-03) |
| Busy + notice | `src/App.tsx`, `src/AppShell.tsx` | `handleExportWiki`, `wiki-export-done` | WIKI-07 (D-07, D-08) |
| Vitest | `LibraryView.test.tsx`, `useLibrary.test.ts` | visibility + flow | WIKI-07 / VALIDATION |

<output>
Create `.planning/phases/12-obsidian-zip-export/12-02-SUMMARY.md` when done
</output>
