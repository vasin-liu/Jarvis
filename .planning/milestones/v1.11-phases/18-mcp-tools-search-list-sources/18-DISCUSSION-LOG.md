# Phase 18 Discussion Log

**Date:** 2026-07-28
**Mode:** --auto

## Gray areas (auto-selected all)

1. Shared kb_readonly placement / dependency direction
2. Search top-k, limit clamp, excerpt length
3. list_sources Indexed filter, fields, hard cap
4. Absolute path privacy in MCP payloads
5. MCP embedder wiring from config
6. Agent text vs MCP JSON response shape
7. Testing bar (cargo only)

## Auto decisions

See `18-CONTEXT.md` `<specifics>` and `<decisions>` D-01..D-12.

## Todos

No backlog todos folded (list-todos count = 0).
