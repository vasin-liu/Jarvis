---
phase: 03-library-tasks-keychain
plan: "05"
type: execute
wave: 5
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
  - "03-04"
files_modified:
  - src-tauri/src/commands/config.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/lib.rs
  - src/lib/tauri.ts
  - src/App.tsx
  - e2e/specs/settings.spec.ts
requirements:
  - CFG-02
  - FE-03
  - QA-01
  - QA-03
must_haves:
  truths:
    - "commands/config.rs adds get_api_key_status() -> { has_key: bool }, set_api_key(key) and clear_api_key(), all guarded by is_e2e_mode() so E2E never touches the keychain (D-03/D-04)"
    - "set_config intercepts an incoming cloud_api_key: routes a non-empty value to secrets::set_api_key (unless E2E), clears the field before state.save_config, and re-populates the runtime field — JSON stays clean (D-07, Pitfall P7)"
    - "the three new commands are registered in tauri::generate_handler! (additions only; no existing command renamed)"
    - "Settings API key input shows a MASKED placeholder when a key exists in the keychain (D-01); a 「清除密钥」/ Clear API key button (data-testid clear-api-key) calls clear_api_key and resets to empty (D-04)"
    - "D-18: manual Lark sync failure surfaces as a visible inline error in the Settings panel (not silent)"
    - "scheduled_sync_enabled / scheduled_sync_interval_secs UI unchanged — no new scheduler interval/toggle controls added (D-19)"
    - "Full E2E suite (e2e/specs/**/*.spec.ts) passes; cargo test --workspace + npm test pass (D-26/D-28/QA-01/QA-03)"
    - "Under JARVIS_E2E=1 the API key Settings section renders without error and does not call the keychain (D-03)"
  artifacts:
    - path: src-tauri/src/commands/config.rs
      provides: get_api_key_status, set_api_key, clear_api_key + set_config secret interception
      contains: "get_api_key_status"
    - path: src/App.tsx
      provides: Settings API key UI (masked placeholder + Clear button) + D-18 inline lark error
      contains: "data-testid=\"clear-api-key\""
    - path: src/lib/tauri.ts
      provides: getApiKeyStatus / setApiKey / clearApiKey wrappers
      contains: "getApiKeyStatus"
    - path: e2e/specs/settings.spec.ts
      provides: masked-placeholder + clear-key E2E assertions
      contains: "clear-api-key"
  key_links:
    - from: src/App.tsx
      to: src/lib/tauri.ts
      via: "API key status/set/clear wrappers"
      pattern: "getApiKeyStatus|clearApiKey"
    - from: src/lib/tauri.ts
      to: src-tauri/src/commands/config.rs
      via: "invoke get_api_key_status / set_api_key / clear_api_key"
      pattern: "get_api_key_status"
    - from: src-tauri/src/commands/config.rs
      to: crates/config/src/secrets.rs
      via: "secrets::set_api_key / delete_api_key"
      pattern: "secrets::"
    - from: src-tauri/src/lib.rs
      to: src-tauri/src/commands/config.rs
      via: "generate_handler! registers the 3 new commands"
      pattern: "get_api_key_status"
---

<objective>
Complete the keychain UX and close the Phase 3 gate: add `get_api_key_status` / `set_api_key` / `clear_api_key` commands to `commands/config.rs` (E2E-guarded), make `set_config` route any incoming `cloud_api_key` to the keychain and strip it before save, update the Settings API key UI to show a masked placeholder when a key exists with a 「清除密钥」 Clear button, surface manual Lark sync failures inline in Settings (D-18), and run the FULL E2E suite + workspace tests as the merge gate.

Purpose: Deliver the CFG-02 user-facing surface (masked secret field + clear, no plaintext on save) and complete FE-03 by finalizing the Settings-side controls, then enforce QA-01/QA-03/D-26/D-28.
Output: 3 new config commands + `set_config` interception, `tauri.ts` API key wrappers, Settings API key UI (masked + clear), D-18 inline Lark error, extended `settings.spec.ts`, full green suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-library-tasks-keychain/03-CONTEXT.md
@.planning/phases/03-library-tasks-keychain/03-RESEARCH.md
@.planning/phases/03-library-tasks-keychain/03-VALIDATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.cursor/rules/e2e-required.mdc
@.cursor/rules/frontend-taste.mdc
@src-tauri/src/commands/config.rs
@src-tauri/src/commands/mod.rs
@src-tauri/src/lib.rs
@src-tauri/src/e2e.rs
@crates/config/src/secrets.rs
@src/App.tsx
@src/lib/tauri.ts
@e2e/specs/settings.spec.ts
@e2e/specs/full-ui.spec.ts
</context>

<threat_model>
scope: secret_ux_plus_phase_gate
asvs_level: L1
trust_boundaries:
  - "Tauri IPC boundary — 3 new commands accept/return secret status; the raw key is never returned to the frontend (only has_key boolean)"
  - "OS keychain via crates/config/src/secrets.rs"
  - "JARVIS_E2E=1 CI boundary — no keychain access (D-03)"
stride_register:
  - threat_id: T-03-02
    category: Information Disclosure
    component: API key Settings UI + get_api_key_status
    disposition: mitigate
    mitigation_plan: "get_api_key_status returns ONLY { has_key: bool } — never the key value or last-4 (D-01 explicitly forbids last-4). The input renders a masked placeholder when has_key is true; the actual secret is never sent back to the webview. set_api_key writes to keychain only; clear_api_key deletes. All three are no-ops/empty under is_e2e_mode (D-03)."
  - threat_id: T-03-02b
    category: Information Disclosure
    component: set_config carrying cloud_api_key back to JSON
    disposition: mitigate
    mitigation_plan: "Per Pitfall P7, set_config strips cloud_api_key from the incoming AppConfig before state.save_config(), routing a non-empty value to secrets::set_api_key first; #[serde(skip)] (Slice A) is the backstop so even an un-stripped value cannot land in JSON."
  - threat_id: T-03-02c
    category: Denial of Service
    component: keychain calls in command handlers
    disposition: mitigate
    mitigation_plan: "Every secrets::* call in commands/config.rs is wrapped in is_e2e_mode() guard and maps keyring::Error to a redacted Result<_, String> (no key in message), per D-06. Startup is never blocked."
disposition_summary: "Finalizes secret handling at the IPC + UI layer with strict has_key-only disclosure and E2E bypass; phase gate enforces full regression coverage."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: API key commands + set_config interception + registration (backend)</name>
  <files>src-tauri/src/commands/config.rs, src-tauri/src/commands/mod.rs, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/commands/config.rs (get_config, set_config, get_index_status), src-tauri/src/e2e.rs (is_e2e_mode line 11), crates/config/src/secrets.rs (get_api_key/set_api_key/delete_api_key from Slice A), src-tauri/src/commands/mod.rs, src-tauri/src/lib.rs (generate_handler! 947-1000), .planning/phases/03-library-tasks-keychain/03-RESEARCH.md (§6 providers integration, §8 new commands note, Pitfall P7), 03-CONTEXT.md D-01/D-03/D-04/D-06/D-07/D-24</read_first>
  <action>
    Per §8/D-24: add three `#[tauri::command]` fns to `src-tauri/src/commands/config.rs`:
    - `get_api_key_status(state) -> Result<ApiKeyStatus, String>` returning a small serde struct `{ has_key: bool }` (serde rename_all camelCase to match frontend). In E2E mode return `{ has_key: false }` without touching the keychain. Otherwise compute `has_key = !secrets::get_api_key().unwrap_or_default().is_empty()` (D-06 — no panic). NEVER return the key value (D-01, T-03-02).
    - `set_api_key(key: String, state) -> Result<(), String>`: in E2E mode no-op Ok(()); otherwise `secrets::set_api_key(&key)` mapped to a redacted String error, then update the in-memory `AppState` runtime field (`config.embedding.cloud_api_key = key`) and `reload_providers` so cloud calls pick it up.
    - `clear_api_key(state) -> Result<(), String>` (D-04): in E2E no-op; otherwise `secrets::delete_api_key()` (tolerate not-found), clear the runtime field, `reload_providers`.

    Per Pitfall P7/D-07: modify `set_config` so that BEFORE `state.save_config(&config)` it (1) takes the incoming `config.embedding.cloud_api_key`; (2) if non-empty and not E2E, call `secrets::set_api_key(&key)`; (3) set `config.embedding.cloud_api_key = String::new()` so the saved JSON has no secret (backed by #[serde(skip)]); (4) keep behavior identical otherwise (restart_watcher / restart_scheduler unchanged). Repopulate the in-memory runtime field after save so providers stay configured.

    Register the three new commands in `commands/mod.rs` (`pub use config::{get_config, get_index_status, set_config, get_api_key_status, set_api_key, clear_api_key};`) and in `lib.rs` `tauri::generate_handler!` — ADDITIONS ONLY, no existing command renamed or removed.
  </action>
  <verify>cargo test --workspace</verify>
  <acceptance_criteria>
    - `commands/config.rs` defines `get_api_key_status`, `set_api_key`, `clear_api_key`, all `#[tauri::command]`
    - `get_api_key_status` returns only a `has_key` boolean (no key/last-4 field) — verify struct has a single bool field (D-01/T-03-02)
    - `rg "is_e2e_mode" src-tauri/src/commands/config.rs` shows each secrets call is E2E-guarded (D-03)
    - `set_config` clears `cloud_api_key` before `save_config` and routes non-empty keys to `secrets::set_api_key` (P7) — `rg "secrets::set_api_key" src-tauri/src/commands/config.rs` matches in set_config
    - `lib.rs` `generate_handler!` lists `get_api_key_status`, `set_api_key`, `clear_api_key` (additions only)
    - `cargo test --workspace` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Settings API key UI (masked + clear) + D-18 inline Lark error + tauri.ts wrappers + settings.spec.ts</name>
  <files>src/lib/tauri.ts, src/App.tsx, e2e/specs/settings.spec.ts</files>
  <read_first>src/App.tsx (Settings Cloud API Key input 1646-1661, Lark panel 1541-1592, relocated Lark sync from Slice C, setConfig handler ~690, syncLark handler ~757), src/lib/tauri.ts (wrapper style + Lark wrappers from Slice C), e2e/specs/settings.spec.ts, e2e/specs/full-ui.spec.ts, e2e/helpers.ts (openNav, setReactInputValue, clickViaDom), .planning/phases/03-library-tasks-keychain/03-RESEARCH.md (§8 wrappers, Validation Deliverable 4), 03-CONTEXT.md D-01/D-04/D-18</read_first>
  <action>
    Per §8: add to `src/lib/tauri.ts`: `getApiKeyStatus()`→`get_api_key_status` (returns `{ has_key: boolean }`), `setApiKey(key)`→`set_api_key` with arg `{ key }`, `clearApiKey()`→`clear_api_key`. No IPC rename.

    Per D-01/D-04 — rewrite the Cloud API Key field in the Settings section of `src/App.tsx` (currently bound to `config.cloud_api_key`, lines 1646-1661):
    - On Settings mount / when cloud provider is selected, call `getApiKeyStatus()` and track `hasApiKey` state.
    - The input is `type="password"`, `autoComplete="off"`, `data-testid="cloud-api-key-input"`. When `hasApiKey` is true and the user has not typed a replacement, show a MASKED placeholder (e.g. `••••••••（已保存）`) and an empty value (D-01 — not last-4, not "empty = keep existing"; typing a new value replaces).
    - On save of a typed key: call `setApiKey(value)` (NOT via set_config carrying the plaintext), then refresh `hasApiKey`.
    - Add a 「清除密钥」 button `data-testid="clear-api-key"` that calls `clearApiKey()` then sets `hasApiKey=false` and clears the input (D-04).
    - The frontend AppConfig `cloud_api_key` field is no longer the secret transport for save (the dedicated commands are); leaving it empty in the set_config payload is fine (backend strips anyway, P7). Do not remove the TS field (TS nesting/cleanup is Phase 6).

    Per D-18: when the relocated Settings Lark sync (from Slice C) fails, render the error inline in the Settings panel (e.g. a `data-testid="lark-sync-error"` element near `lark-status-panel`) instead of only a transient/global error — user-visible feedback.

    Per e2e-required.mdc / Validation Deliverable 4: extend `e2e/specs/settings.spec.ts` with assertions that (a) the API key input renders with a masked placeholder semantics in E2E (since E2E has no key, assert the input + clear button exist and the section does not error), and (b) clicking `clear-api-key` invokes the command without error. Keep existing index-status/rebuild assertions. Use `openNav("settings", '[data-testid="settings-panel"]')`.
  </action>
  <verify>npm run test:e2e:local -- --spec e2e/specs/settings.spec.ts</verify>
  <acceptance_criteria>
    - `src/lib/tauri.ts` exports `getApiKeyStatus`, `setApiKey`, `clearApiKey`
    - `src/App.tsx` Settings renders `data-testid="cloud-api-key-input"` (password) and `data-testid="clear-api-key"`; the masked placeholder appears when `hasApiKey` is true (D-01)
    - Saving a typed key calls `setApiKey` (not set_config with plaintext); `rg "setApiKey\(" src/App.tsx` matches
    - `data-testid="lark-sync-error"` (or equivalent inline error element) present in the Settings Lark panel (D-18)
    - `e2e/specs/settings.spec.ts` asserts the API key input + clear button and that clear works under E2E without error
    - `npm run test:e2e:local -- --spec e2e/specs/settings.spec.ts` passes; `npx tsc --noEmit` clean
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Full-phase gate — workspace + npm + full E2E suite (03-05-02)</name>
  <files>e2e/specs/full-ui.spec.ts</files>
  <read_first>.planning/phases/03-library-tasks-keychain/03-VALIDATION.md (Full-phase gate), .planning/phases/03-library-tasks-keychain/03-RESEARCH.md (Full-phase gate checklist), 03-CONTEXT.md D-26/D-28</read_first>
  <action>
    Final integration gate per D-26/D-28 and the Research Full-phase gate. Rebuild artifacts the E2E run depends on: `npm run build && cargo build --release --manifest-path src-tauri/Cargo.toml`.

    Run, in order, and fix any regression in the file that introduced it (no scope creep beyond the touched files): `cargo test --workspace`, `npm test`, `npx tsc --noEmit`, then the FULL suite `npm run test:e2e:local` (smoke, navigation, qa, agent, full-ui, lark, memory, settings).

    Confirm the phase-level invariants hold (manual diff review where not auto-assertable): config.json on disk has no `cloud_api_key` after a migration run; Library/Tasks JSX fully gone from App.tsx; `commands/index.rs` + `commands/lark.rs` exist; manual Lark URL paste no longer in Library. Add a `full-ui.spec.ts` assertion only if a primary-journey gap is found (e.g. API key section visible in Settings); otherwise leave specs as-is per D-13.
  </action>
  <verify>npm run test:e2e:local</verify>
  <acceptance_criteria>
    - `cargo test --workspace` exits 0 (D-28/QA-03)
    - `npm test` exits 0 and `npx tsc --noEmit` clean (D-28)
    - `npm run test:e2e:local` full suite green (D-26/QA-01) — smoke, navigation, qa, agent, full-ui, lark, memory, settings
    - Manual gate confirmed: no `cloud_api_key` in saved config.json; Library/Tasks JSX absent from App.tsx; `commands/index.rs` + `commands/lark.rs` present; Lark paste absent from Library
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cargo test --workspace` passes
- [ ] `npm test` passes; `npx tsc --noEmit` clean
- [ ] `npm run test:e2e:local` full suite green (D-26)
- [ ] `get_api_key_status` returns only `has_key` (no key value, D-01/T-03-02)
- [ ] `set_config` strips + routes `cloud_api_key` (P7); no plaintext in saved JSON
- [ ] Settings shows masked placeholder + 「清除密钥」 (D-01/D-04)
- [ ] Lark sync failure visible inline in Settings (D-18)
- [ ] E2E (JARVIS_E2E=1) never touches keychain (D-03)
</verification>

<success_criteria>
- API key managed via dedicated keychain-backed commands; never returned to UI (CFG-02/D-01)
- Clear button removes keychain entry (D-04); set_config never persists plaintext (D-07/P7)
- Lark sync errors visible in Settings (D-18); E2E keychain-free (D-03)
- Full E2E + workspace + npm green; phase merge gate satisfied (QA-01/QA-03/D-26/D-28)
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Location |
|---------------|------|----------|
| `get_api_key_status` / `set_api_key` / `clear_api_key` | Tauri commands | `src-tauri/src/commands/config.rs` |
| `set_config` secret interception | command logic | `src-tauri/src/commands/config.rs` |
| `getApiKeyStatus` / `setApiKey` / `clearApiKey` | IPC wrappers | `src/lib/tauri.ts` |
| `cloud-api-key-input` / `clear-api-key` | data-testid | `src/App.tsx` (Settings) |
| `lark-sync-error` inline error | data-testid | `src/App.tsx` (Settings) |
| API key mask + clear E2E assertions | spec | `e2e/specs/settings.spec.ts` |

---

## Multi-Source Coverage Audit (Phase 3)

### Requirement coverage

| Requirement | Covered by | Verify |
|-------------|------------|--------|
| **FE-03** (Library + Tasks views extracted with hooks; index/Lark actions preserved) | Plan 03 (LibraryView+useLibrary), Plan 04 (TasksView+useTasks), Plan 05 (Settings controls) | npm test + full E2E |
| **FE-06** (testids preserved with JSX moves) | Plan 03 (library testids), Plan 04 (task testids), Plan 05 (api-key testids) | full-ui/lark/settings specs |
| **SHELL-03** (index/Lark commands in modules; progress events preserved) | Plan 02 (index.rs, lark.rs, library tasks) | cargo test --workspace |
| **CFG-02** (cloud_api_key in keychain; plaintext stripped on save) | Plan 01 (serde skip + secrets.rs), Plan 05 (set_config interception + UI) | cargo test -p config + settings.spec |
| **CFG-03** (legacy migration on first load) | Plan 01 (load_config_with_migration + migration.rs) | cargo test -p config |
| **QA-01** (E2E green after phase) | Plan 05 Task 3 (full suite) | npm run test:e2e:local |
| **QA-03** (cargo test --workspace + npm test green) | Plan 02 + Plan 05 | both commands |

### Locked decision coverage (D-01 … D-28)

| Decision | Plan | Decision | Plan |
|----------|------|----------|------|
| D-01 masked placeholder | 05 | D-15 extend lark.spec + full-ui | 03 |
| D-02 startup auto-migration | 01 | D-16 hide manual Lark paste in Library | 03 |
| D-03 E2E skips keyring | 01, 05 | D-17 Lark health in Settings | 03 (relocate), 05 |
| D-04 Clear API key button | 05 | D-18 Lark sync error inline in Settings | 05 |
| D-05 service `jarvis` / account `cloud_api_key` | 01 | D-19 no scheduler UI changes | 03/05 (none made) |
| D-06 graceful keyring degradation | 01, 05 | D-20 commands/index.rs | 02 |
| D-07 cloud_api_key #[serde(skip)] / runtime-only | 01, 05 | D-21 commands/lark.rs | 02 |
| D-08 controlled component pattern | 03, 04 | D-22 tasks in library.rs (no tasks.rs) | 02 |
| D-09 two hooks useLibrary + useTasks | 03, 04 | D-23 thin wrappers | 02 |
| D-10 index-progress listen stays in App | 03 | D-24 secrets via crates/config/secrets.rs | 01, 05 |
| D-11 full JSX extraction | 03, 04 | D-25 index_ops.rs untouched | 02 |
| D-12 types/library.ts + types/tasks.ts | 03, 04 | D-26 full E2E before merge | 05 |
| D-13 update existing specs, no new lib/tasks spec | 03, 04 | D-27 preserve testids same PR | 03, 04, 05 |
| D-14 Settings 「立即同步」 primary path | 03 | D-28 cargo test --workspace + npm test | 02, 05 |

### Deferred items (NOT implemented — confirmed absent)

- `commands/tasks.rs` separate module — NOT created (D-22 / Plan 02)
- Settings view extraction / `useJarvisConfig` — deferred to Phase 5–6 (Settings edits stay inline in App.tsx)
- `memory://` URI scheme — Phase 4
- Nested TS `AppConfig` mirroring Rust — Phase 6 (TS `cloud_api_key` field retained)
- Scheduler interval/toggle UI changes — none (D-19)
- Main nav AppShell / Sidebar extraction — Phase 6

**Coverage result:** All 4 phase requirements (FE-03, SHELL-03, CFG-02, CFG-03) + QA-01/QA-03 mapped; all 28 decisions D-01…D-28 assigned to a plan; no deferred item implemented.

<output>
After completion, create `.planning/phases/03-library-tasks-keychain/03-05-SUMMARY.md`
</output>
