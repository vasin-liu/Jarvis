---
phase: 02-chat-extraction-config-types
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/config/src/types.rs
  - crates/config/src/providers.rs
  - crates/config/src/file.rs
  - src-tauri/src/lib.rs
  - src-tauri/src/state.rs
  - src-tauri/src/e2e.rs
  - src-tauri/src/sync_scheduler.rs
  - src-tauri/src/insights_ops.rs
  - src-tauri/src/index_ops.rs
autonomous: true
requirements:
  - CFG-01
must_haves:
  truths:
    - "AppConfig is composed of five nested structs (EmbeddingConfig, ChatConfig, LarkConfig, SyncConfig, AgentConfig) via #[serde(flatten)] (D-20)"
    - "Existing flat config.json deserializes unchanged and re-serializes to the same flat top-level keys (no nesting objects in JSON) (D-21)"
    - "Each persisted field name appears in exactly one nested struct (no serde(flatten) key collision)"
    - "AppConfig::embedding_dim() remains a method on AppConfig and delegates to self.embedding (no caller of embedding_dim changes)"
    - "config_flat_json_roundtrip unit test passes via cargo test -p config (D-24)"
    - "cargo test --workspace exits 0 after all consumer field accesses are updated to nested paths"
  artifacts:
    - path: crates/config/src/types.rs
      provides: EmbeddingConfig, ChatConfig, LarkConfig, SyncConfig, AgentConfig nested structs + flattened AppConfig + roundtrip test
      contains: "#[serde(flatten)]"
    - path: crates/config/src/providers.rs
      provides: provider factory using nested config field paths
      contains: "cfg.embedding."
    - path: crates/config/src/file.rs
      provides: config load/save using nested defaults
      contains: "SyncConfig"
  key_links:
    - from: crates/config/src/providers.rs
      to: crates/config/src/types.rs
      via: "AppConfig nested field access (cfg.embedding.embedder, cfg.chat_cfg.chat)"
      pattern: "cfg\\.embedding\\."
    - from: src-tauri/src/sync_scheduler.rs
      to: crates/config/src/types.rs
      via: "cfg.sync.scheduled_sync_* field access"
      pattern: "\\.sync\\.scheduled_sync_"
    - from: src-tauri/src/lib.rs
      to: crates/config/src/types.rs
      via: "cfg.lark.* / cfg.sync.* / cfg.agent_cfg.* field access"
      pattern: "\\.lark\\.lark_cli_bin"
---

<objective>
Split the flat `AppConfig` (28 persisted fields + 1 `#[serde(skip)]`) into five nested Rust structs using `#[serde(flatten)]` so existing `config.json` files load and save with identical flat top-level keys, then update every consumer field-access site across the config crate and `src-tauri`.

Purpose: Deliver CFG-01 — a backward-compatible config shape refactor that enables later phases to grow config buckets without touching disk format.
Output: Nested `EmbeddingConfig`/`ChatConfig`/`LarkConfig`/`SyncConfig`/`AgentConfig` in `crates/config/src/types.rs`, a flat-JSON roundtrip unit test, and updated field-access paths in all callers. Zero user-visible behavior change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-chat-extraction-config-types/02-CONTEXT.md
@.planning/phases/02-chat-extraction-config-types/02-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@crates/config/src/types.rs
@crates/config/src/providers.rs
@crates/config/src/file.rs
@src-tauri/src/sync_scheduler.rs
</context>

<threat_model>
scope: structural_refactor_only
asvs_level: L1
trust_boundaries:
  - "App data path: config.json read/written via config::file load/save"
  - "No IPC, network, shell, or auth surface added or changed"
stride_register:
  - threat_id: T-01
    category: Information Disclosure
    component: EmbeddingConfig.cloud_api_key
    disposition: accept
    mitigation_plan: "cloud_api_key remains plaintext in flat JSON exactly as before — keychain migration is Phase 3 (CFG-02/CFG-03). #[serde(flatten)] does NOT remove or relocate the key. Do not add any logging/printing of AppConfig or cloud_api_key."
  - threat_id: T-02
    category: Tampering
    component: AppConfig serde roundtrip
    disposition: mitigate
    mitigation_plan: "Roundtrip test (D-24) asserts no key dropped/renamed and no nesting object appears in JSON, preventing silent config-shape corruption of user data."
disposition_summary: "Shape-only refactor. No new attack surface; existing plaintext secret handling unchanged and explicitly deferred to Phase 3."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Nest AppConfig into five structs + roundtrip test (config crate compiles)</name>
  <files>crates/config/src/types.rs, crates/config/src/providers.rs, crates/config/src/file.rs</files>
  <read_first>crates/config/src/types.rs, crates/config/src/providers.rs, crates/config/src/file.rs, .planning/phases/02-chat-extraction-config-types/02-RESEARCH.md (Pattern 4 + field-to-struct mapping), .planning/phases/02-chat-extraction-config-types/02-CONTEXT.md (D-20..D-24)</read_first>
  <action>
    TDD red-first: add `#[cfg(test)] mod tests` to `crates/config/src/types.rs` with `config_flat_json_roundtrip` exactly as in 02-RESEARCH.md "Roundtrip config test (D-24)" before changing the struct.

    Per D-20/D-22, define five nested structs in `crates/config/src/types.rs` with field membership EXACTLY as the 02-RESEARCH "AppConfig field-to-struct mapping" table:
    - `EmbeddingConfig`: embedder, mock_embed_dim, ollama_base_url, ollama_embed_model, ollama_embed_dim, fastembed_model, fastembed_dim, cloud_base_url, cloud_api_key, cloud_embed_model, cloud_embed_dim
    - `ChatConfig`: chat, ollama_chat_model, cloud_chat_model
    - `LarkConfig`: lark_cli_bin, lark_identity
    - `SyncConfig`: watch_folders, cursor_projects_root, auto_summarize_on_index, auto_extract_tasks_on_index, scheduled_sync_enabled, scheduled_sync_interval_minutes, scheduled_sync_watch_folders, scheduled_sync_lark, scheduled_sync_cursor
    - `AgentConfig`: auto_learn_from_chat, agents, active_agent_id, enabled_skill_ids, enabled_hook_ids, enabled_plugin_ids, agent_orchestration_mode, pipeline_agent_ids, granted_plugin_permissions

    Per D-21: `AppConfig` holds the five structs with field names `embedding`, `chat_cfg`, `lark`, `sync`, `agent_cfg`, each annotated `#[serde(flatten)]`; keep `fastembed_cache_dir: Option<PathBuf>` with `#[serde(skip)]`. Derive `Debug, Clone, PartialEq, Serialize, Deserialize` on all five + AppConfig.

    Per Pitfall 6: MOVE each existing `#[serde(default = "...")]` / `#[serde(default)]` annotation onto the corresponding INNER struct field (not AppConfig). Keep all existing `default_*()` helper fns.

    Rewrite `impl Default for AppConfig` to construct nested struct literals; implement `Default` (or inline construction) for each nested struct preserving the exact current default values from the existing `Default for AppConfig`.

    Per Pitfall 5: keep `AppConfig::embedding_dim(&self)` on `AppConfig`, rewriting its body to read `self.embedding.embedder`, `self.embedding.mock_embed_dim`, `self.embedding.ollama_embed_dim`, `self.embedding.fastembed_model`, `self.embedding.fastembed_dim`, `self.embedding.cloud_embed_dim`.

    Update config-crate-internal consumers so the crate compiles:
    - `crates/config/src/providers.rs`: rewrite all `cfg.<field>` accesses to nested paths (`cfg.embedding.embedder`, `cfg.chat_cfg.chat`, `cfg.embedding.cloud_*`, `cfg.embedding.ollama_*`, etc.). Do NOT change function signatures or provider behavior.
    - `crates/config/src/file.rs`: rewrite any direct field accesses / defaults to nested paths.
  </action>
  <verify>cargo test -p config</verify>
  <acceptance_criteria>
    - `crates/config/src/types.rs` defines structs `EmbeddingConfig`, `ChatConfig`, `LarkConfig`, `SyncConfig`, `AgentConfig`
    - `AppConfig` contains exactly five `#[serde(flatten)]` fields and one `#[serde(skip)]` field (`fastembed_cache_dir`)
    - `grep "#\[serde(flatten)\]" crates/config/src/types.rs` returns exactly 5 matches
    - `AppConfig::embedding_dim` is still defined on `AppConfig` and its signature is unchanged
    - `cargo test -p config` exits 0 and runs `config_flat_json_roundtrip`
    - The roundtrip test asserts `output.get("watch_folders").is_some()`, `output.get("embedder").is_some()`, `output.get("sync").is_none()` (no nesting in JSON)
    - No persisted field name appears in two structs (verified by serde deserialize succeeding in the test)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update src-tauri consumer field-access sites to nested paths</name>
  <files>src-tauri/src/lib.rs, src-tauri/src/state.rs, src-tauri/src/e2e.rs, src-tauri/src/sync_scheduler.rs, src-tauri/src/insights_ops.rs, src-tauri/src/index_ops.rs</files>
  <read_first>src-tauri/src/lib.rs, src-tauri/src/state.rs, src-tauri/src/e2e.rs, src-tauri/src/sync_scheduler.rs, src-tauri/src/insights_ops.rs, src-tauri/src/index_ops.rs, .planning/phases/02-chat-extraction-config-types/02-RESEARCH.md ("Field access sites that need updating")</read_first>
  <action>
    Rewrite every `AppConfig` field access in `src-tauri` to its nested path. Mapping rule (field → nested path):
    - embedder/mock_embed_dim/ollama_base_url/ollama_embed_model/ollama_embed_dim/fastembed_model/fastembed_dim/cloud_base_url/cloud_api_key/cloud_embed_model/cloud_embed_dim → `.embedding.<field>`
    - chat/ollama_chat_model/cloud_chat_model → `.chat_cfg.<field>`
    - lark_cli_bin/lark_identity → `.lark.<field>`
    - watch_folders/cursor_projects_root/auto_summarize_on_index/auto_extract_tasks_on_index/scheduled_sync_enabled/scheduled_sync_interval_minutes/scheduled_sync_watch_folders/scheduled_sync_lark/scheduled_sync_cursor → `.sync.<field>`
    - auto_learn_from_chat/agents/active_agent_id/enabled_skill_ids/enabled_hook_ids/enabled_plugin_ids/agent_orchestration_mode/pipeline_agent_ids/granted_plugin_permissions → `.agent_cfg.<field>`

    Apply to the binding actually used in each file (`cfg`, `config`, `self.config`, struct-literal field assignment, etc.). DO NOT change `embedding_dim()` call sites — `cfg.embedding_dim()` stays as-is (Pitfall 5).

    When constructing `AppConfig` literals (e.g. test/e2e overrides in `e2e.rs`), build nested struct literals or start from `AppConfig::default()` and mutate nested fields — keep resulting behavior identical.

    Constraint: this task changes ONLY field-access paths and struct construction. No IPC names, command signatures, provider selection logic, or scheduling behavior may change.
  </action>
  <verify>cargo test --workspace</verify>
  <acceptance_criteria>
    - `cargo test --workspace` exits 0
    - `cargo build --manifest-path src-tauri/Cargo.toml` succeeds
    - `rg "\.scheduled_sync_" src-tauri/src/sync_scheduler.rs` shows accesses go through `.sync.` (no bare `cfg.scheduled_sync_*`)
    - `rg "cfg\.lark_cli_bin|cfg\.cursor_projects_root|cfg\.watch_folders" src-tauri/src` returns no matches (all migrated to nested paths)
    - `git diff` introduces no change to any `#[tauri::command]` function signature or IPC name
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cargo test -p config` passes (CFG-01 roundtrip)
- [ ] `cargo test --workspace` passes
- [ ] `cargo build` in src-tauri succeeds
- [ ] Re-serialized config JSON has flat top-level keys only (no `sync`/`embedding` objects)
- [ ] `embedding_dim()` call sites unchanged
- [ ] No IPC signature or name change
</verification>

<success_criteria>
- `AppConfig` is five nested structs flattened to the existing flat JSON (D-20/D-21)
- Existing `config.json` loads + round-trips with identical keys (D-24 test green)
- All consumer field accesses compile against nested paths; `cargo test --workspace` green (QA-03 partial)
- `cloud_api_key` handling unchanged (Phase 3 defers keychain)
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Location |
|---------------|------|----------|
| `EmbeddingConfig` | struct | `crates/config/src/types.rs` |
| `ChatConfig` | struct | `crates/config/src/types.rs` |
| `LarkConfig` | struct | `crates/config/src/types.rs` |
| `SyncConfig` | struct | `crates/config/src/types.rs` |
| `AgentConfig` | struct | `crates/config/src/types.rs` |
| `AppConfig` (nested, flattened) | struct | `crates/config/src/types.rs` |
| `AppConfig::embedding_dim` (delegating) | method | `crates/config/src/types.rs` |
| `config_flat_json_roundtrip` | unit test | `crates/config/src/types.rs` |

<output>
After completion, create `.planning/phases/02-chat-extraction-config-types/02-01-SUMMARY.md`
</output>
