---
phase: 17-mcp-transport-read-only-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/mcp/Cargo.toml
  - crates/mcp/src/lib.rs
  - crates/mcp/src/main.rs
  - crates/mcp/src/paths.rs
  - crates/mcp/src/error.rs
  - crates/mcp/tests/paths.rs
autonomous: true
requirements:
  - MCP-03
must_haves:
  truths:
    - "Workspace member crates/mcp builds as console binary jarvis-mcp without GUI subsystem attribute (D-01)"
    - "Workspace pins rmcp exactly 2.2.0 with server + transport-io + macros; MSRV stays 1.85 (D-02)"
    - "--db overrides JARVIS_DATA_DIR which overrides default AppData com.jarvis.app/kb.sqlite (D-05)"
    - "Missing resolved DB path returns Err and does not create a file (D-06)"
  artifacts:
    - path: crates/mcp/Cargo.toml
      provides: "[[bin]] name = jarvis-mcp; deps store/config/rmcp/tokio/serde_json/thiserror; no rusqlite"
    - path: crates/mcp/src/paths.rs
      provides: "resolve_db_path / default_app_data_dir / ensure_db_exists"
    - path: crates/mcp/tests/paths.rs
      provides: "Precedence + missing-DB tests with tempfile"
  key_links:
    - from: crates/mcp/src/paths.rs
      to: src-tauri/src/state.rs
      via: "Same kb.sqlite + config.json layout under com.jarvis.app"
      pattern: "join(\"kb.sqlite\")"
    - from: crates/mcp/Cargo.toml
      to: Cargo.toml
      via: "workspace member + rmcp workspace.dependency (D-02)"
      pattern: "rmcp = { version = \"2.2.0\""
---

<objective>
Scaffold `crates/mcp` + `jarvis-mcp` binary and pure path-resolution helpers with failing-then-passing path tests.

Purpose: MCP-03 needs a shippable console binary and documented KB resolution; D-01/D-02/D-05/D-06 lock packaging and fail-closed missing DB. MCP-04 is only partially touched here via crate existence (tool allowlist lands in 17-03).
Output: Workspace member `mcp`, compiling `jarvis-mcp` stub entry, `paths`/`error` modules, green `cargo test -p mcp paths`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-mcp-transport-read-only-scaffold/17-CONTEXT.md
@.planning/phases/17-mcp-transport-read-only-scaffold/17-RESEARCH.md
@.planning/phases/17-mcp-transport-read-only-scaffold/17-PATTERNS.md
@.planning/phases/17-mcp-transport-read-only-scaffold/17-VALIDATION.md
@Cargo.toml
@crates/lark/Cargo.toml
@src-tauri/src/state.rs
@src-tauri/src/main.rs
@crates/config/src/file.rs
@crates/agent/tests/real_embed_hang.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED path-resolution tests for --db / JARVIS_DATA_DIR / default</name>
  <read_first>
    - .planning/phases/17-mcp-transport-read-only-scaffold/17-CONTEXT.md (D-05, D-06)
    - .planning/phases/17-mcp-transport-read-only-scaffold/17-RESEARCH.md (§3 path table)
    - .planning/phases/17-mcp-transport-read-only-scaffold/17-PATTERNS.md (paths.rs analog)
    - crates/config/tests/migration.rs (env save/restore + TempDir)
    - src-tauri/src/state.rs (kb.sqlite layout)
  </read_first>
  <files>crates/mcp/tests/paths.rs, crates/mcp/src/paths.rs, crates/mcp/src/error.rs, crates/mcp/src/lib.rs, crates/mcp/Cargo.toml, Cargo.toml, crates/mcp/src/main.rs</files>
  <behavior>
    - resolve_db_prefers_db_flag: when --db path and JARVIS_DATA_DIR both set, resolved file equals --db path
    - resolve_db_from_data_dir_env: JARVIS_DATA_DIR alone → {dir}/kb.sqlite
    - resolve_db_default_app_data: neither flag nor env → {default_app_data_dir}/kb.sqlite matching com.jarvis.app
    - ensure_db_missing_errors_without_create: ensure_db_exists on nonexistent path returns Err; path still absent after call
  </behavior>
  <action>
    Create workspace member crates/mcp per D-01: append "crates/mcp" to root Cargo.toml members. Pin rmcp in [workspace.dependencies] exactly as D-02: version 2.2.0, default-features false, features server + transport-io + macros. Confirm package identity with `cargo info rmcp@2.2.0` (official Model Context Protocol Rust SDK / modelcontextprotocol) before locking — T-17-SC; do not take 3.x beta; do not bump rust-version.

    Create crates/mcp/Cargo.toml mirroring crates/lark/Cargo.toml style: version/edition/rust-version.workspace = true; [[bin]] name = "jarvis-mcp" path = "src/main.rs"; deps rmcp/store/config/tokio/serde/serde_json/thiserror via workspace or path; [dev-dependencies] tempfile = "3"; never add rusqlite to mcp (store sole SQLite owner).

    Create crates/mcp/src/error.rs (thiserror McpError + Result alias), crates/mcp/src/paths.rs with public API: default_app_data_dir() (Windows APPDATA\com.jarvis.app; Linux XDG_DATA_HOME or ~/.local/share/com.jarvis.app; macOS ~/Library/Application Support/com.jarvis.app), resolve_db_path(db_flag: Option&lt;PathBuf&gt;, data_dir_env: Option&lt;PathBuf&gt;) implementing D-05 precedence, ensure_db_exists(path) returning Err if !is_file() per D-06 (no Store::open, no file create).

    Thin lib.rs: mod error; mod paths; re-export Result, McpError, resolve_db_path, ensure_db_exists, default_app_data_dir. Stub main.rs: fn main() {} or eprintln + exit 0 — must NOT set the Windows GUI subsystem attribute (D-01 anti-pattern from src-tauri main).

    Write crates/mcp/tests/paths.rs first (RED then GREEN): tempfile fixtures; save/restore JARVIS_DATA_DIR like config migration tests; assert all four behaviors above. Do not implement tool routers here (Plan 17-03).
  </action>
  <verify>
    <automated>cargo test -p mcp paths -- --test-threads=4</automated>
  </verify>
  <acceptance_criteria>
    - Root Cargo.toml lists crates/mcp; workspace.dependencies has rmcp 2.2.0 with the three features
    - crates/mcp/Cargo.toml has [[bin]] name = "jarvis-mcp" and no rusqlite dependency
    - cargo test -p mcp paths exits 0
    - ensure_db_missing_errors_without_create proves file absent after Err
    - crates/mcp/src/main.rs has zero matches for the Windows GUI subsystem attribute string
  </acceptance_criteria>
  <done>Path helpers and mcp crate scaffold compile; path precedence + missing-DB tests green (D-01, D-02, D-05, D-06).</done>
</task>

<task type="auto">
  <name>Task 2: Minimal CLI arg parse for --db wired to paths API</name>
  <read_first>
    - crates/mcp/src/paths.rs
    - crates/mcp/src/main.rs
    - .planning/phases/17-mcp-transport-read-only-scaffold/17-RESEARCH.md (§3 startup sequence steps 1–3)
  </read_first>
  <files>crates/mcp/src/main.rs, crates/mcp/src/paths.rs, crates/mcp/src/lib.rs</files>
  <action>
    Add a pure parse_db_flag(args: impl IntoIterator&lt;Item = String&gt;) -&gt; Option&lt;PathBuf&gt; in paths.rs (or lib) that reads `--db` followed by value from argv — hand-rolled, no clap (research discretion). Unit-test via paths tests or inline #[cfg(test)] that `--db C:\tmp\kb.sqlite` parses and unknown flags are ignored.

    In main.rs for this plan only: parse args, resolve path via resolve_db_path(db_flag, env::var_os("JARVIS_DATA_DIR").map(PathBuf::from)), call ensure_db_exists; on Err print clear message to stderr and process::exit(1) (D-06). Do NOT call Store::open yet (Plan 17-04). Do NOT start rmcp stdio yet. Keep binary a console process (D-01).
  </action>
  <verify>
    <automated>cargo test -p mcp -- --test-threads=4</automated>
  </verify>
  <acceptance_criteria>
    - parse_db_flag covered by at least one automated test
    - main compiles with cargo build -p mcp
    - Missing DB path still exits non-zero without creating kb.sqlite when exercised via ensure_db_exists
  </acceptance_criteria>
  <done>CLI --db parse + fail-closed preflight available for Plan 17-04 stdio wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host CLI args / env → paths.rs | Untrusted `--db` and `JARVIS_DATA_DIR` from MCP host or shell |
| mcp crate → filesystem | Must not create KB as side effect of resolution |
| Cargo registry → workspace | New `rmcp` dependency supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | resolve_db_path / --db | high | mitigate | Prefer explicit `--db` file; env only appends `kb.sqlite` under given dir; never open via Store until preflight `is_file` (D-06) |
| T-17-02 | Elevation of Privilege | ensure_db_exists | high | mitigate | Missing DB returns Err + exit 1; no `Store::open` / schema init in this plan |
| T-17-03 | Information Disclosure | default_app_data_dir | medium | mitigate | Document path only; do not log API keys; config load deferred to 17-04 |
| T-17-SC | Tampering | rmcp 2.2.0 cargo install | high | mitigate | `cargo info rmcp@2.2.0` identity check; pin exact 2.2.0; reject 3.x beta (D-02); official MCP Rust SDK only |
</threat_model>

<verification>
- cargo test -p mcp paths
- cargo build -p mcp
- rg/rusqlite absent from crates/mcp/Cargo.toml
</verification>

<success_criteria>
- D-01/D-02/D-05/D-06 implemented for path layer
- jarvis-mcp binary target exists and builds as console
- Path tests green; ready for stub tools (17-03) and stdio main (17-04)
</success_criteria>

## Artifacts

| Symbol | Kind | Location | Phase |
|--------|------|----------|-------|
| `crates/mcp` workspace member | crate + bin | crates/mcp/ | 17-01 |
| `jarvis-mcp` | [[bin]] | crates/mcp/Cargo.toml | 17-01 |
| `rmcp` 2.2.0 | workspace dep | Cargo.toml | 17-01 |
| `resolve_db_path` / `ensure_db_exists` / `default_app_data_dir` | fns | crates/mcp/src/paths.rs | 17-01 |
| `McpError` | thiserror enum | crates/mcp/src/error.rs | 17-01 |
| `paths` integration tests | tests | crates/mcp/tests/paths.rs | 17-01 |

<output>
Create `.planning/phases/17-mcp-transport-read-only-scaffold/17-01-SUMMARY.md` when done
</output>
