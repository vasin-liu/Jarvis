---
phase: 05-agent-protocol
plan: "04"
type: execute
wave: 4
depends_on:
  - "05-03"
files_modified:
  - src/types/ipc.ts
  - src/App.tsx
  - src/views/ChatView.tsx
  - src/views/ChatView.test.tsx
  - e2e/specs/agent.spec.ts
  - e2e/specs/full-ui.spec.ts
  - .planning/phases/05-agent-protocol/05-VALIDATION.md
autonomous: true
requirements:
  - AGT-02
  - AGT-03
must_haves:
  truths:
    - "ChatView renders amber agent-tool-parse-warnings panel below assistant message when tool_parse_warnings.length > 0 (D-13)"
    - "App.tsx sets lastToolParseWarnings from AskResponse on agent ask; clears on new ask; warnings only on final message not orchestration step cards (D-22)"
    - "agent-mode-toggle, agent-tool-calls, orchestration-mode-* testids preserved (D-28)"
    - "npm run test:e2e:local agent.spec.ts and full-ui.spec.ts pass with JSON Mock path (D-27, AGT-03)"
    - "cargo test --workspace && npm test && npx tsc --noEmit green (D-26)"
  artifacts:
    - path: src/views/ChatView.tsx
      provides: agent-tool-parse-warnings amber panel
      contains: "agent-tool-parse-warnings"
    - path: src/types/ipc.ts
      provides: AskResponse.tool_parse_warnings TypeScript type
      contains: "toolParseWarnings"
    - path: e2e/specs/agent.spec.ts
      provides: agent E2E regression with structured JSON format
      contains: "agent mode"
  key_links:
    - from: src/App.tsx
      to: src/views/ChatView.tsx
      via: lastToolParseWarnings prop
      pattern: "lastToolParseWarnings"
    - from: src/App.tsx
      to: ask_agent_in_session IPC
      via: resp.toolParseWarnings or tool_parse_warnings from invoke
      pattern: "tool_parse_warnings"
---

## Phase Goal

**As a** Jarvis user in agent mode, **I want to** see amber warnings when tool calls fail to parse, **so that** I understand the model reply even when the agent loop stopped early.

<objective>
Wire tool_parse_warnings from IPC to ChatView UI; run Phase 5 E2E gate; update validation sign-off document.

Purpose: Complete AGT-02 user-visible half (D-13) and AGT-03 E2E gate (D-27); close phase quality gates (D-26, D-28).
Output: ChatView warnings panel, App state wiring, ipc.ts types, green agent + full-ui E2E, updated 05-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.cursor/rules/e2e-required.mdc
@.cursor/rules/frontend-taste.mdc
</execution_context>

<context>
@.planning/phases/05-agent-protocol/05-CONTEXT.md
@.planning/phases/05-agent-protocol/05-RESEARCH.md
@src/views/ChatView.tsx
@src/App.tsx
@src/types/ipc.ts
@e2e/specs/agent.spec.ts
@e2e/specs/full-ui.spec.ts
@e2e/helpers.ts
</context>

<threat_model>
scope: e2e_and_ui
asvs_level: L1
trust_boundaries:
  - "AskResponse warnings → React render (plain text only)"
stride_register:
  - threat_id: T-05-D1
    category: Tampering
    component: ChatView warnings panel
    disposition: mitigate
    mitigation_plan: "Render warnings as text nodes only; no dangerouslySetInnerHTML; data-testid agent-tool-parse-warnings for E2E."
  - threat_id: T-05-D2
    category: Denial of Service
    component: agent ask stuck on 思考中
    disposition: mitigate
    mitigation_plan: "Preserve Chinese regression test in agent.spec.ts; verify parse Failed does not panic shell."
disposition_summary: "UI XSS and E2E regressions are primary risks."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: ChatView warnings UI + IPC types (05-04-01)</name>
  <files>src/types/ipc.ts, src/App.tsx, src/views/ChatView.tsx, src/views/ChatView.test.tsx</files>
  <read_first>src/views/ChatView.tsx agent-tool-calls panel styling, src/App.tsx agent ask invoke paths (ask_agent_in_session and stream), 05-CONTEXT.md D-13 and D-22, .cursor/rules/frontend-taste.mdc</read_first>
  <action>
    Extend AskResponse in src/types/ipc.ts with toolParseWarnings?: string[] (mirror serde camelCase tool_parse_warnings from Rust).

    App.tsx agent ask handlers (both stream and non-stream):
    - Add state lastToolParseWarnings: string[] default [].
    - Clear lastToolParseWarnings when starting new ask.
    - After successful invoke, set from resp.toolParseWarnings ?? resp.tool_parse_warnings ?? [] (handle serde shape).
    - Do NOT push warnings into orchestration step card props — only pass to final assistant message area (D-22).

    ChatView.tsx:
    - Add optional prop lastToolParseWarnings?: string[].
    - Below assistant answer / adjacent to agent-tool-calls panel, when lastToolParseWarnings.length > 0 render amber inline panel with data-testid="agent-tool-parse-warnings".
    - List each warning as plain text line; match liquid-glass amber styling consistent with existing agent-tool-calls panel; honor prefers-reduced-motion.
    - Keep agent-tool-calls panel unchanged for successful tool execution path (D-13, D-28).

    Add or extend ChatView.test.tsx: render with sample warnings asserts testid present and warning text visible.

    Run npm test and npx tsc --noEmit.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - rg "agent-tool-parse-warnings" src/views/ChatView.tsx matches
    - rg "toolParseWarnings" src/types/ipc.ts matches
    - npm test exits 0
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: E2E gate + validation doc update (05-04-02)</name>
  <files>e2e/specs/agent.spec.ts, e2e/specs/full-ui.spec.ts, .planning/phases/05-agent-protocol/05-VALIDATION.md</files>
  <read_first>e2e/specs/agent.spec.ts, e2e/helpers.ts askAgentQuestion, 05-RESEARCH.md Pitfall 3 E2E metadata, .cursor/rules/e2e-required.mdc</read_first>
  <action>
    Run unit gate first: cargo test -p agent -p llm && cargo test --workspace --quiet; npm test. Fix any failures before E2E.

    Run npm run build:e2e then:
    - npm run test:e2e:local -- --spec e2e/specs/agent.spec.ts
    - npm run test:e2e:local -- --spec e2e/specs/full-ui.spec.ts

    agent.spec.ts (AGT-03, D-27):
    - Keep existing Chinese regression and orchestration mode tests unchanged.
    - Optionally add non-flaky assertion that agent-mode ask completes with answer length > 0 (already present).
    - Do NOT require agent-tool-calls visible in E2E if start_ask_e2e path lacks tool metadata (RESEARCH Pitfall 3) — JSON path verified by unit/integration tests.
    - If stable, add comment noting warnings panel testid available for future parse-failure injection test.

    Update .planning/phases/05-agent-protocol/05-VALIDATION.md:
    - Mark Wave 0 gaps complete.
    - Update per-task verification map task IDs 05-01-01 through 05-04-02 aligned with replanned waves.
    - Set nyquist_compliant true when all automated commands documented.

    Phase gate command documented: cargo test --workspace && npm test && npm run test:e2e:local.
  </action>
  <verify>
    <automated>cargo test --workspace --quiet && npm run build:e2e && npm run test:e2e:local -- --spec e2e/specs/agent.spec.ts && npm run test:e2e:local -- --spec e2e/specs/full-ui.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - agent.spec.ts green
    - full-ui.spec.ts green
    - 05-VALIDATION.md lists all four plans with updated task map
    - cargo test -p agent exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] ChatView warnings panel renders with testid
- [ ] Unit + tsc gate green
- [ ] agent.spec.ts green
- [ ] full-ui.spec.ts green
- [ ] 05-VALIDATION.md updated
- [ ] Phase 5 ROADMAP success criteria met
</verification>

<success_criteria>
AGT-02 UI complete; AGT-03 E2E green; phase ready for /gsd-verify-work 5.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Requirement |
|----------|------|-------------|
| Warnings UI panel | `src/views/ChatView.tsx` | AGT-02 |
| Frontend IPC types | `src/types/ipc.ts` | AGT-02 |
| App state wiring | `src/App.tsx` | AGT-02 |
| E2E regression | `e2e/specs/agent.spec.ts`, `full-ui.spec.ts` | AGT-03 |
| Validation sign-off | `.planning/phases/05-agent-protocol/05-VALIDATION.md` | QA gate |

<output>
Create `.planning/phases/05-agent-protocol/05-04-SUMMARY.md` when done
</output>
