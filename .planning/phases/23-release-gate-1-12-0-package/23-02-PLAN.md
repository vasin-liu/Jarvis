---
phase: 23
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - docs/release/v1.12.0-smoke.md
  - package.json
  - src-tauri/tauri.conf.json
  - CHANGELOG.md
autonomous: false
requirements: [SHIP-01, SHIP-02, SHIP-03, SHIP-04]
user_setup:
  - "Interactive Windows cold-start smoke must be executed and Result marked PASS in docs/release/v1.12.0-smoke.md before version bump"

must_haves:
  truths:
    - "Smoke checklist Result is PASS before any version bump"
    - "package.json and tauri.conf.json are 1.12.0"
    - "CHANGELOG.md covers 1.12.0 hardening + 1.9–1.11 summary"
    - "tauri build produces a Windows bundle artifact; path recorded in smoke doc"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "1.12.0 release notes"
    - path: "package.json"
      provides: "version 1.12.0"
    - path: "src-tauri/tauri.conf.json"
      provides: "version 1.12.0"
  key_links:
    - from: "docs/release/v1.12.0-smoke.md"
      to: "package.json"
      via: "PASS gate before bump"
---

<objective>
After interactive smoke PASS: bump to 1.12.0, write CHANGELOG, run tauri build, record final artifact smoke.

Purpose: Complete SHIP-01 execution + SHIP-02/03/04.
Output: 1.12.0 metadata + Windows package + filled smoke doc.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-release-gate-1-12-0-package/23-CONTEXT.md
@.planning/phases/23-release-gate-1-12-0-package/23-01-SUMMARY.md
@docs/superpowers/plans/2026-07-29-release-hardening.md
@docs/release/v1.12.0-smoke.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Interactive cold-start smoke (blocking)</name>
  <files>docs/release/v1.12.0-smoke.md</files>
  <action>
**HARD GATE (D-01 / D-07):** Do not bump versions until smoke Result = PASS.

Operator (or agent with real GUI access) must:

1. Build/run a release or near-release binary as needed for cold-start (prefer `npm run tauri build` after bump is forbidden here — use existing release binary if present, or `npm run tauri build` with **current 1.8.0** labels for pre-bump smoke, OR `cargo build -p tauri-app --release` / `npm run tauri build` documenting that pre-bump artifact still shows 1.8.0).
2. Fill Machine + Date in smoke doc.
3. Check interactive boxes that pass (cold start ~5s, Settings readiness pending→ready, Library/Chat).
4. Mark Result **PASS** or **FAIL**.

If Result is FAIL or still unchecked → **STOP**. Do not continue to Task 2. Ask user to fix or re-run smoke.

If agent cannot drive the GUI in this session: pause chain, present exact checklist path, wait for user to fill PASS, then resume `/gsd-execute-phase 23`.
  </action>
  <verify>
    <manual>docs/release/v1.12.0-smoke.md Result section has PASS checked</manual>
  </verify>
  <done>Smoke Result PASS recorded in the checklist.</done>
</task>

<task type="auto">
  <name>Task 2: Bump 1.12.0 + CHANGELOG</name>
  <files>package.json, src-tauri/tauri.conf.json, CHANGELOG.md</files>
  <action>
**Precondition:** Smoke doc Result PASS is checked. If not, abort.

1. Set `"version": "1.12.0"` in `package.json` and `src-tauri/tauri.conf.json` only.

2. Create root `CHANGELOG.md`:

```markdown
# Changelog

## 1.12.0 — YYYY-MM-DD

### Fixed / Hardened
- FastEmbed deferred init readiness visible in Settings (pending / ready / failed)
- Release cold-start path verified via Windows smoke checklist

### Included since 1.8.0 (summary)
- **1.9** Structural refactor (views/hooks, Tauri commands, keychain, `memory://`, JSON agent protocol)
- **1.10** Wiki compile layer + Obsidian zip export + citation E2E trust
- **1.11** Library related-docs panel + read-only `jarvis-mcp` (`search` / `list_sources`)

See `.planning/MILESTONES.md` for planning archives.
```

Use today's date (or smoke Date field).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "(Get-Content package.json | Select-String '\"version\": \"1.12.0\"') -and (Get-Content src-tauri/tauri.conf.json | Select-String '\"version\": \"1.12.0\"') -and (Test-Path CHANGELOG.md)"</automated>
  </verify>
  <done>Versions 1.12.0 + CHANGELOG.md exist.</done>
</task>

<task type="auto">
  <name>Task 3: tauri build + final smoke record + commit</name>
  <files>docs/release/v1.12.0-smoke.md, package.json, src-tauri/tauri.conf.json, CHANGELOG.md</files>
  <action>
1. Run `npm run tauri build` (long-running; allow ample time). Expect exit 0.

2. Locate Windows artifact under `src-tauri/target/release/bundle/` (nsis/msi/exe). Record path in smoke doc Artifacts section. Update `package.json` version after bump note to 1.12.0.

3. Final smoke: launch/install artifact if feasible; check "Installer/portable artifact launches" box. If GUI unavailable, record artifact path and note launch verification deferred to operator — but prefer launching the built `.exe` once if possible.

4. Commit: `chore: release 1.12.0 — hardening + Windows package metadata`
   Files: package.json, src-tauri/tauri.conf.json, CHANGELOG.md, docs/release/v1.12.0-smoke.md

5. Do **not** `git tag` or `git push` unless user explicitly asks.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Test-Path 'src-tauri/target/release/bundle'"</automated>
  </verify>
  <done>Windows bundle exists; smoke doc updated; release metadata committed.</done>
</task>

</tasks>

<verification>
Smoke PASS before bump; versions 1.12.0; CHANGELOG present; bundle dir exists; release commit done.
</verification>

<success_criteria>
- SHIP-01 executed (PASS recorded)
- SHIP-02 / SHIP-03 / SHIP-04 satisfied
- No premature bump relative to smoke
</success_criteria>

<output>
After completion, create `.planning/phases/23-release-gate-1-12-0-package/23-02-SUMMARY.md`
</output>
