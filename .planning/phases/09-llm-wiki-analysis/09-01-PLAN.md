---
phase: 09-llm-wiki-analysis
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/insights/src/error.rs
  - crates/insights/src/wiki.rs
  - crates/insights/src/lib.rs
  - crates/llm/src/mock.rs
autonomous: true
requirements:
  - WIKI-03
must_haves:
  truths:
    - "InsightsError::InvalidWikiJson exists and maps extract/serde failures (D-12; ROADMAP criterion 2 half)"
    - "MockChatModel system prompt containing 「笔记编译」 returns minimal valid wiki JSON with non-empty summary, one entity, empty concepts (D-01, D-02, D-03)"
    - "analyze_source_for_wiki with MockChatModel yields populated summary and at least one entity (ROADMAP criterion 1)"
    - "Chinese system prompt requires bare JSON only, includes 「笔记编译」, and sparse ≤5–8 guidance; user message is title + body via truncate_chars 12_000 (D-05, D-06, D-08)"
    - "analyze_source_for_wiki does not read or write sources.summary (D-07)"
    - "No queue-based MockChatModel::new(vec) API added (D-01)"
  artifacts:
    - path: crates/insights/src/error.rs
      provides: "InsightsError::InvalidWikiJson"
      contains: "InvalidWikiJson"
    - path: crates/llm/src/mock.rs
      provides: "「笔记编译」 keyword branch"
      contains: "笔记编译"
    - path: crates/insights/src/wiki.rs
      provides: "analyze_source_for_wiki + parse helpers"
      contains: "analyze_source_for_wiki"
    - path: crates/insights/src/lib.rs
      provides: "pub re-export analyze_source_for_wiki"
      contains: "analyze_source_for_wiki"
  key_links:
    - from: analyze_source_for_wiki
      to: ChatModel.complete
      via: "system prompt must contain 「笔记编译」 so Mock branch fires"
      pattern: "笔记编译"
    - from: parse_wiki_analysis
      to: InsightsError::InvalidWikiJson
      via: "extract/serde errors map to typed variant"
      pattern: "InvalidWikiJson"
    - from: analyze_source_for_wiki
      to: store.source_chunk_text
      via: "Indexed guard + truncate_chars(12000); no set_source_summary"
      pattern: "truncate_chars"
---

<objective>
Deliver WIKI-03 analyze half foundation: typed `InvalidWikiJson`, keyword Mock 「笔记编译」 happy-path JSON (D-01…D-03, D-12), and `analyze_source_for_wiki` that loads an indexed source, calls ChatModel, and returns populated `WikiAnalysis` under Mock (D-05…D-08; ROADMAP criterion 1).

Purpose: Phase 09 starts with a green Mock→analysis path before fail-closed FS proof (Plan 09-02 / WIKI-05).
Output: `InvalidWikiJson`; Mock wiki branch + unit test; public `analyze_source_for_wiki` + parse helpers; `analyze_parses_mock_json` green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-llm-wiki-analysis/09-CONTEXT.md
@.planning/phases/09-llm-wiki-analysis/09-RESEARCH.md
@.planning/phases/09-llm-wiki-analysis/09-PATTERNS.md
@.planning/phases/09-llm-wiki-analysis/09-VALIDATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/superpowers/plans/2026-07-16-wiki-compile-layer.md
@.cursor/rules/tdd-goal-driven.mdc
@crates/insights/src/wiki.rs
@crates/insights/src/tasks.rs
@crates/insights/src/summarize.rs
@crates/insights/src/error.rs
@crates/insights/src/lib.rs
@crates/llm/src/mock.rs

<interfaces>
Target public API (CONTEXT + RESEARCH overrides vs plan Task 3):
- pub async fn analyze_source_for_wiki(store: &Store, chat: &dyn ChatModel, source_id: &str) -> Result&lt;WikiAnalysis&gt;
- Private parse_wiki_analysis / extract_first_json_object (discretion: stay in wiki.rs)
- InsightsError::InvalidWikiJson(String) — not generic Parse (D-12)
- MockChatModel: keyword heuristic only — no MockChatModel::new(vec) (D-01)

Reuse Phase 08 types already in wiki.rs: WikiAnalysis, WikiEntity, WikiConcept (no #[serde(default)] on required fields — D-10).

Load pattern: mirror extract_tasks_from_source Indexed / EmptySource / truncate — omit all store writes including set_source_summary (D-07).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: InvalidWikiJson + Mock 「笔记编译」 branch (09-01-01)</name>
  <files>crates/insights/src/error.rs, crates/llm/src/mock.rs</files>
  <read_first>
crates/insights/src/error.rs,
crates/llm/src/mock.rs,
.planning/phases/09-llm-wiki-analysis/09-CONTEXT.md (D-01, D-02, D-03, D-12),
.planning/phases/09-llm-wiki-analysis/09-PATTERNS.md (Mock branch order + InvalidTasksJson analog),
.planning/phases/09-llm-wiki-analysis/09-VALIDATION.md (09-01-01)
  </read_first>
  <behavior>
    - InsightsError::InvalidWikiJson(String) exists with thiserror message containing wiki json (symmetric with InvalidTasksJson) (D-12)
    - mock_wiki_compile_json: system content containing 「笔记编译」 → reply contains summary and entities keys and deserializes as valid WikiAnalysis shape with non-empty summary, one entity, empty concepts array (D-01, D-02, D-03)
    - Branch is checked before 任务提取 / 摘要 / 记忆提取 so wiki prompts do not fall through to prose or JSON-array replies
  </behavior>
  <action>
    RED first per D-12 and D-01…D-03: add `#[tokio::test] async fn mock_wiki_compile_json` in `crates/llm/src/mock.rs` asserting system containing 「笔记编译」 yields JSON with non-empty summary, one entity object, and concepts empty array. Confirm test fails before the branch exists.

    GREEN: add `InsightsError::InvalidWikiJson(String)` to `crates/insights/src/error.rs` with `#[error("failed to parse wiki json: {0}")]` (D-12). In `build_answer`, insert `system.contains("笔记编译")` **before** 任务提取 / 摘要 / 记忆提取 branches (D-01, D-02). On match return minimal valid wiki JSON per D-03 (discretionary literals; RESEARCH sketch is fine). Do **not** add queue-based `MockChatModel::new(vec![…])` (D-01). Production Mock stays happy-path only — no garbage replies (D-04 deferred to Plan 09-02 test doubles).
  </action>
  <verify>
    <automated>cargo test -p llm mock_wiki_compile_json -- --exact --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "InvalidWikiJson" crates/insights/src/error.rs` matches
    - `rg -n "笔记编译" crates/llm/src/mock.rs` matches inside build_answer before 任务提取
    - `cargo test -p llm mock_wiki_compile_json -- --exact --test-threads=1` exits 0
    - No new Mock constructor taking a Vec of replies appears in crates/llm/src/mock.rs (D-01)
  </acceptance_criteria>
  <done>InvalidWikiJson variant + Mock 「笔记编译」 happy-path JSON locked by unit test</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: analyze_source_for_wiki happy path (09-01-02)</name>
  <files>crates/insights/src/wiki.rs, crates/insights/src/lib.rs</files>
  <read_first>
crates/insights/src/tasks.rs (load→LLM→parse; sample_indexed),
crates/insights/src/summarize.rs (message shape; do NOT copy set_source_summary),
crates/insights/src/wiki.rs (WikiAnalysis types),
crates/insights/src/lib.rs,
.planning/phases/09-llm-wiki-analysis/09-CONTEXT.md (D-05, D-06, D-07, D-08, D-09, D-10),
.planning/phases/09-llm-wiki-analysis/09-RESEARCH.md (Pattern 1–2),
.planning/phases/09-llm-wiki-analysis/09-VALIDATION.md (analyze_parses_mock_json)
  </read_first>
  <behavior>
    - analyze_parses_mock_json: indexed sample source + MockChatModel → Ok(WikiAnalysis) with non-empty summary and entities.len() &gt;= 1 (ROADMAP criterion 1; D-03 via Mock)
    - analyze_does_not_touch_source_summary: after analyze, store get_source summary field remains None (or unchanged) (D-07)
    - System prompt is Chinese, includes 「笔记编译」, requires bare JSON only (no markdown-fence instruction as the allowed output format), English field names, sparse ≤5–8 guidance (D-05, D-06)
    - User message uses 标题 / 正文 with truncate_chars at 12_000 (D-08)
  </behavior>
  <action>
    RED per ROADMAP criterion 1 and D-05…D-08: add colocated `#[cfg(test)]` in `wiki.rs` with `sample_indexed` helper (mirror tasks.rs) plus `analyze_parses_mock_json` and `analyze_does_not_touch_source_summary`. Confirm `cargo test -p insights analyze_parses_mock_json -- --exact` fails.

    GREEN: implement `pub async fn analyze_source_for_wiki` mirroring tasks Indexed / EmptySource / truncate guards (D-08 MAX 12_000). Build Chinese system prompt containing 「笔记编译」, bare-JSON-only contract, English keys matching WikiAnalysis, sparse quality-first ≤5–8 guidance (D-05, D-06 — discretionary wording). User content: `标题：{title}` + `正文：` + excerpt (D-08). Call `chat.complete`; parse via private `parse_wiki_analysis` using fence strip then first `{…}` object then strict `serde_json::from_str::&lt;WikiAnalysis&gt;` (D-09, D-10) mapping failures to `InvalidWikiJson` (D-12). Do **not** call `set_source_summary`, `summarize_source`, or any task CRUD (D-07).

    Re-export `analyze_source_for_wiki` from `crates/insights/src/lib.rs`. Do not add write_wiki_pages_to_dir or tempfile yet (Plan 09-02 / D-13). Do not index WikiPage or depend on indexer/embedder.
  </action>
  <verify>
    <automated>cargo test -p insights analyze_parses_mock_json analyze_does_not_touch_source_summary -- --exact --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub async fn analyze_source_for_wiki" crates/insights/src/wiki.rs` matches
    - `rg -n "analyze_source_for_wiki" crates/insights/src/lib.rs` matches pub use
    - `rg -n "笔记编译" crates/insights/src/wiki.rs` matches system prompt string
    - `rg -n "set_source_summary" crates/insights/src/wiki.rs` returns no matches (D-07)
    - `cargo test -p insights analyze_parses_mock_json analyze_does_not_touch_source_summary -- --exact --test-threads=1` exits 0
    - `cargo test -p insights -p llm -- --test-threads=1` exits 0 (wave merge gate)
  </acceptance_criteria>
  <done>Mock-driven analyze_source_for_wiki returns populated WikiAnalysis without touching sources.summary</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Indexed source text → ChatModel messages | Untrusted corpus truncated to 12k chars; may contain prompt-injection |
| ChatModel reply text → WikiAnalysis | Untrusted LLM/Mock output crosses into structured domain types |
| Unit tests → network | Must never call live Ollama/cloud providers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | parse_wiki_analysis / analyze_source_for_wiki | high | mitigate | Strict serde into WikiAnalysis; missing/wrong types → InvalidWikiJson before any FS (D-09, D-10, D-12); no soft defaults on WikiAnalysis fields |
| T-09-02 | Information Disclosure | source body → chat.complete | medium | mitigate | Same truncate_chars(12_000) as summarize/tasks (D-08); no new cloud path; tests use MockChatModel only |
| T-09-03 | Tampering | Mock branch order | medium | mitigate | 「笔记编译」 checked before 摘要/任务提取/记忆提取 (D-02); mock_wiki_compile_json locks reply shape |
| T-09-04 | Denial of Service | hostile long source text | low | accept | Existing 12k char truncate bound (D-08) |
| T-09-SC | Tampering | cargo installs | low | accept | No new crates.io packages in this plan |
</threat_model>

<verification>
- `cargo test -p llm mock_wiki_compile_json -- --exact --test-threads=1` green
- `cargo test -p insights analyze_parses_mock_json analyze_does_not_touch_source_summary -- --exact --test-threads=1` green
- Wave merge: `cargo test -p insights -p llm -- --test-threads=1` green
- No E2E this phase (library-only; Phase 13)
</verification>

<success_criteria>
WIKI-03 analyze foundation: MockChatModel 「笔记编译」 → populated WikiAnalysis via analyze_source_for_wiki; InvalidWikiJson typed; sources.summary untouched (D-01…D-03, D-05…D-08, D-12; ROADMAP criterion 1).
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Requirement |
|----------|------|-------------|
| `InsightsError::InvalidWikiJson` | `crates/insights/src/error.rs` | WIKI-03 / D-12 |
| Mock 「笔记编译」 branch + `mock_wiki_compile_json` | `crates/llm/src/mock.rs` | WIKI-03 / D-01…D-03 |
| `analyze_source_for_wiki` | `crates/insights/src/wiki.rs` | WIKI-03 / D-05…D-08 |
| `parse_wiki_analysis` / `extract_first_json_object` (private) | `crates/insights/src/wiki.rs` | WIKI-03 / D-09–D-10 |
| Re-export `analyze_source_for_wiki` | `crates/insights/src/lib.rs` | WIKI-03 |
| `analyze_parses_mock_json` + `analyze_does_not_touch_source_summary` | `crates/insights/src/wiki.rs` tests | WIKI-03; ROADMAP #1 |

<output>
Create `.planning/phases/09-llm-wiki-analysis/09-01-SUMMARY.md` when done
</output>
