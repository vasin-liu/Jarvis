---
phase: 18-mcp-tools-search-list-sources
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - crates/agent/src/tools.rs
autonomous: true
requirements:
  - MCP-01
  - MCP-02
must_haves:
  truths:
    - "Agent search_knowledge calls search_kb (not a forked retrieve with different config) (D-01, D-02, D-03, MCP-01)"
    - "Agent list_sources calls list_indexed_sources; tool names remain search_knowledge / list_sources (D-02, D-06, MCP-02)"
    - "Agent formats Chinese text + Citation from structured hits; when truncated, honest footer with total_indexed (D-11 presentation edge)"
  artifacts:
    - path: crates/agent/src/tools.rs
      provides: "search_knowledge / list_sources arms use kb_readonly helpers"
  key_links:
    - from: crates/agent/src/tools.rs
      to: crates/retriever/src/kb_readonly.rs
      via: "search_kb / list_indexed_sources (D-01)"
      pattern: "search_kb|list_indexed_sources"
    - from: crates/agent/src/tools.rs
      to: rag::Citation
      via: "Build Citation from KbHit; optional get_source for source_uri only — never feed uri into MCP path"
      pattern: "Citation"
---

<objective>
Refactor agent `search_knowledge` / `list_sources` to consume `kb_readonly` so in-app tools share hybrid search and Indexed inventory with MCP (anti-drift).

Purpose: ROADMAP SC#3 — agent and MCP share one helper; D-02 keeps agent tool names at the edge.
Output: Surgical tools.rs change; green `cargo test -p agent`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-mcp-tools-search-list-sources/18-CONTEXT.md
@.planning/phases/18-mcp-tools-search-list-sources/18-RESEARCH.md
@.planning/phases/18-mcp-tools-search-list-sources/18-PATTERNS.md
@.planning/phases/18-mcp-tools-search-list-sources/18-01-SUMMARY.md
@crates/agent/src/tools.rs
@crates/retriever/src/kb_readonly.rs
@crates/rag/src/types.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Agent search_knowledge / list_sources thin wrappers over kb_readonly</name>
  <read_first>
    - .planning/phases/18-mcp-tools-search-list-sources/18-CONTEXT.md (D-01, D-02, D-03, D-05, D-06, D-11)
    - .planning/phases/18-mcp-tools-search-list-sources/18-PATTERNS.md (agent tools section; Citation URI note)
    - crates/agent/src/tools.rs (search_knowledge / list_sources arms)
    - crates/retriever/src/kb_readonly.rs (API from 18-01)
    - crates/rag/src/types.rs (Citation fields)
  </read_first>
  <files>crates/agent/src/tools.rs</files>
  <behavior>
    - search_knowledge empty hits still returns Chinese empty message + empty citations
    - search_knowledge builds Citation from KbHit fields (chunk_id, source_id, title→source_title, loc, excerpt); LLM lines use shared excerpt (not unbounded full chunk text)
    - list_sources lines remain "- {title} ({kind})"; when KbSourceList.truncated, append Chinese truncation footer including total_indexed
    - Tool match arms still named search_knowledge and list_sources (D-02) — do not rename for MCP
  </behavior>
  <action>
    Per D-01/D-02/D-03: in execute_tool arms only, replace direct retrieve(..., retriever) and inline IndexStatus filter with:

    - search_knowledge: parse query; call search_kb(store, embedder, query, None).await?; map errors via existing AgentError path; empty → keep existing Chinese empty string + vec![]; format lines from hit.excerpt; build Citation — if Citation.source_uri is required by consumers, call store.get_source once for uri only (do not put uri into shared KbHit / MCP payloads). Intentional Phase 18 alignment: helper uses RetrieverConfig::default inside search_kb (ignore passed RetrieverConfig for this arm if it would diverge — D-03 locks default).

    - list_sources: list_indexed_sources(store)?; format title/kind lines; if truncated append footer （已截断，共 {total_indexed} 条） per RESEARCH open-question recommendation.

    Update imports: use retriever::{search_kb, list_indexed_sources, ...}; drop unused retrieve/IndexStatus imports if no longer needed elsewhere in the file.

    Do not touch plugin/mutate/memory tool arms. Do not add mcp dependency. Do not wrap execute_tool for MCP.

    Add or extend a focused #[tokio::test] (or assert via existing agent tests) that search_knowledge / list_sources succeed against tempfile + MockEmbedder fixture after refactor — enough to prove arms call helpers without live LLM (D-12). Prefer thin smoke over duplicating all kb_readonly cases.
  </action>
  <verify>
    <automated>cargo test -p agent -- --test-threads=4</automated>
  </verify>
  <done>Agent tools share kb_readonly; names unchanged; cargo test -p agent green (D-01, D-02, MCP-01, MCP-02).</done>
</task>

<task type="auto">
  <name>Task 2: Confirm no forked retrieve in agent knowledge arms</name>
  <read_first>
    - crates/agent/src/tools.rs
  </read_first>
  <files>crates/agent/src/tools.rs</files>
  <action>
    Grep-verify search_knowledge / list_sources arms call search_kb and list_indexed_sources. Ensure those arms do not call retrieve( directly with a custom config. Leave other agent code paths alone. Re-run cargo test -p agent.
  </action>
  <verify>
    <automated>cargo test -p agent -- --test-threads=4</automated>
  </verify>
  <done>Anti-drift: agent knowledge listing/search only via kb_readonly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool args → agent execute_tool | Query string from model |
| Agent presenter → Citation UI | URI may still appear in Citation for in-app RAG only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-05 | Information Disclosure | Citation.source_uri | medium | accept | In-app Citation URI is existing RAG behavior; must not flow into MCP JSON (enforced in 18-03) |
| T-18-06 | Tampering | execute_tool surface | high | mitigate | Do not expose mutate tools via MCP; only refactor read arms; no execute_tool wrap from mcp |
| T-18-07 | Denial of Service | list_sources agent output | medium | mitigate | Inherit LIST_SOURCES_MAX=200 + truncation footer (D-07) |
| T-18-SC | Tampering | cargo installs | high | accept | No new packages |
</threat_model>

<verification>
cargo test -p agent green; knowledge arms use shared helpers.
</verification>

<success_criteria>
- search_knowledge / list_sources call kb_readonly
- Agent presentation remains Chinese + Citation
- Truncation honest when list capped
</success_criteria>

<output>
Create `.planning/phases/18-mcp-tools-search-list-sources/18-02-SUMMARY.md` when done
</output>
