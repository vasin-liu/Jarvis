---
phase: 23
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/release/v1.12.0-smoke.md
autonomous: true
requirements: [SHIP-01, TRUST-01, TRUST-02]
user_setup: []

must_haves:
  truths:
    - "docs/release/v1.12.0-smoke.md exists with Windows cold-start checks and empty Result"
    - "cargo test -p embedder green"
    - "cargo test -p tauri-app readiness_ green"
    - "SettingsView Vitest green"
    - "package.json and tauri.conf.json still 1.8.0 (no premature bump)"
  artifacts:
    - path: "docs/release/v1.12.0-smoke.md"
      provides: "Release smoke checklist template"
  key_links:
    - from: "docs/release/v1.12.0-smoke.md"
      to: "SHIP-01"
      via: "pre-bump authority for FastEmbed cold-start"
---

<objective>
Create the v1.12.0 Windows release smoke checklist and prove Mock-path automation is green without bumping versions.

Purpose: Satisfy SHIP-01 checklist existence + TRUST-01/02 automated evidence before any version bump.
Output: Smoke template committed; trust suite PASS; versions still 1.8.0.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-release-gate-1-12-0-package/23-CONTEXT.md
@.planning/phases/23-release-gate-1-12-0-package/23-RESEARCH.md
@docs/superpowers/plans/2026-07-29-release-hardening.md
@package.json
@src-tauri/tauri.conf.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create smoke checklist template</name>
  <files>docs/release/v1.12.0-smoke.md</files>
  <action>
Create `docs/release/v1.12.0-smoke.md` (create `docs/release/` if needed) with content matching the design/plan Task 4 template:

- Title: v1.12.0 Release Smoke (Windows)
- Fields: Build command, Machine, Date (blank)
- Checks (unchecked):
  - Cold start: main window paints and accepts clicks within ~5s while FastEmbed may still load
  - Settings → embedder readiness shows pending then ready (FastEmbed default)
  - Optional broken cache: failed + message; app does not freeze
  - Library + Chat smoke (Mock chat OK)
  - Installer/portable artifact launches after install/extract
- Artifacts section: installer path blank; note expected post-bump version 1.12.0
- Result: unchecked PASS and FAIL options; note FAIL → do not bump

Do **not** mark any checkboxes PASS. Do **not** edit package.json or tauri.conf.json.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Test-Path 'docs/release/v1.12.0-smoke.md'"</automated>
  </verify>
  <done>Smoke checklist file exists with empty Result.</done>
</task>

<task type="auto">
  <name>Task 2: Run pre-bump automated trust suite</name>
  <files>docs/release/v1.12.0-smoke.md</files>
  <action>
Run (sequentially; all must PASS):

1. `cargo test -p embedder -- --test-threads=4`
2. `cargo test -p tauri-app readiness_ -- --test-threads=4`
3. `npx vitest run src/views/SettingsView.test.tsx`

Optionally append a short "Automated pre-bump" section to the smoke doc listing command results and date — do **not** mark interactive Result PASS.

Verify versions still `"1.8.0"` in `package.json` and `src-tauri/tauri.conf.json`.

Commit: `docs: add v1.12.0 Windows release smoke checklist`
  </action>
  <verify>
    <automated>cargo test -p embedder -- --test-threads=4</automated>
  </verify>
  <done>Trust suite green; smoke template committed; versions still 1.8.0.</done>
</task>

</tasks>

<verification>
Smoke file exists; embedder + readiness_ + Settings Vitest green; no version bump.
</verification>

<success_criteria>
- SHIP-01 checklist file created (execution of interactive checks is Plan 02)
- TRUST-01 automated Mock-path evidence recorded
- TRUST-02: no FastEmbed download introduced in verify commands
</success_criteria>

<output>
After completion, create `.planning/phases/23-release-gate-1-12-0-package/23-01-SUMMARY.md`
</output>
