---
phase: 16-related-docs-library-panel
plan: 03
type: tdd
wave: 2
depends_on:
  - 16-01
files_modified:
  - src/views/LibraryView.tsx
  - src/views/LibraryView.test.tsx
autonomous: true
requirements:
  - REL-01
  - REL-03
  - REL-04
must_haves:
  truths:
    - "Selecting an indexed Library source shows related-docs panel with loading, empty, and error states (REL-01; D-10/D-11/D-12 copy)"
    - "Panel is fully hidden when no source selected — no placeholder chrome (D-02)"
    - "Related rows show title, sourceKindLabel(kind), and snippet — never similarity/RRF scores (REL-04, D-09)"
    - "Clicking a related row selects that neighbor, scrolls source-row into view, and refetches related (REL-03, D-13, D-14)"
    - "Row blank click toggles selection; action buttons do not toggle selection (D-05, D-08); selected row uses cyan highlight (D-07)"
    - "Non-indexed sources remain selectable; panel shows D-11 copy and skips IPC (D-06)"
    - "Rapid selection uses last-wins generation counter — stale IPC ignored (D-15)"
    - "Under-list panel with independent overflow scroll and light motion honoring reduced motion (D-01, D-03, D-04)"
  artifacts:
    - path: src/views/LibraryView.tsx
      provides: "selection state, related panel, navigate/scroll/refetch, data-testids"
    - path: src/views/LibraryView.test.tsx
      provides: "Vitest coverage for REL-01/03/04 + D-02..D-15 behaviors"
  key_links:
    - from: LibraryView
      to: listRelatedSources
      via: "invoke when status===indexed; skip when non-indexed"
      pattern: "fetchSeq last-wins"
    - from: related row click
      to: source-row-{id}
      via: "setSelected + scrollIntoView + refetch"
      pattern: "D-13 related-chain"
---

<objective>
Deliver the Library related-docs panel UX: selection model, loading/empty/error/non-indexed states, title+kind+snippet rows, and navigate-on-click with last-wins refetch (REL-01, REL-03, REL-04; locked D-01..D-15).

Purpose: Users see honest neighbors and can browse related-chain without graph UI or scores.
Output: LibraryView selection + under-list related panel per 16-UI-SPEC; Vitest green for panel behaviors.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-related-docs-library-panel/16-CONTEXT.md
@.planning/phases/16-related-docs-library-panel/16-UI-SPEC.md
@.planning/phases/16-related-docs-library-panel/16-RESEARCH.md
@.planning/phases/16-related-docs-library-panel/16-PATTERNS.md
@.planning/phases/16-related-docs-library-panel/16-01-SUMMARY.md
@src/views/LibraryView.tsx
@src/views/LibraryView.test.tsx
@src/lib/sourceDisplay.ts
@src/lib/tauri.ts
@src/index.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Vitest related-docs selection and panel states</name>
  <read_first>
    - src/views/LibraryView.test.tsx (existing patterns + motion mock)
    - src/hooks/useLibrary.test.ts (mock ../lib/tauri pattern)
    - .planning/phases/16-related-docs-library-panel/16-UI-SPEC.md (copy + data-testid contract)
    - .planning/phases/16-related-docs-library-panel/16-CONTEXT.md (D-02, D-05..D-15, D-10..D-12)
    - .planning/phases/16-related-docs-library-panel/16-VALIDATION.md
  </read_first>
  <files>src/views/LibraryView.test.tsx</files>
  <behavior>
    - Panel absent when no selection (query related-docs-panel null) — D-02
    - Click source-row blank on indexed → related-docs-panel mounts; eventually list or empty; loading shows 加载相关文档… (D-12)
    - Mock listRelatedSources resolving [] → related-docs-empty text 暂无相关文档 (D-10)
    - Select pending/failed source → related-docs-empty text 仅已索引来源可查看相关文档; listRelatedSources not called (D-06, D-11)
    - Mock reject → related-docs-error contains 相关文档加载失败 (D-12)
    - Ready row shows title, kind label via sourceKindLabel, snippet; no score/affinity/% text (D-09, REL-04)
    - Second click same source-row clears selection and unmounts panel (D-08)
    - Click summarize-* (or other action testid) does not mount panel / does not select (D-05)
    - Click related-docs-row-{id} sets that source-row aria-selected/data-selected true and calls listRelatedSources again with neighbor id (D-13)
    - Optional: scrollIntoView called with behavior auto when useReducedMotion true (D-14)
  </behavior>
  <action>
    Extend LibraryView.test.tsx with describe("LibraryView related docs"). Mock motion.div if needed (existing mock only stubs section — extend motion mock to pass through div). Mock ../lib/tauri listRelatedSources with vi.fn(). Add fixtures for indexed + pending sources. Write assertions for behaviors above. Tests MUST fail RED until Task 2 implements UI — do not implement LibraryView in this task.
  </action>
  <verify>
    <automated>npx vitest run src/views/LibraryView.test.tsx 2>&1 | findstr /I /C:"FAIL" /C:"fail" /C:"Test Files" /C:"Tests"</automated>
  </verify>
  <acceptance_criteria>
    - New describe("LibraryView related docs") exists with cases covering D-02, D-05, D-06, D-08, D-09, D-10, D-11, D-12, D-13
    - At least one new test fails RED due to missing panel/selection behavior (not import crash only)
    - Existing LibraryView tests still present
  </acceptance_criteria>
  <done>RED Vitest suite defines related-docs contract before UI implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — LibraryView selection, related panel, navigate</name>
  <read_first>
    - src/views/LibraryView.tsx
    - src/views/LibraryView.test.tsx (Task 1 RED suite)
    - src/lib/sourceDisplay.ts (sourceKindLabel)
    - src/lib/tauri.ts (listRelatedSources)
    - src/index.css (.nav-btn-active cyan tokens)
    - .planning/phases/16-related-docs-library-panel/16-UI-SPEC.md (layout, spacing, colors, motion, testids)
    - .planning/phases/16-related-docs-library-panel/16-CONTEXT.md (D-01..D-15)
  </read_first>
  <files>src/views/LibraryView.tsx, src/views/LibraryView.test.tsx</files>
  <behavior>
    - Same behaviors as Task 1 — all must pass after this task
  </behavior>
  <action>
    Implement in LibraryView (keep selection + related fetch state in the view or a tiny colocated hook — do not bloat App.tsx/useLibrary unless required):

    Selection (D-05, D-07, D-08): selectedSourceId string|null; click row blank toggles; action button column uses stopPropagation; selected row gets data-testid source-row-{id}, aria-selected, data-selected, cyan highlight classes matching nav-btn-active (bg-cyan-500/15 ring-1 ring-cyan-400/30).

    Panel (D-01 under-list below source-list; D-02 mount only when selected; D-03 max-h-48 overflow-auto; D-04 motion.div fade/translate with useReducedMotion): data-testid related-docs-panel, role=region, aria-label 相关文档, heading 相关文档. States/testids/copy per UI-SPEC: related-docs-loading (加载相关文档…), related-docs-empty (D-10 or D-11 text), related-docs-error (相关文档加载失败 + optional detail), related-docs-list + related-docs-row-{sourceId}.

    Fetch (D-06, D-15): if status !== indexed → nonindexed empty copy, skip listRelatedSources; else increment fetchSeq, set loading, invoke listRelatedSources; apply result only if seq matches (last-wins). Map reject to error state.

    Rows (D-09, REL-04): render title, sourceKindLabel(kind), snippet as React text nodes only — never dangerouslySetInnerHTML; never show scores.

    Navigate (D-13, D-14): related row click → select neighbor → querySelector source-row-{id} scrollIntoView({ behavior: reduceMotion ? "auto" : "smooth", block: "nearest" }) → refetch.

    Follow 16-UI-SPEC typography/color/spacing. No graph UI. No new npm packages. Minimal surgical diff.
  </action>
  <verify>
    <automated>npx vitest run src/views/LibraryView.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All LibraryView related docs Vitest cases pass
    - Full LibraryView.test.tsx suite exits 0
    - Panel hidden when no selection; D-10/D-11/D-12 strings exact
    - data-testid contract from UI-SPEC present (related-docs-panel, loading, empty, error, list, related-docs-row-*, source-row-*)
    - No score/affinity/% UI strings in related panel markup
  </acceptance_criteria>
  <done>REL-01/03/04 Library panel UX green under Vitest; ready for focused E2E (D-16 in Plan 16-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User click → selection/fetch | UI events; source ids from own Library list |
| IPC response → panel render | Untrusted-shaped JSON; treat as data, render as text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Tampering | unknown source_id via IPC error | medium | mitigate | Show related-docs-error with D-12 copy; do not crash view |
| T-16-03 | Tampering / XSS | snippet / title render | medium | mitigate | React text children only; never dangerouslySetInnerHTML |
| T-16-07 | Spoofing | score UI leakage | low | mitigate | Render only title/kind/snippet fields (D-09); Vitest asserts no score text |
| T-16-SC | Tampering | npm installs | high | mitigate | No new packages |

</threat_model>

<verification>
- npx vitest run src/views/LibraryView.test.tsx — all pass
- Manual spot-check not required (automated covers states); D-16 E2E in Plan 16-04
</verification>

<success_criteria>
- REL-01 loading/empty/error/hidden behaviors true in Vitest
- REL-04 row content without scores
- REL-03 navigate select+scroll+refetch covered in unit tests
- Locked decisions D-01..D-15 implemented in LibraryView
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | Location | Phase |
|--------|------|----------|-------|
| `selectedSourceId` state | React state | src/views/LibraryView.tsx | 16-03 |
| `fetchSeq` last-wins guard | useRef counter | src/views/LibraryView.tsx | 16-03 |
| `related-docs-panel` | UI region + testid | src/views/LibraryView.tsx | 16-03 |
| `related-docs-loading` / `empty` / `error` / `list` | status nodes | src/views/LibraryView.tsx | 16-03 |
| `related-docs-row-{sourceId}` | clickable related row | src/views/LibraryView.tsx | 16-03 |
| `source-row-{id}` | selectable list row | src/views/LibraryView.tsx | 16-03 |
| `LibraryView related docs` describe | Vitest suite | src/views/LibraryView.test.tsx | 16-03 |

<output>
Create `.planning/phases/16-related-docs-library-panel/16-03-SUMMARY.md` when done
</output>
