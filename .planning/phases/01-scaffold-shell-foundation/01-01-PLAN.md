---
phase: 01-scaffold-shell-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/lib.rs
  - src-tauri/src/state.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/commands/config.rs
  - src-tauri/src/commands/library.rs
  - src-tauri/src/commands/sync.rs
autonomous: true
requirements:
  - SHELL-01
must_haves:
  truths:
    - "AppState struct and impl methods live in state.rs; lib.rs re-exports crate::AppState for e2e.rs and sync_scheduler.rs"
    - "Eight IPC commands (get_config, set_config, get_index_status, source_count, list_sources, remove_source, get_sync_status, run_scheduled_sync_cmd) compile from commands/* modules with unchanged signatures"
    - "generate_handler! lists the same 52 command identifiers as pre-refactor; no IPC renames"
    - "index_ops.rs is not modified"
  artifacts:
    - path: src-tauri/src/state.rs
      provides: AppState struct + impl (config, embedder, chat, reload_providers, save_config, restart_watcher, restart_scheduler, initial_scan_with_progress)
      contains: "pub(crate) struct AppState"
    - path: src-tauri/src/commands/mod.rs
      provides: Command module re-exports
      contains: "pub use config::"
    - path: src-tauri/src/commands/config.rs
      provides: get_config, set_config, get_index_status
      contains: "#[tauri::command]"
    - path: src-tauri/src/commands/library.rs
      provides: source_count, list_sources, remove_source
      contains: "#[tauri::command]"
    - path: src-tauri/src/commands/sync.rs
      provides: get_sync_status, run_scheduled_sync_cmd
      contains: "#[tauri::command]"
  key_links:
    - from: src-tauri/src/lib.rs
      to: src-tauri/src/commands/mod.rs
      via: "mod commands; use commands::{...}"
      pattern: "mod commands"
    - from: src-tauri/src/commands/config.rs
      to: src-tauri/src/state.rs
      via: "State<AppState> parameter"
      pattern: "State<'_, AppState>"
    - from: src-tauri/src/lib.rs
      to: src-tauri/src/state.rs
      via: "pub(crate) use state::AppState"
      pattern: "pub\\(crate\\) use state::AppState"
---

<objective>
Extract Tauri shell foundation: move `AppState` to `state.rs` and split the first 8 IPC commands into `commands/config.rs`, `commands/library.rs`, and `commands/sync.rs` with zero behavior change.

Purpose: Establish SHELL-01 extraction points so later phases can move remaining ~44 commands without touching domain crates.
Output: `state.rs`, `commands/mod.rs`, `commands/{config,library,sync}.rs`; slimmed `lib.rs` retaining `init_state`, `run()`, agent/Lark glue, and `generate_handler!`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scaffold-shell-foundation/01-CONTEXT.md
@.planning/phases/01-scaffold-shell-foundation/01-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src-tauri/src/lib.rs
@src-tauri/src/index_ops.rs
@src-tauri/src/sync_scheduler.rs
@src-tauri/src/e2e.rs
</context>

<threat_model>
scope: structural_refactor_only
trust_boundaries:
  - "Tauri IPC boundary (unchanged invoke names)"
  - "App data paths (config.json, kb.sqlite) via AppState::save_config"
stride_register:
  - threat_id: T-01
    category: Information Disclosure
    component: commands/config.rs
    disposition: monitor
    mitigation_plan: "Do not add debug logging of AppConfig or cloud_api_key in moved command code; preserve save_config write path (D-15)"
  - threat_id: T-02
    category: Tampering
    component: set_config command
    disposition: monitor
    mitigation_plan: "set_config must still call state.save_config, restart_watcher, restart_scheduler in same order as lib.rs L223-228"
disposition_summary: "No new network, shell, or auth surfaces. Structural move only per RESEARCH security section."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create state.rs and re-export AppState from lib.rs</name>
  <files>src-tauri/src/state.rs, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/lib.rs, src-tauri/src/sync_scheduler.rs, src-tauri/src/e2e.rs, .planning/phases/01-scaffold-shell-foundation/01-CONTEXT.md</read_first>
  <action>
    Per D-05/D-06: Create `src-tauri/src/state.rs` and move from `lib.rs`:
    - `AppState` struct (current L54-68) with identical fields and types
    - Full `impl AppState` block (current L77-183): `config`, `embedder`, `chat`, `reload_providers`, `save_config`, `restart_watcher`, `restart_scheduler`, `initial_scan_with_progress`
    - Mark struct and impl methods `pub(crate)` so `sync_scheduler.rs` and commands can access fields
    - Move only the imports needed by state.rs (config, embedder, llm, watcher, sync_scheduler spawn, index_ops emit helpers via `crate::emit_index_progress` / `crate::emit_index_complete`, e2e `is_e2e_mode`, etc.)
    In `lib.rs`: add `mod state;` and `pub(crate) use state::AppState;` (D-05) so existing `use crate::AppState` in `e2e.rs` and `use crate::{..., AppState}` in `sync_scheduler.rs` compile unchanged.
    Keep in `lib.rs` per D-06/D-07: `lark_opts`, `init_state`, `run()`, `emit_index_progress`, `emit_index_complete`, all non-extracted commands, agent/Lark/seed glue.
    Do NOT touch `index_ops.rs` (D-16).
  </action>
  <verify>cargo test --workspace</verify>
  <acceptance_criteria>
    - `src-tauri/src/state.rs` exists and contains `pub(crate) struct AppState`
    - `src-tauri/src/state.rs` contains `fn save_config`, `fn restart_watcher`, `fn restart_scheduler`, `fn initial_scan_with_progress`
    - `src-tauri/src/lib.rs` contains `pub(crate) use state::AppState` and does NOT contain `struct AppState {`
    - D-16: `git diff --name-only` does NOT include `src-tauri/src/index_ops.rs`; `git diff src-tauri/src/index_ops.rs` is empty
    - `cargo test --workspace` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extract commands/config.rs and commands/library.rs</name>
  <files>src-tauri/src/commands/mod.rs, src-tauri/src/commands/config.rs, src-tauri/src/commands/library.rs, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/lib.rs, src-tauri/src/state.rs, .planning/phases/01-scaffold-shell-foundation/01-RESEARCH.md</read_first>
  <action>
    Per D-01/D-03/D-04: Create `src-tauri/src/commands/` directory.
    `commands/config.rs` — move verbatim bodies from lib.rs:
    - `get_config` (L208-211): `State&lt;AppState&gt;` → `Result&lt;AppConfig, String&gt;`
    - `get_index_status` (L213-221): delegates to `index_ops::index_status_view`
    - `set_config` (L223-229): calls `save_config`, `restart_watcher`, `restart_scheduler` in same order
  `commands/library.rs` — move verbatim bodies from lib.rs:
    - `source_count` (L185-192)
    - `list_sources` (L194-197)
    - `remove_source` (L199-206)
    Each command keeps `#[tauri::command]` and identical fn names (IPC contract).
    `commands/mod.rs`: `mod config; mod library;` plus `pub use config::{get_config, set_config, get_index_status};` and `pub use library::{source_count, list_sources, remove_source};`
    In `lib.rs`: add `mod commands;`, import the six fns from `commands::`, remove the six moved fn definitions. Keep `generate_handler!` listing all 52 names unchanged (D-03, D-19).
  </action>
  <verify>cargo test --workspace</verify>
  <acceptance_criteria>
    - `src-tauri/src/commands/config.rs` contains exactly 3 `#[tauri::command]` functions: `get_config`, `set_config`, `get_index_status`
    - `src-tauri/src/commands/library.rs` contains exactly 3 `#[tauri::command]` functions: `source_count`, `list_sources`, `remove_source`
    - `src-tauri/src/lib.rs` `generate_handler!` still lists `get_config`, `set_config`, `get_index_status`, `source_count`, `list_sources`, `remove_source`
    - `cargo test --workspace` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Extract commands/sync.rs and verify IPC registry</name>
  <files>src-tauri/src/commands/sync.rs, src-tauri/src/commands/mod.rs, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/lib.rs, src-tauri/src/sync_scheduler.rs, src-tauri/src/state.rs</read_first>
  <action>
    Per D-02 (include if low risk): Create `commands/sync.rs` moving verbatim from lib.rs:
    - `get_sync_status` (L1007-1010): `Ok(sync_status_view(&state))`
    - `run_scheduled_sync_cmd` (L1012-1018): `run_scheduled_sync(&app, &state).await`
    Add to `commands/mod.rs`: `mod sync;` and `pub use sync::{get_sync_status, run_scheduled_sync_cmd};`
    Update `lib.rs` imports and remove moved fn bodies. If a mod-cycle compile error appears, skip sync.rs per D-02 and document in SUMMARY — but attempt inclusion first (only 11 lines, no command-to-command deps).
    Final check: `generate_handler!` (L1260-1313) must list all 52 command identifiers with same snake_case names. Grep `#[tauri::command]` across `lib.rs` + `commands/*.rs` — expect 52 total.
  </action>
  <verify>cargo test --workspace</verify>
  <acceptance_criteria>
    - Either `src-tauri/src/commands/sync.rs` exists with `get_sync_status` and `run_scheduled_sync_cmd`, OR SUMMARY documents skip with compile-error reason (D-02 fallback)
    - `generate_handler!` contains exactly 52 command identifiers (count comma-separated entries)
    - `cargo test --workspace` exits 0
    - `src-tauri/src/index_ops.rs` unchanged (D-16)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cargo test --workspace` passes
- [ ] `cargo build` in src-tauri succeeds
- [ ] 52 IPC commands registered in `generate_handler!`
- [ ] `index_ops.rs` has no diff
</verification>

<success_criteria>
- AppState in state.rs; 8 commands in commands/*; lib.rs retains init_state + run + remaining commands
- Zero IPC signature or name changes (D-19)
- All Rust workspace tests green (QA-03 partial — full gate in 01-03)
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Location |
|---------------|------|----------|
| `AppState` | struct | `src-tauri/src/state.rs` |
| `AppState::config` | method | `src-tauri/src/state.rs` |
| `AppState::embedder` | method | `src-tauri/src/state.rs` |
| `AppState::chat` | method | `src-tauri/src/state.rs` |
| `AppState::reload_providers` | method | `src-tauri/src/state.rs` |
| `AppState::save_config` | method | `src-tauri/src/state.rs` |
| `AppState::restart_watcher` | method | `src-tauri/src/state.rs` |
| `AppState::restart_scheduler` | method | `src-tauri/src/state.rs` |
| `AppState::initial_scan_with_progress` | method | `src-tauri/src/state.rs` |
| `get_config` | `#[tauri::command]` | `src-tauri/src/commands/config.rs` |
| `set_config` | `#[tauri::command]` | `src-tauri/src/commands/config.rs` |
| `get_index_status` | `#[tauri::command]` | `src-tauri/src/commands/config.rs` |
| `source_count` | `#[tauri::command]` | `src-tauri/src/commands/library.rs` |
| `list_sources` | `#[tauri::command]` | `src-tauri/src/commands/library.rs` |
| `remove_source` | `#[tauri::command]` | `src-tauri/src/commands/library.rs` |
| `get_sync_status` | `#[tauri::command]` | `src-tauri/src/commands/sync.rs` (optional) |
| `run_scheduled_sync_cmd` | `#[tauri::command]` | `src-tauri/src/commands/sync.rs` (optional) |
| `commands` | module | `src-tauri/src/commands/mod.rs` |

<output>
After completion, create `.planning/phases/01-scaffold-shell-foundation/01-01-SUMMARY.md`
</output>
