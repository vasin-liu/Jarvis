---
phase: 05-agent-protocol
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - crates/llm/src/mock.rs
  - crates/llm/src/lib.rs
  - crates/agent/src/tools.rs
  - crates/agent/src/orchestrate.rs
  - crates/rag/src/types.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - AGT-01
  - AGT-02
  - AGT-03
must_haves:
  truths:
    - "MockChatModel agent tool round emits bare JSON line without XML wrapper when system contains 可用工具 (D-18, D-19)"
    - "build_tools_prompt documents JSON line only — no XML examples in primary instructions (D-05)"
    - "AskResponse.tool_parse_warnings with serde default wired through agent_response_to_ask for ask_agent_in_session and ask_agent_in_session_stream (D-14)"
    - "run_orchestrated merges all steps tool_parse_warnings prefixed with [agent_name]: on final AgentResponse only (D-20, D-21, D-22)"
    - "cargo test -p agent -p llm passes; agent_uses_search_tool exercises JSON primary path (AGT-03)"
  artifacts:
    - path: crates/llm/src/mock.rs
      provides: JSON line Mock agent tool response
      contains: search_knowledge
    - path: crates/rag/src/types.rs
      provides: AskResponse.tool_parse_warnings IPC field
      contains: tool_parse_warnings
    - path: crates/agent/src/orchestrate.rs
      provides: merged orchestration warnings
      contains: tool_parse_warnings
  key_links:
    - from: src-tauri/src/lib.rs
      to: crates/rag/src/types.rs
      via: agent_response_to_ask maps tool_parse_warnings
      pattern: "tool_parse_warnings"
    - from: crates/llm/src/mock.rs
      to: crates/agent/src/tool_parse.rs
      via: JsonToolCallParser primary path in agent loop
      pattern: "search_knowledge"
---

## Phase Goal

**As a** Jarvis user in agent mode, **I want to** Mock and prompt aligned to JSON line protocol with parse warnings in IPC metadata, **so that** E2E and frontend can surface failures without silent XML dependency.

<objective>
Switch Mock and build_tools_prompt to strict JSON line protocol; extend AskResponse and Tauri mapping; merge orchestration step warnings into final response metadata.

Purpose: Complete AGT-01 Mock/OpenAI-compatible text path (D-05, D-18), AGT-02 IPC metadata (D-14, D-20..D-22), AGT-03 backend prerequisite for E2E.
Output: JSON Mock, JSON-only prompt, AskResponse field, orchestrate warning merge, agent_response_to_ask wiring in lib.rs (moved in Plan 05-03).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-agent-protocol/05-CONTEXT.md
@.planning/phases/05-agent-protocol/05-RESEARCH.md
@crates/llm/src/mock.rs
@crates/agent/src/tools.rs
@crates/agent/src/orchestrate.rs
@crates/rag/src/types.rs
@src-tauri/src/lib.rs
</context>

<threat_model>
scope: protocol_migration
asvs_level: L1
trust_boundaries:
  - "MockChatModel output → JsonToolCallParser → agent loop"
stride_register:
  - threat_id: T-05-03
    category: Integrity
    component: Mock/E2E protocol drift
    disposition: mitigate
    mitigation_plan: "grep mock.rs for tool_call tag must be zero after edit; cargo test -p agent agent_uses_search_tool must pass without XML stderr in happy path."
  - threat_id: T-05-04
    category: Tampering
    component: orchestration warning merge
    disposition: mitigate
    mitigation_plan: "Integration test: two-step pipeline with forced Failed in second step accumulates prefixed warnings on final AgentResponse only."
disposition_summary: "Mock/prompt drift and lost orchestration warnings are primary risks."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Mock JSON emission + JSON-only build_tools_prompt (05-02-01)</name>
  <files>crates/llm/src/mock.rs, crates/agent/src/tools.rs</files>
  <read_first>crates/llm/src/mock.rs lines 43-53, crates/agent/src/tools.rs build_tools_prompt, 05-CONTEXT.md D-05 and D-18</read_first>
  <action>
    Update MockChatModel build_answer branch when system.contains("可用工具") and no prior tool result message (D-18):
    - Return single bare JSON line: {"name":"search_knowledge","arguments":{"query":"agent"}} with no XML tags and no surrounding prose.

    Rewrite build_tools_prompt (D-05):
    - Change header to instruct: reply with exactly one bare JSON line per tool call, format {"name":"...","arguments":{...}}, no markdown fences, no XML.
    - Replace all &lt;tool_call&gt; example lines with bare JSON line examples for each built-in tool (search_knowledge, list_sources, list_tasks, list_memories, add_memory, get_memory, forget_memory, update_memory, complete_task variants).
    - Plugin tool examples likewise use bare JSON lines only.
    - Remove XML examples entirely from primary instructions (XML handled only by deprecated parser fallback, not documented as valid output).

    Update or add llm crate test asserting agent Mock response contains {"name" and does not contain tool_call substring.

    Run cargo test -p agent agent_uses_search_tool to confirm JSON primary path (no XML fallback on happy path).
  </action>
  <verify>
    <automated>cargo test -p agent agent_uses_search_tool && cargo test -p llm</automated>
  </verify>
  <acceptance_criteria>
    - rg "tool_call" crates/llm/src/mock.rs — no match in agent tool return string
    - rg "tool_call" crates/agent/src/tools.rs build_tools_prompt section — no match in example lines (function body may reference tool_call only if removed entirely)
    - cargo test -p agent agent_uses_search_tool exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: AskResponse IPC + orchestration warning merge (05-02-02)</name>
  <files>crates/rag/src/types.rs, crates/agent/src/orchestrate.rs, src-tauri/src/lib.rs</files>
  <read_first>crates/rag/src/types.rs AskResponse, src-tauri/src/lib.rs agent_response_to_ask and ask_agent_in_session_stream, crates/agent/src/orchestrate.rs run_orchestrated loop, 05-CONTEXT.md D-14 and D-20..D-22</read_first>
  <action>
    Add to AskResponse in crates/rag/src/types.rs: #[serde(default)] pub tool_parse_warnings: Vec&lt;String&gt;.

    Update agent_response_to_ask in lib.rs to copy resp.tool_parse_warnings into AskResponse (D-14). Ensure both ask_agent_in_session and ask_agent_in_session_stream return full AskResponse including warnings after stream completes — no separate Tauri event this phase.

    In run_orchestrated (D-20, D-21, D-22):
    - Accumulate mut all_warnings: Vec&lt;String&gt; across pipeline steps.
    - After each run_agent call, for each warning in resp.tool_parse_warnings push format!("[{}]: {}", profile.name, w) using profile display name not id.
    - Include all_warnings on final merged AgentResponse; do NOT attach warnings to OrchestrationStep structs or step cards metadata.
    - Single-agent pipeline (ids.len()==1) passes through run_agent warnings unchanged (no double prefix).

    Add #[cfg(test)] in orchestrate.rs or agent tests: orchestrate_warnings_merge — two-profile pipeline where second agent returns Failed parse; assert final tool_parse_warnings contains [任务助手]: or configured profile.name prefix.

    Add rag types serde round-trip test if missing: deserialize JSON without tool_parse_warnings field defaults to empty vec.
  </action>
  <verify>
    <automated>cargo test -p agent orchestrate && cargo test -p rag</automated>
  </verify>
  <acceptance_criteria>
    - rg "tool_parse_warnings" crates/rag/src/types.rs matches
    - rg "tool_parse_warnings" src-tauri/src/lib.rs agent_response_to_ask matches
    - cargo test -p agent exits 0 including orchestrate warning test
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Mock emits JSON line only
- [ ] build_tools_prompt has JSON-only examples
- [ ] AskResponse and agent_response_to_ask carry warnings
- [ ] Orchestration merges prefixed warnings on final response
- [ ] cargo test -p agent -p llm green
</verification>

<success_criteria>
Structured JSON path end-to-end for Mock agent loop; IPC metadata ready for ChatView in Plan 05-04; orchestration warnings merged per D-20..D-22.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Requirement |
|----------|------|-------------|
| JSON Mock agent response | `crates/llm/src/mock.rs` | AGT-03 |
| JSON-only tools prompt | `crates/agent/src/tools.rs` | AGT-01 |
| IPC warnings field | `crates/rag/src/types.rs` | AGT-02 |
| Orchestration warning merge | `crates/agent/src/orchestrate.rs` | AGT-02 |
| Tauri response mapping | `src-tauri/src/lib.rs` | AGT-02 |

<output>
Create `.planning/phases/05-agent-protocol/05-02-SUMMARY.md` when done
</output>
