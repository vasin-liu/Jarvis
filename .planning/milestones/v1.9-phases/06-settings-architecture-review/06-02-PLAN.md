---
phase: 06-settings-architecture-review
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src-tauri/src/sync_scheduler.rs
  - src-tauri/src/commands/sync.rs
  - src/views/SettingsView.tsx
  - e2e/specs/settings.spec.ts
  - e2e/specs/full-ui.spec.ts
  - e2e/playwright/nav-smoke.spec.ts
  - package.json
  - e2e/playwright/nav-smoke.spec.ts
  - package.json
autonomous: true
requirements:
  - FE-05
  - QA-02

must_haves:
  truths:
    - "Scheduled sync errors persisted in store.meta as last_scheduled_sync_error (string) + last_scheduled_sync_error_at (unix timestamp) on failure (D-16)"
    - "Partial failures (failed > 0 even when indexed > 0) write error summary to meta (D-17)"
    - "Successful scheduled sync clears error meta keys (D-18)"
    - "Sync error displayed inline in sync accordion section, not global banner (D-19)"
    - "SyncStatusView extended with lastScheduledSyncError + lastScheduledSyncErrorAt with camelCase serde and Option/default (D-21)"
    - "settings.spec.ts extended with accordion section, sync error panel, and scheduled sync tests (D-41)"
    - "full-ui.spec.ts extended with Settings journey covering accordion navigation (D-41)"
    - "All existing E2E specs remain green"
    - "e2e/playwright/ supplemental nav smoke exists; WDIO remains authoritative CI gate (D-42, D-43)"
  artifacts:
    - path: src-tauri/src/sync_scheduler.rs
      provides: "Error meta persistence on scheduler failure + SyncStatusView error fields"
      contains: "last_scheduled_sync_error"
    - path: e2e/specs/settings.spec.ts
      provides: "Extended E2E tests for accordion and sync error"
      contains: "settings-section-sync"
    - path: e2e/specs/full-ui.spec.ts
      provides: "Settings journey in full UI spec"
      contains: "settings-section"
  key_links:
    - from: src-tauri/src/sync_scheduler.rs
      to: crates/store/src/store.rs
      via: "set_meta/get_meta for sync error persistence"
      pattern: "set_meta"
    - from: src/views/SettingsView.tsx
      to: src-tauri/src/commands/sync.rs
      via: "invoke get_sync_status reads error fields from SyncStatusView"
      pattern: "get_sync_status"
---

## Phase Goal

**As a** Jarvis power user, **I want to** have a cleanly organized Settings view with accordion sections and a slim App shell, **so that** configuration is easy to navigate and the codebase is maintainable.

<objective>
Wire sync error persistence in the Rust scheduler, extend SyncStatusView IPC payload, surface errors in the Settings sync accordion, and extend E2E coverage for the new accordion layout.

Purpose: Close the "scheduled sync errors swallowed" structural concern (QA-02) and verify the Settings extraction from Plan 06-01 with E2E tests.
Output: sync_scheduler.rs error meta writes, SyncStatusView error fields, Settings sync section error display, extended settings.spec.ts + full-ui.spec.ts.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-settings-architecture-review/06-CONTEXT.md
@.planning/phases/06-settings-architecture-review/06-RESEARCH.md
@.planning/phases/06-settings-architecture-review/06-01-SUMMARY.md
@.planning/ROADMAP.md
@src-tauri/src/sync_scheduler.rs
@src-tauri/src/commands/sync.rs
@src/views/SettingsView.tsx
@e2e/specs/settings.spec.ts
@e2e/specs/full-ui.spec.ts
@e2e/helpers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync scheduler error meta persistence + SyncStatusView extension (06-02-01)</name>
  <files>src-tauri/src/sync_scheduler.rs, src-tauri/src/commands/sync.rs</files>
  <read_first>src-tauri/src/sync_scheduler.rs (run_scheduled_sync function, SyncStatusView struct, sync_status_view function, line 68 error swallow), src-tauri/src/commands/sync.rs (get_sync_status, run_scheduled_sync_cmd), crates/store/src/store.rs (set_meta, get_meta signatures), 06-CONTEXT.md D-16 D-17 D-18 D-21, 06-RESEARCH.md Pattern 3 and Pattern 4</read_first>
  <action>
    In src-tauri/src/sync_scheduler.rs, modify the scheduler loop error handling per D-16/D-17/D-18:
    - Replace the existing `let _ = rt.block_on(run_scheduled_sync(&app, &state));` pattern. Capture the result. On Err(e): write error string to store.meta key last_scheduled_sync_error and current unix timestamp to last_scheduled_sync_error_at via state.store.set_meta.
    - On Ok with combined report: if failed > 0 (partial failure per D-17), write an error summary (format: indexed count and failed count) to both meta keys.
    - On Ok with failed == 0 (full success per D-18): clear both meta keys by writing empty string.
    - Use SystemTime::now().duration_since(UNIX_EPOCH) for the timestamp.

    Extend SyncStatusView struct per D-21: add two fields with Option type:
    - last_scheduled_sync_error: Option<String>
    - last_scheduled_sync_error_at: Option<i64>
    Both with #[serde(default)] so deserialization from old builds does not fail.

    Update sync_status_view function to read the two new meta keys (last_scheduled_sync_error, last_scheduled_sync_error_at) and populate the new SyncStatusView fields. Parse error_at as i64. If meta value is empty string or missing, use None.

    No changes to commands/sync.rs — get_sync_status already delegates to sync_status_view. Verify it still compiles and the IPC payload shape is backward-compatible (new fields are Option with serde default).
  </action>
  <verify>
    <automated>cargo test -p tauri-app</automated>
  </verify>
  <acceptance_criteria>
    - cargo build -p tauri-app exits 0
    - rg "last_scheduled_sync_error" src-tauri/src/sync_scheduler.rs matches at least 3 times (struct field + write + read)
    - SyncStatusView has 5 fields (3 existing + 2 new)
    - cargo test -p tauri-app exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Settings sync error UI + E2E extension (06-02-02)</name>
  <files>src/views/SettingsView.tsx, e2e/specs/settings.spec.ts, e2e/specs/full-ui.spec.ts</files>
  <read_first>src/views/SettingsView.tsx (sync section from Plan 06-01), src/types/ipc.ts (SyncStatusView with error fields from Plan 06-01), e2e/specs/settings.spec.ts (existing 3 tests), e2e/specs/full-ui.spec.ts (existing journey), e2e/helpers.ts (helper patterns), 06-CONTEXT.md D-19 D-20 D-25 D-41, 06-UI-SPEC.md Error Handling section and Copywriting Contract</read_first>
  <action>
    In SettingsView.tsx sync accordion section per D-19: if syncStatus.lastScheduledSyncError is truthy, render an inline error panel showing the error message and formatted timestamp. Use the copy from 06-UI-SPEC.md Copywriting Contract for sync error state. Keep the run-scheduled-sync button visible per D-20. Error displays in the sync section only, not global banner per D-19. Lark manual errors (larkSyncError) stay in Lark section per D-25.

    Extend e2e/specs/settings.spec.ts per D-41:
    - Add test: settings accordion sections exist (check for settings-section-providers, settings-section-index, settings-section-sync testids via toExist, not toBeDisplayed, since some are collapsed).
    - Add test: Providers and Index sections are expanded by default (children visible).
    - Add test: existing rebuild-index and cloud-api-key-input testids are still accessible (Providers and Index are default expanded, so toBeDisplayed should work).
    - Add test: sync section can be expanded and run-scheduled-sync button is visible after expansion.
    - Preserve all 3 existing tests (index health, rebuild button, API key UI).

    Extend e2e/specs/full-ui.spec.ts per D-41:
    - Add a Settings journey step: navigate to Settings, verify settings-panel exists, verify at least one accordion section-* testid exists, verify navigation back to chat works.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0
    - npm run test:e2e:local exits 0 (all specs pass including extended settings.spec.ts and full-ui.spec.ts)
    - rg "settings-section-sync" e2e/specs/settings.spec.ts matches
    - rg "settings-section" e2e/specs/full-ui.spec.ts matches
    - All existing E2E specs remain green
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Playwright supplemental smoke (06-02-03)</name>
  <files>e2e/playwright/nav-smoke.spec.ts, package.json</files>
  <read_first>06-CONTEXT.md D-42 D-43, e2e/README.md, package.json scripts, .cursor/rules/e2e-required.mdc</read_first>
  <action>
    Per D-42: create e2e/playwright/ directory with a minimal playwright-cli nav smoke spec covering full navigation (chat → library → tasks → memory → settings) and Settings panel visibility. Use deterministic JARVIS_E2E=1 app launch pattern documented in e2e/README.md.

    Per D-43: add npm script e2e:playwright:local (or document in e2e/README.md) that runs the supplemental spec only. Do NOT add Playwright to CI workflow (.github/workflows/ci.yml); WDIO npm run test:e2e remains the merge gate.

    Optional: add @playwright/test as devDependency if needed for the smoke script; no new runtime app dependencies.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - e2e/playwright/nav-smoke.spec.ts exists
    - package.json or e2e/README.md documents how to run supplemental Playwright smoke
    - .github/workflows/ci.yml does not replace or duplicate WDIO e2e job with Playwright
    - D-42 and D-43 cited in 06-02-SUMMARY.md when plan completes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sync_scheduler thread → store.meta | Background thread writes error strings to SQLite; strings are internal Rust error messages |
| store.meta → SyncStatusView IPC → React | Error string crosses from backend to frontend for display |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Info Disclosure | Sync error string may contain filesystem paths | low | mitigate | Error strings are Rust std error messages from local filesystem ops; no API keys or credentials in these paths. React renders as text node. |
| T-06-04 | Availability | store.set_meta failure in scheduler loop | low | accept | set_meta failures are already handled with let _ = pattern; scheduler continues on next interval regardless |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed this plan |
</threat_model>

<verification>
- [ ] cargo test -p tauri-app passes
- [ ] cargo test --workspace passes
- [ ] npm test passes
- [ ] npm run test:e2e:local passes (all specs including extended settings + full-ui)
- [ ] Sync error meta keys written on scheduler failure
- [ ] Sync error cleared on successful sync
- [ ] Sync error displayed inline in sync section only
</verification>

<success_criteria>
Sync error persistence wired end-to-end: scheduler writes to meta on failure, SyncStatusView reads and serves via IPC, Settings UI displays inline in sync accordion. E2E tests cover accordion layout and existing testid preservation. QA-02 structural concern "scheduled sync errors swallowed" resolved.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Requirement |
|----------|------|-------------|
| Sync error meta persistence | `src-tauri/src/sync_scheduler.rs` | QA-02 (D-16, D-17, D-18) |
| SyncStatusView error fields | `src-tauri/src/sync_scheduler.rs` | FE-05 (D-21) |
| Sync error UI display | `src/views/SettingsView.tsx` | FE-05 (D-19) |
| Extended settings E2E | `e2e/specs/settings.spec.ts` | FE-05 (D-41) |
| Extended full-ui E2E | `e2e/specs/full-ui.spec.ts` | FE-05 (D-41) |
| Playwright supplemental smoke | `e2e/playwright/nav-smoke.spec.ts` | D-42, D-43 |

<output>
Create `.planning/phases/06-settings-architecture-review/06-02-SUMMARY.md` when done
</output>
