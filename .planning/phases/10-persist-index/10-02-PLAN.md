---
phase: 10-persist-index
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - crates/insights/src/wiki.rs
  - crates/insights/src/lib.rs
autonomous: true
requirements:
  - WIKI-04
must_haves:
  truths:
    - "rebuild_index_md_from_disk unit tests: two prior pages from different dirs appear in catalog; sections lex-sorted; empty sections omitted; index.md never listed as entry (D-01…D-05, D-13)"
    - "On re-compile, generated pages owned by this source URI that disappeared from current draft set are deleted from disk and Store (delete_chunks_for_source then delete_source) (D-06, D-07)"
    - "Pages lacking generated: true are never deleted on cleanup and are never overwritten on write; they are still indexed from on-disk bytes (D-08, D-09, D-12)"
    - "After compile, on-disk page frontmatter still omits digest YAML keys (D-10); hash remains Document-only"
    - "compile_cleanup_and_user_edit_* integration test covers stale cleanup + user-edit skip + still-indexed user note"
  artifacts:
    - path: crates/insights/src/wiki.rs
      provides: "stale cleanup helpers + rebuild unit tests + user-edit integration"
      contains: "cleanup"
    - path: crates/insights/src/wiki.rs
      provides: "rebuild_index_md_* unit coverage"
      contains: "rebuild_index_md"
  key_links:
    - from: compile_wiki_for_source cleanup
      to: Store::delete_chunks_for_source + delete_source
      via: "wiki://{slug} for stale generated pages (library remove_source pair — not forget_memory)"
      pattern: "delete_chunks_for_source"
    - from: D-09 skip write
      to: index_document
      via: "read_to_string existing file then hash_text (D-12)"
      pattern: "skipped_user_edit"
    - from: rebuild_index_md_from_disk
      to: disk sources|entities|concepts
      via: "scan-rebuild after cleanup (D-01)"
      pattern: "rebuild_index_md_from_disk"
---

<objective>
Complete WIKI-04 vault correctness: harden `rebuild_index_md_from_disk` against D-01…D-05 with unit tests; wire stale generated-page cleanup (D-06…D-08) + user-edit sacred path (D-09, D-12) into `compile_wiki_for_source`; prove digest stays out of Markdown frontmatter (D-10). No UI; E2E still deferred to Phase 13.

Purpose: Multi-source vaults stay consistent; user-curated notes survive re-compile; cleaned pages leave no orphan chunks.
Output: rebuild unit tests; cleanup helpers; `compile_cleanup_and_user_edit_*` green; cleaned count populated in WikiCompileSummary.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-persist-index/10-CONTEXT.md
@.planning/phases/10-persist-index/10-RESEARCH.md
@.planning/phases/10-persist-index/10-PATTERNS.md
@.planning/phases/10-persist-index/10-VALIDATION.md
@.planning/phases/10-persist-index/10-01-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@crates/insights/src/wiki.rs
@crates/insights/src/lib.rs
@src-tauri/src/commands/library.rs
@crates/store/src/store.rs
@crates/agent/src/skills.rs
@.cursor/rules/tdd-goal-driven.mdc
@.cursor/rules/e2e-required.mdc

<interfaces>
From Plan 10-01 (must exist):
- compile_wiki_for_source, WikiCompileSummary, rebuild_index_md_from_disk, WikiDisabled/WikiPageInput/Index, compile_wiki_cmd

This plan adds / hardens:
- Unit tests rebuild_index_md_* covering D-01…D-05
- cleanup_stale_wiki_pages_for_source (name discretionary) — FS delete + Store delete_chunks_for_source then delete_source for wiki://{slug}
- Frontmatter peeks: generated:true writable/deletable; sources contains input URI (D-07); never delete non-generated (D-08)
- Integration: compile_cleanup_and_user_edit_* (pre-seed markdown fixtures)
- Assert compile does not introduce digest keys into YAML frontmatter (D-10); keep frontmatter_omits_* green

Do not add UI, zip export, E2E, auto_on_insights, or progress events.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rebuild_index_md_from_disk unit coverage D-01…D-05 (10-02-01)</name>
  <files>crates/insights/src/wiki.rs</files>
  <read_first>
crates/insights/src/wiki.rs (rebuild_index_md_from_disk from Plan 01; build_index_markdown; wikilink),
.planning/phases/10-persist-index/10-CONTEXT.md (D-01, D-02, D-03, D-04, D-05, D-13),
.planning/phases/10-persist-index/10-RESEARCH.md (Pitfall 1; Peek rules),
.planning/phases/10-persist-index/10-VALIDATION.md (10-02-01),
.planning/phases/10-persist-index/10-01-SUMMARY.md
  </read_first>
  <behavior>
    - rebuild_index_md_includes_multi_dir_pages: pre-seed sources/a.md and entities/b.md under tempfile → rebuild → index.md lists both with ## Sources and ## Entities (D-01, D-02)
    - rebuild_index_md_lex_sorts_and_uses_title: two entities with titles → links are [[slug|title]] and appear in path/slug lexicographic order (D-03, D-04)
    - rebuild_index_md_omits_empty_sections: only sources pages present → no ## Entities / ## Concepts headers (D-05)
    - rebuild_index_md_ignores_root_clutter: root note.md and existing index.md are not catalog entries (D-02, D-13)
  </behavior>
  <action>
    RED first: add unit tests named with prefix `rebuild_index_md_` as above. Pre-seed markdown files with Phase 08 frontmatter shape (title, type, sources, generated: true). Call `rebuild_index_md_from_disk` and assert file contents. Confirm failures if Plan 01 helper is incomplete.

    GREEN: refine `rebuild_index_md_from_disk` until D-01…D-05 hold — scan only `sources/`, `entities/`, `concepts/` `*.md`; display from frontmatter title else slug; lex sort; omit empty sections; never treat root `index.md` as an entry source. Reuse `wikilink`. No serde_yaml. Do not index catalog file as WikiPage here.
  </action>
  <verify>
    <automated>cargo test -p insights rebuild_index_md_ -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "fn rebuild_index_md_includes_multi_dir_pages|fn rebuild_index_md_lex_sorts_and_uses_title|fn rebuild_index_md_omits_empty_sections|fn rebuild_index_md_ignores_root_clutter" crates/insights/src/wiki.rs` matches four tests
    - `cargo test -p insights rebuild_index_md_ -- --test-threads=1` exits 0
    - Rebuilt index.md contains `# Wiki` and uses `[[` wikilink form for entries
  </acceptance_criteria>
  <done>Scan-rebuild helper locked by unit tests for D-01…D-05</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stale cleanup D-06…D-08 wired into compile (10-02-02)</name>
  <files>crates/insights/src/wiki.rs</files>
  <read_first>
crates/insights/src/wiki.rs (compile_wiki_for_source write/index order from Plan 01),
src-tauri/src/commands/library.rs (delete_chunks_for_source then delete_source),
crates/store/src/store.rs (delete APIs),
.planning/phases/10-persist-index/10-CONTEXT.md (D-06, D-07, D-08),
.planning/phases/10-persist-index/10-RESEARCH.md (Pitfall 6 orphan chunks; Prescribed persist order step 5),
.planning/phases/10-persist-index/10-PATTERNS.md (Source delete pair — anti-analog forget_memory)
  </read_first>
  <behavior>
    - After write loop, before scan-rebuild: scan three content dirs for *.md whose frontmatter sources list contains the input source URI AND generated:true AND slug not in current compile draft set → fs::remove_file + delete_chunks_for_source(wiki://slug) + delete_source(wiki://slug); increment cleaned (D-06, D-07)
    - Never delete when generated:true is absent/false even if sources still lists the URI (D-08)
    - On Store error after FS delete → return Err (ordered best-effort; re-compile recovers)
  </behavior>
  <action>
    RED: extend toward `compile_cleanup_and_user_edit_` integration (or a focused `compile_removes_stale_generated_page` test): seed Indexed local source; first compile producing entity page A; mutate Mock/analysis path or pre-seed a stale generated page tagged with that source URI that is not in the next compile set; second compile must remove stale file and WikiPage row; cleaned &gt;= 1.

    GREEN: implement cleanup helper used inside `compile_wiki_for_source` after write loop and before `rebuild_index_md_from_disk` (D-06…D-08). Always pair `delete_chunks_for_source` then `delete_source` — do not copy memory forget_memory delete-only path (Pitfall 6). Populate `WikiCompileSummary.cleaned`. Path-safe joins only under wiki_root (T-10-01).
  </action>
  <verify>
    <automated>cargo test -p insights -- --test-threads=1 compile_cleanup_and_user_edit_ compile_removes_stale</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "delete_chunks_for_source" crates/insights/src/wiki.rs` matches cleanup path
    - `rg -n "delete_source" crates/insights/src/wiki.rs` matches after chunk delete
    - `rg -n "cleaned" crates/insights/src/wiki.rs` matches summary field updates
    - At least one test whose name matches `compile_cleanup_and_user_edit_` or `compile_removes_stale` exits 0 via `cargo test -p insights -- --test-threads=1`
    - Non-generated pages are not removed: test asserts a pre-seeded user note file still exists after cleanup compile (D-08) — may land fully in Task 3 if split; if so Task 2 at minimum proves generated stale deletion
  </acceptance_criteria>
  <done>Stale generated pages deleted from FS+Store; cleaned counted</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: User-edit path D-09/D-12 + D-10 frontmatter assert (10-02-03)</name>
  <files>crates/insights/src/wiki.rs</files>
  <read_first>
crates/insights/src/wiki.rs (D-09 skip write from Plan 01; cleanup from Task 2; frontmatter_omits_content_hash),
.planning/phases/10-persist-index/10-CONTEXT.md (D-09, D-10, D-11, D-12),
.planning/phases/10-persist-index/10-RESEARCH.md (Pitfall 4, 7),
.planning/phases/10-persist-index/10-VALIDATION.md (10-02-02, 10-02-03)
  </read_first>
  <behavior>
    - compile_cleanup_and_user_edit_preserves_note: pre-seed entities/x.md without generated:true (user-curated) tagged with source URI; compile that would overwrite same slug → file body unchanged; skipped_user_edit &gt;= 1; Store still has wiki://entities/x Indexed from on-disk text (D-08, D-09, D-12)
    - compile_output_frontmatter_has_no_digest_key: after successful compile, read a written generated page YAML fence and assert it does not introduce a digest/hash YAML key beyond Phase 08 fields title/type/sources/generated (D-10)
    - Hash input remains full on-disk markdown body via ingest::hash_text (D-11) — never hash the skipped draft body when D-09 fires (Pitfall 7)
  </behavior>
  <action>
    RED→GREEN: finish integration test `compile_cleanup_and_user_edit_preserves_note` (or single combined `compile_cleanup_and_user_edit_flow`) covering stale generated delete + user-edit skip + still-indexed user file. Fix any D-09/D-12 bugs (always `read_to_string` path before `hash_text` / `index_document`).

    Add assertion test that compiled generated page frontmatter stays limited to Phase 08 keys (D-10). Do not reintroduce digest fields into `build_frontmatter`. Keep existing `frontmatter_omits_content_hash` green.

    Task verify stays filtered (Nyquist ≤30s): cleanup/user-edit + frontmatter tests only. Reserve full `cargo test -p insights -p indexer -p memory -- --test-threads=1` for **wave/phase gate** (Plan 02 wave merge / phase verification) — not this task's `<automated>`. Do not run E2E; state in SUMMARY that Phase 13 owns wiki.spec.ts journey.
  </action>
  <verify>
    <automated>cargo test -p insights -- --test-threads=1 compile_cleanup_and_user_edit_ frontmatter_omits_content_hash</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "fn compile_cleanup_and_user_edit_" crates/insights/src/wiki.rs` matches at least one integration test
    - `cargo test -p insights -- --test-threads=1 compile_cleanup_and_user_edit_` exits 0
    - `cargo test -p insights -- --exact --test-threads=1 frontmatter_omits_content_hash` exits 0
    - Wave/phase gate (not task verify): `cargo test -p insights -p indexer -p memory -- --test-threads=1` exits 0
    - ROADMAP Phase 10 success criteria 1–4 covered across Plan 01+02 tests
    - No files under `e2e/specs/` modified; no React UI changes
  </acceptance_criteria>
  <done>User-edit sacred + D-10 frontmatter contract; filtered insights tests green at task; full insights/indexer/memory suite at wave/phase gate; E2E deferred Phase 13</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontmatter on disk → cleanup/delete decisions | User can edit YAML; only generated:true pages are deletable |
| Cleanup → Store delete | Mistaken ownership match could remove wrong wiki:// rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-06 | Tampering | stale cleanup ownership | high | mitigate | Require sources contains input URI AND generated:true AND slug ∉ current set (D-06…D-08); unit/integration locked |
| T-10-07 | Tampering | user-edit overwrite | high | mitigate | Skip write when not generated:true (D-09); never delete non-generated (D-08); still index disk (D-12) |
| T-10-08 | Tampering | orphan vec/FTS on cleanup | medium | mitigate | Always delete_chunks_for_source before delete_source |
| T-10-01 | Tampering | path traversal on scan/delete | high | mitigate | Same wiki_root confinement as Plan 01 |
| T-10-SC | Tampering | cargo installs | low | accept | No new packages this plan |
</threat_model>

<verification>
- `cargo test -p insights rebuild_index_md_ -- --test-threads=1` green
- `cargo test -p insights -- --test-threads=1 compile_cleanup_and_user_edit_` green
- `cargo test -p insights -- --exact --test-threads=1 frontmatter_omits_content_hash` green
- Phase gate: `cargo test -p insights -p indexer -p memory -- --test-threads=1` green
- **No** `npm run test:e2e:local` — E2E deferred Phase 13 (no UI this phase)
</verification>

<success_criteria>
WIKI-04 complete: scan-rebuild correct for multi-dir vaults (D-01…D-05); stale generated cleanup (D-06…D-08); user-edit skip+index (D-09, D-12); DB-only digest (D-10); Plan 01 gates/index/idempotent/Tauri retained.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Requirement / Decision |
|----------|------|------------------------|
| `rebuild_index_md_*` unit tests | `crates/insights/src/wiki.rs` | D-01…D-05 |
| Stale cleanup helper (e.g. `cleanup_stale_wiki_pages_for_source`) | `crates/insights/src/wiki.rs` | D-06…D-08 |
| `compile_cleanup_and_user_edit_*` | `crates/insights/src/wiki.rs` | D-06…D-09, D-12 |
| Frontmatter peek helpers (generated / sources / title) | `crates/insights/src/wiki.rs` | D-03, D-07…D-09 |
| *(from Plan 01)* `compile_wiki_for_source`, `WikiCompileSummary`, `compile_wiki_cmd`, `WikiDisabled`, `WikiPageInput`, `Index`, `rebuild_index_md_from_disk` | see 10-01-PLAN.md | WIKI-04 / D-14…D-17 |

<output>
Create `.planning/phases/10-persist-index/10-02-SUMMARY.md` when done
</output>
