---
phase: 07-wiki-kind-config
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/store/src/types.rs
  - src-tauri/src/index_ops.rs
autonomous: true
requirements:
  - WIKI-02
must_haves:
  truths:
    - "SourceKind::WikiPage.as_str() equals wiki_page and parse round-trips (D-13, D-14)"
    - "source_kind_roundtrips includes WikiPage; parse of unknown strings still returns None (D-13)"
    - "reindex_source has a WikiPage arm that fail-closes via mark_failed and does not call index_path (D-14)"
    - "cargo test -p store passes; tauri-app crate compiles with exhaustive SourceKind match"
  artifacts:
    - path: crates/store/src/types.rs
      provides: "SourceKind::WikiPage + as_str/parse + wiki_page_kind_roundtrips"
      contains: "WikiPage"
    - path: src-tauri/src/index_ops.rs
      provides: "WikiPage reindex stub arm"
      contains: "SourceKind::WikiPage"
  key_links:
    - from: crates/store/src/types.rs
      to: src-tauri/src/index_ops.rs
      via: "exhaustive match source.kind must include WikiPage or app fails to compile"
      pattern: "SourceKind::WikiPage"
    - from: crates/store/src/types.rs
      to: SQLite sources.kind TEXT
      via: "as_str on write / parse on read (no schema migration)"
      pattern: "wiki_page"
---

<objective>
Add first-class `SourceKind::WikiPage` (`wiki_page`) with store round-trip tests and a fail-closed `reindex_source` arm so the Tauri app compiles after the enum grows (WIKI-02 / D-13 / D-14).

Purpose: Foundation for later wiki persist/index without breaking exhaustive matches or treating wiki URIs as local files.
Output: WikiPage variant + tests; `index_ops` stub arm; green `cargo test -p store` and tauri-app compile.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-wiki-kind-config/07-CONTEXT.md
@.planning/phases/07-wiki-kind-config/07-RESEARCH.md
@.planning/phases/07-wiki-kind-config/07-PATTERNS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/superpowers/plans/2026-07-16-wiki-compile-layer.md
@.cursor/rules/tdd-goal-driven.mdc
@crates/store/src/types.rs
@src-tauri/src/index_ops.rs

<interfaces>
From crates/store/src/types.rs (current — extend, do not reshape serde rename):
- enum SourceKind { … Memory }
- fn as_str(&self) -> &'static str
- fn parse(s: &str) -> Option of SourceKind  // unknown → None (D-13)
- test source_kind_roundtrips loops all variants + parse("bogus") → None

From src-tauri/src/index_ops.rs:
- async fn reindex_source(…) -> Result&lt;bool, String&gt;
- exhaustive match source.kind { LocalFile | Lark* | CursorTranscript | Memory }
- fn mark_failed(store, source, error) -> Result&lt;(), String&gt;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WikiPage kind + store round-trip tests (07-01-01)</name>
  <files>crates/store/src/types.rs</files>
  <read_first>
crates/store/src/types.rs (SourceKind enum, as_str, parse, #[cfg(test)] source_kind_roundtrips),
.planning/phases/07-wiki-kind-config/07-CONTEXT.md (D-13, D-14),
.planning/phases/07-wiki-kind-config/07-RESEARCH.md (Architecture §1 Code Examples),
docs/superpowers/plans/2026-07-16-wiki-compile-layer.md (Task 1 store steps)
  </read_first>
  <behavior>
    - wiki_page_kind_roundtrips: WikiPage.as_str() == "wiki_page"; parse("wiki_page") == Some(WikiPage)
    - source_kind_roundtrips includes WikiPage in the variant array; parse("bogus") remains None
  </behavior>
  <action>
    Per D-13 and D-14 (and Memory precedent): RED — add test `wiki_page_kind_roundtrips` asserting as_str/parse for WikiPage before the variant exists; extend `source_kind_roundtrips` array with WikiPage. Confirm `cargo test -p store wiki_page_kind_roundtrips` fails.

    GREEN — add enum variant `WikiPage`; map as_str → `"wiki_page"`; parse arm `"wiki_page"` → WikiPage; keep unknown → None (no Unknown variant). Do not add schema migrations; do not change SourceKind serde rename_all. No other crates in this task.
  </action>
  <verify>
    <automated>cargo test -p store wiki_page_kind_roundtrips source_kind_roundtrips -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "WikiPage" crates/store/src/types.rs` matches variant + as_str + parse
    - `rg -n "wiki_page_kind_roundtrips" crates/store/src/types.rs` matches
    - `cargo test -p store` exits 0
  </acceptance_criteria>
  <done>WikiPage round-trips as wiki_page; unknown parse stays None; store tests green</done>
</task>

<task type="auto">
  <name>Task 2: Fail-closed WikiPage reindex arm (07-01-02)</name>
  <files>src-tauri/src/index_ops.rs</files>
  <read_first>
src-tauri/src/index_ops.rs (reindex_source match ~559–653, mark_failed helper),
.planning/phases/07-wiki-kind-config/07-RESEARCH.md (Architecture §3 + Pitfall P2),
.planning/phases/07-wiki-kind-config/07-PATTERNS.md (index_ops section)
  </read_first>
  <action>
    Per D-14 and RESEARCH P2: after adding WikiPage, the exhaustive `match source.kind` in `reindex_source` must gain a WikiPage arm or the app crate fails to compile.

    Add arm that calls `mark_failed(store, source, "wiki page reindex not implemented")?` then returns `Ok(false)`. Must not fall through to LocalFile / `index_path` (wrong-path / compile-loop risk). Do not implement wiki persist or disk reindex (Phase 10). Do not touch insights_ops or Settings.
  </action>
  <verify>
    <automated>cargo check -p tauri-app</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "SourceKind::WikiPage" src-tauri/src/index_ops.rs` matches inside reindex_source
    - `rg -n "wiki page reindex not implemented" src-tauri/src/index_ops.rs` matches
    - `cargo check -p tauri-app` exits 0
    - `cargo test -p store` still exits 0
  </acceptance_criteria>
  <done>tauri-app compiles; WikiPage reindex fail-closes without indexing as a local file</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Store kind string ↔ Rust enum | Untrusted/legacy TEXT in `sources.kind` parsed via `SourceKind::parse` |
| Reindex path selection | Kind drives whether URI is treated as filesystem path vs protocol URI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | `reindex_source` WikiPage arm | high | mitigate | Fail-closed `mark_failed` stub; never call `index_path` for WikiPage (D-14 / RESEARCH P2) |
| T-07-02 | Elevation of Privilege | Unknown kind parse | low | accept | Keep parse → None (D-13); existing callers degrade; no Unknown variant |
| T-07-03 | Tampering | Kind string confusion | medium | mitigate | Unit tests lock `"wiki_page"` ↔ WikiPage; extend `source_kind_roundtrips` |
| T-07-SC | Tampering | cargo/npm installs | low | accept | No new packages in this plan — N/A |
</threat_model>

<verification>
- `cargo test -p store` green including `wiki_page_kind_roundtrips`
- `cargo check -p tauri-app` green with WikiPage arm present
- No wiki compile pipeline, Settings UI, or E2E wiki journey introduced
</verification>

<success_criteria>
WIKI-02 store half: WikiPage persists as kind string wiki_page in tests; exhaustive match fixed; reindex cannot treat wiki pages as local files.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Requirement |
|----------|------|-------------|
| SourceKind::WikiPage | `crates/store/src/types.rs` | WIKI-02 (D-13, D-14) |
| wiki_page_kind_roundtrips | `crates/store/src/types.rs` | WIKI-02 |
| WikiPage reindex stub | `src-tauri/src/index_ops.rs` | WIKI-02 (D-14) |

<output>
Create `.planning/phases/07-wiki-kind-config/07-01-SUMMARY.md` when done
</output>
