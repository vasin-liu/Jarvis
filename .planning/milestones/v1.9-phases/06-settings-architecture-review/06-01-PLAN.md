---
phase: 06-settings-architecture-review
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/config.ts
  - src/types/config.test.ts
  - src/types/ipc.ts
  - src/hooks/useAppEvents.ts
  - src/hooks/useJarvisConfig.ts
  - src/hooks/useChat.ts
  - src/AppShell.tsx
  - src/views/SettingsView.tsx
  - src/views/ChatView.tsx
  - src/App.tsx
autonomous: true
requirements:
  - FE-05

must_haves:
  truths:
    - "SettingsView renders 6 accordion sections (Providers, Index, Sync, Lark, Agent, Advanced) with Providers and Index expanded by default (D-09, D-10)"
    - "useJarvisConfig owns all settings state: config, syncStatus, larkTokens, apiKeyDraft, skills, hooks, plugins, agentDrafts; App passes only busy/err (D-08)"
    - "AppShell renders sidebar navigation, global err banner, and busy overlay; App.tsx is view router + cross-view coordination only (D-01, D-02)"
    - "useAppEvents owns global index-progress and jarvis-ask-done listeners; App subscribes and passes props (D-04)"
    - "Chat/agent handlers (handleAgentAsk, tool-call state, e2eMode, askHandleCount) migrated from App.tsx into useChat/ChatView (D-05)"
    - "All existing settings data-testid selectors preserved; new settings-section-* testids added (D-15)"
    - "Nested TS config types in src/types/config.ts mirror Rust; Vitest roundtrip test passes (D-27, D-28, D-29)"
    - "Lark manual sync inputs collapsed under Advanced accordion with hint text (D-22, D-23, D-24)"
    - "No barrel src/views/index.ts (D-06); direct imports only"
  artifacts:
    - path: src/types/config.ts
      provides: "Nested EmbeddingConfig, SyncConfig, AgentConfig types"
      contains: "EmbeddingConfig"
    - path: src/types/config.test.ts
      provides: "Vitest roundtrip test for nested config types"
      contains: "describe"
    - path: src/hooks/useAppEvents.ts
      provides: "Global event listener hook for index-progress and jarvis-ask-done"
      contains: "useAppEvents"
    - path: src/hooks/useJarvisConfig.ts
      provides: "All settings state and actions"
      contains: "useJarvisConfig"
    - path: src/AppShell.tsx
      provides: "Sidebar nav + err banner + busy overlay"
      contains: "AppShell"
    - path: src/views/SettingsView.tsx
      provides: "Full accordion settings extraction with 6 sections"
      contains: "AccordionSection"
  key_links:
    - from: src/App.tsx
      to: src/AppShell.tsx
      via: "App renders AppShell wrapping active view"
      pattern: "AppShell"
    - from: src/views/SettingsView.tsx
      to: src/hooks/useJarvisConfig.ts
      via: "SettingsView calls useJarvisConfig for all settings state"
      pattern: "useJarvisConfig"
    - from: src/App.tsx
      to: src/hooks/useAppEvents.ts
      via: "App calls useAppEvents for global event listeners"
      pattern: "useAppEvents"
---

## Phase Goal

**As a** Jarvis power user, **I want to** have a cleanly organized Settings view with accordion sections and a slim App shell, **so that** configuration is easy to navigate and the codebase is maintainable.

<objective>
Extract Settings view with full accordion layout, create AppShell, useAppEvents, useJarvisConfig, nested TS config types, and migrate Chat handlers out of App.tsx — delivering FE-05 and completing FE-01.

Purpose: Transform App.tsx from a 2034-line monolith into a thin view router by extracting all settings state/JSX and chat handlers to dedicated modules.
Output: SettingsView.tsx (accordion), useJarvisConfig.ts, AppShell.tsx, useAppEvents.ts, config.ts types + roundtrip test, slimmed App.tsx.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-settings-architecture-review/06-CONTEXT.md
@.planning/phases/06-settings-architecture-review/06-RESEARCH.md
@.planning/phases/06-settings-architecture-review/06-UI-SPEC.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/App.tsx
@src/views/SettingsView.tsx
@src/hooks/useChat.ts
@src/views/ChatView.tsx
@src/types/ipc.ts
@.cursor/rules/frontend-taste.mdc
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Nested TS config types + Vitest roundtrip + useAppEvents + useJarvisConfig (06-01-01)</name>
  <files>src/types/config.ts, src/types/config.test.ts, src/hooks/useAppEvents.ts, src/hooks/useJarvisConfig.ts, src/types/ipc.ts</files>
  <read_first>src/types/ipc.ts (current flat AppConfig, SyncStatusView), src/hooks/useChat.ts (controlled hook pattern), src/App.tsx lines 1-200 (state declarations, event listeners), 06-CONTEXT.md D-04 D-08 D-27 D-28 D-29, 06-RESEARCH.md Pattern 1 and Pattern 6, 06-UI-SPEC.md</read_first>
  <behavior>
    - Test: nested EmbeddingConfig has embedder field matching valid provider strings
    - Test: nested SyncConfig has watchFolders array, scheduledSyncEnabled boolean
    - Test: nested AgentConfig has fields for agent profiles
    - Test: a flat camelCase IPC payload can be mapped to/from nested config shape
  </behavior>
  <action>
    Create src/types/config.ts per D-27: define EmbeddingConfig, SyncConfig, AgentConfig interfaces mirroring Rust nested structs from crates/config/src/types.rs. Export a flat-to-nested and nested-to-flat conversion pair. The flat shape matches the existing camelCase IPC payload from get_config/set_config.

    Create src/types/config.test.ts per D-29: Vitest roundtrip test verifying flat IPC payload converts to nested and back without data loss. Test that all camelCase field names match what Rust serde produces.

    Create src/hooks/useAppEvents.ts per D-04: a hook that sets up tauri listen for index-progress and jarvis-ask-done events. Returns callbacks or state that App.tsx subscribes to. Do not duplicate event listeners in individual views.

    Create src/hooks/useJarvisConfig.ts per D-08: owns all settings-local state extracted from App.tsx (config, indexStatus, syncStatus, larkDocToken, larkSheetToken, larkMailId, larkChatId, larkStatus, larkSyncError, hasApiKey, apiKeyDraft, newWatchFolder, skills, hooks, plugins, agent draft state). Accepts UseJarvisConfigOptions with onError, busy, setBusy per existing hook pattern from useChat.ts. Wraps all settings actions (refreshConfig, handleSaveConfig, handleRebuild, handleLarkSync, etc.) in useCallback. Uses nested config types internally per D-28; saves whole config via set_config invoke.

    Extend src/types/ipc.ts: add lastScheduledSyncError and lastScheduledSyncErrorAt optional fields to SyncStatusView interface per D-21 (frontend-side prep for Plan 06-02 backend work).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0 with config roundtrip tests passing
    - npx tsc --noEmit exits 0
    - rg "EmbeddingConfig" src/types/config.ts matches
    - rg "useAppEvents" src/hooks/useAppEvents.ts matches
    - rg "useJarvisConfig" src/hooks/useJarvisConfig.ts matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: AppShell + Chat handler migration + App.tsx slim (06-01-02)</name>
  <files>src/AppShell.tsx, src/views/ChatView.tsx, src/hooks/useChat.ts, src/App.tsx</files>
  <read_first>src/App.tsx (sidebar JSX, handleAgentAsk/handleRagAsk/handleAsk, tool-call state, e2eMode/askHandleCount), src/views/ChatView.tsx (current props), src/hooks/useChat.ts (current exports), 06-CONTEXT.md D-01 D-02 D-05 D-06 D-07 D-35 D-44, 06-UI-SPEC.md layout sections</read_first>
  <action>
    Create src/AppShell.tsx per D-01: extract sidebar navigation JSX from App.tsx. Include global err banner at top per D-35 and busy overlay. Receive busy, setBusy, err, setErr, view, setView, children as props (prop-drill from App per D-07). Preserve all existing nav button classes and testids (nav-chat, nav-library, nav-tasks, nav-memory, nav-settings). Settings copy in concise 中文 per D-44.

    Migrate Chat/agent handlers per D-05: move handleAgentAsk, handleRagAsk, handleAsk, tool-call state (lastToolCalls, lastToolParseWarnings, lastOrchestrationSteps), agentMode, e2eMode, askHandleCount from App.tsx into useChat hook / ChatView. Ensure jarvis-ask-done E2E listener wiring stays functional. ChatView receives these via useChat return values instead of App props.

    Slim App.tsx per D-02: App.tsx retains view switch, cross-view coordination (busy/err, view state), renders AppShell wrapping active view. Uses useAppEvents from Task 1. Remove sidebar JSX and chat handlers. Settings JSX may remain temporarily until Task 3 extracts it to SettingsView. No barrel src/views/index.ts per D-06.

    Record App.tsx line count after this task in commit message per D-03 (soft target &lt;300 is final after Task 3).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0
    - rg "AppShell" src/AppShell.tsx matches
    - App.tsx does not contain handleAgentAsk or handleRagAsk function definitions
    - App.tsx renders AppShell with active view as children
    - ChatView uses useChat for agent/RAG ask handlers
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: SettingsView full accordion extraction (06-01-03)</name>
  <files>src/views/SettingsView.tsx, src/App.tsx</files>
  <read_first>src/App.tsx (remaining settings JSX after Task 2), src/hooks/useJarvisConfig.ts, src/AppShell.tsx, 06-CONTEXT.md D-08 D-09 D-10 D-11 D-12 D-13 D-14 D-15 D-22 D-23 D-24 D-25 D-26, 06-UI-SPEC.md Component Inventory + Test ID Contract, 06-RESEARCH.md Pattern 2 (accordion)</read_first>
  <action>
    Rewrite src/views/SettingsView.tsx from stub to full accordion extraction per D-09/D-10:
    - Build internal AccordionSection component using motion/react AnimatePresence with 300ms ease-out per 06-RESEARCH.md Pattern 2; honor prefers-reduced-motion via useReducedMotion per 06-UI-SPEC.md Accessibility.
    - 6 sections in order per D-10: Providers (expanded), Index (expanded), Sync (collapsed), Lark (collapsed), Agent basics (collapsed), Advanced (collapsed).
    - Move all settings JSX from App.tsx into appropriate accordion sections.
    - Providers section: embedder/chat provider selects + cloud API key input per D-11 (rebuild/reinit buttons in Index section).
    - Index section: index-status panel + rebuild-index + reinit-rebuild-index buttons per D-11.
    - Sync section: watch folders per D-12, scheduled sync toggles, run-scheduled-sync button per D-20, sync error display inline per D-19 (reads from syncStatus.lastScheduledSyncError — will be null until Plan 06-02 wires backend).
    - Lark section: lark-cli path + identity per D-26, connection check, primary UX per D-23. Manual doc/sheet/mail/IM URL inputs collapsed under Advanced accordion per D-22 with hint text per D-22. Preserve lark-sync-url-input etc. testids per D-24 (visible when advanced expanded).
    - Agent section: basic agent selection per D-13.
    - Advanced section: Agent CRUD + skills/hooks/plugins per D-13, Lark manual sync advanced inputs.
    - Add settings-section-providers, settings-section-index, settings-section-sync, settings-section-lark, settings-section-agent, settings-section-advanced testids per D-15.
    - Preserve ALL existing testids listed in 06-RESEARCH.md (settings-panel, index-status, rebuild-index, reinit-rebuild-index, cloud-api-key-input, clear-api-key, lark-check-connection, lark-detect-cli, lark-status-panel, lark-sync-url-input, lark-sync-submit, cursor-projects-root, pipeline-mode-toggle, router-mode-toggle, run-scheduled-sync) per D-15.
    - Config save is immediate per-field via set_config per D-14.
    - SettingsView receives busy, err, setBusy, setErr from App; calls useJarvisConfig for everything else per D-08.

    Wire App.tsx settings route to SettingsView only; remove all settings JSX from App.tsx per D-02.

    Record final App.tsx line count in commit message per D-03 (soft target &lt;300).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0
    - npx tsc --noEmit exits 0
    - rg "AccordionSection" src/views/SettingsView.tsx matches
    - rg "useJarvisConfig" src/views/SettingsView.tsx matches
    - rg "settings-section-providers" src/views/SettingsView.tsx matches
    - App.tsx does not contain settings JSX (cloud-api-key-input, rebuild-index, etc.)
    - App.tsx line count recorded in SUMMARY (soft target &lt;300 per D-03)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust IPC → React | Config payloads cross from Tauri backend to frontend; all fields are user-controlled local data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Info Disclosure | Sync error string rendering in Settings | low | mitigate | React renders error strings as text nodes (not dangerouslySetInnerHTML); no HTML injection risk |
| T-06-02 | Tampering | Config save immediate per-field | low | accept | Local desktop app; user is sole operator of their own config.json; no multi-user access control needed |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed this phase; all dependencies are existing workspace deps |
</threat_model>

<verification>
- [ ] npm test passes (config roundtrip + ChatView tests)
- [ ] npx tsc --noEmit passes
- [ ] All existing settings testids preserved in SettingsView accordion
- [ ] App.tsx is view router + coordination only (no settings JSX, no chat handlers)
- [ ] AppShell renders sidebar, err banner, busy overlay
- [ ] useAppEvents owns global event listeners
- [ ] useJarvisConfig owns all settings state
</verification>

<success_criteria>
FE-05 settings extraction complete with accordion layout; FE-01 App.tsx slim complete (view router only); Chat handlers migrated to useChat/ChatView; AppShell created; all testids preserved; type-safe nested config types with roundtrip test.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Requirement |
|----------|------|-------------|
| Nested TS config types | `src/types/config.ts` | FE-05 (D-27) |
| Config roundtrip test | `src/types/config.test.ts` | FE-05 (D-29) |
| Global event hook | `src/hooks/useAppEvents.ts` | FE-05 (D-04) |
| Settings config hook | `src/hooks/useJarvisConfig.ts` | FE-05 (D-08) |
| App shell component | `src/AppShell.tsx` | FE-05 (D-01) |
| Settings accordion view | `src/views/SettingsView.tsx` | FE-05 (D-09, D-10) |
| Chat handler migration | `src/hooks/useChat.ts`, `src/views/ChatView.tsx` | FE-05 (D-05) |
| Slimmed App.tsx | `src/App.tsx` | FE-01 (D-02) |

<output>
Create `.planning/phases/06-settings-architecture-review/06-01-SUMMARY.md` when done
</output>
