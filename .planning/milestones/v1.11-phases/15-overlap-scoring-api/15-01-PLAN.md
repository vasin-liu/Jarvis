---
phase: 15-overlap-scoring-api
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - crates/retriever/src/related.rs
  - crates/retriever/src/lib.rs
autonomous: true
requirements:
  - REL-02
must_haves:
  truths:
    - "cargo test -p retriever related fails RED with stub related_sources (tests compile, assertions fail)"
    - "Integration tests cover seed exclude, rollup, top_n, empty seed, missing source, summary preference, snippet truncate, only-seed corpus, wiki neighbor per D-01..D-11"
    - "Fixtures use shared FTS keywords so MockEmbedder tests are deterministic (not vector-only)"
    - "RelatedSource DTO has source_id, title, kind, snippet only — no score field (D-09)"
  artifacts:
    - path: crates/retriever/src/related.rs
      provides: "RelatedSource stub, related_sources stub, #[cfg(test)] module with all REL-02 tests"
    - path: crates/retriever/src/lib.rs
      provides: "mod related + pub use related_sources, RelatedSource"
  key_links:
    - from: crates/retriever/src/related.rs
      to: crates/retriever/src/retrieve.rs
      via: "related_sources stub will call retrieve in Plan 15-02"
      pattern: "use crate::retrieve::{retrieve, RetrieverConfig}"
    - from: crates/retriever/src/related.rs
      to: crates/store/src/types.rs
      via: "Source, SourceKind, ChunkHit types in tests and DTO"
      pattern: "store::Store::open + get_source in fixtures"
---

<objective>
Establish failing (RED) test coverage for `related_sources` before implementation.

Purpose: REL-02 and locked decisions D-01..D-11 need regression tests; Nyquist Wave 0 requires automated verify on every behavior before GREEN.
Output: `related.rs` test module + compiling stubs; `lib.rs` re-exports; `cargo test -p retriever related` fails with meaningful assertion errors (not compile errors).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-overlap-scoring-api/15-CONTEXT.md
@.planning/phases/15-overlap-scoring-api/15-RESEARCH.md
@.planning/phases/15-overlap-scoring-api/15-PATTERNS.md
@.planning/phases/15-overlap-scoring-api/15-VALIDATION.md
@crates/retriever/src/retrieve.rs
@crates/retriever/src/lib.rs
@crates/rag/src/ask.rs
@crates/store/src/types.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RelatedSource stub and failing REL-02 integration tests</name>
  <read_first>
    - crates/retriever/src/related.rs (create)
    - crates/retriever/src/retrieve.rs (test fixture pattern lines 75-106)
    - crates/rag/src/ask.rs (multi-file indexing + truncation pattern)
    - .planning/phases/15-overlap-scoring-api/15-CONTEXT.md (D-01..D-11)
    - .planning/phases/15-overlap-scoring-api/15-VALIDATION.md (test name map)
  </read_first>
  <files>crates/retriever/src/related.rs</files>
  <behavior>
    - related_excludes_seed_and_rolls_up_by_source: seed A with summary containing distinctive token overlap-scoring-alpha; neighbor B shares token in body; seed id absent from results; at most one row per neighbor source_id
    - related_empty_seed_text: source with blank title and None/whitespace summary returns Ok(vec![]) not Err
    - related_missing_source: unknown source_id returns Err (StoreError::NotFound propagated)
    - related_prefers_summary: seed with non-empty summary ranks neighbor matching summary FTS terms over title-only mismatch
    - related_respects_top_n: default top_n None yields at most 5; top_n=2 yields at most 2; top_n=0 yields Ok(vec![])
    - related_snippet_truncate: neighbor with long chunk text yields snippet at most 161 chars ending with U+2026 when truncated; valid UTF-8 for CJK fixture
    - related_only_seed: corpus of single indexed source returns Ok(vec![])
    - related_allows_wiki_neighbor: seed Local file can surface neighbor indexed as WikiPage kind when ranked (no kind filter per D-10)
  </behavior>
  <action>
    Create crates/retriever/src/related.rs with public struct RelatedSource { source_id, title, kind, snippet } — Debug, Clone, PartialEq only; no score field (D-09). Add pub async fn related_sources(...) stub returning Ok(vec![]) always so tests fail RED.

    Add #[cfg(test)] mod tests with private helper index_fixture_store() building tempfile DB via Store::open, MockEmbedder::new(4), index_path for markdown files. Fixture layout: (1) seed doc seed-alpha.md titled Alpha with summary containing overlap-scoring-alpha; (2) neighbor doc neighbor-alpha.md body containing overlap-scoring-alpha shared keyword; (3) disjoint doc unrelated-zeta.md with vocabulary zeta-unrelated-only; (4) optional wiki neighbor via store insert/index pattern using SourceKind::WikiPage if indexer supports, else manually set kind after index via store API if available — goal is one WikiPage neighbor in corpus for related_allows_wiki_neighbor.

    Implement all eight test functions named exactly as in 15-VALIDATION.md. Do NOT implement real rollup/retrieve logic — stub only. Do NOT add score threshold assertions (D-06 defers weak-affinity empty). Do NOT use Store::source_chunk_text for seed query in tests (D-01..D-03).
  </action>
  <verify>
    <automated>cargo test -p retriever related -- --test-threads=4 2>&1 | findstr /C:"test result:"</automated>
  </verify>
  <acceptance_criteria>
    - related.rs compiles; related_sources stub exists
    - All eight related_* tests exist and run; at least six fail assertions (RED), not compile errors
    - RelatedSource has exactly four public fields: source_id, title, kind, snippet — no score field
    - No rusqlite imports in related.rs (store API only)
  </acceptance_criteria>
  <done>RED suite present: cargo test -p retriever related exits non-zero with failing behavior assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire related module exports in lib.rs</name>
  <read_first>
    - crates/retriever/src/lib.rs
    - crates/retriever/src/related.rs
    - crates/rag/src/lib.rs (re-export pattern)
  </read_first>
  <files>crates/retriever/src/lib.rs</files>
  <action>
    Add mod related; before mod retrieve (alphabetical). Add pub use related::{related_sources, RelatedSource}; alongside existing retrieve exports. Do not change retrieve.rs or rrf.rs behavior.
  </action>
  <verify>
    <automated>cargo test -p retriever related_excludes_seed -- --test-threads=4 --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - crates/retriever/src/lib.rs declares mod related and re-exports related_sources and RelatedSource
    - cargo check -p retriever succeeds
    - External callers can use retriever::related_sources and retriever::RelatedSource from crate root
  </acceptance_criteria>
  <done>Retriever crate public API surface includes related_sources and RelatedSource; tests still RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → Store | Tempfile SQLite; test-only; no production data |
| related_sources (future) → Store | source_id and top_n from caller; validated in Plan 15-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Tampering | Store::get_source in tests | medium | mitigate | Tests use parameterized Store APIs only; no raw SQL in related.rs |
| T-15-02 | Denial of Service | top_n in related_sources | medium | mitigate | Plan 15-02 will clamp top_n 1..=32 and top_n=0 → []; tests assert related_respects_top_n |
| T-15-03 | Spoofing | RelatedSource DTO | low | mitigate | No score field in struct or tests (D-09) |
| T-15-SC | Tampering | cargo dev-deps | high | mitigate | No new packages this phase; existing workspace path deps only — slopcheck N/A |

</threat_model>

<verification>
- cargo test -p retriever related — exits non-zero (RED expected)
- cargo check -p retriever — succeeds
- No new Cargo.toml dependencies added
</verification>

<success_criteria>
- Wave 0 test stubs exist for all REL-02 behaviors mapped in 15-VALIDATION.md
- lib.rs exports related_sources and RelatedSource
- Executor can proceed to Plan 15-02 GREEN without writing new test names
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | Location | Phase |
|--------|------|----------|-------|
| `RelatedSource` | struct (stub) | crates/retriever/src/related.rs | 15-01 |
| `related_sources` | async fn (stub) | crates/retriever/src/related.rs | 15-01 |
| `related_*` tests (×8) | #[tokio::test] | crates/retriever/src/related.rs mod tests | 15-01 |
| `index_fixture_store` (or equivalent) | test helper | crates/retriever/src/related.rs mod tests | 15-01 |
| `mod related` + re-exports | module wiring | crates/retriever/src/lib.rs | 15-01 |

<output>
Create `.planning/phases/15-overlap-scoring-api/15-01-SUMMARY.md` when done
</output>
