---
phase: 16-related-docs-library-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/retriever/Cargo.toml
  - crates/retriever/src/related.rs
  - src-tauri/src/commands/library.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/lib.rs
  - src/types/library.ts
  - src/lib/tauri.ts
autonomous: true
requirements:
  - REL-01
  - REL-04
must_haves:
  truths:
    - "RelatedSource serializes to camelCase JSON with sourceId, title, kind, snippet only — no score field (D-09, REL-04)"
    - "Thin Tauri command list_related_sources(source_id, top_n?) calls retriever::related_sources via store + embedder and maps Err to String (REL-01)"
    - "Frontend listRelatedSources(sourceId, topN?) invoke wrapper returns RelatedSource[] matching IPC payload"
    - "Command registered in generate_handler! and re-exported from commands/mod.rs"
  artifacts:
    - path: crates/retriever/src/related.rs
      provides: "Serialize + rename_all=camelCase on RelatedSource"
    - path: src-tauri/src/commands/library.rs
      provides: "list_related_sources async command"
    - path: src/lib/tauri.ts
      provides: "listRelatedSources wrapper"
    - path: src/types/library.ts
      provides: "RelatedSource TypeScript interface"
  key_links:
    - from: list_related_sources
      to: retriever::related_sources
      via: "state.store + state.embedder() → related_sources(...).await"
      pattern: "mirror summarize_source_cmd thin-shell"
    - from: src/lib/tauri.ts listRelatedSources
      to: list_related_sources
      via: "invoke with { sourceId, topN }"
      pattern: "camelCase args per Tauri 2"
---

<objective>
Expose Phase 15 `related_sources` across the Tauri IPC boundary with serde + TypeScript wrappers so Library UI can load related neighbors (REL-01 foundation; REL-04 DTO contract with no scores per D-09).

Purpose: Phase 15 deferred Serialize; without IPC the panel cannot fetch. Keep shell thin — store remains sole SQLite owner.
Output: Serializable `RelatedSource`, `list_related_sources` command registered, `RelatedSource` TS type + `listRelatedSources` wrapper.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-related-docs-library-panel/16-CONTEXT.md
@.planning/phases/16-related-docs-library-panel/16-RESEARCH.md
@.planning/phases/16-related-docs-library-panel/16-PATTERNS.md
@.planning/phases/15-overlap-scoring-api/15-02-SUMMARY.md
@crates/retriever/src/related.rs
@src-tauri/src/commands/library.rs
@src-tauri/src/insights_ops.rs
@src/types/library.ts
@src/lib/tauri.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Serialize to RelatedSource and serde workspace dep</name>
  <read_first>
    - crates/retriever/src/related.rs (RelatedSource struct ~lines 10–16)
    - crates/retriever/Cargo.toml
    - src-tauri/src/insights_ops.rs (InsightsReport camelCase Serialize pattern)
    - Cargo.toml (workspace serde features derive)
    - .planning/phases/16-related-docs-library-panel/16-CONTEXT.md (D-09)
  </read_first>
  <files>crates/retriever/Cargo.toml, crates/retriever/src/related.rs</files>
  <behavior>
    - RelatedSource derives Serialize with rename_all camelCase → JSON keys sourceId, title, kind, snippet
    - No score field exists on RelatedSource after change
    - cargo test -p retriever related still passes (semantics unchanged)
  </behavior>
  <action>
    Add `serde = { workspace = true }` under crates/retriever `[dependencies]`. On `RelatedSource`, add `Serialize` derive and `#[serde(rename_all = "camelCase")]` (mirror InsightsReport). Do not change field set, related_sources logic, or add a score field (D-09 / Phase 15 D-09). Do not open SQLite outside store.
  </action>
  <verify>
    <automated>cargo test -p retriever related -- --test-threads=4</automated>
  </verify>
  <acceptance_criteria>
    - crates/retriever/Cargo.toml lists serde workspace dependency
    - RelatedSource has Serialize + rename_all camelCase
    - RelatedSource public fields remain exactly source_id, title, kind, snippet (no score)
    - cargo test -p retriever related exits 0
  </acceptance_criteria>
  <done>RelatedSource is IPC-serializable; retriever related tests still green.</done>
</task>

<task type="auto">
  <name>Task 2: Thin list_related_sources command + TS wrappers</name>
  <read_first>
    - src-tauri/src/commands/library.rs (summarize_source_cmd pattern)
    - src-tauri/src/commands/mod.rs (library pub use list)
    - src-tauri/src/lib.rs (imports + generate_handler!)
    - src/types/library.ts (InsightsReport interface)
    - src/lib/tauri.ts (summarizeSource wrapper)
    - .planning/phases/16-related-docs-library-panel/16-PATTERNS.md
  </read_first>
  <files>src-tauri/src/commands/library.rs, src-tauri/src/commands/mod.rs, src-tauri/src/lib.rs, src/types/library.ts, src/lib/tauri.ts</files>
  <action>
    Add async `#[tauri::command] pub async fn list_related_sources(source_id: String, top_n: Option&lt;usize&gt;, state: State&lt;'_, AppState&gt;) -&gt; Result&lt;Vec&lt;retriever::RelatedSource&gt;, String&gt;` that calls `retriever::related_sources(state.store.as_ref(), state.embedder().as_ref(), &amp;source_id, top_n).await.map_err(|e| e.to_string())` — same thin style as summarize_source_cmd. Re-export `list_related_sources` from commands/mod.rs library pub use. Import and register `list_related_sources` in lib.rs generate_handler! adjacent to summarize_source_cmd.

    In src/types/library.ts add interface RelatedSource { sourceId: string; title: string; kind: string; snippet: string }. In src/lib/tauri.ts add listRelatedSources(sourceId, topN?) invoking "list_related_sources" with { sourceId, topN: topN ?? null }. No UI changes this plan.
  </action>
  <verify>
    <automated>cargo check -p tauri-app 2>&1 | findstr /I /C:"error" /C:"Finished"</automated>
  </verify>
  <acceptance_criteria>
    - list_related_sources exists in library.rs and is in generate_handler!
    - commands/mod.rs re-exports list_related_sources
    - src/types/library.ts exports RelatedSource with camelCase fields matching serde
    - src/lib/tauri.ts exports listRelatedSources
    - cargo check -p tauri-app succeeds (or equivalent package name if different — use workspace tauri app crate)
  </acceptance_criteria>
  <done>IPC path ready for Library panel consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Webview → Tauri command | Untrusted source_id / top_n strings from UI invoke |
| Command → retriever/store | Trusted crate API; store sole SQLite owner |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Tampering | list_related_sources source_id | medium | mitigate | Store::get_source NotFound → Err String; FE shows error state (Plan 16-03 D-12) |
| T-16-02 | Denial of Service | top_n | low | mitigate | Crate already clamps top_n (max 32); pass Option through unchanged |
| T-16-03 | Information Disclosure | RelatedSource.snippet | low | accept | Local single-user KB; snippet is user's own indexed text |
| T-16-04 | Elevation of Privilege | library.rs SQLite | high | mitigate | Command only calls retriever::related_sources; never open Connection outside store |
| T-16-SC | Tampering | cargo/npm installs | high | mitigate | No new registry packages — only workspace serde path dep already audited |

</threat_model>

<verification>
- cargo test -p retriever related — green
- cargo check -p tauri-app (or workspace app crate) — succeeds
- grep confirms no score field on RelatedSource
</verification>

<success_criteria>
- RelatedSource serializes camelCase without scores (D-09)
- list_related_sources registered and callable via listRelatedSources TS wrapper
- REL-01 IPC foundation + REL-04 DTO shape ready for UI plan
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | Location | Phase |
|--------|------|----------|-------|
| `RelatedSource` Serialize | derive + serde attrs | crates/retriever/src/related.rs | 16-01 |
| `serde` dep on retriever | Cargo.toml entry | crates/retriever/Cargo.toml | 16-01 |
| `list_related_sources` | Tauri command | src-tauri/src/commands/library.rs | 16-01 |
| `list_related_sources` re-export | pub use | src-tauri/src/commands/mod.rs | 16-01 |
| `list_related_sources` handler | generate_handler! entry | src-tauri/src/lib.rs | 16-01 |
| `RelatedSource` | TypeScript interface | src/types/library.ts | 16-01 |
| `listRelatedSources` | invoke wrapper fn | src/lib/tauri.ts | 16-01 |

<output>
Create `.planning/phases/16-related-docs-library-panel/16-01-SUMMARY.md` when done
</output>
