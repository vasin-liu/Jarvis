---
phase: 05-agent-protocol
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/agent/src/tool_parse.rs
  - crates/agent/src/tools.rs
  - crates/agent/src/types.rs
  - crates/agent/src/run.rs
  - crates/agent/src/lib.rs
requirements:
  - AGT-01
  - AGT-02
must_haves:
  truths:
    - "ToolCallParser trait with JsonToolCallParser (primary) + XmlToolCallParser (fallback) + CompositeToolCallParser chain (D-01..D-04)"
    - "XmlToolCallParser emits eprintln deprecation when it matches (D-03)"
    - "ToolCallParseOutcome distinguishes Found | NotFound | Failed — Failed is NOT treated as final answer when tool intent detected (D-05)"
    - "AgentResponse.tool_parse_warnings: Vec<String> populated on Failed outcomes (D-05)"
    - "run.rs uses composite parser instead of parse_tool_call -> Option (D-04)"
    - "cargo test -p agent passes including new parser unit tests"
  artifacts:
    - path: crates/agent/src/tool_parse.rs
      provides: ToolCallParser trait + JSON/XML/composite implementations
      contains: "trait ToolCallParser"
    - path: crates/agent/src/types.rs
      provides: AgentResponse.tool_parse_warnings field
      contains: "tool_parse_warnings"
    - path: crates/agent/src/run.rs
      provides: agent loop wired to composite parser
      contains: "ToolCallParseOutcome"
  key_links:
    - from: crates/agent/src/run.rs
      to: crates/agent/src/tool_parse.rs
      via: "composite parser on each LLM reply"
      pattern: "CompositeToolCallParser"
---

<objective>
Implement the structured tool-call parsing foundation: `ToolCallParser` trait with JSON-primary and XML-fallback parsers, replace silent `Option` parsing in the agent loop, and carry parse failures in `AgentResponse.tool_parse_warnings`.

Purpose: Deliver AGT-01 (structured JSON path) and AGT-02 backend half (failures not silent None).
Output: `tool_parse.rs`, updated `run.rs`, extended `AgentResponse`, refactored `tools.rs` (XML logic delegated to XmlToolCallParser).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-agent-protocol/05-CONTEXT.md
@.planning/phases/05-agent-protocol/05-RESEARCH.md
@crates/agent/src/tools.rs
@crates/agent/src/run.rs
@.cursor/rules/tdd-goal-driven.mdc
</context>

<threat_model>
scope: agent_protocol
asvs_level: L1
trust_boundaries:
  - "LLM reply text → tool parser → execute_tool (must not execute on Failed parse)"
stride_register:
  - threat_id: T-05-01
    category: Integrity
    component: ToolCallParser Failed vs NotFound
    disposition: mitigate
    mitigation_plan: "Unit tests: malformed JSON with tool intent returns Failed; plain prose returns NotFound. run.rs must not call execute_tool on Failed."
disposition_summary: "Primary risk is executing wrong tool or hiding parse errors — mitigated by outcome enum + tests."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: ToolCallParser module + tests (05-01-01)</name>
  <files>crates/agent/src/tool_parse.rs, crates/agent/src/lib.rs, crates/agent/src/tools.rs</files>
  <action>
    Create `crates/agent/src/tool_parse.rs`:
    - `ToolCallPayload` stays in tools.rs OR move to tool_parse and re-export (prefer re-export from tools for minimal churn)
    - `ToolCallParseOutcome` enum: Found / NotFound / Failed { message }
    - `trait ToolCallParser { fn id(&self) -> &str; fn parse(&self, text: &str) -> ToolCallParseOutcome; }`
    - `JsonToolCallParser`: parse bare JSON line/object with name+arguments
    - `XmlToolCallParser`: move logic from `parse_tool_call`; on Found emit `eprintln!("[jarvis] XML tool_call format deprecated; use JSON")`
    - `CompositeToolCallParser`: try JSON, then XML; if JSON detects malformed tool-like content → Failed

    Refactor `parse_tool_call` in tools.rs to delegate to composite (keep fn for backward compat in tests, return Option from Found only).

    Export from lib.rs.

    Add unit tests: JSON line, XML fallback, malformed JSON → Failed, plain text → NotFound.
  </action>
  <verify>cargo test -p agent tool_parse</verify>
  <acceptance_criteria>
    - `rg "trait ToolCallParser" crates/agent/src/tool_parse.rs` matches
    - `cargo test -p agent` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire run.rs + AgentResponse warnings (05-01-02)</name>
  <files>crates/agent/src/types.rs, crates/agent/src/run.rs</files>
  <action>
    Add to `AgentResponse`:
    ```rust
    #[serde(default)]
    pub tool_parse_warnings: Vec<String>,
    ```

    In `run.rs` replace `if let Some(payload) = parse_tool_call(&reply)` with match on composite.parse:
    - Found → existing tool execution path
    - NotFound → existing final-answer path (hooks + return)
    - Failed → push warning string, append assistant message noting parse failure, continue loop OR return answer with warnings (prefer: push warning + treat as final answer with warnings if no retry budget — document in code)

    Ensure existing `agent_uses_search_tool` integration test passes (update Mock later in 05-02 if needed — for 05-01 keep XML Mock working via fallback).

    Add unit test for Failed outcome populating warnings (mock reply with broken tool JSON).
  </action>
  <verify>cargo test -p agent</verify>
  <acceptance_criteria>
    - `rg "tool_parse_warnings" crates/agent/src/types.rs` matches
    - `cargo test -p agent` green including run.rs tests
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cargo test -p agent` passes
- [ ] JSON parser handles bare object line
- [ ] XML fallback works with deprecation log
- [ ] Failed parse does not call execute_tool
</verification>

<success_criteria>
AGT-01/AGT-02 parser layer complete; agent loop uses structured outcomes; ready for Mock/UI wiring in Plan 02.
</success_criteria>
