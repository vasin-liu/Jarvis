---
phase: 17-mcp-transport-read-only-scaffold
plan: 04
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
  - 17-03
files_modified:
  - crates/mcp/src/main.rs
  - docs/mcp.md
  - README.md
autonomous: true
requirements:
  - MCP-03
must_haves:
  truths:
    - "jarvis-mcp resolves DB, fails closed if missing, opens Store via store only, serves MCP over stdio (D-05, D-06, D-01)"
    - "docs/mcp.md documents Cursor + Claude Desktop stdio config and JARVIS_DATA_DIR / --db (D-07, MCP-03)"
    - "README points to docs/mcp.md (D-07)"
  artifacts:
    - path: crates/mcp/src/main.rs
      provides: "tokio main: resolve → preflight → load_config → Store::open → JarvisMcp.serve(stdio)"
    - path: docs/mcp.md
      provides: "Host config snippets + build path + path precedence"
    - path: README.md
      provides: "Short pointer to docs/mcp.md"
  key_links:
    - from: crates/mcp/src/main.rs
      to: crates/store/src/store.rs
      via: "Sole SQLite open after ensure_db_exists; WAL from 17-02"
      pattern: "Store::open"
    - from: crates/mcp/src/main.rs
      to: crates/config
      via: "load_config + embedding_dim (not load_config_with_migration)"
      pattern: "config::load_config"
    - from: docs/mcp.md
      to: target/release/jarvis-mcp.exe
      via: "command path in Cursor mcp.json example"
      pattern: "JARVIS_DATA_DIR"
---

<objective>
Wire `jarvis-mcp` stdio server startup and ship host configuration docs.

Purpose: Complete MCP-03 — spawnable binary + Cursor/Claude Desktop documentation (D-07).
Output: Working stdio main; `docs/mcp.md`; README pointer; `cargo test -p mcp && cargo test -p store` green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-mcp-transport-read-only-scaffold/17-CONTEXT.md
@.planning/phases/17-mcp-transport-read-only-scaffold/17-RESEARCH.md
@.planning/phases/17-mcp-transport-read-only-scaffold/17-PATTERNS.md
@.planning/phases/17-mcp-transport-read-only-scaffold/17-01-SUMMARY.md
@.planning/phases/17-mcp-transport-read-only-scaffold/17-02-SUMMARY.md
@.planning/phases/17-mcp-transport-read-only-scaffold/17-03-SUMMARY.md
@crates/mcp/src/main.rs
@crates/mcp/src/lib.rs
@crates/mcp/src/paths.rs
@src-tauri/src/state.rs
@crates/config/src/file.rs
@README.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire jarvis-mcp main — preflight, Store::open, rmcp stdio serve</name>
  <read_first>
    - crates/mcp/src/main.rs
    - crates/mcp/src/lib.rs (JarvisMcp)
    - crates/mcp/src/paths.rs
    - src-tauri/src/state.rs (config_path / db_path / embedding_dim startup)
    - crates/config/src/file.rs (load_config)
    - .planning/phases/17-mcp-transport-read-only-scaffold/17-RESEARCH.md (§2 main, §3 steps 4–6)
  </read_first>
  <files>crates/mcp/src/main.rs</files>
  <action>
    Replace stub main with #[tokio::main] async fn main per D-01/D-05/D-06 and RESEARCH:

    1. parse `--db` via parse_db_flag(std::env::args())
    2. resolve_db_path + config dir sibling (db.parent() when --db; else data dir)
    3. ensure_db_exists — on Err: eprintln clear message that GUI must create KB first; process::exit(1). Never call Store::open on missing file (D-06).
    4. load_config(config_dir.join("config.json")) — use load_config NOT load_config_with_migration (no keychain side effects)
    5. dim = config.embedding_dim() (fallback AppConfig::default().embedding_dim() if needed)
    6. Store::open(&db_path, dim) via store crate only; wrap Arc if JarvisMcp holds it
    7. JarvisMcp { ... }.serve(rmcp::transport::stdio()).await? then waiting().await?

    Map errors to Box&lt;dyn Error&gt; or McpError at boundary. Do not add GUI subsystem attribute. Do not enable HTTP/SSE transport features. Do not implement real search (stubs from 17-03). Do not touch e2e specs (D-09).
  </action>
  <verify>
    <automated>cargo build -p mcp &amp;&amp; cargo test -p mcp -- --test-threads=4</automated>
  </verify>
  <acceptance_criteria>
    - cargo build -p mcp succeeds producing jarvis-mcp binary
    - cargo test -p mcp exits 0
    - main.rs calls ensure_db_exists before Store::open
    - main.rs uses config::load_config and store::Store::open only (no rusqlite)
    - main.rs does not set the Windows GUI subsystem attribute
  </acceptance_criteria>
  <done>stdio MCP binary builds and unit/integration tests remain green (MCP-03 transport).</done>
</task>

<task type="auto">
  <name>Task 2: Document host config in docs/mcp.md + README pointer</name>
  <read_first>
    - .planning/phases/17-mcp-transport-read-only-scaffold/17-CONTEXT.md (D-07)
    - .planning/research/STACK.md (mcp.json snippet)
    - README.md (Docs lines)
    - .planning/phases/17-mcp-transport-read-only-scaffold/17-RESEARCH.md (§5 docs sections)
  </read_first>
  <files>docs/mcp.md, README.md</files>
  <action>
    Create docs/mcp.md per D-07 / MCP-03 covering:

    1. What jarvis-mcp is (read-only KB bridge; Phase 17 stubs; Phase 18 real search)
    2. Build: `cargo build -p mcp --release` → target/release/jarvis-mcp.exe (Windows) / jarvis-mcp (Unix)
    3. Path precedence: --db &gt; JARVIS_DATA_DIR/kb.sqlite &gt; %APPDATA%\com.jarvis.app\kb.sqlite (and Unix defaults)
    4. Cursor mcp.json example (command / args / env.JARVIS_DATA_DIR) from STACK.md
    5. Claude Desktop config equivalent
    6. Prerequisite: run Jarvis GUI once so kb.sqlite exists (D-06 — no auto-create)

    Add a short README.md pointer (Docs line or Tests section) linking to docs/mcp.md — match existing backtick path style. No Settings UI (MCP-F02 deferred). No WebDriver E2E this phase (D-09).
  </action>
  <verify>
    <automated>cargo test -p mcp -- --test-threads=4 &amp;&amp; cargo test -p store -- --test-threads=4</automated>
  </verify>
  <acceptance_criteria>
    - docs/mcp.md exists and mentions Cursor, Claude Desktop, JARVIS_DATA_DIR, --db, and stub/Phase 18 note
    - README.md contains a pointer to docs/mcp.md
    - Phase gate: cargo test -p mcp &amp;&amp; cargo test -p store both exit 0
  </acceptance_criteria>
  <done>MCP-03 docs shipped; D-07 satisfied; phase cargo gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP host spawn → jarvis-mcp stdio | Untrusted host controls argv/env and JSON-RPC |
| jarvis-mcp → kb.sqlite | Opens user KB read/write at SQLite layer; tools must stay RO |
| Docs → operator machine | Config snippets must not encourage write tools or auto-create KB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-09 | Tampering | main startup | high | mitigate | ensure_db_exists before Store::open (D-06); store sole open site |
| T-17-10 | Elevation of Privilege | stdio tool surface | high | mitigate | Serve JarvisMcp with only search/list_sources stubs from 17-03 (MCP-04) |
| T-17-11 | Information Disclosure | load_config | medium | mitigate | Use load_config without keychain migration; never emit cloud_api_key in tool/stub output or docs examples |
| T-17-12 | Spoofing | docs host config | low | accept | Docs are local-stdio only; no remote HTTP transport (MCP-F03 deferred) |
| T-17-SC | Tampering | no new crates | low | accept | Docs-only + main wiring; rmcp already pinned in 17-01 |
</threat_model>

<verification>
- cargo build -p mcp
- cargo test -p mcp
- cargo test -p store
- docs/mcp.md + README pointer present
</verification>

<success_criteria>
- External hosts can spawn jarvis-mcp over stdio against an existing KB
- tools/list remain exactly {search, list_sources} stubs
- MCP-03 documentation complete; D-01..D-09 covered across plans 01–04
</success_criteria>

## Artifacts

| Symbol | Kind | Location | Phase |
|--------|------|----------|-------|
| `jarvis-mcp` stdio main | binary entry | crates/mcp/src/main.rs | 17-04 |
| `docs/mcp.md` | host docs | docs/mcp.md | 17-04 |
| README MCP pointer | docs link | README.md | 17-04 |

<output>
Create `.planning/phases/17-mcp-transport-read-only-scaffold/17-04-SUMMARY.md` when done
</output>
