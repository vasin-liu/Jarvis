---
phase: 08-deterministic-markdown-renderer
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - crates/insights/src/wiki.rs
autonomous: true
requirements:
  - WIKI-03
must_haves:
  truths:
    - "Empty entities and concepts still emit one source-summary page under sources/ with sources frontmatter and generated true (ROADMAP 1, D-08)"
    - "Non-empty entities emit entities/ pages and summary body contains [[entities/slug|name]] forward links (ROADMAP 2, D-01, D-04)"
    - "Each entity and concept page body contains backlink [[sources/{slug}|{source_title}]] (D-02, D-03)"
    - "Concept pages mirror entity link rules under concepts/ prefix (D-04, D-01)"
    - "index_markdown has # Wiki, lists non-empty sections only, uses [[path|display]] bullets, no YAML frontmatter (D-11, D-12, D-13)"
    - "render_wiki_pages is pure: no std::fs, Store, ChatModel, async, or LLM"
  artifacts:
    - path: crates/insights/src/wiki.rs
      provides: "render_wiki_pages + index_markdown assembly"
      contains: "render_wiki_pages"
  key_links:
    - from: render_wiki_pages
      to: slugify + frontmatter + wikilink helpers
      via: "compose WikiPageDraft.slug as {dir}/{slug} and body_markdown"
      pattern: "sources/"
    - from: WikiCompileResult.index_markdown
      to: Phase 10 disk write
      via: "catalog string only in Phase 08 — no merge I/O"
      pattern: "## Sources"
---

<objective>
Implement `render_wiki_pages` and `index_markdown` assembly so a `WikiAnalysis` plus source URI/title yields stable Markdown page drafts and vault catalog string — no LLM, no disk I/O (WIKI-03 render half; D-01 through D-04, D-11 through D-13; all ROADMAP success criteria).

Purpose: Completes the pure compile transform Phase 09/10 will consume.
Output: Fully tested `render_wiki_pages`; all Phase 08 unit tests green; purity grep clean.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-deterministic-markdown-renderer/08-CONTEXT.md
@.planning/phases/08-deterministic-markdown-renderer/08-RESEARCH.md
@.planning/phases/08-deterministic-markdown-renderer/08-PATTERNS.md
@.planning/phases/08-deterministic-markdown-renderer/08-VALIDATION.md
@.planning/phases/08-deterministic-markdown-renderer/08-01-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/superpowers/plans/2026-07-16-wiki-compile-layer.md
@.cursor/rules/tdd-goal-driven.mdc
@crates/insights/src/wiki.rs

<interfaces>
From Plan 08-01 (must exist before this plan):
- slugify, uniquify_slug, build_frontmatter, wikilink, WikiPageType::as_str
- Public types and render_wiki_pages stub

render_wiki_pages contract (locked):
- Input: &WikiAnalysis, source_uri: &str, source_title: &str
- Output: WikiCompileResult { pages: Vec<WikiPageDraft>, index_markdown: String }
- Always at least one SourceSummary page at slug sources/{source_slug} where source_slug = slugify(source_title) with sources/ directory uniquify (D-06)
- Entity pages at entities/{slug}; concept pages at concepts/{slug} (D-04)
- WikiPageDraft.source_uris = vec![source_uri.to_string()] (RESEARCH Open Q3 RESOLVED)
- body_markdown = frontmatter + body prose (discretion: minimal # title + summary/blurb + link lines per D-01–D-03)
- index_markdown: # Wiki then optional ## Sources / ## Entities / ## Concepts with bullet wikilinks; omit empty sections (D-11, D-12); no frontmatter (D-13)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing render tests for ROADMAP criteria 1–2 (08-02-01)</name>
  <files>crates/insights/src/wiki.rs</files>
  <read_first>
.planning/phases/08-deterministic-markdown-renderer/08-CONTEXT.md (D-01 through D-04, D-08 through D-10),
.planning/phases/08-deterministic-markdown-renderer/08-RESEARCH.md (Failing tests section — override plan Task 2 bare [[Acme]]),
.planning/phases/08-deterministic-markdown-renderer/08-VALIDATION.md,
docs/superpowers/plans/2026-07-16-wiki-compile-layer.md (Task 2 — apply CONTEXT overrides)
  </read_first>
  <behavior>
    - render_always_emits_source_summary: empty entities/concepts → exactly one page; slug starts with sources/; body has sources array with file URI; generated true; no content_hash; index has # Wiki and ## Sources; index lacks ## Entities
    - render_links_entities_bidirectional: one entity Acme → summary contains [[entities/acme|Acme]]; entity page contains [[sources/…|Doc A]] backlink with source title display
    - render_links_concepts_bidirectional: one concept mirrors entity rules under concepts/ prefix
  </behavior>
  <action>
    RED per ROADMAP criteria 1–2 and D-01, D-02, D-03, D-04, D-08: add integration-style unit tests `render_always_emits_source_summary`, `render_links_entities_bidirectional`, and `render_links_concepts_bidirectional` in `wiki.rs` #[cfg(test)] mod. Use plan Task 2 fixtures but assert path|display wikilinks only (not bare [[Acme]]). Assert no content_hash in any body_markdown.

    Run `cargo test -p insights render_always_emits_source_summary render_links_entities_bidirectional render_links_concepts_bidirectional -- --exact` and confirm failure while render_wiki_pages is still stub/ incomplete.

    Do not add std::fs, Store, or ChatModel. Do not implement full render in this task — tests only (RED).
  </action>
  <verify>
    <automated>cargo test -p insights render_always_emits_source_summary render_links_entities_bidirectional render_links_concepts_bidirectional -- --exact --test-threads=1 2>&1 | rg "test result: FAILED|passed: 0"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "render_always_emits_source_summary|render_links_entities_bidirectional|render_links_concepts_bidirectional" crates/insights/src/wiki.rs` matches three test fns
    - RED confirmed: targeted tests fail before GREEN implementation in Task 2
  </acceptance_criteria>
  <done>Three failing render tests encode ROADMAP 1–2 and bidirectional link contract</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement render_wiki_pages page assembly (08-02-02)</name>
  <files>crates/insights/src/wiki.rs</files>
  <read_first>
.planning/phases/08-deterministic-markdown-renderer/08-CONTEXT.md (D-01 through D-10),
.planning/phases/08-deterministic-markdown-renderer/08-PATTERNS.md (pure transform; anti-pattern summarize.rs),
crates/insights/src/wiki.rs (helpers from 08-01)
  </read_first>
  <behavior>
    - All Task 1 render tests pass (including index asserts: # Wiki, ## Sources present; ## Entities absent when entities empty)
    - cjk_name_uses_hash_slug integration: entity 张三 → slug entities/e-{6hex}; summary still wikilinks with path|display using entity name as display
    - Pages use separate uniquify maps for sources/, entities/, concepts/ directories (D-07)
    - index_markdown is non-empty: at minimum # Wiki + ## Sources with a bullet wikilink to the source summary draft (enough for Task 1 source-summary index asserts)
  </behavior>
  <action>
    GREEN per D-04, D-06, D-07, D-08, D-09, D-10, D-01, D-02, D-03 (and minimal D-11 Sources catalog for Task 1 index asserts): replace render_wiki_pages stub with full page assembly plus a callable index builder.

    Algorithm sketch (no fenced implementations):
    1. Compute source_slug via slugify(source_title) + uniquify in sources/ map; build source summary WikiPageDraft at slug sources/{source_slug} with page_type SourceSummary, title source_title, frontmatter + body containing analysis.summary and forward wikilinks to each entity/concept page using wikilink helper.
    2. For each entity in input order: slugify name + uniquify in entities/ map; draft at entities/{slug} with title=name, blurb in body, backlink wikilink to sources/{source_slug}|source_title.
    3. For each concept: same under concepts/ map and concepts/ prefix.
    4. Populate source_uris on every draft with single-element vec of source_uri.
    5. Implement private `build_index_markdown(pages: &[WikiPageDraft]) -> String` (or equivalent) and call it from render_wiki_pages. Minimal GREEN for Task 1/2: emit `# Wiki`, then `## Sources` with a bullet `wikilink` to the source-summary draft. Omit `## Entities` / `## Concepts` when those page types are absent so `render_always_emits_source_summary` index asserts pass. Do not leave index_markdown as an empty placeholder. Full D-11–D-13 edge-case coverage (all three sections, no-frontmatter test) stays in Task 3.

    Return WikiCompileResult with pages vec (summary first or deterministic order — document in SUMMARY) and the built index_markdown.

    Extend or reuse cjk_name_uses_hash_slug test to call render_wiki_pages end-to-end (ROADMAP criterion 3). Ensure slug field on draft matches wikilink path segments.

    Keep function synchronous and infallible. No I/O.
  </action>
  <verify>
    <automated>cargo test -p insights render_always_emits_source_summary render_links_entities_bidirectional render_links_concepts_bidirectional cjk_name_uses_hash_slug -- --exact --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p insights render_always_emits_source_summary render_links_entities_bidirectional render_links_concepts_bidirectional cjk_name_uses_hash_slug -- --exact` exits 0
    - `rg "std::fs|Store|ChatModel|async fn" crates/insights/src/wiki.rs` returns no matches
    - Every page body_markdown contains generated: true and lacks content_hash
  </acceptance_criteria>
  <done>render_wiki_pages produces source/entity/concept drafts with bidirectional path|display links, minimal # Wiki / ## Sources index_markdown, and CJK slugs use e-{hash6}</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: index_markdown catalog edge cases D-11–D-13 (08-02-03)</name>
  <files>crates/insights/src/wiki.rs</files>
  <read_first>
.planning/phases/08-deterministic-markdown-renderer/08-CONTEXT.md (D-11, D-12, D-13),
.planning/phases/08-deterministic-markdown-renderer/08-RESEARCH.md (Pitfall 6),
.planning/phases/08-deterministic-markdown-renderer/08-VALIDATION.md (index_omits_empty_sections),
crates/insights/src/wiki.rs (build_index_markdown from Task 2)
  </read_first>
  <behavior>
    - index_omits_empty_sections: empty entities/concepts analysis → index has ## Sources only (plus # Wiki); no ## Entities or ## Concepts
    - index_with_all_sections: analysis with entity and concept → index contains all three section headings with bullet [[path|display]] lines matching draft slugs/titles
    - index_has_no_yaml_frontmatter: index_markdown does not start with --- delimiter block
  </behavior>
  <action>
    RED per D-11, D-12, D-13: add tests `index_omits_empty_sections`, `index_with_all_sections`, `index_has_no_yaml_frontmatter`. Confirm targeted failures where Task 2's minimal index is incomplete (e.g. `index_with_all_sections` / `index_has_no_yaml_frontmatter` if Entities/Concepts or frontmatter rules are not yet fully locked). Do not re-introduce build_index_markdown from scratch — Task 2 already wires and calls it.

    GREEN: extend/refine `build_index_markdown` (and its call site if needed) for full D-11–D-13: keep `# Wiki` + always-present `## Sources` bullet for the source summary draft; add `## Entities` and `## Concepts` only when at least one draft of that page_type exists (D-12); each bullet uses wikilink(slug, title) with draft.slug as path (includes directory prefix); guarantee no YAML frontmatter on the index string (D-13).

    Final gate: `cargo test -p insights -- --test-threads=1` full insights crate green.
  </action>
  <verify>
    <automated>cargo test -p insights -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p insights index_omits_empty_sections index_with_all_sections -- --exact` exits 0
    - `cargo test -p insights -- --test-threads=1` exits 0 (accepted latency: see 08-VALIDATION.md)
    - `rg -n "fn build_index_markdown|index_markdown" crates/insights/src/wiki.rs` matches
    - ROADMAP success criteria 1–3 covered by automated tests in wiki.rs
  </acceptance_criteria>
  <done>WikiCompileResult.index_markdown complete; full insights test suite green; Phase 08 render half done</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WikiAnalysis content → Markdown strings | Structured input from future LLM parse (Phase 09); treat summary/blurb as untrusted text in bodies |
| index_markdown → future disk index.md | Catalog string only; no write in this phase |
| Slug paths in wikilinks | Must stay consistent between draft.slug and link paths to avoid broken vault navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05 | Tampering | render_wiki_pages link paths | medium | mitigate | Build wikilinks from same slug variables as WikiPageDraft.slug; bidirectional tests (D-02) |
| T-08-06 | Information Disclosure | analysis text in bodies | low | accept | Local-first compile; no network; text mirrors indexed source excerpts |
| T-08-07 | Repudiation | deterministic output | low | accept | Pure function of inputs; unit tests lock stable shapes for Phase 10 idempotency |
| T-08-08 | Tampering | index section injection | low | mitigate | Fixed section headings only; bullet lines from typed drafts not raw user headings (D-11) |
| T-08-SC | Tampering | cargo installs | low | accept | No new packages in this plan |
</threat_model>

<verification>
- `cargo test -p insights -- --test-threads=1` green (all wiki tests)
- Manual purity check: `rg "std::fs|Store|ChatModel" crates/insights/src/wiki.rs` — zero matches
- ROADMAP Phase 08 success criteria 1–3 mapped to render_always_emits_source_summary, render_links_entities_bidirectional, cjk_name_uses_hash_slug (+ concepts + index tests)
- No E2E required (08-UI-SPEC: library-only phase)
</verification>

<success_criteria>
WIKI-03 render half complete: given WikiAnalysis + source URI/title, render_wiki_pages returns stable pages with frontmatter, [[path|display]] wikilinks, and index_markdown — deterministic, no LLM, no disk I/O.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Requirement |
|----------|------|-------------|
| render_wiki_pages | `crates/insights/src/wiki.rs` | WIKI-03 (D-01–D-04, D-08–D-10) |
| build_index_markdown | `crates/insights/src/wiki.rs` | WIKI-03 (D-11–D-13) |
| ROADMAP render unit tests | `crates/insights/src/wiki.rs` | WIKI-03; ROADMAP criteria 1–3 |

<output>
Create `.planning/phases/08-deterministic-markdown-renderer/08-02-SUMMARY.md` when done
</output>
