---
phase: 17-mcp-transport-read-only-scaffold
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/store/src/store.rs
autonomous: true
requirements:
  - MCP-03
must_haves:
  truths:
    - "File-backed Store::open enables SQLite WAL so GUI + MCP can share kb.sqlite (D-08)"
    - "store remains the only crate that opens rusqlite connections"
  artifacts:
    - path: crates/store/src/store.rs
      provides: "PRAGMA journal_mode=WAL in open (+ open_in_memory batch); WAL regression test"
  key_links:
    - from: crates/store/src/store.rs
      to: crates/mcp (future caller)
      via: "MCP calls Store::open only after preflight; WAL reduces SQLITE_BUSY"
      pattern: "journal_mode=WAL"
---

<objective>
Enable SQLite WAL on Store open paths for safe GUI + MCP coexistence.

Purpose: D-08 / MCP-03 concurrency assumption — external `jarvis-mcp` and Tauri GUI may open the same `kb.sqlite` concurrently.
Output: WAL pragma in `Store::open` (and in-memory open batch); green store WAL assertion for file-backed DBs.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-mcp-transport-read-only-scaffold/17-CONTEXT.md
@.planning/phases/17-mcp-transport-read-only-scaffold/17-RESEARCH.md
@.planning/phases/17-mcp-transport-read-only-scaffold/17-PATTERNS.md
@crates/store/src/store.rs
@crates/store/tests/persistence.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED then GREEN — WAL pragma on Store::open</name>
  <read_first>
    - crates/store/src/store.rs (open / open_in_memory / existing #[cfg(test)] mod)
    - crates/store/tests/persistence.rs (tempfile file-backed pattern)
    - .planning/phases/17-mcp-transport-read-only-scaffold/17-CONTEXT.md (D-08)
    - .planning/phases/17-mcp-transport-read-only-scaffold/17-RESEARCH.md (§4 WAL)
  </read_first>
  <files>crates/store/src/store.rs</files>
  <behavior>
    - store_open_enables_wal: after Store::open on tempfile path, PRAGMA journal_mode query returns wal (case-insensitive)
    - open_in_memory still initializes schema without panic after pragma batch change (journal_mode may be memory — do not require wal for in-memory)
  </behavior>
  <action>
    Add failing unit/integration test first (inline #[cfg(test)] in store.rs or crates/store/tests/): open tempfile kb.sqlite via Store::open(&path, 4), query journal_mode through existing connection API if available, else add a small test-only helper that runs `PRAGMA journal_mode` under the Mutex — assert equals "wal". Per D-08.

    Then update execute_batch in both Store::open and Store::open_in_memory from foreign_keys-only to include journal_mode=WAL in the same batch (PATTERNS / RESEARCH). Do not add busy_timeout unless a test fails with SQLITE_BUSY (research: start WAL-only). Do not open SQLite from any other crate. Do not change schema migrations beyond pragma.
  </action>
  <verify>
    <automated>cargo test -p store wal -- --test-threads=4</automated>
  </verify>
  <acceptance_criteria>
    - Store::open file-backed path reports journal_mode wal
    - cargo test -p store passes (full crate, not only wal filter)
    - No rusqlite usage added outside crates/store
  </acceptance_criteria>
  <done>D-08 satisfied: WAL enabled on Store::open; regression test green.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm open_in_memory still green after pragma change</name>
  <read_first>
    - crates/store/src/store.rs (open_in_memory + existing unit tests)
  </read_first>
  <files>crates/store/src/store.rs</files>
  <action>
    Run existing in-memory unit tests (upsert_get_list_delete_source and siblings). If open_in_memory WAL pragma causes unexpected failures, keep foreign_keys + journal_mode=WAL in batch but document in test that in-memory may report memory mode — do not weaken file-backed wal assertion. Surgical fix only if needed.
  </action>
  <verify>
    <automated>cargo test -p store -- --test-threads=4</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p store exits 0
    - File-backed WAL test still present and passing
  </acceptance_criteria>
  <done>Store suite green with WAL; safe for MCP Store::open in Plan 17-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Store::open → SQLite file | Process-local; GUI and MCP may share file |
| Pragma batch → DB engine | Misconfiguration could disable durability or locks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-04 | Denial of Service | GUI+MCP lock contention on kb.sqlite | high | mitigate | Enable WAL in Store::open (D-08); keep transactions short; no second SQLite owner |
| T-17-05 | Tampering | journal_mode pragma | medium | mitigate | Set WAL only via store open path; regression test asserts wal on file-backed DB |
| T-17-SC | Tampering | cargo deps | low | accept | No new packages in this plan — workspace-only edit |
</threat_model>

<verification>
- cargo test -p store wal
- cargo test -p store
</verification>

<success_criteria>
- D-08 WAL live on file-backed Store::open
- Full store test suite green
</success_criteria>

## Artifacts

| Symbol | Kind | Location | Phase |
|--------|------|----------|-------|
| `PRAGMA journal_mode=WAL` | open path change | crates/store/src/store.rs | 17-02 |
| `store_open_enables_wal` (or equiv) | test | crates/store/src/store.rs or tests/ | 17-02 |

<output>
Create `.planning/phases/17-mcp-transport-read-only-scaffold/17-02-SUMMARY.md` when done
</output>
