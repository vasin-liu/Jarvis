---
phase: 04-memory-model-view
plan: "04"
type: execute
wave: 4
depends_on:
  - "04-03"
files_modified:
  - e2e/specs/memory.spec.ts
  - e2e/specs/full-ui.spec.ts
  - e2e/specs/navigation.spec.ts
requirements:
  - FE-04
  - FE-06
  - MEM-01
  - MEM-02
must_haves:
  truths:
    - "npm run build:e2e succeeds before E2E run (frontend changes require rebuild)"
    - "memory.spec.ts add → edit → forget journey passes (D-18)"
    - "full-ui.spec.ts memory block passes (D-20)"
    - "navigation.spec.ts memory nav still works if touched"
    - "cargo test -p memory passes; npm test passes"
    - "No memory-* testid regressions (D-13, FE-06)"
  artifacts:
    - path: e2e/specs/memory.spec.ts
      provides: memory CRUD E2E regression
      contains: "memory-list"
    - path: e2e/specs/full-ui.spec.ts
      provides: full journey memory section green
      contains: "memory-edit-panel"
  key_links:
    - from: e2e/specs/memory.spec.ts
      to: src/views/MemoryView.tsx
      via: "data-testid selectors on Memory view"
      pattern: "memory-forget-"
---

<objective>
Run the Phase 4 E2E gate: rebuild the packaged app, verify memory CRUD journeys in `memory.spec.ts` and `full-ui.spec.ts`, fix any selector/timing regressions from the Memory view extraction, and confirm backend memory tests still green.

Purpose: Close FE-04, MEM-01, MEM-02 with automated user-facing verification before phase sign-off.
Output: Green E2E specs, any minimal spec fixes only (no feature changes).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-memory-model-view/04-CONTEXT.md
@.planning/phases/04-memory-model-view/04-RESEARCH.md
@.cursor/rules/e2e-required.mdc
@e2e/specs/memory.spec.ts
@e2e/specs/full-ui.spec.ts
@e2e/helpers.ts
</context>

<threat_model>
scope: e2e_verification
asvs_level: L1
trust_boundaries:
  - "E2E harness JARVIS_E2E=1 mock path — no live LLM"
stride_register:
  - threat_id: T-04-D1
    category: Tampering
    component: E2E selectors vs moved JSX
    disposition: mitigate
    mitigation_plan: "Fix testids in MemoryView first; only update specs if timing/helpers need adjustment (setReactInputValue pattern from full-ui)."
disposition_summary: "Verification-only plan; fixes limited to test/spec alignment."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Backend + frontend unit gate (04-04-01)</name>
  <files></files>
  <action>
    Run:
    - `cargo test -p memory`
    - `cargo test -p store`
    - `npm test`
    - `npx tsc --noEmit`

    Fix any failures from Plans 01-03 before E2E. Do not proceed to E2E with red unit tests.
  </action>
  <verify>cargo test -p memory && npm test</verify>
  <acceptance_criteria>
    - All four commands exit 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build E2E binary + run memory specs (04-04-02)</name>
  <files>e2e/specs/memory.spec.ts, e2e/specs/full-ui.spec.ts, e2e/specs/navigation.spec.ts</files>
  <action>
    Run `npm run build:e2e` then:
    - `npm run test:e2e:local -- --spec e2e/specs/memory.spec.ts`
    - `npm run test:e2e:local -- --spec e2e/specs/full-ui.spec.ts`

    If failures:
    - Prefer fixing MemoryView testids/props over weakening assertions
    - For edit panel timing: use same patterns as Phase 3 (waitUntil, setReactInputValue in full-ui)
    - Update navigation.spec.ts only if memory nav assertion breaks

    Do NOT change memory business logic to make tests pass — fix extraction regressions only.
  </action>
  <verify>npm run test:e2e:local -- --spec e2e/specs/memory.spec.ts</verify>
  <acceptance_criteria>
    - memory.spec.ts passes
    - full-ui.spec.ts passes (memory section + rest of journey)
    - `npm run build:e2e` completed before test run
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cargo test -p memory` green
- [ ] `npm test` green
- [ ] `memory.spec.ts` green
- [ ] `full-ui.spec.ts` green
- [ ] Phase 4 success criteria from ROADMAP.md met
</verification>

<success_criteria>
Phase 4 user-facing gate complete; ready for `/gsd-execute-phase 4` sign-off or `/gsd-verify-work 4`.
</success_criteria>
