---
phase: 07-wiki-kind-config
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/config/src/types.rs
  - crates/config/src/lib.rs
autonomous: true
requirements:
  - WIKI-01
must_haves:
  truths:
    - "Pre-v1.10 JSON without wiki key deserializes with wiki.enabled and wiki.auto_on_insights both false (D-05, D-08, D-11)"
    - "Explicit nested wiki object round-trips enabled and auto_on_insights; serialize emits nested wiki key (D-04, D-07, D-11)"
    - "Unknown keys inside wiki object do not fail deserialize (D-06)"
    - "AppConfig.wiki uses serde default on the field and is NOT flattened (D-04)"
    - "auto_on_insights field exists but is not wired to insights/compile this phase (D-08, D-09)"
  artifacts:
    - path: crates/config/src/types.rs
      provides: "WikiConfig + AppConfig.wiki + default-off tests"
      contains: "struct WikiConfig"
    - path: crates/config/src/lib.rs
      provides: "pub use WikiConfig"
      contains: "WikiConfig"
  key_links:
    - from: crates/config/src/types.rs
      to: config.json on disk
      via: "nested wiki object beside flattened legacy buckets"
      pattern: "\"wiki\""
    - from: AppConfig::default
      to: WikiConfig::default
      via: "wiki: WikiConfig::default() with both flags false"
      pattern: "wiki: WikiConfig"
---

<objective>
Add nested `WikiConfig { enabled, auto_on_insights }` on `AppConfig` (both default false, never flatten) so pre-v1.10 configs load with wiki off and explicit wiki JSON round-trips (WIKI-01 / D-04…D-11).

Purpose: Opt-in foundation with zero surprise on upgrade; ship dormant `auto_on_insights` now to avoid reshaping config later.
Output: WikiConfig type, AppConfig.wiki field, re-export, config unit tests green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-wiki-kind-config/07-CONTEXT.md
@.planning/phases/07-wiki-kind-config/07-RESEARCH.md
@.planning/phases/07-wiki-kind-config/07-PATTERNS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/superpowers/plans/2026-07-16-wiki-compile-layer.md
@.cursor/rules/tdd-goal-driven.mdc
@crates/config/src/types.rs
@crates/config/src/lib.rs

<interfaces>
Target contracts (from RESEARCH / implementation plan Task 1 — D-04):

WikiConfig:
- fields: enabled: bool, auto_on_insights: bool
- Default: both false
- serde: derive Serialize/Deserialize; struct-level or field-level default as needed

AppConfig:
- add field wiki: WikiConfig with serde default attribute on the field
- MUST NOT apply serde flatten to wiki (unlike embedding/chat/lark/sync/agent_cfg)
- AppConfig::default() sets wiki: WikiConfig::default()

lib.rs currently:
- pub use types::{AgentOrchestrationMode, AppConfig, ChatProvider, EmbedderProvider};
- add WikiConfig to that list
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WikiConfig nested defaults + round-trip (07-02-01)</name>
  <files>crates/config/src/types.rs, crates/config/src/lib.rs</files>
  <read_first>
crates/config/src/types.rs (AppConfig, SyncConfig Default pattern, config_flat_json_roundtrip test),
crates/config/src/lib.rs (pub use types line),
.planning/phases/07-wiki-kind-config/07-CONTEXT.md (D-04 through D-11),
.planning/phases/07-wiki-kind-config/07-RESEARCH.md (Code Examples config tests + Pitfalls P1 P3 P4),
docs/superpowers/plans/2026-07-16-wiki-compile-layer.md (Task 1 WikiConfig shape)
  </read_first>
  <behavior>
    - pre_v110_config_defaults_wiki_off: deserialize flat JSON with no wiki key → enabled false and auto_on_insights false
    - wiki_config_explicit_roundtrip: deserialize with wiki.enabled true, auto_on_insights true, plus an ignored future key → both true; serialize Value has nested wiki.enabled true
    - AppConfig::default().wiki matches WikiConfig::default() (both false)
  </behavior>
  <action>
    Per D-05, D-06, D-07, D-11 (and 07-01 Task 1 RED→GREEN pattern): RED — add `pre_v110_config_defaults_wiki_off` and `wiki_config_explicit_roundtrip` in the existing `#[cfg(test)] mod tests` next to `config_flat_json_roundtrip` (discretion: types.rs). Use a minimal flat JSON fixture consistent with existing config tests (watch_folders, embedder, chat, mock/ollama/lark fields). Explicit fixture includes nested wiki object with an extra ignored key for forward-compat (D-06). Confirm `cargo test -p config pre_v110_config_defaults_wiki_off wiki_config_explicit_roundtrip` fails before WikiConfig exists.

    GREEN — define `WikiConfig` with `enabled` and `auto_on_insights` (dormant field ships now per D-08; do not wire to insights/compile — D-09). Implement Default both false. Add `wiki: WikiConfig` on `AppConfig` with serde default on the field only — do not flatten wiki (D-04 / P1). Update `AppConfig::default()`. Re-export `WikiConfig` from `crates/config/src/lib.rs`.

    Optionally assert in `config_flat_json_roundtrip` that missing wiki input yields both flags false (keep existing sync/embedder flatten asserts). Do not add Settings UI (D-10). Do not modify insights_ops or any compile pipeline.
  </action>
  <verify>
    <automated>cargo test -p config</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pre_v110_config_defaults_wiki_off" crates/config/src/types.rs` matches
    - `rg -n "wiki_config_explicit_roundtrip" crates/config/src/types.rs` matches
    - `rg -n "struct WikiConfig" crates/config/src/types.rs` matches
    - `rg -n "pub wiki: WikiConfig" crates/config/src/types.rs` matches
    - Region near `pub wiki: WikiConfig` must not use serde flatten on that field (D-04)
    - `rg -n "WikiConfig" crates/config/src/lib.rs` matches pub use
    - `rg -n "auto_on_insights" src-tauri/src/insights_ops.rs` count is 0 (D-09 unwired)
    - `cargo test -p config` exits 0
  </acceptance_criteria>
  <done>Nested wiki config defaults off; explicit JSON round-trips; WikiConfig re-exported; insights unwired; config tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.json → AppConfig | User-editable JSON crosses into typed config; missing/extra keys must not enable wiki by surprise |
| Future consumers of wiki.enabled | Flags stored now; runtime gating belongs to later phases |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Tampering | WikiConfig serde shape | high | mitigate | Nested object with field-level serde default; never flatten (D-04 / P1) |
| T-07-05 | Elevation of Privilege | Default flags | high | mitigate | Both flags default false; pre-v1.10 load test (D-05, D-08, WIKI-01) |
| T-07-06 | Tampering | Unknown wiki keys | low | accept | Default serde ignores unknown fields (D-06); no deny_unknown_fields |
| T-07-07 | Spoofing | Premature auto_on_insights wiring | medium | mitigate | Field only; do not touch insights_ops this phase (D-09); grep gate |
| T-07-SC | Tampering | cargo/npm installs | low | accept | No new packages in this plan — N/A |
</threat_model>

<verification>
- `cargo test -p config` green including pre-v1.10 default-off and explicit wiki round-trip
- Nested `"wiki"` present on serialize of explicit fixture
- No Settings wiki controls; no insights wiring
</verification>

<success_criteria>
WIKI-01: Users' existing config.json loads with wiki off; WikiConfig stores enabled + auto_on_insights with safe defaults and nested JSON shape.
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Requirement |
|----------|------|-------------|
| WikiConfig | `crates/config/src/types.rs` | WIKI-01 (D-04, D-08) |
| AppConfig.wiki | `crates/config/src/types.rs` | WIKI-01 (D-04, D-05) |
| Config default/round-trip tests | `crates/config/src/types.rs` | WIKI-01 (D-07, D-11) |
| WikiConfig re-export | `crates/config/src/lib.rs` | WIKI-01 |

<output>
Create `.planning/phases/07-wiki-kind-config/07-02-SUMMARY.md` when done
</output>
